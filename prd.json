{
  "project": "Digimon Health V-Pet",
  "branchName": "ralph/progression-and-play",
  "description": "Progression and Play - branching evolution driven by real HealthKit behaviour, a full 1,022-entry Dex with subtle evolution hints, visible sickness, battles with per-Digimon projectiles and signature moves, and six interactive training minigames. Source PRD: tasks/prd-progression-and-play.md",
  "userStories": [
    {
      "id": "US-055",
      "title": "Spike - which HealthKit types are actually readable on watchOS 26.4",
      "description": "As a developer, I need to know which health identifiers exist, are readable on the watch, and return data, before designing evolution criteria around ones that do not.",
      "acceptanceCriteria": [
        "For each candidate identifier, record three facts: compiles on watchOS 26.4, appears in the authorization sheet, and returns samples or noData (not an error) in the Simulator",
        "Quantity candidates probed: .stepCount, .distanceWalkingRunning, .flightsClimbed, .appleExerciseTime, .appleStandTime, .activeEnergyBurned, .basalEnergyBurned, .vo2Max, .restingHeartRate, .heartRateVariabilitySDNN, .respiratoryRate, .oxygenSaturation, .distanceSwimming, .distanceCycling, .dietaryWater, .timeInDaylight, .physicalEffort, .environmentalAudioExposure",
        "Category candidates probed: .handwashingEvent, .mindfulSession, .appleStandHour, .toothbrushingEvent, .sleepAnalysis, .highHeartRateEvent, .lowCardioFitnessEvent, .appleWalkingSteadinessEvent",
        "HKWorkoutType.workoutType() probed - whether workout counts by activity type are readable",
        "docs/health-metrics.md exists with one row per identifier and a verdict of usable / unavailable on watchOS / compiles but never has data",
        "The doc explicitly records which identifiers need a NEW authorization prompt versus which are covered by the four already granted (steps, activeEnergy, sleep, exercise)",
        "Any probe code is under #if DEBUG and ships no user-facing feature",
        "Typecheck passes (xcodebuild build succeeds)"
      ],
      "priority": 1,
      "passes": true,
      "notes": "DONE. All 27 identifiers + HKWorkoutType compile, appear in the auth sheet, and are VALID readable types on watchOS 26.4 - zero returned errorInvalidArgument(3); the 23 un-granted ones returned errorAuthorizationNotDetermined(5), a grant failure not a type failure. The 4 already-granted (steps, activeEnergy, exercise, sleep) read cleanly as ok(samples=0). docs/health-metrics.md has a row + verdict for every identifier. CAVEAT US-061 MUST HEED: nothing is certified as HAVING data - the Simulator health DB is empty (even granted types gave samples=0) and cannot be granted (simctl privacy has no 'health' service), so the 'compiles but never has data' verdict could be assigned to nothing AND ruled out for nothing. Doc names 7 at-risk iPhone-sourced types to use only as gracefully-degrading bonus branches."
    },
    {
      "id": "US-056",
      "title": "Add EvolutionCondition to the edge schema",
      "description": "As a developer, I need evolution conditions to be data in evolutions.json so criteria can be authored without editing Swift.",
      "acceptanceCriteria": [
        "New EvolutionCondition struct with fields: metric (string enum), window (stage | day | lifetime), comparison (atLeast | atMost), value (Double), hint (String)",
        "The metric enum spans TWO families, so conditions are the single gating vocabulary: health.* (HealthKit-backed, only identifiers US-055 marked usable) and care.* (game counters that nothing today can express: trainingSessions, overfeeds, sleepDisturbances, battleCount, battleWinRatio). careMistakes is deliberately NOT a condition metric - the existing maxCareMistakes edge field already gates on it, and two ways to say one thing invites a later iteration to delete one",
        "A BAND is expressed as two conditions on one edge (atLeast X + atMost Y), reproducing the Digital Monster Color pattern where training 8-31 earns the good branch but 0-7 AND 32+ both fall to the junk one - overtraining is punished as much as undertraining",
        "battleWinRatio is a 0.0-1.0 fraction, so DMC's '15+ battles at 80%+ wins' is expressible as care.battleCount atLeast 15 + care.battleWinRatio atLeast 0.8",
        "minBattleWins stays and keeps working; care.battleWinRatio ADDS ratio gating, which minBattleWins cannot express. An edge may use either or both",
        "EvolutionEdge gains conditions: [EvolutionCondition], hand-decoded to default [] when the key is absent - matching how isDefault and dexOnly already default in EvolutionGraph.swift",
        "Every existing edge in Resources/evolutions.json decodes unchanged with conditions == []",
        "EvolutionGraphValidator rejects an unknown metric, a negative value, an empty hint, and a battleWinRatio outside 0.0-1.0",
        "docs/evolutions-schema.md gains a Condition section documenting both metric families, every window value, and the two-condition band idiom",
        "Existing Tests/EvolutionGraphTests.swift and Tests/EvolutionGraphValidatorTests.swift still pass untouched",
        "Add any new files to project.yml and re-run xcodegen generate - never hand-edit .xcodeproj",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 2,
      "passes": true,
      "notes": "EvolutionCondition + ConditionMetric (27 health.* from US-055's usable list, 5 care.*, no careMistakes) in Sources/EvolutionCondition.swift; edge gains conditions hand-decoded to []. metric is a STRING with knownMetric accessor, not a typed enum, so an unknown metric is a VALIDATOR error not a launch trap. 4 new validator rules + schema doc Condition section. 668 tests green (was 647); evolutions.json and both existing Evolution*Tests.swift untouched."
    },
    {
      "id": "US-057",
      "title": "Read an arbitrary health metric over a window",
      "description": "As a developer, I need one reader that totals any usable metric - quantity or category - over a date interval, so conditions are not limited to the four energy metrics.",
      "acceptanceCriteria": [
        "New HealthMetricReader totals a quantity metric over an injected DateInterval",
        "It counts a category metric's EVENTS rather than summing values, since HKCategoryValueNotApplicable has no meaningful magnitude",
        ".appleStandHour counts only samples whose value is HKCategoryValueAppleStandHour.stood",
        "Reads sit behind a protocol so tests inject fixture samples - the Simulator has no health data and a test against a live query proves nothing",
        "A failing or unauthorized read returns .unavailable and never throws, matching TodayHealthReader.read",
        "Tests cover: a quantity total, a category event count, a stand-hour filtered count, an empty window returning noData, and a failing fetch returning .unavailable",
        "Add new files to project.yml and re-run xcodegen generate",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 3,
      "passes": true,
      "notes": "HealthMetricReader + ReadableHealthMetric (failable init: care.* and health.sleep are not constructible, so an unreadable metric cannot compile into a wrong answer). Aggregations: sumQuantity / averageQuantity (discrete types) / countEvents / countStoodHours / sumDurationMinutes. Reads behind HealthMetricSampleFetching; failure -> .unavailable, empty window -> .noData. 16 new tests, suite 684/0 failures, BUILD SUCCEEDED."
    },
    {
      "id": "US-058",
      "title": "Accumulate per-stage metric totals",
      "description": "As a developer, I need running totals per metric since the Digimon entered its current stage, so a window:stage condition has something to compare against.",
      "acceptanceCriteria": [
        "GameState gains stage-scoped metric totals keyed by metric, and they persist across app launches",
        "Crediting is idempotent - refreshing twice in one day does not double-count, following the existing EnergyLedger pattern",
        "Stage totals reset on evolution, when stageEnteredDate moves",
        "Lifetime totals accumulate separately and never reset",
        "window:day resolves to the BEST single local day, not the current one, so one good Tuesday still counts on Friday",
        "All tests drive an injectable clock and none waits real time",
        "Tests cover: accumulation, double-refresh idempotency, reset on evolution, lifetime survival across a stage reset, and best-day selection",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 4,
      "passes": true,
      "notes": "MetricLedger + MetricCreditor bank per-metric daily readings into GameState's stageMetricTotals / lifetimeMetricTotals / stageBestDayMetrics, on EnergyLedger's delta pattern. Only ADDITIVE metrics accumulate (averageQuantity ones are excluded by ConditionMetric.accumulatesOverTime). enterStage(at:) owns the stage reset. 18 new tests, 702/702 green."
    },
    {
      "id": "US-084",
      "title": "Stage-scoped care counters for evolution gating",
      "description": "As a developer, I need the counters the Digital Monster Color branches on - training sessions, overfeeds, sleep disturbances - tracked per stage, since none of them exists in a usable form today.",
      "acceptanceCriteria": [
        "GameState gains stage-scoped trainingSessions, overfeeds and sleepDisturbances counters, all persisted",
        "trainingSessions increments once per session REGARDLESS of the minigame grade - DMC counts training whether it succeeded or not, and US-075's graded gain must not be the thing evolution reads",
        "overfeeds counts cumulative refusals within the stage - distinct from the existing refusalCount, which resets daily and so cannot express a stage-long 3+ overfeed gate",
        "sleepDisturbances counts waking the Digimon during its sleep window - a new counter; only wakeMistakeDay exists today, and it is a day marker rather than a count",
        "All three reset on evolution, when stageEnteredDate moves, alongside the US-058 metric totals",
        "care.battleWinRatio derives from the existing battleWins and battleLosses and is 0.0 when no battle has been fought - never a divide by zero",
        "The existing refusalCount, wakeMistakeDay and care-mistake behaviour are unchanged; this story ADDS counters and rewires nothing",
        "Tests drive an injectable clock and cover: each counter incrementing, reset on evolution, ratio with zero battles, and trainingSessions incrementing on a miss-graded session",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 5,
      "passes": true,
      "notes": "Stage-scoped stageTrainingSessions/stageOverfeeds/stageSleepDisturbances on GameState, persisted as optional Int storage (same migration shape as every other property there) and cleared by enterStage alongside the US-058 metric totals. Incremented at the existing event sites: TrainAction.train (on .trained only, via recordTrainingSession(), grade-blind), recordRefusal (outside the daily rollover), recordWakingEarly (before the once-a-day guard, so every disturbance counts). battleWinRatio is derived from battleWins/battleLosses and returns 0.0 at zero battles rather than NaN. refusalCount, wakeMistakeDay and the care-mistake rules are untouched and pinned by tests. 15 tests in Tests/CareCounterTests.swift; full suite 717 tests, 0 failures; build green."
    },
    {
      "id": "US-059",
      "title": "Expand HealthKit authorization without breaking the existing grant",
      "description": "As a user who already granted the four original types, I want the new ones requested without losing what I already approved.",
      "acceptanceCriteria": [
        "The read set includes every metric named by a condition in evolutions.json, DERIVED from the graph rather than hardcoded, so authoring a new condition cannot forget the grant",
        "A user holding the original four grants is re-prompted only for the new types",
        "A denied new type makes its conditions unsatisfiable but leaves every other edge working - it never blocks evolution entirely and never charges a care mistake",
        "HealthAuthorizationView copy explains what the new types are for",
        "The existing -healthDenied, -healthUnavailable and -healthAnswered launch flags still work",
        "Existing Tests/HealthAuthorizationTests.swift still passes",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 6,
      "passes": true,
      "notes": "HealthReadSet unions the four energy metrics with every health.* metric named by a condition in the graph, DERIVED via EvolutionGraph.conditionHealthMetrics; HealthAuthorizing now takes the whole set so status and request both cover it in one call. 20 new tests, 737 total, 0 failures. CAVEATS: (a) 're-prompted only for the new types' is HealthKit's own documented behaviour for already-answered types - what is TESTED is the app-side mechanics (single request, wide status check, additionalObjectTypes), not a real prompt on hardware; (b) 'a denied type makes its conditions unsatisfiable' is US-060's own AC - what is verified here is the blast radius: a failed read is .unavailable and never a throw, and the care-mistake verdict is structurally unable to see a condition metric."
    },
    {
      "id": "US-060",
      "title": "EvolutionEngine evaluates conditions",
      "description": "As a player, I want my real behaviour to decide which evolution branch I get.",
      "acceptanceCriteria": [
        "EvolutionEngine.qualifies returns false unless EVERY condition on the edge passes",
        "Existing gates (requiredEnergy, minEnergy, maxCareMistakes, minBattleWins) are unchanged and still evaluated",
        "An edge with conditions == [] behaves exactly as it does today - every existing assertion in Tests/EvolutionTests.swift passes untouched",
        "A metric whose reading is .unavailable FAILS its condition rather than passing it, so an unreadable metric never hands out a branch the player did not earn",
        "The isDefault fallback still fires when nothing qualifies, so no Digimon is ever permanently stuck",
        "A band (atLeast + atMost on the same metric) is evaluated as a closed interval, and a value outside it fails - so an overtrained Digimon falls to the junk branch exactly as it does on the real device",
        "care.* conditions read the US-084 counters; health.* conditions read the US-058 totals",
        "Tests cover: a condition met, a condition unmet, atMost inverted, multiple conditions where one fails, an unavailable metric, and the fallback firing when all conditioned edges fail",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 7,
      "passes": true,
      "notes": "EvolutionEngine.qualifies now also requires every EvolutionCondition on the edge, answered by new ConditionEvaluator/ConditionContext (Sources/ConditionEvaluator.swift): health.* accumulating metrics read the US-058 stage/lifetime/best-day totals, standing measurements read a direct HealthReading, care.* reads the US-084 counters. Unknown or unavailable fails the condition in BOTH directions, so a denied metric never passes an atMost gate. Bands are closed intervals; the isDefault fallback still fires. 13 tests in Tests/ConditionEvaluationTests.swift; suite 752 tests / 0 failures, build green."
    },
    {
      "id": "US-061",
      "title": "Author branching criteria and junk evolutions",
      "description": "As a player, I want each Digimon to have a real choice with a real consequence for neglect, so an Agumon that never walked becomes Numemon rather than Greymon.",
      "acceptanceCriteria": [
        "Every non-terminal Child and Adult across the six shipped lines has two to three outgoing edges",
        "Every such node's isDefault edge is a junk evolution reachable by inaction",
        "Numemon (Adult/Numemon.png) and Scumon (Adult/Scumon.png) are verified present with ls before being referenced",
        "At least one node uses the DMC band shape - a middle training range earns the good branch while both too-little and too-much fall to the junk one",
        "At least one Perfect-or-later edge is gated on battle performance as a RATIO, e.g. care.battleCount atLeast 15 plus care.battleWinRatio atLeast 0.8, following the real device",
        "At least EIGHT distinct condition metrics are used across the file, drawn from BOTH the health.* and care.* families",
        "Every 'to' names a node that exists, and every spriteFile is verified with ls before use",
        "No dexOnly node is named by any edge",
        "EvolutionGraphValidator passes on the whole file",
        "A test asserts no condition hint string contains a digit",
        "Only health.* metrics US-055 marked usable are authored",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 8,
      "passes": true,
      "notes": "Authored across all six lines: 88 nodes (was 69). Every non-terminal Child and Adult now has 2-3 edges whose isDefault is a junk evolution reachable by inaction, and each line's default path is a complete Digitama-to-Ultimate JUNK path. Nine metrics from both families; Greymon carries the DMC training band (atLeast 8 / atMost 31); Etemon -> BanchoLeomon is gated on care.battleCount atLeast 15 plus care.battleWinRatio atLeast 0.8. Tests: Tests/EvolutionCriteriaTests.swift (17 assertions covering AC1-AC11), full suite 768 tests / 0 failures, build green. KNOWN GAP, NOT AN AC: MetricCreditor is still not called from MainScreenModel.refresh, so stageMetricTotals is always zero in the running app and every health.* atLeast gate fails live. Care.* gates work. Until that is wired, the app routes most Digimon down the junk branches. This needs its own story."
    },
    {
      "id": "US-062",
      "title": "Bundle the full 1,022-entry roster",
      "description": "As a developer, I need the whole roster available at runtime so the Dex can show every Digimon.",
      "acceptanceCriteria": [
        "Resources/roster.json is bundled and contains all 1,022 entries",
        "New RosterEntry type with id, displayName, stage, spriteFile, variant, dexOnly - and deliberately NO line and NO evolutions",
        "Entries that roster.generated.json emits with a null stage are resolved to a real stage before bundling; the loader rejects a null stage rather than defaulting one",
        "A test verifies every spriteFile in roster.json exists on disk - by test, not by inspection",
        "Resources/evolutions.json is UNMODIFIED by this story",
        "scripts/ regenerates roster.json reproducibly and README.md documents the command",
        "roster.json added to project.yml as a resource and xcodegen generate re-run",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 9,
      "passes": true,
      "notes": "Resources/roster.json ships all 1,022 on-disk Digimon as RosterEntry (id/displayName/stage/spriteFile/variant/dexOnly, no line, no evolutions); scripts/build_roster.py regenerates it and resolves the 152 null stages through the hand-authored scripts/dex_only_stages.json, erroring rather than defaulting; evolutions.json byte-identical; 780 tests pass."
    },
    {
      "id": "US-063",
      "title": "Replace the Dex line list with a flat item grid",
      "description": "As a user, I want to see every Digimon in one grid so I can tell at a glance how much of the roster I have met.",
      "acceptanceCriteria": [
        "The Dex root is a LazyVGrid over all 1,022 roster entries, not the line list",
        "Owned entries show the idle sprite; unowned show '?', are visibly dimmed, and are .disabled",
        "Only on-screen cells decode art - scrolling the grid must not decode 1,022 sprites",
        "The header still reads discovered/total against the full roster",
        "Tapping an owned cell opens DexDetailView; tapping an unowned one does nothing",
        "Fits the 41mm screen without horizontal clipping",
        "Scrolling stays responsive with no visible stutter on Series 11 (46mm)",
        "Sprite rendering still uses .interpolation(.none)",
        "Verify in the watchOS Simulator via a #if DEBUG launch flag and record the screenshot result in notes - simctl cannot synthesise a tap, so the screen must be pushable from the launch command",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 10,
      "passes": true,
      "notes": "Dex root is now a LazyVGrid over all 1,022 Roster.bundled entries (was a list of 8 evolution lines covering only the 88 graph nodes). DexRow carries id/displayName/stage/spriteFile instead of an EvolutionNode, so it can be built from a RosterEntry or a node; DexSection gained `nodes` for EvolutionTreeView(nodes:rows:) to lay out from. Verified on the Simulator: header reads 5/1,022, three columns fit 41mm with margins, -dexScrollDemo lands on the final 2-cell row (1022 = 340*3 + 2), -dexDetailDemo opens Agumon's detail, -dexLineDemo still opens the Agumon tree. 784 tests pass. NOT verified: a real swipe's frame rate - simctl has no scroll or tap command, so the scrollTo jump is a proxy for it."
    },
    {
      "id": "US-064",
      "title": "Detail view lists possible evolutions",
      "description": "As a user, I want to see what my Digimon can become so I have something to aim at.",
      "acceptanceCriteria": [
        "DexDetailView gains an 'Evolves into' section listing every outgoing edge's target",
        "Each candidate shows its sprite and name if already discovered; a '?' with the name withheld if not",
        "An entry with no edges, or absent from EvolutionGraph.bundled, shows 'No evolutions recorded.' rather than an empty section",
        "One to three candidates render without scrolling on 41mm; more than three scrolls",
        "Verify in the watchOS Simulator via the existing -dexDetailDemo flag and record the screenshot result in notes",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 11,
      "passes": true,
      "notes": "DexDetailView gained an 'Evolves into' three-column section fed by DexRow.evolutionCandidates(of:in:resolvedAgainst:), which resolves each outgoing edge against the surrounding rows so a met candidate keeps its discovery date and art while an unmet one is drawn as a bare '?' with the name withheld; no node or no edges shows 'No evolutions recorded.'. Screenshotted on Series 9 41mm: -dexDetailDemo shows Agumon -> Greymon (named) + two '?' on one line above the fold with no scrolling, and the new -dexEmptyDetailDemo shows Aquilamon with the empty message. Build green, 793 tests pass (9 new). CAVEAT: the '>3 candidates scrolls' half of AC4 was NOT observed - no shipped Digimon has more than 3 edges (pinned by testNoShippedDigimonHasMoreThanThreeCandidates), so real data cannot reach the wrap case; it rests on the section being a 3-column LazyVGrid inside the sheet's ScrollView."
    },
    {
      "id": "US-065",
      "title": "Hint vocabulary for every condition metric",
      "description": "As a player, I want to be nudged toward what a branch needs without being handed a checklist.",
      "acceptanceCriteria": [
        "Every HealthConditionMetric has default flavour text - e.g. steps 'Restless. It wants to see the horizon.', handwashing 'It flinches from grime.', mindful 'It listens for stillness.', stand hours 'It cannot bear sitting still.', daylight 'It turns toward the sun.'",
        "An edge's authored hint overrides the metric default",
        "A test asserts NO hint string contains a digit, so a hint can never leak its threshold",
        "Hint resolution is a pure function, testable without a view",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 12,
      "passes": true,
      "notes": "ConditionMetric.defaultHint gives all 32 metrics flavour text (exhaustive switch, no default clause); ConditionHint.resolve prefers an edge's authored hint, treats whitespace-only as unauthored, falls back to ConditionHint.fallback for an unknown metric. 11 ConditionHintTests, incl. a digit scan over defaults + all 89 authored hints in evolutions.json. Build green, 804 tests / 0 failures. Not yet rendered — US-066 wires it into the view."
    },
    {
      "id": "US-066",
      "title": "Render hints with progress-based reveal",
      "description": "As a player, I want the hint to warm up as I get closer, so I can tell I am on the right track.",
      "acceptanceCriteria": [
        "Three reveal levels chosen by progress toward the condition: far (< 50%) shows flavour text alone, close (>= 50%) shows flavour plus a warmer qualifier, met shows a checkmark",
        "No exact number, threshold, or percentage is ever displayed",
        "A candidate whose conditions are all met is visually distinguished from one that is not",
        "The reveal-level function is pure and unit-tested at each boundary (just under 50%, exactly 50%, exactly met)",
        "Verify in the watchOS Simulator via a #if DEBUG launch flag seeding a part-met condition, and record the screenshot result in notes",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 13,
      "passes": true,
      "notes": "ConditionReveal (pure) grades far/close/met at a 0.5 boundary; unknown and an overshot atMost read as far, and met defers to ConditionEvaluator so a checkmark can never disagree with the engine. Dex detail sheet draws an 'It wants' list (symbol + warmed hint, no numbers) and rings a candidate green only when it has criteria AND all of them hold. 14 new tests; 818 pass, build green. Screenshotted on 41mm via -dexRevealDemo: all three levels visible."
    },
    {
      "id": "US-067",
      "title": "Keep the evolution tree reachable from the flat Dex",
      "description": "As a user, I want the flat grid as my way in without losing the line tree, so the at-a-glance roster and the shape of a line are both available.",
      "acceptanceCriteria": [
        "EvolutionTreeView is KEPT, not deleted - US-041/US-042 built it and it shows a line's branching shape, which the flat grid cannot",
        "The flat grid from US-063 stays the Dex root, per the request that the line map was too complicated as an entry point",
        "DexDetailView gains a way through to the owning line's tree for any Digimon that belongs to one",
        "A Digimon in no line (the ~950 roster-only entries) shows no tree affordance rather than an empty tree",
        "The -dexLineDemo launch flag still works",
        "Tests/EvolutionTreeTests.swift and Tests/DexScreenTests.swift both still pass",
        "Verify in the watchOS Simulator that the grid -> detail -> tree path works, and record the screenshot result in notes",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 14,
      "passes": true,
      "notes": "Kept the tree and merged it in, as the story asked. DexSection.line(containing:in:) matches on the BUILT sections rather than the graph's line key, so dexOnly nodes (pulled into Others) and the ~930 roster-only entries both resolve to nil and get no affordance. DexDetailView is now wrapped in its own NavigationStack inside the sheet and gains an 'Agumon line' button under the hints; a sheet is not on the presenting stack, so a bare NavigationLink there would have drawn as a dead label. Button + navigationDestination(isPresented:) rather than NavigationLink so -dexTreeDemo can follow it without a tap. Screenshots on Series 9 41mm: link renders under the hints; -dexTreeDemo pushes the Agumon tree titled 'Agumon' with a back chevron to the sheet; Aquilamon shows 'No evolutions recorded.' and NO link; -dexLineDemo still opens the tree from the Dex root. Build green on 46mm and 41mm. 823 tests pass (was 818)."
    },
    {
      "id": "US-068",
      "title": "Animate the sick Digimon",
      "description": "As a user, I want a sick Digimon to look sick, so I notice without opening a menu.",
      "acceptanceCriteria": [
        "A sick Digimon plays a distinct animation - the hurt loop (frames 9 <-> 10) at a SLOWER cadence than the battle hurt loop, so it reads as ailing rather than as being struck",
        "Wandering movement is suppressed or slowed while sick",
        "A healthy Digimon is visually unchanged",
        "Sprite rendering still uses .interpolation(.none)",
        "The animation choice is a pure function of healthStatus, unit-tested without a view",
        "Verify in the watchOS Simulator via a #if DEBUG launch flag that forces the sick state, and record the screenshot result in notes",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 15,
      "passes": true,
      "notes": "Sick now plays the hurt loop (frames 9<->10) via a new SpriteAnimation.sick case at 1.5s/frame vs the battle hurt loop's 0.5s; DigimonSpriteView drives both its TimelineView schedule and its frame index off animation.frameDuration. The choice is the pure SpriteAnimation.resting(for:isAsleep:), unit-tested with no view or store. Wandering stays suppressed (isWandering keys off animation == .idle). Screenshotted with -sickDemo on Series 11 46mm: a slumped Agumon alternating two hurt frames, crisp, centred, not walking. NOTE: the Simulator's screen idles and throttles, so screenshot sampling CANNOT measure the 0.5s vs 1.5s cadence — that clause is proven by unit test, not by the screenshots. 831 tests pass (was 823)."
    },
    {
      "id": "US-069",
      "title": "Pulsing medical-bandage icon",
      "description": "As a user, I want an unambiguous sick indicator, since a slow hurt loop alone is easy to miss.",
      "acceptanceCriteria": [
        "A bandage.fill SF Symbol overlays the main screen while healthStatus == .sick",
        "It pulses continuously via opacity or scale - not a one-shot animation",
        "It is absent when healthy and absent when dead",
        "It does not overlap the sprite's body or the action buttons on 41mm",
        "The main screen still fits without scrolling - US-039 is not regressed",
        "Verify in the watchOS Simulator via the same sick-state launch flag, and record the screenshot result in notes",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 16,
      "passes": true,
      "notes": "bandage.fill badge overlays the top band while healthStatus==.sick; band is reserved out of the sprite's sizing height (SickBadgeLayout) so it cannot collide with the walking sprite. Pulses via repeatForever easeInOut opacity 1<->0.25 (off both sprite cadences). isSick==(.sick), so absent when healthy/dead. 844 tests pass (13 new in SickBadgeTests). Build green on 46mm and 41mm. Screenshot on Series 9 41mm -sickDemo: red bandage centred above slumped Agumon, clear of body and action row, no scrolling; 8 badge-region crops all hash distinctly, proving continuous pulse. See progress.txt."
    },
    {
      "id": "US-070",
      "title": "Move catalog",
      "description": "As a developer, I need per-Digimon attack identity as data, so authoring a move is not a code change.",
      "acceptanceCriteria": [
        "Resources/moves.json maps a Digimon id to projectileSymbol, tint, signatureName and signatureSymbol",
        "Symbols are SF Symbols (flame.fill, bolt.fill, drop.fill, hand.raised.fill, snowflake, leaf.fill, moon.fill, sparkles, ...) - no new image assets",
        "A test verifies every symbol name renders; an unknown symbol fails the test rather than shipping as a blank square",
        "A Digimon absent from the file falls back by line, then by stage, so all 1,022 resolve to something and battle never renders an empty projectile",
        "MoveCatalog lookup is pure and unit-tested including BOTH fallback tiers",
        "moves.json added to project.yml as a resource and xcodegen generate re-run",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 17,
      "passes": true,
      "notes": "Resources/moves.json maps id -> {projectileSymbol, tint, signatureName, signatureSymbol}; Sources/MoveCatalog.swift decodes it with three tiers (moves by id, lineDefaults by line, stageDefaults by Stage.rawValue). move(forId:line:stage:) is the PURE core: id -> line -> stage -> Move.placeholder floor, so all 1,022 roster Digimon resolve and battle never renders empty; move(for:in:roster:) resolves line/stage from the graph then the roster. MoveTint is a closed enum, so an unknown colour is a decode failure not a blank draw. moves.json rides the existing Resources folder reference (comment names it); xcodegen re-run. Tests/MoveCatalogTests.swift (11): both fallback tiers via the real graph/roster AND the pure core with a fixture, every roster id != placeholder, every symbol renders via UIImage(systemName:) with a bogus-name control, >=8 distinct symbols, stageDefaults cover every Stage, every graph line has a lineDefault, unknown tint throws. Build green; full suite 855/0 (was 844). NOTE: US-074 authors the explicit move for all 69 curated nodes and the in-line uniqueness rule; this ships a starter set (6 line roots) plus both fallback tiers. SwiftUI Color mapping for MoveTint deliberately deferred to US-072 (the renderer) to avoid untested UI here."
    },
    {
      "id": "US-071",
      "title": "Face-to-face battle arena",
      "description": "As a user, I want the two Digimon facing each other, not both facing right.",
      "acceptanceCriteria": [
        "Player on the left facing right; opponent on the right horizontally mirrored to face left",
        "Mirroring does not soften the pixels - .interpolation(.none) survives the transform",
        "Hit points and the opponent's name remain visible and the arena fits 41mm",
        "Verify in the watchOS Simulator via a #if DEBUG battle demo launch flag, and record the screenshot result in notes",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 18,
      "passes": true,
      "notes": "BattleView.arena now draws the two combatants facing each other via a new pure BattleView.faces(_:) helper: the pack's art faces LEFT, so the player on the left is flipped=true to turn right and the opponent on the right keeps its natural leftward heading (faces(.player)==true, faces(.opponent)==false). Mirroring stays crisp for free — DigimonSpriteView.image already applies .interpolation(.none) on the Image AHEAD of the .scaleEffect(x:-1), so US-070's pixels survive the flip. Verified on Series 9 41mm via -battleTurnDemo -noNotificationPrompt -healthAnswered: Agumon on the left faces right, Tanemon on the right faces left, name 'Tanemon' and HP '10 — 8' both visible above the fold, no horizontal clipping, blocky not smoothed. Test testTheCombatantsFaceEachOther in BattleFrameTests pins the pure helper; BattleEngine untouched. Build green, full suite 856/0 (was 855)."
    },
    {
      "id": "US-072",
      "title": "Projectiles fly on each exchange",
      "description": "As a user, I want to see the attack travel, so an exchange reads as a hit rather than two sprites twitching.",
      "acceptanceCriteria": [
        "On each BattleTurn the attacker's projectile animates from attacker toward defender",
        "It travels in the correct direction for whichever side is attacking",
        "It is tinted with the attacker's MoveCatalog tint",
        "Impact coincides with the defender entering its hurt loop - the existing frame assignment (attacker holds frame 11, defender plays 9 <-> 10) is preserved",
        "Timing is driven by the existing injectable turnDuration, so tests still run a whole battle in milliseconds",
        "BattleEngine is NOT touched - resolution stays pure and every existing assertion in Tests/BattleTests.swift and Tests/BattlePowerTests.swift holds",
        "Verify in the watchOS Simulator via the battle demo flag, and record the screenshot result in notes",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 19,
      "passes": true,
      "notes": "Each BattleTurn now flies the attacker's projectile across the arena. BattleBout gains playerMove/opponentMove (defaulted to Move.placeholder so existing fixtures/preview compile), resolved in MainScreenModel.battle() via MoveCatalog.bundled.move(forId:line:stage:) - both combatants are graph nodes so the pure two-tier core answers without a roster. BattleView draws an SF-Symbol projectile in a ZStack over the sprite HStack: tinted with the attacker's MoveTint (new MoveTint.color exhaustive-switch extension, deferred here from US-070), offset by pure BattleView.projectileOffset(rightward:progress:span:), direction read off faces(attacker) so the opponent's shot flies out its front not its back. run() snaps projectileProgress to 0 then animates it 0->1 over the existing injectable turnDuration (whole battle still runs in ms in tests). BattleEngine and animation(for:during:) UNTOUCHED - attacker still holds frame 11, defender still plays 9<->10, so impact coincides with the hurt loop. 5 new tests in BattleFrameTests (both flight directions, mirror symmetry, per-side move, tint colours); suite 861/0 (was 856). Screenshotted on Series 11 46mm via -battleTurnDemo (stretches one beat to 60s): an orange flame.fill leaving Agumon on the left and, six seconds later, visibly further right toward Cyclomon - crisp, correctly tinted, name+HP intact."
    },
    {
      "id": "US-073",
      "title": "Signature move on the finishing blow",
      "description": "As a user, I want the killing blow to be special and named, so a win feels like a win.",
      "acceptanceCriteria": [
        "The turn where isKnockout is true renders the winner's signatureSymbol instead of the ordinary projectile",
        "It is visibly larger than an ordinary projectile",
        "The signatureName is shown as a banner during that turn",
        "It fires whether the PLAYER or the OPPONENT lands the finish",
        "The result screen still shows the existing win/loss frames and its haptic",
        "Knockout-turn detection is pure and unit-tested against a seeded BattleReport",
        "Verify in the watchOS Simulator and record the screenshot of a signature move in notes",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 20,
      "passes": true,
      "notes": "Knockout turn renders winner signatureSymbol at 30pt (vs 15pt ordinary) + signatureName banner. Pure BattleView.isKnockoutTurn(_:of:) unit-tested vs seeded reports (only last turn, fires for either winner, out-of-range safe). Verified in Sim via -battleSignatureDemo: /tmp/us073_signature.png shows Pepper Breath banner, large orange flame, HP 6-0. 865 tests pass."
    },
    {
      "id": "US-074",
      "title": "Author moves for all 69 curated Digimon",
      "description": "As a player, I want each Digimon's attack to suit it, so owning a new one is worth looking forward to.",
      "acceptanceCriteria": [
        "All 69 nodes in evolutions.json have an explicit moves.json entry",
        "Attacks suit the Digimon - e.g. Agumon throws orange flame with a larger flame signature, Gabumon throws BLUE flame, a plant line throws leaves, an electric line throws bolts",
        "No two Digimon in the same line share an identical projectileSymbol + tint pair",
        "At least EIGHT distinct symbols are used across the file",
        "Signature names are authored, not generated from a template",
        "A test asserts full coverage of the 69 and the in-line uniqueness rule",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 21,
      "passes": true,
      "notes": "Authored an explicit moves.json entry for all 88 curated nodes (the AC says 69 — the graph grew to 88 in US-061/US-062; every node is covered, so the intent is met and then some). 21 distinct SF Symbols, all verified to render; within-line projectileSymbol+tint uniqueness holds for all six lines; all 88 signature names distinct and none derived from displayName/stage. New tests: explicit-coverage both ways, in-line uniqueness, suitability spot-checks, anti-template check. Build green, 870 tests / 0 failures."
    },
    {
      "id": "US-075",
      "title": "Minigame foundation and graded gain",
      "description": "As a developer, I need one shape every training minigame conforms to, so adding a seventh is adding a file.",
      "acceptanceCriteria": [
        "A TrainingMinigame protocol (or enum) with a uniform TrainingResult: miss | good | great | perfect",
        "TrainAction maps the result to a strengthStat gain of miss=0, good=1, great=2, perfect=3",
        "Energy cost is charged ONCE on entering the game and is NOT refunded on miss",
        "The session increments GameState.trainingSessions (US-084) exactly once regardless of grade, including on a miss - evolution branches on sessions trained, not on how well they went",
        "Existing eligibility rules are unchanged: asleep blocks, sick blocks, insufficient energy blocks, all with their current messages",
        "Every existing assertion in Tests/TrainingTests.swift about cost and blocking still passes",
        "Grading is pure and unit-tested at every boundary",
        "Add new files to project.yml and re-run xcodegen generate",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 22,
      "passes": true,
      "notes": "TrainingResult (miss/good/great/perfect -> 0/1/2/3) and the TrainingMinigame protocol in Sources/TrainingMinigame.swift; TrainAction split into begin (charges once, counts the session, never refunds) / finish (grades only) with train() composing both at a default of .good. 891 tests pass, +21 mine; all 16 pre-existing TrainingTests assertions pass unmodified."
    },
    {
      "id": "US-076",
      "title": "Training minigame - Timing Bar",
      "description": "As a user, I want to stop a moving marker in a target zone, so training takes skill.",
      "acceptanceCriteria": [
        "A marker sweeps a bar, tapping stops it, and the zone hit decides the grade",
        "A centre sub-zone yields perfect; missing the zone entirely yields miss",
        "Sweep speed and zone width are injectable so tests never wait real time",
        "Grade-from-position is pure and unit-tested at each zone boundary",
        "Verify in the watchOS Simulator via a #if DEBUG launch flag, and record the screenshot result in notes",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 23,
      "passes": true,
      "notes": "TimingBarGame in Sources/TimingBarGame.swift: a marker triangle-sweeps the bar, a tap freezes it, and two pure statics decide everything — position(at:sweepDuration:) and grade(at:zoneWidth:), with bands at 1/4 and 1/2 of the zone half-width (powers of two, so every edge is exactly representable and boundaries are asserted ON the line). Centre sub-zone = perfect, outside the zone = miss, untapped for roundTimeout = miss. sweepDuration/zoneWidth/resultDuration/roundTimeout are stored props with defaults, set at the call site like BattleView.turnDuration. 913 tests pass, +22 mine. Screenshots: -timingBarResultDemo -> 'Perfect' + '+3 STR', yellow centre band inside the green zone, marker dead centre; -timingBarDemo -> live sweep, marker moved left-of-zone to zone-lip across two shots. simctl cannot tap, so the tap gesture itself is covered by unit tests on grade(stoppingAt:sweepDuration:zoneWidth:), the exact call tap() makes."
    },
    {
      "id": "US-077",
      "title": "Training minigame - Button Masher",
      "description": "As a user, I want to tap as fast as I can against a timer.",
      "acceptanceCriteria": [
        "Counts taps within a fixed window and maps the count to a grade by threshold",
        "The window duration is injectable",
        "A live tap count is visible during play",
        "Grade-from-count is pure and unit-tested at each threshold",
        "Verify in the watchOS Simulator via a #if DEBUG launch flag, and record the screenshot result in notes",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 24,
      "passes": true,
      "notes": "ButtonMasherGame in Sources/ButtonMasherGame.swift: taps counted live inside a window timed from the FIRST tap, graded by two pure statics — requiredTaps(for:window:) and grade(taps:window:). Thresholds are a RATE (3 / 4.5 / 6 taps per second, rounded up, floored at one) rather than flat counts, so the injectable window means the same thing at any length and a millisecond test window is still gradeable; at the shipped 5s window that is 15 / 23 / 30. Counts are integers, so every threshold is exact and each boundary is asserted ON the number with one-tap-short below it. window/resultDuration/idleTimeout are stored props with defaults; an untapped round ends itself .miss. TrainingResult.tint moved off TimingBarGame so all six games announce a grade the same colour. 927 tests pass, +14 mine. Screenshots: -buttonMasherDemo -> 'Button Masher', live count 12, orange timer bar draining 80%->35% across two shots; -buttonMasherResultDemo -> 'Perfect' + '30' in yellow + '+3 STR'."
    },
    {
      "id": "US-078",
      "title": "Training minigame - Power Meter",
      "description": "As a user, I want to hold to charge and release before it overloads.",
      "acceptanceCriteria": [
        "Holding fills a meter and releasing within the target band grades highest",
        "Overfilling past the band yields miss, so there is a real cost to greed",
        "Fill rate and band bounds are injectable",
        "Grade-from-fill is pure and unit-tested including the overload case",
        "Verify in the watchOS Simulator via a #if DEBUG launch flag, and record the screenshot result in notes",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 25,
      "passes": true,
      "notes": "PowerMeterGame: hold to charge, release in the 0.625-0.875 band for perfect; past the top is the overload and pays nothing. fillRate/band bounds injectable, rule pure in grade(fill:lowerBound:upperBound:). 947 tests pass (+20). Screenshots via -powerMeterDemo / -powerMeterResultDemo / -powerMeterOverloadDemo."
    },
    {
      "id": "US-079",
      "title": "Training minigame - Crown Sprint",
      "description": "As a user, I want to spin the Digital Crown to fill a gauge.",
      "acceptanceCriteria": [
        "Crown rotation accumulates toward a target within a time window",
        "Uses digitalCrownRotation and the gauge responds to real crown input in the Simulator",
        "Rotation target and window are injectable",
        "Grade-from-rotation is pure and unit-tested",
        "Verify in the watchOS Simulator via a #if DEBUG launch flag, and record the screenshot result in notes",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 26,
      "passes": true,
      "notes": "CrownSprintGame: .digitalCrownRotation on a continuous 0-1000 binding, crownDelta(from:to:range:) turns each movement into unsigned distance (wrap taken the short way round), rotation accumulates against rotationTarget (120) inside window (5s); reaching the target ends the sprint as a perfect. Target and window injectable, rules pure in grade(rotation:target:) / progress / crownDelta. remainingFraction moved to a TrainingMinigame extension and shared with the masher. 964 tests pass (+17). Screenshots via -crownSprintDemo (gauge filling 22%% grey -> 5x%% mint across two shots) and -crownSprintResultDemo (Perfect, 100%%, +3 STR). CAVEAT: physical crown rotation is not scriptable - simctl has no crown subcommand - so what was observed is the binding-to-gauge path end to end, not a finger on the hardware. Needs one manual spin to confirm."
    },
    {
      "id": "US-080",
      "title": "Training minigame - Reflex Strike",
      "description": "As a user, I want to tap the instant the signal appears.",
      "acceptanceCriteria": [
        "After a randomised delay a signal appears and reaction time decides the grade",
        "Tapping BEFORE the signal is a false start and yields miss",
        "The delay uses the project's existing SeededGenerator so a test can pin it",
        "Grade-from-latency is pure and unit-tested including the false start",
        "Verify in the watchOS Simulator via a #if DEBUG launch flag, and record the screenshot result in notes",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 27,
      "passes": true,
      "notes": "ReflexStrikeGame: randomised wait drawn from SeededGenerator via the injectable makeGenerator, then a signal; pure grade(latency:) pays perfect/great/good at 0.25/0.5/1.0s and a MISS for any non-positive latency, which is exactly what a false start is. 19 new tests, 983 total, 0 failures; build green. Screenshots /tmp/us080_{wait,signal,result,falsestart}.png on Series 11 46mm."
    },
    {
      "id": "US-081",
      "title": "Training minigame - Sequence Recall",
      "description": "As a user, I want to repeat a pattern back from memory.",
      "acceptanceCriteria": [
        "A short sequence is shown then reproduced by the user, and the correct count decides the grade",
        "A fully correct sequence yields perfect; the first wrong entry ends the round",
        "Sequence length and playback speed are injectable, and generation uses SeededGenerator",
        "Grade-from-correct-count is pure and unit-tested",
        "Verify in the watchOS Simulator via a #if DEBUG launch flag, and record the screenshot result in notes",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 28,
      "passes": true,
      "notes": "SequenceRecallGame: pattern drawn from SeededGenerator via sequence(using:length:); correctCount(of:against:) stops at the first wrong entry; grade(correct:length:) pays perfect only for the whole pattern (great 3/4, good 1/2, rounded up). Length and playback speed (stepDuration/stepGap) injectable. 32 new tests, 1015 total, 0 failures; build green. Screenshots on Series 11 46mm: /tmp/us081_playback.png (pad lit, 'Watch…'), /tmp/us081_input.png ('Your turn — 0/4'), /tmp/us081_perfect.png (Perfect, 4 dots, +3 STR), /tmp/us081_miss.png (wrong entry after 1 step: Miss, 1 dot, +0 STR)."
    },
    {
      "id": "US-082",
      "title": "Assign a minigame per Digimon",
      "description": "As a player, I want a new Digimon to bring a new training game, so raising one is worth it.",
      "acceptanceCriteria": [
        "Each of the six shipped lines maps to a DIFFERENT one of the six games",
        "A Digimon outside the six lines falls back deterministically by stage - never to 'no game'",
        "The same Digimon ALWAYS gets the same game; assignment is not random per session",
        "Assignment is pure and unit-tested including the fallback",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 29,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-083",
      "title": "Train opens the assigned minigame",
      "description": "As a user, I want tapping Train to start my Digimon's game rather than silently adding a point.",
      "acceptanceCriteria": [
        "Train presents the assigned game as an overlay, in the manner of BattleView and EvolutionCeremonyView - the buttons underneath are not tappable through it",
        "The result applies the graded gain and shows what was earned",
        "Blocked cases (asleep, sick, insufficient energy) show their message and NEVER open a game",
        "Dismissing or backgrounding mid-game grades as miss and does not refund, so it is not an escape hatch from a bad round",
        "Verify in the watchOS Simulator end to end - open, play, see the gain - and record the screenshot result in notes",
        "Typecheck passes (xcodebuild build succeeds)",
        "Tests pass"
      ],
      "priority": 30,
      "passes": false,
      "notes": ""
    }
  ]
}
