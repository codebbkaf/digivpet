# Ralph Progress Log
Started: 2026-07-22
Feature: Adventure Maps, Multi-Digimon Party, Jogress and Full Roster Wiring
         (ralph/adventure-maps-party-and-jogress)
Source PRD: tasks/prd-adventure-maps-party-and-jogress.md
Previous wave archived to: archive/2026-07-22-animation-quality-and-play-rules/

56 stories, US-114 .. US-169, in five phases:
  A  US-114..115  screen layout (light button to the toolbar, map background)
  B  US-116..122  the map system (catalog, validator, step accrual, list, strip, detail, opponents)
  C  US-123..129  owning several Digimon (profile + migration, multi-state, freeze, party, drops)
  D  US-130..132  Jogress (data + validator, recipe authoring, performing one)
  E  US-133..169  roster wiring: 11 device trees, then 26 orphan sweeps

Measured baseline at the start of this wave (regenerate with Appendix B of the PRD):
  1025 roster entries; 868 playable, 157 dexOnly
  88 nodes in Resources/evolutions.json across 6 lines
  780 playable orphans - Digitama 51, Baby I 38, Baby II 42, Child 109,
                         Adult 168, Perfect 153, Ultimate 203, Armor-Hybrid 16
  57 Digitama sprites, 6 of them wired
---

## Codebase Patterns
- **CONSUMPTION WAVE: A "SPENDABLE CHARGE" HAS FIVE SEAMS, COPY THE BATTLE CHARGE (US-176) FOR EACH.**
  (1) GameState optional storage `xChargesStorage`/`xProgressStorage` + computed getters (nil reads 0)
  + `creditXCharges(amount:per:maxCharges:)` — the while-loop-with-remainder-dropped-at-cap shape;
  (2) ConsumptionConfig `maxXCharges` in the struct AND consumption.json AND the `caps` validator list
  AND ConsumptionConfigTests' 4 constructors; (3) refresh() claims the day-total off `MetricLedger`
  via `metricLedger.claim(.healthMetric, dayTotal:, now:, calendar:)` — the SHARED delta, read from
  `dayReadings.quantities[.<QuantityMetric>]` (steps/activeEnergy/exercise), never a second HealthKit
  read; (4) a `DashBar(...dashHeight:5)` added to ContentView's currency HStack — SAME row, so sprite
  scale (US-172/US-174) is untouched; (5) `model.xCharges`/`xChargeCap` computed off `state`/config.
  US-177 (train charge from active calories) mirrored all five off US-176.
- **CHANGING AN ACTION'S CURRENCY RIPPLES TO EVERY TEST THAT FUNDED IT.** US-177 moved training off
  Strength/Stamina energy onto a charge; ~12 test files seeded strength energy to fund training and
  had to seed `trainCharges` instead — the 0-charge default silently BLOCKS the round, and a blocked
  round counts no session, so `stageTrainingSessions` assertions flip too. grep
  `TrainAction.begin|TrainAction.train(|\.train()` before assuming a fixture is unaffected. Same trap
  will hit feeding/cleaning if their currency is ever repriced.
- **WHEN ACTION A BORROWS ACTION B's CONSTANTS AND B CHANGES, DECOUPLE A FIRST.** `BattleCost`
  borrowed `TrainAction.energyCostPerTraining`/`payableWith`/`insufficientEnergyReason`; US-177
  removed those from `TrainAction`, breaking Battle.swift's compile. Gave BattleCost its own copies.
  `BackgroundRefreshTests`' refresh-level class is named `ClosedAppRecomputeTests` (not the file name).
- **PHASE E, ULTIMATE SWEEP: RE-RUN `xcodegen generate` AFTER WRITING THE NEW TEST FILE, OR IT NEVER
  RUNS.** US-166 ran `xcodegen generate` FIRST (to pick up the JSON) then wrote
  `UltimateSweepIToMTests.swift` — the project glob had already been captured, so `build-for-testing`
  compiled the whole target GREEN and the suite went 0 failures while the new class was simply not in
  the bundle (its Test Suite line never appeared, test count did not rise). Generate AGAIN after the
  file exists; confirm with `grep -c <ClassName> DigiVPet.xcodeproj/project.pbxproj` and by seeing the
  count climb.
- **PHASE E, ULTIMATE RUNG: A JOGRESS-RESULT ULTIMATE IS WIRED UNLESS A DEVICE TREE RESERVES IT TO
  NO-NODE (the Cernumon precedent, US-164).** `jogress.json` naming a result is NOT a reason to skip
  it — `cernumon`, `aegisdramon`, `millenniumon` are all jogress results AND wired nodes. What makes
  C-D set Chaosdramon/Chaosmon aside is that `DMCVersion5/4TreeTests` and `PendulumMetalEmpireTreeTests`
  pin them to NO evolution node. US-166 wired Mastemon (directly off Angewomon, both jogress parents
  being Perfects) and Mitamamon (Cernumon shape, off Garudamon, its jogress parents both Ultimates)
  because no tree reserved either. `UltimateSweepCToDTests.jogressReachable` is the set of the C-D
  results left unwired, and its "true remainder" assertions (64->37 here) are relative to the whole
  current bucket, so every later sweep moves them.
- **PHASE E, ULTIMATE RUNG: A LATER SWEEP THAT FORKS A PERFECT AN EARLIER SWEEP TREATED AS A LEAF
  BREAKS TWO SHAPES, NOT ONE.** US-166 branched Megalo Growmon, which US-163/A-B had cleared as a
  LEAF (its only edge the isDefault climb to Breakdramon). That fails `UltimateSweepAToBTests`
  `testEveryEarnedBranchCarriesCriteriaAndEveryLeafClimbDoesNot` (`evolutions.count == 1` — special-
  case it) AND `testTheFiveClearedLeavesClimbForADigimonThatEarnedNothing`, which read the climb as
  `evolutions.first` — that is now an EARNED branch, so change it to `first(where: \.isDefault)`. Grep
  every UltimateSweep file's `leafParents` for a parent you intend to branch, not just the PerfectSweep
  `branchedBySweeps` dicts.
- **PHASE E, ULTIMATE RUNG: ONE ORPHAN COSTS ONE NODE, AND THAT IS THE WHOLE STORY.** The rung is
  TERMINAL, so no out-edge is owed; `EvolutionCriteriaTests.branchingNodes` is Child and Adult only,
  so a forked Perfect owes no junk floor; and every line but `algomon` already has a Perfect, so
  none can be opened. US-163 wired thirty orphans with thirty nodes. The cost is entirely knock-on.
- **PHASE E: FORKING A PERFECT IS THE NEW BREAKAGE, AND TWO GREPS FIND IT.** Every Perfect carried
  exactly one `isDefault` climb until US-163; six `PerfectSweep*Tests` assert `evolutions.count == 1`
  over their `swept` list and four device-tree files assert an exact `targets(of:)` list. Run
  `grep -n "is not a single climb" Tests` and `grep -rn 'XCTAssertEqual(try targets(of:' Tests` for
  every parent BEFORE authoring, and read a climb as `first(where: \.isDefault)`, never as `first`.
- **`SeedRosterTests.earnedPath` PREFERS THE EARNED EDGE, SO BRANCHING A PERFECT ON US-002's NAMED
  LINES SILENTLY REDIRECTS THEM.** `lilimon` and `metalgreymon` are both walked by
  `testTheThreeNamedLinesAreTheOnesShipped`; an earned branch on either moves the line's endpoint
  away from Rosemon / WarGreymon. US-163 lost its BOLDED parent for BanchoLilimon to this and took
  Lilamon (the sister form, cited on the same page) instead. Grep the test before branching on
  `palmon`, `dmc-v1` or `dmc-v2`.
- **A LEAF'S FIRST IN-EDGE IS ITS `isDefault` CLIMB AND SO CARRIES NO CRITERIA.**
  `SeedRosterTests.testEveryNonTerminalNodeHasExactlyOneDefaultEdge` fails a node with edges and no
  fallback. That is also the standing reading of "no edge is unconditional": the AC binds the EARNED
  edges, because US-020 takes an `isDefault` edge whatever its gates say.
- **A LINE CAN BE CLOSED TO A NODE BY ART RATHER THAN BY SHAPE.** Armagemon belongs on `diablomon`
  and cannot go there: its bolded parent Kuramon is a Baby I (`invalidStageTransition`) and BOTH
  Perfect rungs the Kuramon swarm climbs through, Chrysalimon and Infermon, are idle-only
  (`edgeToDexOnlyNode`) — and the Perfect rung closed in US-162, so nothing can be authored into it.
  Check the cited line's Perfect rung for dexOnly art before assuming a thematic home is available.
- **AN X-ANTIBODY MEGA'S WHOLE `Evolves From` IS USUALLY ULTIMATES, SO THE STRONG VARIANT RULE IS
  THE ONLY READING — AND IT IS FREE.** Hang it on the Perfect its base form hangs on, with a
  distinct `requiredEnergy`. Barbamon X, Beelzebumon X and Belphemon X all landed that way.
- **WIKIMON: USE `opensearch` BEFORE CONCLUDING A DIGIMON HAS NO PAGE.**
  `curl -s "https://wikimon.net/api.php?action=opensearch&search=<name>&limit=50&format=json"`.
  A raw fetch of a wrong title returns an EMPTY BODY, not a 404. `Agumon YnK` is
  `Agumon -Yuki no Kizuna-`; `BlackWarGreymon X` is `Black War Greymon (X-Antibody)`.
- **PHASE E: A LINE'S RUNG OPENS UNDER ITS JUNK CHAMPION, AND THAT IS WHAT PROMOTES ITS EGGS.**
  US-161 left `adventure02` closed because XV-mon carries only one of the line's two eggs; US-162
  branched Nise Drimogemon instead — the junk Adult that takes the `isDefault` fall of ALL THREE
  Children — and promoted both at once. Same for `commandramon`'s Damemon. **Look at which Champion
  every Child of the line falls into before looking at which Champion is cited**; US-133's Scumon
  rule already makes an earned branch off a junk Adult legal, and the junk one is reachable from
  every thread by construction.
- **PHASE E: A LINE CAN BE PERMANENTLY UNOPENABLE — CHECK `reachesUltimate` FROM ITS OWN EGGS
  FIRST.** `algomon`'s Ghost Digitama reaches only Algomon (Adult), Mimicmon and Witchmon; the
  Labramon -> Siesamon thread that every citation points at hangs off `paomon`, one of the thirteen
  Baby I no Digitama can reach. Opening the rung there breaks US-159's "no unraisable egg on a line
  that HAS a Perfect rung" invariant — which is asserted in ANOTHER story's file
  (`PerfectSweepNToRTests.testVitalsFiveDigitamaWereAllPromotedAtOnce`), so it is easy to miss until
  the suite is red.
- **PHASE E: BEFORE AUTHORING, GREP FOR EVERY CLIMB TARGET AND EVERY PARENT.** `grep -rn
  'Set(graph.parents' Tests` and `grep -rn 'evolutions.count,' Tests` are now the two greps that
  matter most: US-162 broke seven parent-set ledgers and six `evolutions.count` claims across nine
  files. Fix each by NAMING the new parent or with a named exception — never by loosening to a
  superset or a `>=`. A "still owed" pin can also live in THREE files at once (the three Saiyu
  Warriors Perfects), so `grep -rn '<the id>' Tests` for every orphan in the band as well.
- **PHASE E: A JUNK FLOOR CAN BE UNCITED, AND THEN THE COMMENT MUST SAY THE WORD FLAVOUR.** Every
  floor US-157..US-161 authored was on its Champion's own Wikimon `Evolves To`; US-162's two are
  not, because none of `commandramon`'s or `adventure02`'s Champions cites a junk Perfect at all.
  `RosterTests`' alias list and `EvolutionCriteriaTests.junkIds` move either way.
- **`penc-sw` IS THE ONLY LINE WHERE EVERY NODE'S COMMENT MUST NAME ITS DEVICE** ("Saiyu Warriors").
  Three files enforce it — `ChildSweepAToFTests`, `BabyIISweepTests`, `DigitamaSweepBabyITests` —
  and the rule is invisible from the node you are writing.
- **PHASE E: A JUNK FLOOR IS PER-LINE, NOT PER-CHAMPION, AND A FLOOR THAT ALREADY CLIMBS IS THE
  ONLY FREE ONE.** US-161 opened `vital` with ten leaf Champions and bought ONE alias
  (`vital_darumamon`) that served every one of them — do not multiply the alias cost by the number
  of leaf parents when pricing a rung. Among floors that already exist, prefer one with an
  out-edge: `locomon` (-> GrandLocomon) cost `penc-me` nothing, while CatchMamemon and Karakurumon
  are terminal and each adds a dead end to `ChildSweepAToFTests`' ledger. Check `junkIds` in
  `EvolutionCriteriaTests` and the candidate's own `evolutions` before choosing.
- **PHASE E: WRITE A SWEEP TEST'S `intersection`/`subtracting` LEDGERS FROM THE FINISHED TABLES,
  LAST.** Every sweep since US-157 carries a pair of lines partitioning "authored by me" from
  "already there". US-161's was written from the plan (two pre-existing floors) and not re-counted
  after a third was chosen during authoring — the only failure in 2322 tests. When a sweep's own
  new test fails on a set-difference of its own tables, suspect the TABLE, not `evolutions.json`.
- **PHASE E: THE JUNK-FLAVOURED SHEETS RAN OUT AT US-160 — A NEW JUNK FLOOR IS NOW A LINE-SCOPED
  ALIAS AND REMOVES NO ORPHAN.** US-148/151/157 each spent an unused gag sheet (Numemon X,
  CatchMamemon, Karakurumon, Pandamon); by US-160 not one of the 58 Perfect still orphaned was a
  gag Digimon, so `diablomon`'s floor draws the Gerbemon sheet under `diablomon_gerbemon`, the
  `dmcv2_vademon` pattern. Price the consequences BEFORE authoring: no orphan removed, `RosterTests`'
  alias list moves, and `EvolutionCriteriaTests.junkIds` gains a scoped name. The five lines still
  without a Perfect rung — `xros`, `vital`, `adventure02`, `algomon`, `commandramon` — all pay this.
- **PHASE E: THE VARIANT RULE HAS A STRONGER FORM — SAME PARENT, NOT JUST SAME LINE — AND AT THE
  PERFECT RUNG IT IS USUALLY FREE.** Six of US-160's nine variants hang off the very Champion their
  base form hangs off, which costs no argument and makes the climb the base form's own Mega for
  nothing. **Read `graph.parents(of: base)` BEFORE the variant's own `Evolves From`.** The limit:
  a base form's line can hold the cited CLIMB and no cited PARENT (MetalMamemon on `dmc-v2`), and
  then the variant follows a cited parent elsewhere — `ChildSweepMToZTests.
  testEveryVariantSitsWithItsBaseFormOrFollowsACitedParent`'s escape hatch. Check BOTH ends.
- **PHASE E: WIKIMON'S X-ANTIBODY PAGE TITLES ARE SPACED, AND THE UNSPACED GUESS RETURNS AN EMPTY
  BODY RATHER THAN A REDIRECT.** `MegaSeadramon (X-Antibody)` is 0 bytes; `Mega Seadramon
  (X-Antibody)` is the page. Same for Megalo Growmon, Metal Greymon (Virus), Metal Mamemon, Metal
  Tyranomon. Treat a zero-length body as "try the other spacing", never as "no page" — plain forms
  redirect, parenthesised ones do not.
- **PHASE E: AN EARLIER SWEEP'S `evolutions.count == 2` CLAIM BREAKS THE MOMENT YOU BRANCH THAT
  CHAMPION AGAIN, AND SO DOES ITS `authoredUltimates` PARENT SET.** US-160 broke three of the first
  (Gaogamon, ShimaUnimon, its own Meicoomon) and two of the second (ChaosDukemon, AncientVolcamon).
  Fix with a NAMED exception (`parent == "gaogamon" ? 3 : 2`) or a named extra parent, never by
  loosening to `>=` or to a superset. Add `grep -n "evolutions.count, 2" Tests` and
  `grep -n authoredUltimates Tests` to the pre-flight list beside `inLine.count` and `sweepEggs`.
- **PHASE E: A BOLDED PARENT IS WORTH SPENDING ON THE CLAIMANT WHOSE ALTERNATIVES COST A WHOLE
  RUNG.** US-160 gave `tamers`' last leaf Growmon to Manticoremon rather than to its own bolded
  child MegaloGrowmon Orange: the Orange form was pinned to Growmon (Orange) by the variant rule
  anyway, and every other reading of Manticoremon opened a Perfect rung, a junk floor and a Mega on
  a line that had none. One leaf bought two nodes and Dukemon with it. Say so in the node comment.
- **PHASE E: A BOLDED PARENT CAN BE UNDRAWABLE FOR A REASON OF SHAPE, NOT OF ART — CHECK ITS STAGE.**
  Lucemon Falldown's sole bolded `Evolves From` is Lucemon, a CHILD, and
  `GraphValidationError.invalidStageTransition` refuses a Child -> Perfect edge. Every earlier
  "cannot" in this series was dexOnly art or a missing sheet. Price the STAGE of a bolded parent
  before pricing anything else, and when the canonical arrow is illegal say so in the node comment —
  US-159's tests grep for it.
- **PHASE E: WHEN A RANGE HOLDS A BASE FORM AND ITS X, SOLVE THE X FIRST — ITS ONE POSSIBLE PARENT
  DECIDES BOTH LINES.** LadyDevimon has forty cited parents across nine lines; LadyDevimon X has
  exactly one usable one anywhere (Numemon X, `tamers`), because its bolded parent is the base form
  itself — a Perfect, never an in-edge. Choosing the base form's line off its OWN citations would
  have split the pair and broken the variant rule.
- **PHASE E: A JUNK CHAMPION IS A LEGAL PARENT FOR AN EARNED BRANCH, AND SOMETIMES THE ONLY ONE.**
  Numemon X, Raremon and Scumon all carry one. Prove it in BOTH directions through the engine — the
  earned branch for a raised Digimon, the junk fall for a neglected one — rather than reasoning about
  it. Watch the knock-on: a Child whose ONLY onward edge is the junk fall (Terriermon) then reaches a
  Mega only by being neglected, which moves `EggHatchingTests`.
- **PHASE E, PERFECT RUNG AND ABOVE: the cheapest in-edge is one that ALSO clears a dead end, and
  every leaf you branch costs a junk-floor edge in the same breath.** A Champion sweep only ever
  added leaves; a Perfect sweep removes them, because a Perfect needs a Champion beneath it and after
  six Adult sweeps most Champions are leaves. US-157 cleared nine and the ledger in
  `ChildSweepAToFTests` fell for the first time in Phase E (105 -> 97). The catch:
  `EvolutionCriteriaTests` fails any branching Champion with no `isDefault` edge onto a junk Perfect
  of its OWN line, so the fallback has to be written in the same edit as the earned branch.
- **A PERFECT'S CLIMB IS A SINGLE `isDefault` EDGE — `minEnergy` 150, `maxCareMistakes` 2, NO
  conditions — and that is legal because `EvolutionCriteriaTests.branchingNodes` filters to Child and
  Adult.** The 2..5 ceiling and the junk-floor rule do not reach this rung. The AC's "no edge is
  unconditional" binds the EARNED edges; a condition on an `isDefault` edge is data that lies,
  because US-020 takes it whatever the gates say.
- **`Roster.bundled` reads `Resources/roster.json` (one entry per SHEET); Appendix B reads
  `roster.generated.json` (which mirrors `evolutions.json`, aliases and all).** A range test written
  off `roster.entries` will disagree with the orphan count by exactly the number of line-scoped
  aliases in the band — three in A-C. Both numbers are right; say which is which in the test.
- **Opening a line's TOP rung promotes its eggs.** `MainScreenModel.startingDigitamaId` filters on
  `graph.reachesUltimate`, so US-157's first four `tamers` Ultimates turned five Digitama from
  unraisable into legal starting eggs and moved BOTH lists in `EggHatchingTests`. `wanyamon` is the
  last line with Perfects and no Mega — expect the same there.
- **Author a multi-node edit with a script, after proving the JSON round-trips.**
  `json.dumps(d, indent=2, ensure_ascii=False)` reproduces `evolutions.json`, `elements.json` and
  `moves.json` byte-for-byte (no trailing newline). US-157 added 29 nodes across three files that
  way; see `scripts/us157_author.py`. And check `moves.json` `signatureName` against the WHOLE file —
  it is unique globally while `projectileSymbol|tint` is unique only per line, and a variant naturally
  wants to reuse its base form's attack name.
- **PHASE E: a "no sheet for this name" claim from an EARLIER story can be a SPELLING miss — check
  the roster before inheriting it.** `DMCVersion1TreeTests` recorded since US-133 that the Version 1
  document's Tyranomon had "no colour sheet at all, only `Black and White Sprites/Adult/
  Tyranomon.png`". The pack writes the colour sheet with TWO n's — `Adult/Tyrannomon.png`, roster id
  `tyrannomon`, playable — so an arrow the seed tree called undrawable had been drawable the whole
  time, and US-155 drew it. Wikimon's spelling and the sprite pack's differ often enough (Tyranomon/
  Tyrannomon, Ex-Tyranomon/extyranomon) that "no entry for `<wikimon spelling>`" proves nothing.
  Resolve a name through `roster.entries` by DISPLAY NAME normalised, never by guessing the id.
- **PHASE E: "this parent is FULL" IS A CLAIM ABOUT ENERGIES, AND IT CAN BE OFF BY ONE — RE-COUNT
  THE EARNED BRANCHES BEFORE INHERITING IT.** US-153 wrote that Gammamon was full and handed
  WezenGammamon a Gabumon stand-in; US-154 and US-155 both passed that advice on unchecked, and it
  was wrong for three stories. Gammamon carried BetelGammamon (spirit), GulusGammamon (strength) and
  KausGammamon (stamina) plus a junk edge to Turuiemon that SHARES spirit with the first — so
  `Set(node.evolutions.compactMap(\.requiredEnergy)).count` read 3-of-4-looking-closed while the
  count that actually matters, `evolutions.filter { !$0.isDefault }.count`, was 3 with vitality free.
  **Count EARNED branches against `EnergyType.allCases`, never distinct energies over ALL edges** —
  the junk fallback is allowed to share, which is exactly what makes the two counts disagree. US-156
  drew the bolded arrow it was told was impossible and left Gabumon its energy. Same shape as
  US-155's Tyranomon spelling: an inherited "cannot" is worth ten minutes of checking.

- **PHASE E ADULT SWEEPS: the four-Rookie ceiling is reached from BELOW, and a sweep can spend three
  of them at once.** US-155 filled Agumon, Bakumon and Muchomon — each took its LAST free energy —
  because the cheap placements all hung off Rookies that already had three earned branches. That is
  the normal shape late in the rung, not a warning sign: a Child with one energy left is exactly the
  Child whose line already holds both ends. But it closes the node for good, so say which Rookies a
  story filled in a test (`testTheThreeRookiesThisStoryFilledHaveAnEnergyForEveryEarnedBranch`) and
  expect the knock-on to reach the SEED tests — `SeedRosterTests`, `EnergyBarsTests`,
  `DexScreenTests`, `ConditionRevealTests` and `EvolutionTreeTests` all pin Agumon's branch list.
- **PHASE E: the `SeedRosterTests` per-line "verified-available set" is APPENDED to, never widened.**
  `patamonLineVerifiedSet` and `piyomonLineVerifiedSet` are closed lists of display names that every
  Adult-and-up node on `dmc-v3`/`dmc-v4` must be in. US-148 through US-151 each added their names
  with a comment saying which sweep and why; US-155 did the same. Do not relax the containment
  check — the list IS the record of which sheets were eyeballed.
- **PHASE E: grep the WHOLE tree markdown before spending an orphan on an invented junk node.** A
  later device section may draw it as an earned branch (US-140 lost WaruMonzaemon to US-142's V5
  section this way). The same grep is what the story's own "this name is not in the document" test
  does — write it before authoring, not after.
- **PHASE E: a missing rung is a REHOME, not a dropped thread — including a missing MIDDLE one.**
  Fetch the Wikimon page for the rung above the gap and the rung below it and intersect their
  Evolves From / Evolves To lists; a stand-in that appears in both sits on the same thread the
  document draws. US-140 did this for a missing bottom, US-141 for a missing middle (Deramon ->
  Blossomon) and a missing top (Crossmon -> Rafflesimon). Cite the page in the node `comment` — a
  test greps for "Wikimon".
- **PHASE E: two of a document's names can be ONE Digimon.** The V0 section draws HolyAngemon over
  Angemon and MagnaAngemon over Wizardmon; they are the same Digimon under a JP and a dub name,
  with one sheet between them, so US-143 wired one node with two parents and two children rather
  than inventing a second id on the same art. Do not diff the bullet list by eye — sweep every
  `*mon` word out of the section's own text and assert each resolves to a playable roster entry,
  under its own spelling or a recorded substitution. That sweep is what turns "a name is missing"
  into "a name is a duplicate", and it is a test, not a one-off grep.
- **PHASE E: `elements.json` and `moves.json` need a `lineDefaults` entry for a NEW LINE**, not
  only the per-id rows — `ElementCatalogTests.testEveryGraphLineHasALineDefault` and its moves
  twin. Missed by US-143's first pass; it is a ninth place on the knock-on list, and it only
  applies when the story opens a line rather than adding to one.
- **PHASE E SWEEPS: a sweep authors ONE RUNG, so two invariants the device trees had are gone.**
  US-144 broke both on purpose and pinned each as a claim rather than letting it rot:
  (a) **a line may have SEVERAL Digitama** — an alternate egg hangs off the line whose species it
  belongs to rather than opening a one-node line, so `dmc-v2` has three eggs; the "one egg per
  line" assertion in ALL SIX tree-test files became a pinned egg LIST, and every
  `...IsReachableFromItsDigitama` seeds its BFS from every egg of the line.
  (b) **a node below Ultimate may be a dead end** — the Baby I an egg hatches into has to exist
  before the Baby II above it does. The replacement for "zero dead ends" is a LEDGER
  (`DigitamaSweepTests.testTheOnlyDeadEndsBelowUltimateAreTheOnesThisSweepOpened`) that fails in
  BOTH directions: a ninth dead end fails it, and so does wiring one of the eight onward. The next
  sweep is meant to edit that list down and is told to rather than having to notice.
- **PHASE E ADULT SWEEPS: intersect the orphan's Wikimon `Evolves From` AND `Evolves To` against the
  graph BEFORE choosing its line.** Choosing the line off the parent alone is what forces a new
  Perfect rung later; choosing it off both ends at once is why US-152 wired five Champions for five
  nodes while US-151 needed eleven for seven. Seven lines still owe a Perfect rung, so the saving is
  real every time it lands.
- **PHASE E SWEEPS: intersect the story's OWN orphans' `Evolves To` lists WITH EACH OTHER before
  opening a rung above.** When every cited target of a range is a sheet with no node — which is the
  normal case for the `tamers` X-Antibody thread — the orphan-by-orphan intersection against the
  graph finds nothing and the rung above HAS to open. US-154 paid two nodes for four Champions
  instead of four, because Grademon is in both Meramon X's and Pegasmon X's `Evolves To` (Digital
  Monster X Ver.3) and Mametyramon is Bx-43, the card that fuses Monochromon X and Pteranomon X.
  And a line that ALREADY has a junk Perfect makes each new one cost ONE node, not the two US-151
  paid: no floor, and `EvolutionCriteriaTests.junkIds` does not move.
- **PHASE E SWEEPS: an energy shared with the JUNK fallback is legal; one shared with another EARNED
  branch is not.** US-154's Musyamon took the only energy Pico Devimon had left, strength, which its
  junk edge to Gokimon is also gated on. That is the Scumon arrangement US-133 recorded — the earned
  edge asks 60 and the junk one 0, so each wins exactly when it should. Prove it through the engine
  in both directions rather than reasoning about it; `AdultSweepMToRTests.testMusyamonSharesStrength
  WithPicoDevimonsJunkEdgeAndStillWins` is the shape.
- **PHASE E SWEEPS: `testEveryNodeInTheLineIsReachableFromItsDigitama` CARRIES ITS OWN
  `inLine.count`, and it is NOT the `sweepEggs` test.** Four device-tree files failed on this second,
  separate count after US-154's data landed. `grep -n 'inLine.count' Tests` alongside
  `grep -n sweepEggs Tests` — the knock-on list needs both.
- **PHASE E: derive a handover's RANGE from the roster, not from the letter band you remember.**
  US-153 handed WezenGammamon to US-154 as an "M-R orphan"; its display name starts with W, so it is
  US-156's. The advice attached to it was still right and still needed — re-pin it in the new story
  rather than letting it fall between two ranges.
- **PHASE E ADULT SWEEPS: when TWO lines are both cited, the tiebreak is which one already has the
  rung above.** US-153's Kinkakumon had a Saiyu Warriors reading (Fujamon/Takinmon, `penc-sw`) and a
  Vital Bracelet BE one (Jellymon -> Zudomon, `penc-ds`); both are real citations, the first is the
  better theme, and the second cost two fewer nodes because `penc-sw` still has no Perfect rung.
  Take the cheap one, but write the rejected reading into the node `comment` AND pin both halves as
  a test ("the `penc-sw` Children still do not reach it" + "`penc-sw` still has no Perfect"), so the
  story that opens that rung is told Kinkakumon is a rehome candidate instead of having to notice.
- **PHASE E: a Child is FULL at three earned branches plus the junk fallback**, because
  `EvolutionEngine` picks on dominant energy first and there are four energy types. US-153 took
  Gammamon to the ceiling (spirit/strength/stamina + junk). When a sweep fills a Child, say so in a
  test rather than in a comment — the next sweep needs another parent, not a fourth branch, and
  `ChildSweepGToLTests`' `branchedByALaterSweep` entry for it can never move again.
- **PHASE E: when the BOLDED Wikimon parent is `dexOnly`, read further down the SAME Evolves From
  before calling the rung unwireable.** `edgeToDexOnlyNode` forbids the edge, but the next cited
  parent is usually already on a line the file holds - US-152's Gawappamon took Gomamon three
  bullets below the idle-only Kamemon and kept its citation, where US-151's Burgamon had to be
  rehomed on a line argument because every one of its parents was unusable.
- **PHASE E: a handover claim written as a test FLIPS when the next story honours it; it does not
  die.** US-151 asserted `graph.node(id: "flarelizamon") == nil` to hand FlareLizamon to US-152.
  US-152 wired it and rewrote the same test as MegaloGrowmon's exact parent set. Same fact, other
  side, still fails on a parent nobody wrote down. Leave the next sweep a claim of that shape.
- **PHASE E: `Set(graph.parents(of:))` EQUALITY is the assertion an Adult sweep breaks.** Three exist
  - HolyAngemon (two -> three -> four parents over US-143/151/152), Andromon in
  `PendulumMetalEmpireTreeTests`, MegaloGrowmon in `AdultSweepAToDTests`. Fix by NAMING the new
  parent and which arrow it is, never by loosening to a superset. `grep -rn 'Set(graph.parents'
  Tests` before choosing an out-edge target.
- **PHASE E: a device tree's closing "the N orphans this story removed" test has a `sweepEggs`
  escape hatch, and that - not the count - is where a later story's node goes.** It keeps the tree's
  claim about its OWN nodes true. Every device line touched by a sweep wants an entry.
- **PHASE E ADULT SWEEPS: an Adult's out-edge must land on a Perfect of its OWN line, and NINE
  lines have no Perfect rung at all.** `testNoEdgeCrossesALine` plus a per-line boundary test in
  every device-tree file make a cross-line edge impossible, and `EvolutionCriteriaTests` demands a
  junk fallback drawn from `junkIds` - so opening a line's Perfect rung costs TWO nodes, the earned
  branch and the junk floor under it, and the floor has to be added to `junkIds`. US-151 opened
  `wanyamon` and `tamers`; `xros`, `vital`, `adventure02`, `algomon`, `commandramon`, `diablomon`
  and `penc-sw` are still owed one. This is the same problem US-148/US-149 solved one rung down.
- **`Roster.bundled` is `Resources/roster.json` (1,022 entries); `roster.generated.json` (1,025) is
  the Appendix B mirror and carries three line-scoped aliases the shipped file does not.** Count a
  story's RANGE off the shipped roster when the number goes into a test, and its ORPHAN COUNT off
  the generated one. They disagree, and US-151 wrote 46 where the test wanted 41.
- **Fetch Wikimon as wikitext, never as a summarised page:**
  `curl -s "https://wikimon.net/index.php?title=<Page>&action=raw"`. The rendered-page fetch drops
  and invents Evolves From / Evolves To entries. Redirects come back as `#REDIRECT [[Real Page]]`.
- **A rung can be UNWIREABLE rather than missing, and then the argument is "which line already holds
  this family".** All three of Burgamon Adult's Wikimon parents are idle-only or absent, and
  `edgeToDexOnlyNode` forbids an edge to an idle-only Digimon, so no stand-in intersection existed.
  US-151 put both Burgermon on `dmc-v4` because Torikara Ballmon - whose only canon relatives are
  those two - is already a Baby II there and Digitamamon, a cited Evolves To, is its Perfect.
- **A class-level Wikimon citation ("Any Blue Lv.3 Digimon from the DCG") is a real citation and is
  checkable.** Deckerdramon has nothing else; its out-edge takes the "also Evolves from Cyborg
  Lv.4" clause onto Andromon, which this line really does reach from Guardromon, and the test
  asserts that parent edge. Say in the `comment` which half is cited and which is flavour.
- **The shared `met` "did everything right" context CANNOT take an `atMost` edge.** Copy
  `AdultSweepAToDTests.context(for:)`, which derives the context from the edge's own conditions,
  rather than loosening a criterion to fit the fixture.
- **PHASE E SWEEPS: THE DIGITAMA ARE SPENT AS OF US-145 — all 57 carry their one hatch edge.** A
  Baby I's only possible in-edge is an egg, so **13 playable Baby I can never be reached** and no
  later story can author that away. The count and the "no unspent egg" half are pinned by
  `DigitamaSweepLToZTests.testTheBabyIStillOrphanedCannotBeReachedByAnyRemainingEgg`. Read it
  before promising an in-edge at Baby I.
- **PHASE E SWEEPS: 45 free Digitama and 33 free Baby I is the allocation the whole phase rests on.**
  `EggHatcher.hatchTarget` reads `node.evolutions.first`, so an egg has exactly ONE hatch edge and a
  later story CANNOT give a new Baby I an in-edge by adding a second edge to an already-spent egg.
  So an egg either pairs 1:1 with a Baby I it opens, or doubles up onto an existing one — and
  US-144/US-145 between them decide, for good, which Baby I ever get an in-edge. US-144 spent 22
  eggs: 12 doubled up (their species was already wired, or is dexOnly/absent so no thread could
  reach it), 10 opened a Baby I. That leaves US-145 23 eggs for 25 still-orphaned Baby I, so
  **US-145 must double up as little as it can** or some Baby I can never be reached.
- **PHASE E SWEEPS: THE GLOBAL REACHABILITY TEST LIVES IN `EvolutionCriteriaTests`, not in
  `EvolutionGraphTests`, and every device line has its OWN twin.** `testEveryNonEggNodeIsReachable
  FromSomeDigitama` was a global zero from US-008 until US-146; each of the six `Pendulum*TreeTests`
  and five `DMC*TreeTests` also asserts "every node on this line is reachable from one of its eggs".
  So a node with no in-edge breaks the test of WHATEVER LINE you put it on. Grep for both BEFORE
  choosing a line, and when a node genuinely cannot be reached, pin it by NAME rather than relaxing
  the check — US-146 turned the global one into a 21-name list that still fails on a 22nd, and
  proved separately that each stranded Baby II has no reachable parent so the list cannot absorb a
  node nobody bothered to wire.
- **PHASE E SWEEPS: A BABY II'S ONLY POSSIBLE PARENT IS A BABY I, AND A CHILD'S IS A BABY II.**
  US-147 spent the "every Baby I has exactly one edge" invariant to give twelve parentless Baby II
  an in-edge: nine Baby I gained a conditional second edge beside their unconditional fallback.
  The same move is the only one available at every rung above, so expect to branch the rung below
  and to edit the newest sweep's "who branches, and for which orphan" test rather than adding a
  branch quietly. An in-edge also FORCES THE LINE — `testNoEdgeCrossesALine` — so the parent, not
  flavour, is what decides where a sweep node lives.
- **PHASE E SWEEPS: an egg's promise is a BFS, and it is worth running before you author.**
  US-144/145 hung 57 eggs on threads; a thread that never arrives at the egg's own species is a
  promise the file does not keep, and `BabyIISweepTests.testTheEggsWhoseSpeciesThisRungReachesNowArriveAtIt`
  caught exactly that on `lala_digitama`. Decide which parent gets a Child by asking which egg is
  owed it.
- **PHASE E SWEEPS: a rung's scope is the RUNG, not the names still orphaned when you start.** A
  sweep AC asks for an in-edge AND an out-edge; an earlier sweep may have met only one of them for
  most of the range. US-144/US-145 de-orphaned 25 Baby I with in-edges alone, so US-146's real job
  was 33 out-edges, not 13 nodes. Check which criterion the earlier story met before scoping.
- **PHASE E SWEEPS: fetch the Wikimon page of a Digimon that is NOT on disk.** Wikimon has pages
  the sprite pack lacks, so `curl wikimon.net/Gurimon` still gives "Evolves To: Koemon" — and the
  stand-in is whichever rung ON DISK shares that target. One sweep of ~37 `curl`s over every orphan
  at the stage above, parsed for `Evolves_From`/`Evolves_To`, answers every pairing question in a
  sweep at once and is reusable by the next one. Beware: an `Evolves To` at Baby I can be a DigiXros
  FUSION list and not an evolution at all (Bombmon's is), in which case say FLAVOUR in the comment
  and let a test grep for the word.
- **PHASE E SWEEPS: the graph has more nodes at a rung than the roster has playable Digimon.**
  `graph.nodes(at: .babyI)` is 45; `roster.entries` playable Baby I is 43 — `piyo_yuramon` and
  `pencnsp_botamon` are line-scoped aliases with no roster entry. Assert a rung's SHAPE over graph
  nodes and its COVERAGE over roster entries; iterating roster entries silently skips every alias.
- **PHASE E SWEEPS: the eleven-file knock-on list for a rung-sized story.** Node count in BOTH
  `ElementCatalogTests` and `EvolutionGraphValidatorTests`; line sizes in `EvolutionGraphTests`;
  the game -> [lines] literal in `MinigameAssignmentTests`; `MoveCatalogTests` wants a unique
  symbol|tint WITHIN a line and a globally unique `signatureName`;
  `ElementCatalogTests.testElementsVaryWithinALine` forbids a single-element line, which any 2-node
  line trips; and `EggHatchingTests`' two starting-egg lists move whenever a thread newly reaches an
  Ultimate. Plus `lineDefaults` in elements.json AND moves.json, `DexModel.lineTitles` and
  `MinigameAssignment.byLine` for each new line.
- **PHASE E SWEEPS: a new game must not start on an unfinished thread.**
  `MainScreenModel.startingDigitamaId` filters candidates through
  `EvolutionGraph.reachesUltimate(from:)` (US-144). Without it a fresh install could land on
  `guil_digitama`, hatch Jyarimon and stop there. A sweep that opens a thread does NOT need to do
  anything for this; it is automatic. But a sweep that FINISHES one silently adds a starting egg,
  and `EggHatchingTests.testTheDefaultChooserPicksARandomDigitama` pins both halves of the split.
- **PHASE E SWEEPS: Wikimon two hops down is how you find a species' Baby I.** Fetch the CHILD's
  page, read `Evolves From` for its Baby II, then that Baby II's `Evolves From`. Better still,
  fetch every free Baby I and read its `Evolves To` once — 33 curl calls gave US-144 the whole
  Baby I -> Baby II map in two minutes, and US-146/US-147 can reuse the same sweep. **`curl` works
  and Python's `urllib` HANGS** in this sandbox; shell out. The parse is
  `re.split(r'<h2[^>]*>', html)` then `<span class="mw-headline" id="...">` for the section name.
- **PHASE E SWEEPS: a dexOnly or ABSENT species cannot be given a thread, so its egg doubles up.**
  Betamon, Kamemon, Kudamon and Kudamon 2006 are idle-only; **Espimon has no sheet at all**, not
  even an idle frame. Their eggs went onto the device line whose In-Training Wikimon draws that
  species from (Betamon and Kamemon both from Pukamon -> `penc-ds`, Espimon from Caprimon ->
  `penc-me`). Where even that is absent, say so in the comment and call it a flavour choice rather
  than dressing it as a citation — `kuda_digitama`'s comment does exactly that.
- **PHASE E SWEEPS: `roster.generated.json` HAS THE LINE-SCOPED ALIASES IN IT AND
  `Roster.bundled.entries` DOES NOT.** Scope a rung's ORPHAN count off the generated file (it
  mirrors the graph, which is what Appendix B needs) and a story's RANGE off `Roster.bundled`, or
  the range over-counts by every alias — US-149 read 45 G-L Children where the test saw 43.
- **PHASE E SWEEPS: a device tree pins its own In-Training's branch LIST and edge COUNT.** Hanging
  one more Child off a device-tree Baby II breaks tests that have nothing to do with counts or
  lines: US-149 tripped `SeedRosterTests`' Pagumon target list and Nightmare Soldiers' "the
  In-Training rung is unconditioned" claim, which a sweep's earned branch necessarily violates.
  `grep -rn '<parent-id>' Tests` BEFORE choosing that parent. The fix for the second kind is to
  scope the tree's claim to the edges the TREE draws, not to relax it.
- **PHASE E SWEEPS: the "every Champion of this line falls to the junk Perfect" loop needs
  `&& !$0.evolutions.isEmpty` in every device-tree file a sweep touches** — US-148 fixed Nightmare
  Soldiers, US-149 Virus Busters and Metal Empire. Wind Guardians and Deep Savers still carry the
  unguarded version and fail the moment a sweep puts an Adult on them.
- **PHASE E SWEEPS: check `EvolutionCriteriaTests`' stranded lists BEFORE choosing a parent, not
  after.** US-149 moved Gammamon from Pusurimon (stranded) to Hiyarimon — same line, equally free,
  reachable — and kept two nodes off the list for nothing. Where every Baby II of a line is stranded
  (`xros`, `penc-sw`: no Digitama, and all 57 eggs were spent in US-144/145) the arithmetic is
  inherited and the honest move is to add to the list with the parent that explains it.
- **PHASE E SWEEPS: the four-energy ceiling decides where a VARIANT lives, but "its base form's
  LINE" is far easier to satisfy than "its base form's PARENT".** Check the line for a SECOND
  In-Training before rehoming — US-149's Kokuwamon X kept `penc-me` through Kozenimon when Caprimon
  was full, while Gomamon X could not, because `penc-ds` has exactly one Baby II in the whole line.
- **PHASE E SWEEPS: a historical claim stops being checkable once a later story writes to the same
  file.** `ChildSweepAToFTests`' "this line had no Champion before" was checked as "every Adult here
  is one US-148 authored"; US-149 added sixteen to `tamers` alone. Replace such an assertion with
  the half that stays true and say why in the comment — do not delete it, and do not let it rot.
- **PHASE E SWEEPS: `care.battleCount` is answerable ONLY over the `lifetime` window; every other
  `care.*` counter ONLY over `stage`.** `ConditionContext.careValue` hands back `.unknown` for
  `(.careBattleCount, .stage)`, so such an edge is UNREACHABLE rather than merely hard — and the
  validator has no opinion about windows, so only the engine tests catch it. US-150 shipped four of
  them into a first draft; `ChildSweepMToZTests.testNoBattleCriterionAsksForAWindowTheContextCannot
  Answer` now pins the rule over `evolutions.json`. It does NOT cover `maps.json`, where US-128
  recorded the same trap in 12 Digitama slots and left it alone on purpose — that is a data story,
  and this pattern is why it is worth doing.
- **PHASE E SWEEPS: re-run the egg BFS when a sweep WIRES a species an earlier egg doubled up for.**
  US-145 parked three eggs on someone else's Baby I because their own species had no node. US-150
  gave all three a node and let that decide one placement — Phascomon went to its CITED line's
  second In-Training rather than to a free one on flavour — so every one of the three now arrives.
  `DigitamaSweepLToZTests`' "the species is still unwired" guards exist to make you notice.
- **PHASE E SWEEPS: prefer a SECOND CITATION over a rehome when the first-choice parent is full.**
  US-149's Gomamon X had to be rehomed by flavour; US-150's Shakomon X hit the identical trap
  (`penc-ds` has one In-Training and it is full) and instead followed the other parent Wikimon
  names for it. A citation is checkable in a test; a rehome is only a comment.
- **PHASE E: a substring search misses a letter INSIDE a name.** `Jureimon` is not a substring of
  `Jyureimon`, so the document's own bracketed spelling does not find its own sheet. Search a
  PREFIX, not the whole name, before calling anything absent.
- **PHASE E: a document name with no `find -iname` hit is usually a DUB name, not an absent
  Digimon.** Try the Japanese one: Mammothmon/`Mammon`, Wizardmon/`Wizarmon`, Pumpkinmon/`Pumpmon`,
  Piedmon/`Piemon`, Myotismon/`Vamdemon`, Candlemon/`Candmon`, Apemon/`Hanumon`. Also check the
  roster by SUBSTRING and read every hit's `dexOnly` — a playable twin can hide behind an
  idle-only one under the document's own spelling (Octomon/`Octmon`).
- **`care.battleWinRatio` must not gate a CHILD's earned edge.** The shared reachability fixture
  (`ConditionContext` with `battlesLifetime:`) cannot express a win ratio, so the branch is
  unreachable in tests. Fine at Adult, where nothing walks it.
- SIMULATOR SCREENSHOTS: `export DEVELOPER_DIR=/Applications/Xcode_26_4_1.app/Contents/Developer`,
  build once against Series 11 (46mm, watchOS 26.4), then `simctl install` the SAME .app onto the
  41mm Series 9 (watchOS 10.4) — deployment target is 10.0 and Series 9 is not a usable
  `xcodebuild -destination`. Sim UDIDs: 46mm BA425DD4-8856-4933-8198-0E4B89960E48,
  41mm FB045EDB-DEB9-40AA-B8FB-819E9E742517.
- Launch flags for a screenshot: `-healthAnswered -noNotificationPrompt` plus the demo flag. If a
  notification alert is already queued it SURVIVES terminate+uninstall and eats every shot;
  `simctl erase` on the device is the only fix (then `-healthAnswered` is required or you land on
  onboarding). Cost 10 minutes again this iteration.
- To photograph a pre-story build for a before/after: `git stash push -u`, rebuild, copy the .app
  out of DerivedData FIRST (both builds land on the same path), then `git stash pop`.
- MEASURE screenshots, do not eyeball them. Throwaway CoreGraphics tools compiled with
  `xcrun swiftc` are the pattern (see US-114's /tmp/us114/measure.swift, peak.swift — not
  committed, they hard-code this screen's bands). Two that keep earning their keep:
  a "lit in EVERY frame vs lit in ANY frame" pass over a burst separates static chrome from the
  moving sprite, and a peak/mean-luma pass over a band proves a dim is exactly `dimOpacity`.
- The scrim is invisible over a black background, so it cannot be measured directly. Measure what
  it DIMS instead: the sprite's peak luma with the light on vs off.
- NEVER diff a single screenshot per build when the sprite is in shot. The walk1/walk2 alternation
  is driven by a TimelineView that runs whether or not the sprite translates, so two shots taken
  seconds apart routinely catch opposite frames and read as a large bogus diff. Take a BURST of ~6
  per configuration, compare every pair, and use the pair that agrees — a matched pair goes to 100%
  byte identity while a mismatched one sticks at the ~50% the two poses share.
- Demo flags that write to the SAVE persist across launches, and several have no inverse
  (`-lightOffDemo` has no `-lightOnDemo`). Order the screenshot pass so the default state is
  photographed FIRST, or `simctl erase` between runs — a poisoned shot still looks plausible.
- To prove a constant (an opacity, a scale) is really what the screen paints: render the source
  asset yourself with the same layout math into the measured slot over black, then divide the
  Simulator's band by it. Turns "it looks about right" into a number.
- **THE MAIN SCREEN'S VERTICAL BUDGET IS NOW SPENT — genuinely, as of US-120.** `SpriteScale.fitting`
  floors `slotHeight / 16`, and on a hatched Child the slot lands at **49.5pt on 41mm** (a 48pt
  Digimon, **0.5pt of slack**) and **64.0pt on 46mm** (a 64pt Digimon, **0.0pt of slack**).
  **Any row added to `ContentView`'s VStack costs the Digimon a whole scale step, however thin** —
  US-120 measured a 13.5pt row taking 48 -> 32pt and 64 -> 48pt before it paid for itself. Measure
  the slot BEFORE designing a row. What is left to pay with, in order of preference:
  - **Nothing cheap.** US-120 already spent the five 1pt VStack gaps, `ActionControls`' 2pt top
    padding, the energy bars' row spacing, and took the name line and the stats values to 9pt.
    `MainScreenTypography` holds those two sizes and a test fails if they grow.
  - The bottom safe area — 26pt on 41mm, 36pt on 46mm, real and unused, verified with an
    `.ignoresSafeArea(.container, edges: .bottom)` probe — but spending it puts the action row's
    outer circles inside the display's corner arc, which is what the inset is for.
  - The top band holds the clock and BOTH toolbar buttons (US-114's light, the Dex). Not reclaimable.
  - **Price a trade before declining it.** US-120's first iteration set the chrome trim aside as "a
    screen-wide legibility regression"; measured, half of it was pure whitespace costing nothing.
- To measure the sprite SLOT in one command: the map band under `-mapDemo=<asset>` IS the slot,
  exactly, so diff that shot against a `-mapDemo=none` one — **but only count a row as slot if
  >=75% of the WIDTH differs.** The map band is full width (344 of 352px on 41mm); `-mapDemo=none`
  also flips the map strip to its prompt state and nudges the name row, so a naive "differs at all"
  diff swallows those two text rows and over-reports the slot by ~23pt. To measure the sprite SCALE:
  a brand-new save is a Digitama whose sheet yields no frames, so `DigimonSpriteView.placeholder`
  draws a stroked box of exactly `16 x scale` points — a free ruler that needs no matched walk frame.
  On a HATCHED save use the sprite's ink WIDTH, not its height: height changes with the walk frame.
- **A demo flag's "inverse" is rarely a pure inverse** — `-mapDemo=none` undoes US-115's background
  AND changes US-120's strip. When a before/after rests on one flag, check what else that flag
  reaches, or the diff attributes every change to the thing you were testing.
- **Build a pre-story baseline in a `git worktree`, not a `git stash`.** The two builds land in
  different DerivedData directories, so there is no copy-the-app-out-first race and the working tree
  is never disturbed. `git worktree add /tmp/<name> <commit>`, then `git worktree remove --force`.
  A baseline that reproduces the previous iteration's published numbers is what makes a
  before/after worth anything — check that it does before trusting the "after".
- `xcodegen generate` is required for a NEW source file even though `project.yml` globs `Sources`.
  The symptom is "cannot find type 'X' in scope" on a file that plainly exists.
- **`Roster` and `EvolutionGraph` are DIFFERENT sets: 1025 entries vs 88 authored nodes.** Data
  authored against the roster (map opponent pools, Digitama slots) CANNOT be resolved through the
  graph — it silently comes back empty and the caller falls through to whatever its fallback is.
  US-122 shipped a whole story doing nothing for one build because of this. Until Phase E wires the
  780 orphans, anything meaning "any Digimon" goes through `Roster`. `EvolutionNode` is
  constructible from a `RosterEntry` with an empty `line`, and both `MoveCatalog` and
  `ElementCatalog` degrade gracefully through an empty line — that is the bridge (`MapOpponentBand.nodes`).
- **A test that asserts "something came back" cannot catch a silent fallback.** Assert that what
  came back is FROM the set you asked about. In US-122 that one line was the difference between a
  false pass and finding the bug.
- **Anything under `Tests/Fixtures/` is globbed into the test target — including files a TOOL leaves
  behind.** Even a read-only `sqlite3 ... "select ..."` on the committed `.store` creates `-wal` and
  `-shm` sidecars, and the next `xcodegen generate` bakes them in; the build then fails with
  `The file "pre-us123.store-shm" couldn't be opened`. Delete the sidecars AND re-run `xcodegen`.
- **`sqlite3` on a store is the way to check what a `@Model` change actually WROTE**, and it is what
  separates "the value read back" from "the value was persisted": a computed accessor with a `nil`
  default returns the same answer either way. `pragma table_info(ZGAMESTATE)` for the columns,
  `select` for the rows. Copy the fixture out first (see above).
- **An optional-backed storage property is the house pattern for adding a field to an
  already-shipped `@Model`**, and the reason belongs on the STORAGE property: SwiftData will not
  migrate a non-optional attribute into an existing store without a default — it opens, then the
  next save fails with "is a required value". Choose the `nil` reading in the direction that is SAFE
  for an old save, not merely convenient (US-124: `isActive` reads `nil` as ACTIVE, because the only
  record that can carry one is the single Digimon of a pre-box store).
- **A one-shot migration guarded by "does X exist yet" does not run for a player who already ran the
  build that created X.** US-124's `isActive` stamp lives inside `loadOrCreateProfile`, which
  returns early once there is a profile — so it needs the `nil`-default backstop as well. When
  adding a migration, ask which SHIPPED build the player is coming from, not just which schema.
- **NEVER chain a `GameStore` temporary into a `@Model` read in a test:
  `try GameStore(url: url).loadOrCreateProfile()` CRASHES the whole test host.** ARC releases the
  store the instant the call returns, a released store's `ModelContext` is reset, and the returned
  `PlayerProfile`/`GameState` is destroyed with it — the next assertion trips
  `SwiftData/BackingData.swift:835: Fatal error: This model instance was destroyed by calling
  ModelContext.reset`. Bind the store to a local first (`let store = try GameStore(url: url)`), and
  keep it alive for as long as anything it handed out is read. Cost a full test run in US-129.
- **A crashed test host makes `xcodebuild test` LIE about the total, and the lie looks like a pass.**
  It relaunches and the final summary counts only the tests since the relaunch — US-129 saw
  `Executed 962 tests, with 0 failures` from a suite of 1623, with `** TEST FAILED **` the only
  signal, and that came bundled with an unrelated `xcrun: error: unable to find utility "simctl"`
  from diagnostics collection. ALWAYS compare the summary against
  `grep -rcE '^\s+func test' Tests/*.swift`; if it is short, grep the log for `Fatal error` and
  `Restarting after unexpected exit`. A trailing `simctl` diagnostics error on its own is
  environmental and does NOT mean a test failed — read the Executed line.
- **When a story splits "the machinery" from "the data" (US-130/US-131 for Jogress, and every one of
  Phase E's authoring stories), the machinery story's "validator reports zero findings over the
  shipped file" test is VACUOUS — say so, and buy back what it cannot prove.** An empty
  `{"recipes": []}` exercises no field name, so a typo in a key survives the story and surfaces as a
  `fatalError` at launch in the next one (every `bundled` here traps by design). Two cheap fixes,
  both used in US-130: decode a LITERAL in the authored shape through the real `JSONDecoder`, and
  leave an explicit `...UntilUS1NN` marker test that FAILS the day the data lands, so the next
  iteration is told rather than having to notice.
- **A Digimon's RUNG comes from the sprite FOLDER, not from the source tree** — `import_roster.py`
  reads the stage off `16x16 Digimon Sprites/<Stage>/`, and a graph node whose `stage` disagrees
  resolves to no art and fails the validator's `missingSprite`. The device trees do NOT always
  agree: US-134's Whamon is a Champion in the Digital Monster V2 document and a `Perfect/` sheet
  here. So "the art exists" is only half the check — `find -iname` tells you the folder too, and
  a name at the wrong rung has to be rehomed (US-134 put Whamon beside its Perfect rather than
  beneath it) rather than authored where the document puts it.
- **Before removing or repointing an edge, check whether it is the target's ONLY in-edge**
  (`graph.parents(of:)`). US-134's V2 tree left no room for Gabumon's old junk fallback, and
  deleting that edge would have left Geremon reachable from nothing — a shipped Digimon quietly
  turned back into an orphan by the very story that removes them. Rehome it; a test should pin the
  new parent.
- **A device slug line renames in exactly six places, and the list has not changed across three
  trees now (US-133/134/135).** `elements.json` `lineDefaults`, `moves.json` `lineDefaults`,
  `MinigameAssignment.byLine`, `DexModel.lineTitles` (ADD, the others MOVE), plus
  `EvolutionGraphTests`' per-line count dictionary and any `$0.line == "<old>"` filter in
  `SeedRosterTests`. `perl -pi -e 's/"line": "old"/"line": "dmc-vN"/g'` on `evolutions.json` does
  the data half; then `grep -rn "<old>" Sources Tests docs Resources scripts` and read every hit —
  a node id equal to the old line key (`patamon`, `gabumon`) means most hits must NOT change.
- **`-dexLineDemo=<line>` does nothing without `-dexDemo`.** The line flag is read inside `DexView`;
  `-dexDemo` is what pushes the Dex at launch from `ContentView`. Alone it lands you on the main
  screen and the shot looks like the flag was ignored. The bundle id is `com.digivpet.DigiVPet`,
  NOT `...watchkitapp` — `simctl launch` fails with FBSOpenApplicationServiceErrorDomain code 4 if
  you guess it.
- **A device tree's junk Champion is the one BOTH of its Rookies fall to** — Numemon in Ver.1,
  Vegimon in Ver.2, Scumon in Ver.3, Nanimon in Ver.4, Raremon in Ver.5. That is the `isDefault`
  edge, and `EvolutionCriteriaTests.junkIds` has to name it or the junk-fallback test fails.
- **Raise a ceiling ONE step, to what the file now holds, never to what a later story will need.**
  US-133 raised the out-degree ceiling to four and wrote that four was what the whole document
  fits in; US-134 found Gabumon five-wide, and V4's Palmon and V5's Gizamon are SIX. Read the
  source document yourself rather than trusting the previous story's summary of it — and leave the
  guard as tight as the data, since a ceiling looser than the file it guards catches nothing.
- **To screenshot the MIDDLE of a scrolling sheet, shorten what is below it rather than hunting for
  a scroll anchor.** `-dexTreeLinkDemo` lands at the very bottom and the candidate grid is then off
  the top; `-dexWidestDetailDemo -dexBranchDemo` selects one branch, which cuts the hint list from
  eight rows to two and brings the grid back onto the same screen. `simctl` has no scroll command,
  so the demo flags ARE the scrollbar.
- **Update `prd.json` with `python3` + `json.load`/`json.dump(..., indent=2, ensure_ascii=False)`,
  then re-add the trailing newline.** It round-trips the existing formatting exactly — the diff for
  a `passes`/`notes` update is 2 lines — so a 1,000-line JSON file never has to be hand-edited.
  Check with `git diff --stat prd.json` before committing.
- **Price a ceiling raise off the DRAWABLE row, not the document's row.** US-136's Palmon was
  billed by two earlier stories as six-wide and needing a raise; both of that section's
  art-absent names were in that very row, so the real row was four and no ceiling moved. `ls`
  first, then decide. US-137's Gizamon went the same way even though US-136 HAD run `ls`
  and still predicted a raise: **in every device tree the junk Champion is itself a name in the
  row**, so a Rookie's edge count simply IS its drawable count — six named minus one absent is
  five edges, not six. Four earned is the hard limit regardless (distinct energies, four types).
  No ceiling has moved since US-134.
- **A device-slug rename touches a seventh place the six-place list misses: OTHER stories' tree
  tests.** US-134 scoped `dmcv2_vademon`/`dmcv2_ebemon` AROUND the `gazimon` line, so
  `DMCVersion2TreeTests` asserted `shared.line == "gazimon"` and that two node `comment`s contained
  that word. `grep -rn '<old-slug>' Sources Tests docs Resources scripts` and read every hit — and
  remember the slug is usually also a real node id, so most hits must NOT change.
- **AC9 of every Phase E story ("every edge has at least one condition with a non-empty hint")
  cannot be met literally — say which reading you took.** `EvolutionCriteriaTests`
  requires the `isDefault` junk edge to carry NO criteria (US-020's fallback ignores an edge's
  gates, so a gated junk edge lies about how it is taken), and the Digitama/Baby rungs are
  unconditioned for the same reason. The reading that IS true and worth asserting, as three
  claims: every EARNED edge at Child-and-up is conditioned, every condition in the whole file has
  a non-blank hint, every junk edge is unconditioned.
- **Prose in `docs/` is not a test, and it drifts ahead of the data.** The schema doc had described
  the V5 line's Rookies as "five Champions apiece, four of them shared" since US-046 — true of the
  source document, false of `evolutions.json`, where both had three, until US-137. If a claim about
  shipped data matters, put it in an assertion; a doc bullet will be read as fact and cost a story
  its estimate.
- **A line-scoped alias node (`piyo_yuramon`, `dmcv4_palmon`) needs THREE data entries**: a node in
  `evolutions.json`, an `elements.json` entry and a `moves.json` entry — and the move's
  `signatureName` must be globally UNIQUE (`MoveCatalogTests`), so it cannot be copied from the
  Digimon it aliases. Give it a second real attack of that Digimon's.
- **A Phase E story may legitimately remove ZERO orphans**, when everything in its section was
  already wired and the only new node is an alias. Count it with Appendix B both ways — regenerate
  `roster.generated.json` AND re-run the count against `git show HEAD:Resources/evolutions.json` —
  and ASSERT the zero in a test, or the next reader reads it as a slip.
- **A test that needs "something OUTSIDE the graph" must DERIVE it, never name it.** Phase E is
  pulling the whole roster into `evolutions.json`, so every hard-coded "roster-only" example is on
  a timer: US-138 ate `megaseadramon`, which three tests used as their keyword-tier/stage-tier
  example. `roster.entries.first { graph.node(id: $0.id) == nil }` survives the next sweep.
- **A story that ADDS a line, rather than renaming a seed, touches six whole-file facts the
  rename stories never did**: `EvolutionGraphTests`' per-line count dict, `graph.nodes.count` in
  `ElementCatalogTests` AND `EvolutionGraphValidatorTests`, `EggHatchingTests`' seeded-Digitama set
  (a new line means a new starting egg), `SeedRosterTests.seedLines`, and `RosterTests`' exact
  alias list. Budget for them before writing data. Relatedly: **a "this story added no nodes" test
  must count its OWN LINE** — US-137's counted the file and US-138 broke it from another tree.
- **A new line's Digitama must be a real ROSTER Digitama id, never a line-scoped alias.**
  `maps.json` grants eggs by roster id and `Resources/roster.json` excludes alias ids, so an alias
  egg can never drop and the line is unstartable however well it is wired. Prefer an orphan egg
  belonging to one of the tree's own Rookies; it buys an orphan too.
- **An alias node removes NO orphan** — it shares a roster entry that was already wired. Count
  removals as "new nodes carrying a plain roster id", and assert the split.
- **A dexOnly ROOKIE is far more expensive than a dexOnly leaf**: its whole subtree has to be
  rehomed onto a sibling Rookie or it is stranded. Prefer a pairing the shipped file ALREADY
  makes, which turns an invention into a precedent.
- **PHASE E: a name can be BOTH present and unusable, and `find` alone gets it wrong.** US-142's
  Machmon is in the roster AND on disk - under `Idle Frame Only/`, so `dexOnly`. Run both checks on
  every name: does a 48x64 sheet exist, and is the roster entry playable. Either alone is half a
  search (US-139's Octomon/Octmon, US-141's garbamon, US-142's Machmon).
- **PHASE E: grep a junk pick against the SECTION and against the WHOLE FILE - they answer
  different questions.** A name an EARLIER section drew may still be fine (it is just an alias:
  US-142's `pencme_raremon`). A name a LATER section draws must not be spent (US-142 left
  Chaosdramon alone; `jogress.json` was the second reason). Doing only the whole-file grep rejects
  good picks; doing only the section grep steals a later tree's node.
- **PHASE E: a hard-coded COUNT is on the same timer as a hard-coded example.** Phase E moves the
  orphan total every story, so `> 800` in `DexScreenTests` became exactly 800 in US-142. Assert a
  PROPORTION of whatever the roster holds, not a literal.
- **PHASE E: leave a marker test that FAILS the day the next story lands.** US-140 left
  `XCTAssertNil(graph.node(id: "warumonzaemon"))` with a comment naming US-142. It fired on the
  first run, explained itself, and was rewritten into the claim it was guarding. Cheaper and
  louder than a note.
- **A Perfect MAY branch.** The 2..5 out-degree rule and the junk-fallback rule are both scoped to
  Child and Adult; above that all a node needs is exactly one `isDefault` edge. US-142's
  `pencme_andromon` has two parents and two children because the document draws Andromon twice.
- **`Resources/*.md` is bundled with the app**, so a test can read the source evolution-tree
  markdown at run time and assert what the document does and does NOT say — that is how an
  invented junk chain becomes an assertion instead of a comment.
- **A source-document name with "no sprite" usually has ANOTHER SPELLING — searching the document's
  spelling is half a search.** US-139 found six in one section: four with nothing at all under the
  document's name and a real 48x64 sheet under a romanization or dub name the pack uses
  (Pichimon/`Pitchmon`, Bukamon/`Pukamon`, Syakomon/`Shakomon`, Dragomon/`Dagomon`), and two with a
  **dexOnly TWIN** — the roster holds BOTH `octomon` (idle-only) and `octmon` (animated), and both
  `marinedevimon` and `marindevimon`. The twin is the nastier shape: the file exists and the roster
  entry exists, and only `dexOnly` says it is unusable. **Search the roster by SUBSTRING of the
  display name and check every hit's `dexOnly` before calling a name absent** — otherwise a story
  rehomes or drops a subtree it never needed to.
- **Adding a LINE is an eight-place knock-on**, in the order they fail: `EvolutionGraphTests`'
  per-line count dict; `graph.nodes.count` in `ElementCatalogTests` AND
  `EvolutionGraphValidatorTests`; `EggHatchingTests`' seeded-Digitama set; `SeedRosterTests`'
  `seedLines`; `RosterTests`' exact alias list; `EvolutionCriteriaTests.junkIds` (the new tree's
  invented Child-rung and Adult-rung junk targets); and `MinigameAssignmentTests`' sharer map.
  Budget for all eight before writing data.
- **`json.dump` cannot rewrite `Resources/evolutions.json` — the file is mixed-encoding.** Comments
  written before US-136 hold `—` escapes and later ones hold literal em dashes, so neither
  `ensure_ascii` setting round-trips and any dump reformats ~30 unrelated lines. Append new nodes
  TEXTUALLY: `json.dumps` each node with `indent=2`, indent every line four spaces, splice before
  the final `\n  ]\n}`. Do `elements.json` / `moves.json` by string insertion too — their entries
  are one-line objects a dump would explode. Keep non-ASCII out of new comments.
- **The app's bundle id for `simctl` is `com.digivpet.DigiVPet`** (`project.yml`). A wrong id fails
  `simctl launch` with an opaque FBSOpenApplicationServiceErrorDomain code=4 while
  `simctl io screenshot` still writes a plausible PNG of the watch face — which reads as the very
  black-screen bug the Phase E screenshot exists to catch.
- **PHASE E SWEEPS: THE FOUR ENERGIES ARE A HARD CEILING OF FOUR BRANCHES PER PARENT.**
  `EvolutionEngine` filters on `dominant == requiredEnergy` and the rung sweeps assert distinct
  energies per node, so a Baby II can carry at most FOUR edges, ever. Koromon and Pyocomon were both
  already full in US-148, which is why Agumon 2006 could not go on Agumon's own line and Elecmon
  Violet could not go on its only cited parent's. Compute `{vitality,strength,spirit,stamina} -
  used` over every candidate parent BEFORE promising a placement.
- **PHASE E SWEEPS: A CHILD-OR-ABOVE SWEEP IS THREE RUNGS OF WORK.** An in-edge branches the rung
  below; an out-edge needs TWO edges (`EvolutionCriteriaTests` wants 2..5 at Child and Adult) and
  one of them must land on a JUNK node of the same line. Six lines opened by the Digitama sweeps had
  no Champion rung at all, so US-148 had to invent a junk floor for each before any of their
  Children could branch. Check `graph.nodes.filter { $0.line == L && $0.stage == <rung above> }`
  before estimating a sweep.
- **PHASE E SWEEPS: TAKE JUNK NODES OUT OF THE ORPHAN POOL, NOT OUT OF AN ALIAS.** `junkIds` is a
  TEST's list, not a validator rule, and US-138/139/140/143 already set the precedent of picking a
  thematically-junk unused sheet. US-148 found six (Numemon X, Manekimon, Mimicmon, Troopmon,
  Damemon, Tsuchidarumon), so six junk floors removed six orphans instead of the zero a
  line-scoped alias removes. Search the orphan pool by flavour first.
- **PHASE E SWEEPS: A JUNK EDGE SHARES ITS EARNED SIBLING'S ENERGY and sits below it on
  `minEnergy`** — the Agumon/Numemon shape from US-061. The distinct-energy rule is scoped to EARNED
  edges, so a two-edge Child needs only one energy; giving the junk edge its own would make it
  unreachable whenever the player's dominant type is the other one.
- **PHASE E SWEEPS: CHECK THE STRANDED LISTS WHILE PICKING A PARENT, NOT AFTERWARDS.** An in-edge
  from a stranded parent strands the child and everything above it. US-148 moved Funbeemon off
  Puroromon (stranded) onto Tanemon — equally cited by Wikimon — and kept a whole thread reachable
  for free; only Fujamon's two Champions were unavoidable, because `penc-sw` has no Digitama at all.
- **PHASE E SWEEPS: hold the whole story as DATA in a throwaway authoring script that REFUSES TO
  WRITE until it has self-checked.** `/tmp/us148/author.py` verified in one pass: no id already a
  node, no edge crossing a line, no parent branching twice on one energy, no repeated symbol|tint
  within a line, no repeated `signatureName` globally, no digit in any hint. It caught three
  duplicate signature names before a byte hit the repo. Splice `evolutions.json` textually (US-136);
  a leaf node authored by the previous sweep is matched on its literal `"evolutions": []` line.
- **PHASE E: AN APPENDIX B "ORPHAN" CAN BE A DIGIMON ALREADY ON SCREEN — GREP THE GRAPH FOR ITS
  `spriteFile` BEFORE AUTHORING A NODE.** `roster.generated.json` carries the SHEET id from disk
  AND the graph's id, so a node whose id does not match its own sheet leaves the sheet counting as
  an orphan while its art renders. US-158 found `ex-tyranomon` that way: `dmc-v5` had drawn it
  since US-137 as `extyranomon`, and the fix was a RENAME (node id, three in-edges, the
  `elements.json` and `moves.json` rows) rather than a node. Careful — `hiandromon`/`hi-andromon`
  is the opposite case and is DELIBERATE: two real sheets, one node, pinned by `RosterTests`.
- **PHASE E, PERFECT RUNG: `projectileSymbol|tint` COLLISIONS ARE THE NORMAL CASE NOW, AND THE
  CHECK BELONGS BEFORE THE EDIT.** Eight of US-158's twenty-one first-draft moves collided — the
  pair is unique PER LINE and a big line already holds 49 rows, so `flame.fill` on `penc-nso` had
  two free tints left. Changing the SYMBOL beats reaching for an odd tint. `signatureName` is the
  global one and still bites on variants named after their base form.
- **PHASE E: `linesWithAPerfect.subtracting(linesWithAnUltimate)` IS EMPTY AS OF US-158, AND EGG
  PROMOTIONS NOW COME FROM THE MIDDLE OF A THREAD.** `wanyamon` was the last line with Perfects and
  no Mega; opening it promoted all four of its Digitama at once, because
  `MainScreenModel.startingDigitamaId` filters on `graph.reachesUltimate`. Any sweep that opens a
  Perfect rung on one of the six lines that still have none — `xros`, `vital`, `adventure02`,
  `algomon`, `commandramon`, `diablomon` — owes an Ultimate over it in the same story or it
  re-opens the gap, and should grep `reachesUltimate` before assuming `EggHatchingTests` is inert.
- **PHASE E: ONE DIGIMON WITH TWO SHEETS IS TWO NODES; ONE SHEET WITH TWO NAMES IS ONE NODE.**
  US-143 merged HolyAngemon and MagnaAngemon because one sheet served both; US-158 gave Fantomon
  and Phantomon a node each because the pack ships both PNGs and the roster is one entry per SHEET.
  The rule that keeps it honest either way: no single Champion may reach the same Digimon twice
  under two spellings, so hang the second sheet off a DIFFERENT parent of the same line and pin
  that the two parent sets are disjoint.
- **SCREENSHOT STORIES: A STALE `simctl` SYSTEM ALERT CAN SURVIVE UNINSTALL/REINSTALL AND HIDE THE
  SCREEN — REBOOT THE SIM TO CLEAR IT.** US-172's first four screenshots (both sizes, both builds)
  all landed on the notification-permission alert DESPITE `-noNotificationPrompt`, which does work.
  The alert was a SpringBoard-level modal left over from an earlier session; `simctl terminate` +
  `uninstall` + reinstall + relaunch did NOT dismiss it, and `simctl` cannot tap it. `xcrun simctl
  shutdown <id> && boot <id>` (then reinstall) clears it. If a main-screen screenshot comes back as
  a full-screen system sheet, suspect a stale alert before you suspect your launch flags.
- **SCREENSHOT STORIES: MEASURE THE MAP SLOT BY A CONTIGUOUS FULL-WIDTH SCAN DOWN FROM ITS FIXED TOP
  EDGE, NOT BY COLOUR.** The map fills the slot full width; the name/strip/bars below it are centred
  with black side margins. Scan rows down from just under the stats strip while >80% of the width is
  non-black (lum>18), stop after ~4 black rows. This reproduced US-120's independently-measured
  49.0pt (41mm) / 64.0pt (46mm) baseline to the point, which is what makes a growth number
  trustworthy. Colour/green detection and edge-column probes both leak into the sprite or the energy
  bars and gave wrong answers. Screenshots are @2x: divide px by 2 for pt. 41mm frame 352x430,
  46mm 416x496.

## 2026-07-22 — US-114: The light button moves to the top-left toolbar
- `LightButton` is now `ToolbarItem(placement: .topBarLeading)` in ContentView, the mirror of the
  Dex book at `.topBarTrailing`. `LightLayer` lost its `cycle` closure and draws the scrim ONLY;
  `LightButtonLayout.inset` is deleted; the button lost the `Circle` disc behind its glyph, which
  only ever existed to keep it legible through the 0.85 scrim it no longer stands in.
- Files: Sources/ContentView.swift, Sources/LightSwitchView.swift, Tests/LightSwitchTests.swift,
  Tests/MainScreenLayoutTests.swift (the last two are doc comments made true again, no new asserts —
  nothing in the suite ever referenced `inset` or the closure, so nothing broke).
- PASSED: build green. `Executed 1324 tests, with 0 failures` (unchanged; this story adds none).
- PASSED, MEASURED on BOTH screens, pre-story build vs post-story build, fresh install each,
  `-healthAnswered -noNotificationPrompt -wanderDemo`. Screenshots + throwaway measurers in
  /tmp/us114 (NOT committed). All boxes are screenshot pixels, i.e. @2x.
  - THE LAMP LEFT THE ROOM. Its old home — the slot's top-leading corner, 48x48px = 24x24pt,
    exactly `diameter` — is x8-55 y137-184 on 41mm and x8-55 y153-200 on 46mm in the pre-story
    shots, and in the post-story shots that whole band is EMPTY: zero pixels over a threshold of
    30, across 150 frames, on both screens. Not "darker" — nothing is drawn there at all.
  - THE LAMP IS OUTSIDE THE SLOT ENTIRELY. Post-story glyph box: 41mm x47-62 y47-73, 46mm
    x57-72 y58-84. The pre-story lamp sat at `slot.minY` exactly (its offset had no y inset), so
    those pre-story boxes READ OFF the slot's top edge: y137 on 41mm, y153 on 46mm. The new glyph
    clears it by 64px on 41mm and 69px on 46mm. That is AC8's "no button overlaps the sprite at
    any walk offset" proved for ALL offsets rather than for the ones sampled — the sprite cannot
    leave the slot, and the button is no longer in it.
  - THE SPRITE DID NOT MOVE OR SHRINK. Its box is pixel-identical pre- and post-story: 41mm
    x152-199 y156-203, 46mm x172-243 y177-248. The Dex button is identical too (41mm x260-328
    y28-127, peak 147, mean 23.59 to the hundredth in both).
  - US-112 IS UNCHANGED (AC3). Light on -> off, post-story: sprite peak 255 -> 38 on both screens,
    and 255 x 0.15 = 38.25, the same 38 the pre-story build gives. Action row identical on and off
    and identical between builds (41mm mean 37.82 both, 46mm 28.09 both).
  - THE TOOLBAR LAMP IS NEVER DIMMED (AC4). With the light OFF its peak is still 255, on both
    screens — and AC5's icon still tracks the state: rgb(255,214,10) `lightbulb.fill` lit,
    rgb(255,255,255) `lightbulb.slash` out.
- HONEST CORRECTION TO AC6/AC8, and the one thing the story asked for that is NOT true:
  **the sprite's reachable area is NOT wider after this change, because `LightButtonLayout.inset`
  was never subtracted from anything the sprite could reach.** The PRD says "the sprite's usable
  rect grows by that inset"; in fact `inset` appeared in exactly one expression — the button's own
  `.offset` inside `LightLayer` — and `git show HEAD:Sources/ContentView.swift | grep inset` is
  empty. The walk bound is `max((screenWidth - side) / 2 - 4, 0)` in `WanderingSpriteView` and
  reads only the device width and the sprite's own size, so it is unchanged, which the identical
  sprite boxes above confirm. What grew is the UNOBSTRUCTED area, and by the button's whole 24x24pt
  footprint rather than by the 2pt inset. Marked passing on that reading: the story's goal — the
  switch is out of the room and never covers the Digimon — is met and measured.
- **Learnings for future iterations:**
  - TRAP, PRE-EXISTING AND NOT CAUSED BY THIS STORY, and US-115 will hit it first: with
    `-wanderDemo` the Digimon ANIMATES but DOES NOT WALK on either simulator. Over 150 frames /
    60s, and again over a 40-frame burst taken as fast as `simctl` will go, the sprite box never
    left x152-199 (41mm) / x172-243 (46mm) — dead centre, `facing` never flipped, so
    `advanceOneStep()` was never reached. The walk1/walk2 alternation is the INNER `TimelineView`
    in `DigimonSpriteView`; the OUTER one in `WanderingSpriteView` is what drives the position, and
    it is that one that looks dead. Ruled out: the pose really is `.idle` (the two frames on screen
    match sheet indices 0 and 1 pixel for pixel, and `.sleep`'s 4/5 are a crouch that is plainly
    not what is drawn), and `bound` is not 0 (41mm scale 2 -> 68pt, 46mm scale 3 -> 76pt).
    IDENTICAL in the pre-story build, so this is not a regression from US-114 — but the last wave
    DID photograph the sprite at both walls, so something between then and now broke it. Anyone
    screenshotting a walk needs to find this first.
  - A pre-story build is only comparable on a FRESH INSTALL: a save that has spent a battle draws
    an extra caption line under the action row and that costs the sprite a whole scale step.
  - `.topBarLeading` on watchOS puts the item BESIDE the time, not under it — the 41mm glyph lands
    at y47-73 and the clock is untouched. No layout budget was spent to gain it.
- REMAINS: US-115 onward (1/56 passing).
---


## 2026-07-22 — US-115: The selected map draws behind the Digimon
- `MapBackgroundView` draws the selected map's asset at `MapBackgroundLayout.opacity` (0.35),
  `.resizable().scaledToFill()`, installed as a **`backgroundPreferenceValue`** off the SAME
  `SpriteSlotBoundsKey` anchor the scrim reads, sized to that slot, `.clipped()` and `.offset` into
  place. Background rather than overlay is the whole design: the layer is painted before the VStack,
  so the sprite and every row are drawn on top of it and `LightLayer` — an overlay — is painted
  after all of them. The map being dimmed by the scrim is therefore structural, not arithmetic.
- `MainScreenModel.selectedMapAsset` is the seam: nil for every real save today (the catalog is
  US-116, the persisted pick is US-119), set only by the new DEBUG `-mapDemo=<asset>` flag.
- Files: Sources/MapBackgroundView.swift (new), Sources/ContentView.swift,
  Sources/MainScreenModel.swift, Tests/MapBackgroundTests.swift (new, 7 tests).
- PASSED: build green. `Executed 1331 tests, with 0 failures` (1324 + the 7 new).
- PASSED, MEASURED on 46mm, clean install per build, `-healthAnswered -noNotificationPrompt
  -wanderDemo`. Screenshots + throwaway measurers in /tmp/us115 (NOT committed). Boxes are
  screenshot pixels, i.e. @2x on a 416x496 shot.
  - THE MAP IS CONFINED TO THE ROOM (AC1, AC3). Diffing the no-map shot against the map shot from
    the SAME build: the screen changes in **x4-411 y153-259 and nowhere else**, one contiguous band,
    identically for 01_grassland, 14_farmland and 16_iceland. The stats strip above it, the name
    line, the energy bars, the action row and the whole toolbar are byte-identical — so the map is
    behind none of them, and it grew layout in neither axis.
  - THE MAP IS BEHIND THE DIGIMON, not over it. On matched walk frames, **all 2916 pixels the
    sprite paints are byte-identical with a map behind them**, on all three maps. First run of this
    read 1512/2916 on farmland and that was a FALSE ALARM — see the walk-frame trap below.
  - THE SCRIM COVERS THE MAP (AC5). Map band mean luma 63.485 with the light on -> 31.721 at semi
    (x0.4997) -> 9.480 at off (x0.1493); peak 244.1 -> 122.6 -> 36.3. That is exactly
    `LightState.dimOpacity` applied to the map, which is what "inside the scrimmed region, painted
    below the scrim" means in numbers. Chrome above (rows 100-150) and below (265-495) the slot is
    identical to three decimals with the light on and out. Reproduced twice, on two installs.
  - THE OPACITY ON SCREEN IS THE AUTHORED ONE (AC2). Rendering each asset with the same
    scaledToFill math into the same 416x107 slot over black, then dividing the Simulator's band by
    it, recovers **0.339 / 0.339 / 0.342** for grassland / farmland / iceland against the authored
    0.35. (The ~3% shortfall is the reference render's own colour handling and the display's
    rounded corners, both included in the divisor; it settles 0.35 vs 0.5 or 1.0 beyond doubt.)
  - NO MAP SELECTED IS US-114's SCREEN (AC6). Pre-story build vs post-story build, both fresh
    installs, no map: the ONLY differing pixels on the entire 416x496 screen are **x175-240 y39-62,
    the clock digits**, because the minute rolled between the two runs. Everything else — lamp, Dex
    book, stats strip, sprite, name line, bars, action row, caption — is byte-identical.
  - THE DIGIMON STAYS LEGIBLE (AC7). Over the three brightest assets: the sprite's mean luma is
    190.3 and its **dimmest** pixel is 146.3, while the **brightest** map pixel beside it is 88.4
    (grassland 85.9, farmland 87.0, iceland 88.4). The two ranges do not overlap at all — no pixel
    of the Digimon is ever as dark as the brightest pixel of the brightest map. Mean-vs-mean
    contrast is 2.93:1 / 3.16:1 / 3.01:1.
- **Learnings for future iterations:**
  - **THE WALK-FRAME TRAP, and it will bite any story that diffs the sprite between two builds.**
    A single screenshot from each build is NOT comparable: the inner `TimelineView` in
    `DigimonSpriteView` alternates walk1/walk2 regardless of whether the sprite translates, so two
    shots taken seconds apart routinely catch opposite frames and read as a large bogus diff.
    Fix: take a BURST of ~6 shots per configuration and compare every pair, then use the pair that
    agrees. The frames are trivially separable by lit-pixel count (here 1476 vs 1620 under a scrim,
    2916 vs 2916-with-different-geometry lit) and the matching pair goes to 100% identity while a
    mismatched pair sticks at the ~50% both poses share. Four hypotheses were burned before this
    was the answer.
  - **THE LIGHT STATE PERSISTS ACROSS LAUNCHES AND POISONS LATER SCREENSHOTS.** `-lightOffDemo`
    writes `.off` to the save, and there is no `-lightOnDemo` to undo it, so every subsequent run
    on that container is photographed under a 0.85 scrim — silently, since the shot still looks
    plausible. Always take the light-on shots FIRST, or `simctl erase` between. Lost a burst to it.
  - `.backgroundPreferenceValue` is the mirror of the `.overlayPreferenceValue` already on this
    VStack and costs the sprite nothing: a background is sized by the view it is behind and never
    proposes a size back, so no asset can grow the layout however large it is. Reading the SAME
    anchor as the scrim is what makes "the map is inside the scrimmed region" true by construction
    rather than by two rects happening to agree.
  - Recovering a constant like an opacity from a screenshot works and is worth the twenty lines:
    render the source asset yourself with the same fill math into the measured slot over black,
    then divide. It is the difference between "the map looks faint" and "the screen is painting
    0.339".
  - The pre-existing US-114 trap is unchanged and did NOT block this story: `-wanderDemo` still
    animates without translating. The sprite's fixed position is actually what made the
    byte-identity test above easy, so nobody needs to fix it before US-116.
  - Do NOT put `.interpolation(.none)` on the map backgrounds. The sprite rule exists to stop 16x16
    art being smoothed UP; these are 822x330 assets drawn DOWN into a ~208pt slot, where
    nearest-neighbour drops rows and columns unevenly.
- REMAINS: US-116 onward (2/56 passing). Phase A is done; Phase B starts with the map catalog.
---

## 2026-07-22 — US-116: Map catalog data file and model
- `Resources/maps.json` (16 maps) and `Sources/MapCatalog.swift` — `AdventureMap` (id, displayName,
  assetName, tier, totalSteps, unlockedBy, opponentPool, digitamaSlots), `DigitamaSlot`
  (digitamaId + `[EvolutionCondition]`), and `MapCatalog` in the `Roster` shape: private id index,
  hand-written decoder, `load(from:)` for tests, `bundled` that fatalErrors on bad data.
- `DigitamaSlot` reuses `EvolutionCondition` wholesale rather than getting a parallel type. That is
  what lets US-121 draw a `?` slot's hints through the Dex's own `ConditionReveal`, and it means
  US-117's blank-hint rule is the same rule the graph validator already has.
- Files: Resources/maps.json (new), Sources/MapCatalog.swift (new), Tests/MapCatalogTests.swift
  (new, 17 tests). NO project.yml change — the `Resources` folder is already a `resources` build
  phase, so the file is picked up by existing config.
- PASSED: build green. `Executed 1348 tests, with 0 failures` (1331 + the 17 new).
- PASSED, checked rather than assumed:
  - BUNDLED FOR REAL. `ls` on the built product shows `DigiVPet.app/maps.json`, 28,644 bytes, and
    every test loads it through `MapCatalog.load()` off `Bundle.main` rather than off a fixture.
  - ART RESOLVES. All 16 `assetName`s return a non-nil `UIImage(named:)` at runtime, which is the
    same lookup `Image(_:)` does, and all 16 pass `MapBackgroundLayout.shouldDraw`. The 16
    imagesets were confirmed with `ls Resources/Assets.xcassets` before a name was written.
  - THE FR-6 TABLE IS SPELLED OUT IN THE TEST, not derived from the file — a test that read the
    order and totals back out of `maps.json` would agree with whatever the file said, which is the
    one thing it must not do. Order, tier and totalSteps all match; totals also strictly increase
    and tiers never drop.
  - THE CHAIN IS LINEAR AND COMPLETE. Map 1 has no `unlockedBy`, every other names its predecessor,
    exactly one map has none, and walking the chain forward from `startingMap` reaches all 16 —
    the second test catches a strand that the pairwise one would not.
- SCOPE CALL, deliberate and worth knowing: **`opponentPool` and `digitamaSlots` are authored here,
  not left empty.** No later story authors them, and US-121 (detail view), US-122 (opponent picks)
  and US-128 (drop engine) all consume them — empty lists would have made those untestable. 170
  opponent ids and all 57 Digitama are placed.
- **Learnings for future iterations:**
  - **AUTHOR ROSTER-REFERENCING DATA FROM DISPLAY NAMES THROUGH A RESOLVER SCRIPT, NEVER BY TYPING
    IDS.** /tmp/us116/build_maps.py (throwaway, NOT committed) holds the 16 maps as display names
    and resolves each against `roster.json`, hard-failing on an unknown name, an AMBIGUOUS name, or
    a `dexOnly` entry, then emits the JSON. Every Phase E story has this same problem at ten times
    the scale and should do the same. It earned its keep immediately: `Rapidmon` is TWO roster
    entries — `rapidmon` (Perfect) and `rapidmon_armor` (Armor-Hybrid) — so a hand-typed
    `"rapidmon"` would have been a coin flip nobody would have noticed. Display names are not
    unique in this roster; ids are.
  - The same script asserts the whole-file invariants at authoring time (all 57 Digitama placed,
    none placed twice), so the test suite confirms them rather than discovering them.
  - `Roster.bundled.entries(at: .digitama)` is the clean way to enumerate eggs; there are exactly
    57 and NONE is dexOnly, so every egg on disk is placeable.
  - CONDITION UNITS, read off `HealthMetricReader` rather than guessed — steps: count;
    `health.sleep`: MINUTES (evolutions.json uses 1400–1800 over a stage); `health.activeEnergy`:
    kilocalories; `health.exerciseMinutes` / `health.standTime` / `health.mindfulMinutes`: minutes;
    `health.distanceWalkingRunning`: METRES; `health.standHours` / `health.flightsClimbed` /
    `health.workouts`: counts. Authoring "8 km" as `8` would have shipped a slot that drops on the
    first step.
  - The empty-on-hardware metric family (handwashing, toothbrushing, water, daylight, audio
    exposure) is a real trap for DROP slots specifically: an evolution edge gated on one can be a
    bonus branch beside a reachable one, but a Digitama slot is the ONLY route to that egg, so any
    such gate makes it unobtainable. FR-13 names `health.handwashing` for 06_industrial/Pulse, so
    it is authored as asked but PAIRED with a steps condition to satisfy US-128's "never solely"
    rule; every other slot avoids the family. If US-128 wants a stronger rule than "not solely",
    Pulse is the one slot to revisit.
- REMAINS: US-117 onward (3/56 passing). US-117 now has real data to validate — 16 assetNames,
  170 opponent ids, 57 Digitama slots and a 16-long unlock chain.
---

## 2026-07-22 — US-117: Map catalog validator
- `Sources/MapCatalogValidator.swift` in the `EvolutionGraphValidator` shape, deliberately down to
  the details: a `MapValidationError` enum (8 cases, `Equatable` + `CustomStringConvertible`), an
  `extension MapCatalog` with `validate(roster:assetExists:)` that RETURNS findings and never
  throws, and an injectable `MapCatalog.AssetExistsCheck` / `MapCatalog.assetExists()` mirroring
  `EvolutionGraph.SpriteExistsCheck` / `spriteExists(in:)`. Findings come back in map order with
  the unlock cycles appended last.
- Files: Sources/MapCatalogValidator.swift (new), Tests/MapCatalogValidatorTests.swift (new, 15
  tests). No project.yml change — `Sources` and `Tests` are directory globs.
- PASSED: build green. `Executed 1363 tests, with 0 failures` (1348 + the 15 new).
- PASSED, every rejection rule fired on its own fixture. Each fixture is the sound two-map catalog
  with EXACTLY ONE thing broken, so a reported error can only be the thing that was broken, and a
  control test asserts the sound catalog reports `[]`:
  `missingAsset` (an unknown imageset, and separately an empty `assetName` through the REAL
  check), `unknownOpponent`, `dexOnlyOpponent`, `unknownDigitama`, `notADigitama` (a Child in an
  egg slot), `unknownUnlockedBy`, `unlockCycle` (a 3-map loop, a map that unlocks ITSELF, and a
  loop three maps hang off — reported ONCE), `emptyConditionHint` (whitespace-only).
- PASSED, AC8 against the REAL data rather than a stub: `MapCatalog.load().validate()` reports
  ZERO findings over the shipped 16 maps, 170 opponent ids and 57 Digitama slots, using
  `Roster.bundled` and `UIImage(named:)`. The assertion's failure message joins the findings'
  `description`s, so the day someone retunes a map the test names the map and the id.
- **Learnings for future iterations:**
  - **A "the shipped file is clean" test is worthless without a companion test that the CHECK is a
    check.** `assetExists` is injected, so a green real-file run is also what you get from a stub
    that says yes to everything. `testTheRealAssetCheckDistinguishesShippedArtFromMissingArt`
    pins `01_grassland` true / `17_moonbase` false / `""` false. Any later validator with an
    injected world-touching check wants the same pair of tests.
  - **Order the rules so one mistake does not hide another.** A slot's blank-hint findings are
    collected BEFORE its roster lookup, so a slot with a bad id AND a blank hint reports both and
    the author fixes them in one run rather than two — the same reason `EvolutionGraphValidator`
    validates conditions before resolving the edge target. A test pins it.
  - **Cycle detection walks up from EVERY map, not from the roots.** A cycle that no other map
    hangs off is reachable from no root, so a "walk forward from `startingMap`" check — which is
    what US-116's reachability test does — cannot see it. Each cycle is keyed on its sorted
    members so the same loop found from four maps is one finding, not four.
  - `UIImage(named:)` works in the test bundle because the tests run in the app as TEST_HOST, so
    `.main` is the app bundle. Do NOT reach for `Bundle(for:)` for asset-catalog art — that is the
    XCTest bundle and has none of it. (`MapCatalogTests` already relied on this; it is now pinned.)
  - The validator takes `roster: Roster = .bundled` as a PARAMETER rather than reading the global
    inside, which is what let all nine fixture tests run against a 3-entry roster (one egg, one
    playable, one dexOnly) instead of hunting the real one for an id with the right shape. US-130's
    Jogress validator has the identical problem and should copy this signature.
- REMAINS: US-118 onward (4/56 passing). The map data is now guarded end to end — US-118 starts
  the behaviour half of Phase B by accruing steps to the selected map.
---

## 2026-07-22 — US-118: Steps accrue to the selected map
- `Sources/MapProgress.swift` (new): `@Model MapProgress` — ONE global record holding
  `selectedMapId`, `[mapId: recordedSteps]` and `[mapId: finishedAt]`, in `MetricLedger`'s
  dictionary shape — plus `MapStepCreditor.credit(steps:to:catalog:now:)` beside `EnergyCreditor`
  and `MetricCreditor`. Sixteen rows that are only ever read together are not sixteen records.
- `Sources/MetricLedger.swift`: new `MetricLedger.claim(_:dayTotal:now:calendar:)` and `rollDay`.
  `MetricCreditor` was refactored onto them, so the day-roll and the `max(0, total - baseline)`
  rule now live in exactly ONE place and the map spends the same delta any later metric consumer
  will. This is AC3 made structural rather than promised.
- `Sources/EnergyLedger.swift`: new `HealthDayReadings` + `HealthEnergySource.dayReadings(now:)`,
  keeping one read in both shapes — `byEnergyType` for energy, `quantities[.steps]` for the map.
  `readings(now:)` is now a one-line wrapper, so every existing caller and fixture is untouched.
- `Sources/GameStore.swift`: `MapProgress` added to `schema` + `loadOrCreateMapProgress()`.
- `Sources/MainScreenModel.swift`: holds `metricLedger` and `mapProgress`, takes `maps:` (injected
  like `graph`/`roster`), and gains `selectMap(_:)` and `creditMapSteps(_:)`. `selectedMapAsset` is
  now published FROM the saved selection through the catalog — the US-115 seam, repointed as that
  story predicted, with the view unchanged.
- Files: Sources/MapProgress.swift (new), Sources/MetricLedger.swift, Sources/EnergyLedger.swift,
  Sources/GameStore.swift, Sources/MainScreenModel.swift, Tests/MapProgressTests.swift (new, 23
  tests). No project.yml change — `Sources` and `Tests` are directory globs.
- PASSED: build green. `Executed 1386 tests, with 0 failures` (1363 + the 23 new). All 23 were also
  observed passing on their own with `-only-testing`, grepped rather than inferred.
- PASSED, criterion by criterion:
  - AC4, THE ONE THAT MATTERS: 1,000 steps read twice through the real `refresh()` credits the map
    1,000. A day that grew to 1,600 credits 600 more, not 1,600 more.
  - AC2/AC5: 600 steps on `first`, switch, 400 more -> `first` still 600 and `second` 400. Steps
    walked BEFORE any map was chosen credit nothing.
  - AC6: 999 does not finish a 1,000-step map and 1,000 does; a second and a third crossing keep
    the FIRST stamp. AC7: the counter runs on to 4,500 on that same 1,000-step map.
  - AC1: a second `MainScreenModel` over the same store file — which is what a cold launch is —
    reads back the steps, the stamp and the selection, and publishes `01_grassland` behind the
    Digimon again.
  - AC8: the clock is injected, every reading comes from a `FixtureSampleFetcher`, nothing waits.
  - Extra, not asked for but cheap: map progress survives a `rebirth`, because `resetGame` deletes
    only the `GameState`. US-123/129 both depend on that staying true.
- **Learnings for future iterations:**
  - **`MetricCreditor` HAS NEVER BEEN WIRED INTO THE APP, and that is a real bug waiting for a
    story.** US-058 built `MetricLedger`/`MetricCreditor` and deliberately left the wiring to a
    "which metrics are worth reading every refresh" story that was never written. Nothing in
    `Sources/` calls `MetricCreditor.credit` — so `state.stageMetricTotals` is permanently zero in
    the shipped app, and the **78 `window: "stage"` conditions in evolutions.json** (15 of them on
    `health.steps`) can never be satisfied by a real player. It was left alone here ON PURPOSE:
    wiring it changes evolution eligibility across the entire game and belongs in its own story
    with its own regression pass, not inside a map-accrual story. Whoever takes it: credit through
    `MetricLedger.claim` and have `creditMapSteps` read `credited[.healthSteps]` instead of
    claiming for itself — that is a two-line change and the doc comments on both point at it.
    Do NOT bank `health.sleep` through the same day-roll without thinking: its window is 18:00
    yesterday to noon today, not the local day `rollDay` keys on.
  - **A delta is SPENT once it is claimed, so decide deliberately who claims it.** With no map
    selected this story claims the step delta and DROPS it. The alternative — not claiming — would
    let a player who picks a map at 18:00 retroactively bank the whole day into it, which is
    exactly what "only steps read while that map is selected accrue to it" forbids. Six of the new
    tests failed on the first run because of this and they were RIGHT to: `start()` performs a
    refresh, so a fixture walked before `start()` is already banked by the time a test selects a
    map. Select the map first, then walk.
  - **Do not derive a raw measurement back out of an energy figure.** `readings[.strength]` is
    steps ÷ 100, capped by `EnergyRates.dailyCapPerEnergyType` — it is not a number of steps and
    cannot be turned back into one. That is why `dayReadings` exists; the alternative, a second
    HealthKit read for the map, would be two answers about one day taken microseconds apart with
    no reason to agree.
  - **A demo flag that writes to the SAVE needs its inverse in the same commit.** `-mapDemo=` now
    persists the selection, so it would have poisoned every later screenshot on that container the
    way `-lightOffDemo` did to US-115. `-mapDemo=none` clears it. The Codebase Patterns entry
    above was written from that exact bruise; this is the first flag to obey it up front.
  - `MapProgress` is one record with three properties and no relationships specifically so US-123
    can MOVE those fields onto `PlayerProfile` rather than rewrite them. It is already the thing
    US-123's AC describes.
- REMAINS: US-119 onward (5/56 passing). Phase B's data and accrual are done; US-119 is the first
  screen that reads them, and `MapProgress.recordedByMap` + `finishedAt(forMap:)` are what it
  wants. `MainScreenModel.selectMap(_:)` is already the call US-120's list makes.
---

## 2026-07-22 — US-119: Map list screen
- `Sources/MapListView.swift` (new): `MapListRow` — a pure value built from `MapCatalog` +
  `MapProgress`, carrying the name, the art, the floored counter, both marks, the lock, the unlock
  line and `contents` — plus `MapListMarks` (the three glyph names), `MapListSelector` (what a tap
  does to the selection) and `MapListView` itself. The view is a layout of those fields and holds
  no rule of its own, which is the only reason 30 tests can reach this screen at all.
- `Sources/MainScreenModel.swift`: `mapRows` (computed off the INJECTED catalog, like
  `selectedMapAsset`), and `seedMapListDemoIfRequested`.
- `Sources/ContentView.swift`: `MapListView` pushed onto the app's one `NavigationStack`, beside
  the Dex, so it keeps a back button and US-121 can push a detail on top of it.
- `Sources/MapProgress.swift`: `clearForDemo()`, DEBUG-only.
- Files: Sources/MapListView.swift (new), Sources/MainScreenModel.swift, Sources/ContentView.swift,
  Sources/MapProgress.swift, Tests/MapListTests.swift (new, 30 tests). No project.yml change —
  `Sources` and `Tests` are directory globs.
- PASSED: build green. `Executed 1416 tests, with 0 failures` (1386 + the 30 new). All 30 were also
  observed passing on their own with `-only-testing`, grepped rather than inferred.
- PASSED, criterion by criterion:
  - AC2/AC3: one row per map in catalog order (which is tier order, asserted as an invariant over
    the real file rather than as sixteen re-typed strings), and `progressText` is exactly
    `1222 / 25000`.
  - AC4: `checkmark.seal.fill`/green vs `figure.walk.circle.fill`/orange — different on BOTH
    channels, asserted pairwise and photographed together on one row.
  - AC5: a locked row's `contents` is nil and its line is `Finish <previous displayName>`; all 15
    locked rows over the shipped chain name a real map.
  - AC6: `MapListSelector` hands `current` back unchanged for a locked row, and the view disables
    the button as well.
  - AC7: `50000 / 50000` in full on 41mm and 46mm, no truncation, no ellipsis.
- **Learnings for future iterations:**
  - **NEVER FORMAT A COUNT WITH `NumberFormatter` WHEN THE CRITERION NAMES AN EXACT STRING.** It is
    locale-dependent: `25000` renders as `25,000` in en_US, `25.000` in de_DE and `25 000` (with a
    NON-BREAKING space) in fr_FR — three strings for one AC, and the third would pass a naive
    `contains(",")` check. String interpolation is the whole answer here. The test asserts the
    absence of `,`, `.` and `\u{00A0}` for exactly that reason.
  - **A DEMO FLAG THAT WRITES TO THE SAVE MUST BE IDEMPOTENT, not just reversible.** The Codebase
    Patterns rule above says give it an inverse, and US-118 obeyed it — but that is not enough:
    `-mapListDemo` credited a full map's worth on EVERY launch, so the second run photographed
    `100000 / 50000` and the third would have shown 150000. A doubled counter looks completely
    plausible in a screenshot. The seed now calls `clearForDemo()` first, so N launches and one
    launch produce the same screen. Add "clears before it seeds" to the rule.
  - That accident was worth something on its own: `100000 / 50000` — six digits over five — did not
    truncate on 41mm either, so the layout has more headroom than the AC asks for.
  - **`simctl` cannot scroll, so any list longer than a screen needs a `ScrollViewReader` +
    `-...BottomDemo` hook or its far end can never be photographed.** Third time this project has
    needed one (`-dexScrollDemo`, `-settingsBottomDemo`, now `-mapListBottomDemo`) and the 500ms
    sleep before `scrollTo` is needed every time — `.task` runs before the rows are measured, and
    scrolling to an unmeasured row lands short of it.
  - The queued-notification trap bit again on the 46mm sim and cost another ~5 minutes:
    `-noNotificationPrompt` does NOT dismiss an alert queued by an EARLIER build, so the first shot
    came back as the permission sheet in Chinese. `simctl erase` on the device is still the only
    fix, and `-healthAnswered` is mandatory afterwards or you land on onboarding.
  - **Lock rule is deliberately ONE STEP — "is my predecessor finished" — not a walk up the whole
    chain.** They agree on real data (a map cannot be finished without being selectable), and where
    they differ the one-step answer is the one the row's own "Finish <previous>" sentence promises.
    A lock that contradicts the line printed beside it is worse than a lock that opens early.
  - `isFinished` comes off the finish STAMP rather than `recorded >= total`. The difference bites
    the day a map is retuned longer in an update: the player really did cross the old line, and a
    derived flag would take it back off them. Same reasoning as `MapProgress.markFinished`'s
    once-only stamp, and US-121/122 should read the stamp too.
  - `Int(x.rounded(.down))` for the counter, never `Int(x.rounded())`: a rounded 2999.6 would print
    `3000 / 3000` on a map that is not finished, which is a screen contradicting itself.
- REMAINS: US-120 onward (6/56 passing). `MapListView(rows: model.mapRows) { model.selectMap($0) }`
  is exactly the expression US-120's strip needs to push, and the view already calls `dismiss()`
  after a selection, which is that story's "pops back to the main screen".
---

## 2026-07-22 — US-120: Map strip on the main screen, and switching maps — NOT PASSING (AC4)
- `Sources/MapStripView.swift` (new): `MapStrip` — a pure value carrying the map name, the counter,
  the map id and `isPrompt` — plus `MapStripMarks` (three glyphs), `MapStripLayout` (the font, the
  icon size, the disabled fade and `isPartyReachable`) and `MapStripView` itself, which is a
  `NavigationLink` to `MapListView` beside a faded, unreachable party button. Same split as
  `MapListRow`/`MapListView`: every rule is in the value, the view is a layout of its fields.
- `Sources/MainScreenModel.swift`: `mapStrip`, computed off the injected catalog and `MapProgress`
  exactly as `mapRows` is, so the strip and the list cannot drift; plus `-mapStripWidestDemo`.
- `Sources/ContentView.swift`: one row directly above the energy bars.
- Files: Sources/MapStripView.swift (new), Sources/MainScreenModel.swift, Sources/ContentView.swift,
  Tests/MapStripTests.swift (new, 19 tests). No project.yml change — `Sources` and `Tests` are
  directory globs, but `xcodegen generate` IS still needed for a NEW file or the build cannot see it.
- PASSED: build green. `Executed 1435 tests, with 0 failures` (1416 + the 19 new). All 19 also
  observed passing on their own with `-only-testing`, grepped rather than inferred.
- PASSED, criterion by criterion — everything except AC4:
  - AC1: one row, two controls — a wide leading `NavigationLink` labelled with the selected map's
    name and `recorded / total`, and a trailing party icon.
  - AC2: the party button is BOTH unreachable (`MapStripLayout.isPartyReachable == false`, so it is
    an `Image` and not a `Button` — it has no tap target at all) AND faded to 0.3. Either alone
    would be the half-measure the criterion forbids.
  - AC3: one line. Measured on screen: 13.5pt on 41mm, 14.0pt on 46mm.
  - AC5: a selection made through `selectMap(_:)` moves the strip's text and US-115's background
    together and SURVIVES A RELAUNCH — a second `MainScreenModel` over the same store file, which is
    what a cold launch is, reads back `Second` / `02_river`. `MapListView` already calls `dismiss()`,
    which is the "pops back".
  - AC6: with nothing selected the strip names map one as a prompt (folded-map glyph, secondary
    tint, `mapId` nil rather than "first" — the strip NAMES a map, it does not claim you are there),
    and the game is fully playable: `phase == .playing`, a presentation, energy bars, no gate.
  - AC7, MEASURED on both screens with `-mapStripWidestDemo`, which finishes every map and selects
    the LAST one, so the row reads `Cyberpunk 50000 / 50000` — the longest name beside the widest
    figure this row can ever carry. On 41mm the ink runs: glyph x7-20, "Cyberpunk" x31-137,
    "50000" x147-203, "/" x210-216, "50000" x223-280, an 18.0pt CLEAR GAP, party icon x317-344,
    3.5pt right margin. Nothing truncates: both five-digit runs are full width (57px and 58px) and
    there is no ellipsis. And nothing SHRANK either — "Cyberpunk" is 107px on 41mm and 107px on
    46mm, so `minimumScaleFactor(0.7)` never fired on the narrow screen. The two controls are
    18.0pt apart on 41mm and 49.5pt on 46mm, and the link's `contentShape` stops before the icon.
- **FAILED — AC4, and it is the only one. The sprite slot sits at EXACTLY the scale boundary on
  both watch sizes, so ANY row of ANY height costs the Digimon a whole scale step.**
  `SpriteScale.fitting` floors `slotHeight / 16`. Measured pre-story, hatched Child, map behind
  (`-battleAffordableDemo -mapDemo=01_grassland`, fresh install, both sims):
  - 41mm slot **49.0pt** -> floor(49/16) = 3 -> a 48pt Digimon. Slack: **1.0pt**.
  - 46mm slot **64.0pt** -> floor(64/16) = 4 -> a 64pt Digimon. Slack: **0.0pt**.
  Post-story, same command, same containers:
  - 41mm slot 49.0 -> **35.5pt**, Digimon 48 -> **32pt**.
  - 46mm slot 64.0 -> **50.0pt**, Digimon 64 -> **48pt**.
  On a FRESH INSTALL — the state AC4 actually names, "measured against a US-114 screenshot" — the
  Digimon does NOT shrink: the slot is already 27.5pt (41mm) / 42.5pt (46mm), below
  `SpriteScale.minimum`, so the sprite is pinned at 32.0pt in both builds and the `?` placeholder
  box (which is exactly `16 x scale` and so a perfect ruler) is byte-identical — x144-207 y137-200
  on 41mm, x176-239 on 46mm, before and after. But the SLOT still loses 13.5pt / 14.0pt
  (41mm 27.5 -> 14.0, 46mm 42.5 -> 28.5, measured by diffing a `-mapDemo=01_grassland` shot against
  a `-mapDemo=none` one), and the sprite's existing overflow grows until **the name line runs
  straight through the Digimon on 41mm** — photographed, the "?" spans y137-200 and
  "Agu Digitama · Digitama" now sits at y173-194 on top of it, where before it was at y200-221.
  Both readings of AC4 fail: the slot is smaller in every state, and the Digimon is a full scale
  step smaller in the state a real player spends the game in.
- **EVERY WAY OF PAYING FOR THE ROW WAS MEASURED, and none of them is this story's to spend:**
  - **The bottom safe area is real, unused, and enough** — 26pt on 41mm, 36pt on 46mm, below the
    action row. Proved with a throwaway `.ignoresSafeArea(.container, edges: .bottom)` probe build:
    the slot went 49 -> **75pt** on 41mm and 64 -> **100pt** on 46mm. **But claiming it slides the
    action row's five 30pt circles to within 12pt of the display's bottom edge, and the outer two
    sit 5pt from the side edges — that is inside the corner arc, which is what the inset is for.**
    Half of it is no better: at 12pt up, a ~35-40pt corner radius cuts in ~9-11pt horizontally.
  - **Shrinking the surrounding chrome adds up to about 14pt** — name 12 -> 9pt (3.6), stats 11 ->
    9pt (2.4), `ActionControls`' `.padding(.top, 2)` (2.0), `VStack(spacing: 1)` -> 0 (5.0), energy
    bars spacing (1.0). It would work. It is also a screen-wide legibility regression bought with
    one row, unrequested by this story, and it leaves the layout with zero breathing room for the
    next one.
  - **Merging the strip into the name/stage row does not fit.** At its widest the strip's own ink
    already runs x7-344 of 352 on 41mm; "Agu Digitama · Digitama" needs ~140pt more.
  - Shrinking the strip itself buys nothing: with 1.0pt and 0.0pt of slack, a 9pt row fails too.
- **Learnings for future iterations:**
  - **THE MAIN SCREEN HAS NO VERTICAL BUDGET LEFT — it is at the scale boundary to the point, on
    both watch sizes.** 49.0pt and 64.0pt against a 16pt quantum. Any story that adds a row to
    `ContentView`'s VStack costs the Digimon a third (41mm) or a quarter (46mm) of its size, and no
    amount of making the row thin avoids it. Measure the slot BEFORE designing the row; the map
    band under `-mapDemo=<asset>` is the slot, exactly, and diffing it against `-mapDemo=none`
    isolates it in one command.
  - **The `?` placeholder is a free ruler.** `DigimonSpriteView.placeholder` is a stroked rounded
    rect at exactly `16 x scale` points, and a brand-new save is a Digitama whose sheet yields no
    frames, so a fresh install draws a box whose width IS the sprite scale. Far easier to measure
    than a Digimon's silhouette, and it needs no matched walk frame.
  - **The bottom 26pt/36pt of this screen is safe-area inset, not slack** — verified, not assumed.
    If a later story truly needs a line, that is where it is, and the price is the action row's
    corners.
  - Two screenshot traps bit again, both already in Codebase Patterns and both still worth the
    minutes they cost: `xcodegen generate` is needed for a NEW source file even though `Sources` is
    a glob (the build failed with "cannot find type 'MapStrip' in scope" until it was re-run), and
    the queued notification alert survived into the 46mm run in Chinese and ate a whole screenshot
    pass — `simctl erase` on the device, then `-healthAnswered`, is still the only fix.
  - `-mapStripWidestDemo` is the third mode of `seedMapListDemoIfRequested` and obeys the same two
    rules the others learned the hard way: it clears before it seeds (idempotent), and
    `-mapProgressResetDemo` is its inverse. Both containers were reset and erased after the pass.
- REMAINS: US-120 (6/56 passing, unchanged). The strip itself is finished, tested and photographed;
  what is open is a layout-budget decision that belongs to a person, not to a measurement — accept
  a one-step smaller Digimon, or free a full line first and say which of the three ways above.
---

## 2026-07-22 — US-120: Map strip on the main screen, and switching maps — NOW PASSING
- The previous iteration left AC4 as the only failure and framed it as a decision rather than a
  measurement: accept a one-step smaller Digimon, or free a full line first. **This iteration freed
  the line.** No change to `MapStripView`, `MainScreenModel` or the strip's own tests — the strip was
  already finished. What changed is who pays for its ~13.5pt.
- **The five trims, and why each is the cheapest point on the screen:**
  - `ContentView`'s `VStack(spacing: 1)` -> `0` — **5.0pt**, five gaps. Every row here is already a
    self-contained band of text or controls, so the gaps bought air between things that do not touch.
  - The name/stage line `12` -> `9pt` — **3.6pt**. 9 is already this screen's small size: the stats
    labels and the action row's caption are both 9. The name is also the one thing a glance does not
    need — the sprite says which Digimon it is, and a 48pt sprite says it better than a 12pt caption.
  - `StatsStrip`'s values `11` -> `9pt` — **2.4pt**. The strip's height is its tallest element and
    the values were it. They keep their semibold weight and their tint, which is what separated them
    from the labels; the size was not doing that work.
  - `ActionControls`' `.padding(.top, 2)` — **2.0pt**. The circles are 30pt discs with their own
    visual margin.
  - `EnergyBarsView`'s bar-row spacing `2` -> `1` — **1.0pt**. The hatch caption below keeps its gap;
    text against bars is the one place the separation does real work.
- Files: Sources/ContentView.swift, Sources/EnergyBarsView.swift, Tests/MainScreenLayoutTests.swift.
- PASSED: build green. `Executed 1436 tests, with 0 failures` (1435 + 1 new), the new one also
  observed passing on its own with `-only-testing`.
- **AC4 PASSED, MEASURED — the criterion that cost two iterations.** Pre- vs post-story, both watch
  sizes, fresh install, identical script and identical launch command. The baseline is a REAL BUILD
  of `858fa43` (US-119, the last commit before the strip) made in a `git worktree` — and it
  reproduced the previous iteration's numbers to the point, 49.0pt on 41mm and 64.0pt on 46mm, which
  is what makes the comparison worth anything.
  - **SLOT: 41mm 49.0 -> 49.5pt. 46mm 64.0 -> 64.0pt.** Not smaller on either.
  - **SPRITE**, measured directly as ink on black inside the slot: 41mm **78x72px in BOTH** builds,
    the same size at a different walk offset; 46mm **104px wide in BOTH**. Scale 3 on 41mm and 4 on
    46mm before and after. (The 46mm 104 vs 96 HEIGHT is the walk1/walk2 confound — width is the
    honest signal, and 78/104 = 3/4 confirms both scales independently.)
  - The name line no longer runs through the Digimon, which is the defect the previous iteration
    photographed: sprite ink ends y221 and the name starts y233 on 41mm (6.0pt clear), y266 and y279
    on 46mm (6.5pt clear).
- **A MEASUREMENT BUG THAT NEARLY PRODUCED A FALSE PASS — in the generous direction, which is the
  dangerous one.** Diffing `-mapDemo=<asset>` against `-mapDemo=none` on "rows that differ at all"
  reported the slot as **72.0pt / 87.5pt** — a ~23pt gain from ~14pt of trims, which is what gave it
  away. `-mapDemo=none` does not only remove the map: it flips the strip to its PROMPT state (grey
  folded-map glyph for the orange walk glyph) and nudges the name row, so those two text rows differ
  for reasons that have nothing to do with the background. Fix: the map band is FULL WIDTH (344 of
  352px on 41mm) and a text row differs over at most half of it, so require **>=75% of the width**
  to differ. The band then comes out contiguous on all four shots and lands on the real numbers.
- AC7 re-verified on the new build with `-mapStripWidestDemo`, since the fonts around the strip
  moved: 41mm glyph x7-19, "Cyberpunk" x31-137, "50000" x147-202, "/" x210-216, "50000" x224-279, an
  **18.5pt clear gap**, party icon x317-343, 4.5pt right margin. Both five-digit runs are the full
  56px with no ellipsis, and "Cyberpunk" is 107px on BOTH screens, so `minimumScaleFactor(0.7)` never
  fired on the narrow one. US-115 is intact: the map band is exactly the slot on both sizes.
- **Learnings for future iterations:**
  - **The screen's vertical budget was not exhausted — it was unaudited.** The previous iteration
    priced the chrome trim at "about 14pt" and set it aside as "a screen-wide legibility regression
    bought with one row". Measured, **7 of those 14 points are pure whitespace** (five 1pt VStack
    gaps and a 2pt padding) and cost no legibility at all, and the other ~6 move two labels onto a
    size the same screen already uses in three other places. Price a trade before declining it.
  - **A demo flag's inverse is rarely a pure inverse.** `-mapDemo=none` was built as US-115's undo,
    but it also drives the strip's prompt state — so it changes two things and a diff attributes
    both to the map. When a before/after rests on one flag, check what ELSE that flag reaches.
  - **A result that beats its own arithmetic is a bug report.** +23pt from 14pt of trims was not good
    news, it was a broken measurement. The estimate was right the whole time.
  - Build the pre-story baseline in a **`git worktree`**, not a `git stash`: the two builds land in
    different DerivedData directories, so there is no copy-the-app-out-first race, and the working
    tree is never disturbed. `git worktree remove --force` when done.
- REMAINS: US-121 onward (7/56 passing). Phase B has two stories left, US-121 (map detail) and
  US-122 (opponents from the map); both consume `opponentPool` and `digitamaSlots`, which US-116
  already authored.
---

## 2026-07-22 — US-121: Map detail shows opponents and Digitama with progressive hints
- `Sources/MapDetailView.swift`: `MapDetail` — a pure value built from the catalog, the roster, the
  Dex and the player's counters — plus `MapDetailView`, pushed from `MapListView` with
  `navigationDestination(item:)`. `MainScreenModel` gains `discoveredDigimonIds` and
  `mapDetail(for:)`; `ConditionHintRow` in `DexView.swift` stopped being `private`.
- Files: Sources/MapDetailView.swift (new), Sources/MapListView.swift, Sources/MainScreenModel.swift,
  Sources/ContentView.swift, Sources/DexView.swift, Tests/MapDetailTests.swift (new).
- PASSED: build green. `Executed 1466 tests, with 0 failures` (1436 + 30 new), and all 30 also
  observed passing on their own with `-only-testing`, grepped rather than inferred.
- **THE NAVIGATION CHANGED, and it is the one decision in this story worth a second reader.**
  US-119 made a row tap select the map and pop back. AC1 here says a tap opens the detail. Both
  cannot be a tap on the same row, so travelling moved one step deeper: the row opens the detail,
  and the detail carries a **"Travel here"** button that selects through `MapListSelector` and drops
  BOTH levels. Everything US-120 AC5 asked of a selection still holds of it — made through the
  selector, persisted, moves the US-115 background and the strip, ends on the main screen — and what
  the player gains is seeing what is in a map before committing their steps to it. `MapListSelector`
  and every US-119 test are untouched, because they were always pure functions of `(row, current)`.
  **NOT TAP-VERIFIED**: `simctl` has no tap command, so the two-level pop (`openMapId = nil` then
  `dismiss()`) is reasoned rather than photographed. If SwiftUI honours only one of the two the
  player lands on the map list, which is a degradation and not a hang.
- PASSED, criterion by criterion:
  - **AC1**, screenshotted on 46mm (`-mapDetailFoesDemo`): the pool draws **grouped**, with the
    `Child` heading between Nyaromon/Tokomon and Agumon/Patamon — grouping, not merely ordering.
    Group order is `Stage.allCases` and not `ladderIndex`, because Armor-Hybrid has no rung to sort
    on and `allCases` is already authored in ladder order with the side branch last. Within a group
    the authored order stands: that is what US-116 wrote and what US-122 will band.
  - **AC2 / AC3 / AC4 / AC5 are all on ONE 46mm screenshot** (`-mapDetailSlotsDemo`), which is what
    that flag exists for: `Agu Digitama` with its real sprite and name and **no hint rows**; a `?`
    marked `Ready to find` (sparkles, yellow) over a green-checked "Walk 2,000 steps in a day"; and
    a second `?` with an open circle beside "Fight one battle". Three states, one shot.
  - AC3 is not merely the same WORDING as the Dex's hints — the view draws them with
    **`ConditionHintRow`, the Dex's own row type**, made non-private for this. A second
    implementation could drift a word or a threshold, and the player would be reading two different
    promises about one `ConditionReveal` output.
  - AC4's "no hint rows" is enforced **on the value**: a revealed slot's `conditions` is empty, so
    there is nothing for the view to draw even if it tried. A test asserts the withheld slot beside
    it still has its own, so the emptiness is the reveal and not a slot that lost them on the way in.
  - AC5 is `!revealed && ConditionReveal.allMet` — an egg already owned is one that HAS dropped, so
    it is never "ready" however met its conditions are. Pinned against an unowned slot that IS ready
    on the same context, so the false is the ownership and not the context.
  - **AC6**: `MapDetail.make` returns **nil** for a locked row — not a stripped detail. There is
    nothing to push, the row's button is disabled as well, and a test shows the detail appearing the
    moment the lock opens on the same save, so the nil is the lock and not the fixture.
- Reveal source is the **Dex** (`GameStore.dexIds`), which is written the moment an egg is handed
  over or hatched and survives death and rebirth — exactly the span AC4's "owns or has owned" names.
  US-127's held-Digitama set will refine it; the seam is `MapDetail.make(discovered:)`.
- **Learnings for future iterations:**
  - **A screenshot caught a defect no test could have.** The withheld row drew `?` twice — the art
    tile and the title — which reads as a rendering fault rather than as one withheld egg. Both were
    "correct" by every assertion in the suite. The title is now drawn only for a revealed slot.
  - **A demo flag does not have to write to the save.** All three of this story's
    (`-mapDetailDemo`, `-mapDetailSlotsDemo`, `-mapDetailFoesDemo`) seed through LITERALS — a
    `ConditionContext` and a discovery `Set` unioned at read time — so unlike US-119's there is
    nothing to undo and no `-…ResetDemo` inverse to remember. `DexModel.revealDemoContext` is the
    older example of the same trick. Prefer it: the container cannot be poisoned by something that
    never reaches disk.
  - When two screenshot flags stage the same screen at different scroll positions, the seeding must
    answer to **both** or the scrolled shot photographs an unseeded screen. `isMapDetailDemo` is one
    predicate all three flags go through, for exactly that reason.
  - To photograph that a list is GROUPED rather than merely ordered, scroll to the **first** row of
    the last group with `anchor: .bottom` — the heading lands on screen with the tail of the group
    before it. Scrolling to the last row shows three rows that could have come from anywhere.
  - The starting Digitama is **random among the graph's six**, and only three of them live in the
    starting map — so "a fresh install reveals a slot" is a coin flip, not a fact. Any screenshot
    that needs a revealed entry has to seed it.
  - `xcodebuild`'s DerivedData path for this project is not stable across runs; `find … -name
    DigiVPet.app -path "*Debug-watchsimulator*"` returns several, and the one to install is the
    NEWEST by mtime. Installing the wrong one silently photographs the previous story.
- REMAINS: US-122 onward (8/56 passing). Phase B is finished with US-122, which consumes the same
  `opponentPool` this screen now draws.
---

## 2026-07-22 — US-122: Opponents come from the map and scale with progress
- `Sources/MapOpponents.swift`: `MapOpponentBand` — the pure band arithmetic, with no generator in
  it at all — plus `BattleMatchmaker.choose(in:roster:playerId:map:recorded:using:)`. The strength
  roll moved out of the old `choose` into `BattleMatchmaker.rolled`, so the map path and the
  roster path cannot drift apart.
- Files: Sources/MapOpponents.swift (new), Sources/Battle.swift, Sources/MainScreenModel.swift,
  Tests/MapOpponentTests.swift (new). **No existing test file was touched** — AC8's "existing
  battle tests pass unchanged" holds literally, not just in effect.
- PASSED: build green. `Executed 1496 tests, with 0 failures` (1466 + 30 new). All 30 new tests
  also observed passing on their own, and the 8 pre-existing `BattleMatchmakerTests` with them.

- **THE FIRST BUILD FOUND A REAL COLLISION, and it is the thing to carry forward:**
  **a map's `opponentPool` is authored and validated against the ROSTER, but `BattleMatchmaker`
  resolves against the `EvolutionGraph` — and those are not the same set.** The roster has 1025
  entries; `evolutions.json` authors 88 nodes. Resolving a pool through the graph dropped nearly
  every opponent every map names, the pool came back empty, and the code fell through to the
  roster-wide pick — US-122 doing nothing at all while its own fixture tests passed.
  - Caught ONLY because the shipped-catalog sweep asserts the drawn id is *in the map's pool*
    rather than merely "an opponent came back". `11_city_night` (ten Ultimates) drew `ogremon`.
  - Fix: `MapOpponentBand.nodes` resolves graph-node-first, and promotes a `RosterEntry` to an
    `EvolutionNode` otherwise. An opponent needs a name, a stage and a sheet; it does not need
    outgoing edges. The graph's node still wins where both exist, because it carries the `line`.
  - A promoted node carries an EMPTY `line`, which is honest: a roster-only Digimon is in no Dex
    tree. `MoveCatalog` and `ElementCatalog` both miss `lineDefaults` on it and fall through to
    their stage / keyword tiers, which is what "no line known" should produce.
  - Two element lookups in `MainScreenModel` moved from `types.type(for:in: graph)` to
    `types.type(forId:line:)` off the node already in hand — identical for an on-graph opponent,
    strictly better for an off-graph one (which the graph form would answer `.unauthored`).

- THE BAND RULE, as implemented and as measured:
  - `index(recorded:total:)` is AC2's literal quartile, pinned from BOTH sides at all three
    boundaries so an accidental `<=` fails the suite. `total <= 0` reads as the lowest band rather
    than dividing by zero.
  - The tier clamp is a rung table — t1 `2...3` (Baby II–Child) up to t5 `6...6` (Ultimate alone) —
    and it does real work: `13_factory_town` carries one Adult among its Perfects and Ultimates,
    which is dropped rather than fought at the start of a tier 4 map.
  - Within the clamped pool the four bands INTERPOLATE over the rungs the pool actually spans, so
    index 0 *is* the pool's lowest band and index 3 *is* its highest whatever it holds. A 4-rung
    pool gives a stage per band; the 2-rung shape every shipped map has splits at halfway.
  - AC4's nearest-populated fallback is tested three ways: nearer below, nearer above, and a tie —
    which breaks DOWNWARD, and that tie-break is exactly what keeps AC3's "never above it" true in
    the case where the top band itself is empty.
- AC6 IS ASSERTED AGAINST THE OLD CALL, not against a remembered list: with `map` nil the new
  signature returns the same node and the same rolled power at 50 seeds, and leaves the generator
  in the same place — so the battle that draws from it afterwards resolves identically too. That
  second half matters because `MainScreenModel` carries ONE generator through matchmaking and the
  fight.
- SHIPPED DATA verified twice over, by two implementations:
  - the test sweeps all 16 maps at 6 ratios x 5 seeds — always from the map's own pool, never above
    its tier, the band never falls as progress rises, `01_grassland` starts on Baby II and
    `12_cyberpunk` on Ultimate;
  - and a throwaway Python reimplementation of the rule read straight off `maps.json` +
    `roster.json` produces the same table map-for-map: every map steps up at halfway, tier 5 is one
    band throughout, `13_factory_town` q0 is Perfect and not its stray Adult.
- Also carried the map into the DEBUG `demoBattleSeed` search. It exists to replay exactly what
  `battle()` is about to draw, and since this story that depends on where the player is adventuring.

- **Learnings for future iterations:**
  - **`Roster` (1025) and `EvolutionGraph` (88) are DIFFERENT sets, and which one a feature resolves
    against is a design decision, not a detail.** Data authored against the roster — map pools,
    Digitama slots — cannot be looked up through the graph. Until Phase E wires the 780 orphans,
    anything that says "a Digimon" and means "any of them" must go through the roster.
  - A "did something come back?" assertion cannot catch a silent fallback. Assert what came back is
    from the set you asked about. That one line is the whole reason this story is not a false pass.
  - `EvolutionNode` is constructible from a `RosterEntry` with an empty `line`; both catalogs that
    read a line degrade gracefully through it. That is the bridge for any later story that needs a
    roster-only Digimon where a node is expected.
---

## 2026-07-22 — US-123: PlayerProfile holds what outlives a Digimon, and the existing save migrates
- `Sources/PlayerProfile.swift`: `@Model PlayerProfile` — `lifetimeEnergy`, `selectedMapId`, the
  per-map `recorded`/`finishedAt` dictionaries, and `ownedDigitamaIds` — with `MapStepCreditor`
  moved onto it. `GameStore` gains `loadOrCreateProfile(roster:)` and loses
  `loadOrCreateMapProgress`. `MapProgress` survives as a DRAIN-ONLY legacy `@Model`, still in the
  schema, read by the migration and by nothing else.
- Files: Sources/PlayerProfile.swift (new), Sources/MapProgress.swift, Sources/GameStore.swift,
  Sources/GameState.swift, Sources/EnergyLedger.swift, Sources/BattlePower.swift,
  Sources/Death.swift, Sources/MainScreenModel.swift, Sources/ContentView.swift,
  Sources/MapListView.swift, Sources/MapStripView.swift, Sources/MapDetailView.swift,
  Sources/MapCatalog.swift, Tests/PlayerProfileTests.swift (new), Tests/Fixtures/pre-us123.store
  (new, binary), and 16 existing test files updated for the moved property.
- PASSED: build green. `Executed 1510 tests, with 0 failures` (1496 + 14 new), and all 14 also
  observed passing on their own with `-only-testing`.

- **THE MIGRATION IS TESTED AGAINST A FILE THE PREVIOUS BUILD WROTE, and that is the whole story.**
  AC6 says a test that constructs the NEW shape does not satisfy it — so before a line of code was
  changed, a throwaway `ZZPreUS123FixtureWriter` test was run ON THE US-122 COMMIT, writing a fully
  populated store into the Simulator's tmp. That file is now `Tests/Fixtures/pre-us123.store`
  (96KB), and it is genuinely the old shape: `sqlite3 … "select name from sqlite_master"` shows
  `ZGAMESTATE` and `ZMAPPROGRESS` and no `ZPLAYERPROFILE`. Five tests open a COPY of it (SwiftData
  migrates in place, so the bundle's copy must never be the one opened).
  - AC5 observed in full: `greymon` comes back Adult with stageEnergy 11/22/33/44, both dates, 2
    care mistakes, hunger 3, strength 17, `.sick` with its `sickSince`, a 5–6 battle record, 4
    poops and the light off — and 55/66/77/88 on a profile that did not exist when the file was
    written.
  - The fifth runs the SAME file through `MainScreenModel.start()` — the app's own path, the real
    graph and roster — and gets `.playing`, `greymon`, the same totals through `model.lifetimeEnergy`
    and the migrated `02_river` selection driving US-115's background.
- **AC3 HAS ONE DELIBERATE EXCEPTION AND IT IS WORTH A REVIEWER'S ATTENTION.** The Swift property is
  gone from `GameState`'s API — `private`, exposed only as the read-only `legacyLifetimeEnergy` the
  migration drains — but THE COLUMN STAYS. Deleting it deletes a live player's earnings with no
  second chance to read them. Renaming is no safer: opening the fixture in `sqlite3` shows SwiftData
  FLATTENS an `EnergyTotals` into one column per field named BY POSITION — `ZSTRENGTH1`,
  `ZSTRENGTH2`, `ZSTRENGTH` — not by property name, so a rename is a schema change that has to be
  exactly right for the one value nothing is allowed to lose. Four unread columns is the cheaper
  side of that trade.
- Every read in the game goes through the profile: `EnergyCreditor.credit` gained a `profile:`
  parameter and tops it up beside `stageEnergy`; `GameState.battlePower` and `memorial(displayName:)`
  now TAKE the total rather than reading it; `MainScreenModel.lifetimeEnergy` is the single place
  the "no profile yet" answer (`.zero`) is decided.
- Map progress MIGRATES rather than being dropped. `loadOrCreateProfile` adopts `MapProgress`'
  three fields and DELETES the record — asserted, including that no row is left behind. Two records
  answering "where am I adventuring" is one too many, and the one nothing writes is the one that
  loses.
- `ownedDigitamaIds` is seeded from the DEX, filtered through the roster's stages: the Dex has
  recorded every egg the moment it was handed over since US-016, so "ever owned" is a fact the old
  save already knows rather than a guess. The fixture's Dex holds `greymon` and `agu_digitama`;
  only the egg comes across. `resetGame` and `rebirth` record the egg they hand over.
- `resetGame` now wipes the profile's total EXPLICITLY (it used to fall out of deleting the state)
  and `rebirth` reads-then-restores it, so both pre-story behaviours hold with the value on its new
  home. A test pins that a rebirth keeps 111/222/333/444 AND the 900 steps already walked.

- **Learnings for future iterations:**
  - **To test a migration, write the fixture BEFORE you write the code.** One throwaway test on the
    current commit, run once, and the store harvested out of
    `~/Library/Developer/CoreSimulator/Devices/<UDID>/data/Containers/Data/Application/<uuid>/tmp/`
    (print the path from the test; it appears in the `xcodebuild` log). It costs one test run and it
    is the only artefact that can prove a migration at all. A `git worktree` build would work too,
    but is not needed if you do it first.
  - **`Tests/Fixtures/*` is bundled into the test target automatically** — xcodegen puts unknown
    file types in the resources phase, so no `project.yml` change was needed. Read it with
    `Bundle(for: Self.self).url(forResource:withExtension:)` and COPY IT OUT before opening: SwiftData
    migrates in place, so the second test to run would otherwise find it already migrated.
  - Only the `.store` needs committing. Its `-wal` came out 0 bytes (checkpointed on close) and the
    `-shm` is rebuildable; the tests pass with the two sidecars deleted from the repo.
  - **SwiftData flattens a composite attribute into one column per field, named by POSITION.**
    `EnergyTotals` becomes `ZSTRENGTH/ZVITALITY/…`, `ZSTRENGTH1/…`, `ZSTRENGTH2/…` in declaration
    order. Consequence: removing or reordering one composite is not a local change. `sqlite3
    <store> "pragma table_info(ZGAMESTATE)"` is how to look before leaping.
  - There was already a precedent for schema-migration tests in this repo and it is worth copying:
    `EnergyCreditingTests.testAStoreWrittenBeforeTheLedgerExistedStillOpens` opens a container on an
    explicit OLD `Schema([...])`, and `DominantEnergyTests.PreRecencySchema` declares a whole `@Model`
    of the previous shape inside a private enum. Both work; neither can express a property the new
    build has made private, which is why this story needed a file instead.
  - When a widely-read property moves off a `@Model`, the test churn is in the SETUP, not the
    assertions: 16 files set `state.lifetimeEnergy` to stage a scenario. Three of them
    (`FeedingTests`, `TrainingTests`, `TrainingMinigameTests`) asserted "spending does not rewrite
    the lifetime total" — a claim that is now structural, because those actions are handed the
    `GameState` alone. They were kept, on a `PlayerProfile` the action cannot reach, as the test
    that fails the day someone passes the profile in too.
- REMAINS: US-124 onward (10/56 passing). Phase C continues with US-124's multi-state store, which
  `PlayerProfile` was the precondition for.
---


## 2026-07-22 — US-124: The store can hold many Digimon, one of them active
- `GameState` gains `private var isActiveStorage: Bool?` behind a computed `isActive`, and
  `init(currentDigimonId:stage:isActive:now:)` with `isActive` defaulting to true — so every path
  that exists today (first launch, reset, rebirth) is untouched and US-126 is the first caller that
  will pass false.
- `GameStore` gains `allStates()` (oldest first), `activeState()`, `activate(_:)` and
  `GameStoreError.stateNotInStore`; `savedState()` is now `activeState()`; `loadOrCreate` fetches
  the ACTIVE record; the US-123 profile migration stamps `isActive = true` on a lone record.
- Files: Sources/GameState.swift, Sources/GameStore.swift, Tests/GameBoxTests.swift (new, 23 tests).
  Not one existing test file was edited.
- PASSED: build green. `Executed 1533 tests, with 0 failures` (1510 + 23), and the 23 also observed
  passing on their own with `-only-testing:DigiVPetTests/GameBoxTests`.
- THE INVARIANT LIVES IN THE STORE, NOT ON THE RECORD. "Exactly one active" is a fact about the
  whole store, so a `GameState.isActive` that callers set would be a footgun: flipping one on leaves
  two active unless another is flipped off in the SAME transaction, and only the store can promise
  that. `activate(_:)` writes EVERY record it fetched — not just the two it is moving between — and
  then calls `save()` once. A store hand-corrupted into two active and then activated onto a third
  comes back with exactly one; that is a test, not a hope.
- AC5's "after a failed activate" is a REAL failure and not a contrived throw: `activate` is handed
  a live, inserted `GameState` belonging to a DIFFERENT `GameStore` — what a party screen caching a
  row across stores would do. It is refused on `persistentModelID` BEFORE anything is mutated, so
  there is nothing to roll back; asserted both in memory and through a reopen. A `save()` that
  throws after the flips restores every flag, which is the half no test can reach and which is
  therefore stated in the code.
- AC3 PROVED AGAINST A REAL PRE-BOX FILE AND THEN CHECKED IN SQLITE3. `Tests/Fixtures/pre-us123.store`
  (committed by US-123) predates this story too: `pragma table_info(ZGAMESTATE)` on the committed
  file has **no `ZISACTIVESTORAGE` column at all**. Opened through the new `GameStore` and migrated,
  the row reads `greymon|1`; a box written by this build reads `agu_digitama|1, gabu_digitama|0`. So
  the stamp is a real write and not the default reading itself back.
- TWO MECHANISMS ON PURPOSE, because one is not enough:
  - The migration stamp only fires on a store with no profile yet. **A player who ran the US-123
    build already HAS one**, so `loadOrCreateProfile` returns early and never reaches the stamp.
  - So `nil` ALSO reads as ACTIVE. That is the safe direction rather than a convenience: the only
    record that can carry a `nil` is the single Digimon of a pre-box store, and reading it as frozen
    would hand that player a fresh egg and leave their Digimon in a store nothing looks at. A test
    opens the fixture and calls `savedState()` WITHOUT migrating, to pin exactly that.
  - The stamp is guarded on `states.count == 1`: it is a migration, not a repair. On a store that
    already has a box, the record fetched first is not necessarily the one that was out.
- AC6/AC7: `resetGame` still deletes the WHOLE box — a total wipe means all of them — and its fresh
  egg is born active. `rebirth` still carries `lifetimeEnergy` and still ends with one out.
  `loadOrCreate` returns the active record out of a box where it is NOT the first, which is the case
  that would have deleted two Digimon if it had fallen through to `resetGame`.
- ADDED DEFENCE, not asked for: a box with records but NOTHING active adopts the oldest instead of
  falling through to `resetGame`. Nothing in this code can produce that state — which is the point:
  the cost of being wrong about that is deleting Digimon the player still owns.
- **Learnings for future iterations:**
  - **Never `sqlite3` a file under `Tests/Fixtures/` and leave it.** Even a read-only `select` makes
    `-wal`/`-shm` sidecars, `project.yml` globs that directory, and the next `xcodegen generate`
    bakes them into the test target — the build then fails with `The file "pre-us123.store-shm"
    couldn't be opened`. Delete the sidecars AND re-run `xcodegen` before building. Cost one full
    test cycle this iteration.
  - `FetchDescriptor<GameState>(sortBy: [SortDescriptor(\.birthDate)])` works and is worth using
    over an unordered fetch: the box's order is now player-visible in US-126, and an order that
    depends on what SwiftData hands back reshuffles between launches.
  - An optional-backed `Bool?` whose `nil` default is chosen for MIGRATION reasons wants that
    reasoning written on the storage property, not on the accessor — the next reader's question is
    "why isn't this just `Bool`", and the answer is that SwiftData will not migrate a non-optional
    attribute into an already-shipped store without a default (the store opens, then the next save
    fails with "is a required value").
  - US-125 freezes on `frozenSince`/`frozenDuration`. `isActive == false` is what it will key off,
    and `allStates()` is how it reaches the frozen ones. US-126's party list is `allStates()` plus
    `activate(_:)`, both of which now exist and are tested.
- REMAINS: US-125 onward (11/56 passing).
---

## 2026-07-22 — US-125: Frozen Digimon do not hunger, age, or evolve
- New `Sources/Freeze.swift`: `freeze(at:)` / `thaw(at:)` / `undo(_:)` on `GameState`, plus
  `frozenSince` and a cumulative `frozenDuration` (both new optional-backed storage on the `@Model`).
  `GameStore.activate(_:now:)` gained the `now:` and now moves the freeze clock in the SAME
  transaction the `isActive` flips already ran in — the two are halves of one fact, and a store that
  saved one without the other would either measure a spell that never happened or lose one that did.
- Guards added to `advanceHunger`, `advancePoop`, `claimPoopNotification`, `auditCareMistakes`,
  `recordRefusal`, `recordWakingEarly`, `updateSickness`, `updateDeath`, `claimDeathWarning`, and
  `MainScreenModel.hatchIfReady` / `evolveIfReady`.
- Files: Sources/{Freeze,GameState,GameStore,HungerClock,Poop,CareMistakes,Sickness,Death,
  MainScreenModel}.swift, Tests/FreezeTests.swift (new, 27 tests).
- PASSED: build green. `Executed 1560 tests, with 0 failures` (1533 + 27).
- **TWO MECHANISMS, AND THE STORY NEEDS BOTH.** Guards stop the STORED counters (hunger, poop, care
  mistakes) moving. They cannot touch the readings DERIVED fresh from a saved date — age is
  `now - birthDate` however long ago that was, and nothing runs to guard. So `thaw` translates the
  whole timeline forward by the frozen span instead. Rejected alternative: leave the dates and
  subtract `frozenDuration` at every read — that means finding and correcting every subtraction in
  the game, forever, and one missed reader is a Digimon that quietly starved in the box.
- **BOTH HALVES WERE MEASURED BY BREAKING THEM.** Deleting every `isActive` guard fails all 7
  `FreezeAccrualTests`. Deleting `shiftTimeline` fails 4 `FreezeOffsetTests` with 14 assertion
  failures. Each accrual test asserts the frozen Digimon against the SAME rule run on an active one
  in the same test, so a rule that does nothing at all cannot pass silently.
- AC3/AC4 run the REAL rules after the thaw rather than reading values back — `advanceHunger`,
  `advancePoop`, `auditCareMistakes` — over 3 days and again over 30. Age and time-in-stage come out
  equal to 0.001s; hunger, poop and the mistake count are untouched.
- **DELIBERATE EXCLUSION from the shift, documented on `Freeze`:** the local-day and night KEYS
  (`refusalDay`, `battleDay`, `refusalMistakeDay`, `wakeMistakeDay`, `lightAuditedNight`,
  `lightNotifiedNight`). They are not spans, they are "was this already charged TODAY". A stale key
  reads as "not yet charged", which is right for the fresh day the Digimon comes out into; a key
  dragged forward into today would forgive the first real mistake of the day the player is living in.
- AC5/AC6/AC7 were already structurally true — `MainScreenModel` is built on `loadOrCreate`, which
  returns the ACTIVE record — and are now pinned. **The three store-level tests still pass with every
  guard deleted, and the test file says so rather than claiming credit it has not earned**: what they
  prove is that the refresh never REACHES the frozen record, which is what AC5 actually asks and
  which a later story handing the model the whole box would break.
- **Learnings for future iterations:**
  - **Hand the model the SAME `GameStore` instance the test seeded with**
    (`makeStore: { store }`), never a second one on the same file. Two containers give the test its
    own copy of every record, so a refresh that DID move the Digimon in the box leaves the test's
    copy — and the test — looking exactly like success. The alternative is reopening and re-fetching
    after every assertion point.
  - A `GameStore` that goes out of scope resets its context, and every `GameState` fetched from it
    then traps with "This model instance was destroyed by calling ModelContext.reset". Hold the
    store in a test property for the whole test, not in a `let` inside the seeding helper.
  - **Pick fixture dates away from DST.** Every span in a freeze test is a whole number of 86,400s
    days; a freeze straddling a US transition shifts the timeline by three days minus an hour and
    drifts the LOCAL-DAY assertions for reasons that have nothing to do with freezing. Mid-May is
    clear in both directions; the March dates the notification suite uses are not.
  - **A "nothing was notified" test must not advance the clock far.** The first draft of AC7
    advanced five days and failed: with an empty health fetcher the ACTIVE Digimon racks up four
    silent-day mistakes and a full screen of poop on its own, and its own notices land in the spy.
    Seed the frozen one so that everything it is owed is owed with NOTHING elapsed, and move the
    clock an hour.
  - `MainScreenModel` init takes `@MainActor` collaborators, so a default argument that constructs
    one (`spy: SpyDeliverer = SpyDeliverer()`) will not compile — the default is evaluated in the
    caller's context. Take `nil` and build it in the body, which is what the init itself does.
  - US-126's party screen is now unblocked on both sides: `GameStore.activate(_:now:)` does the
    whole switch (flags + freeze clock + timeline) in one transaction, so the screen only has to
    call it and hand it the clock.
- REMAINS: US-126 onward (12/56 passing).
---

## 2026-07-22 — US-126: Party screen and activating a Digimon
- New `Sources/PartyView.swift`: `PartyStatus` (active/frozen/dead), `PartyRow` (+`rows(for:in:)`),
  `PartyRowLayout`, `PartyView` and its row view. `MainScreenModel` gained `partyRows`,
  `activate(_ row:)` and the DEBUG `seedPartyDemoIfRequested`. `MapStripView` gained a second
  generic `Party` view and its trailing button became a `NavigationLink`;
  `MapStripLayout.isPartyReachable` is now true and US-120's test asserts the opposite of what it did.
- Files: Sources/{PartyView(new),MainScreenModel,MapStripView,ContentView}.swift,
  Tests/PartyTests.swift (new, 17 tests), Tests/MapStripTests.swift (one test flipped).
- PASSED: build green. `Executed 1577 tests, with 0 failures` (1560 + 17).
- **AN UNHATCHED EGG NEEDED NO SPECIAL CASE, and that is the story's one real design decision.** An
  egg the player holds IS a saved `GameState` at `.digitama`, so `allStates()` already lists it and
  AC6's "tapping it starts it hatching" is just AC3 applied to it — the egg becomes the record
  `refresh()` credits, and `hatchIfReady` runs on it. Asserted through the REAL hatch rather than by
  inspection: take the egg out with nothing walked, walk 5,000 steps, run the ordinary refresh, and
  it is `punimon` that comes out — while the Digimon put away still has zero stage energy.
- **DEATH IS READ BEFORE `isActive`**, deliberately: a Digimon can be dead AND out (that is what the
  memorial screen is), and of the two facts "gone" is the one that must not be drawn as a live pet.
- MEASURED on 46mm and 41mm, fresh installs, `-partyDemo` seeding four entries (active Digitama,
  frozen Agumon, frozen Gabu Digitama, dead Greymon): all four readable with no truncation, in two
  shots per device — the top, and `-partyBottomDemo` scrolled to the last row.
- **AC7 was measured, not eyeballed** (`/tmp/us126/blocks.swift`, not committed): a party sprite's
  ink box lands in exact 4x4 device-pixel blocks at alignment (+0,+0), **100% of blocks one flat
  colour** on every row away from the list's own edge-scaling (156/156, 100/100, 90/90; 6–9 distinct
  colours in a whole sprite box). That IS nearest neighbour — a smoothed sprite has no alignment
  that works. The 46mm top row read 94.2% because watchOS scales list rows near the screen edge.
- **TWO VIEW BUGS THE SCREENSHOTS CAUGHT AND NO TEST COULD**, which is the whole argument for AC8:
  - `.disabled(!row.isSelectable)` drew the ACTIVE row grey — watchOS dims a disabled row's entire
    content — so the Digimon the player has OUT read as unavailable. Only a DEAD row is disabled
    now; the active row keeps full strength and its own mark, and its no-op lives in `tap`, which
    dismisses without calling `activate` (a row that swallowed the tap would read as broken).
  - "Gabu Digitama" truncated to "Gabu Digi…" on 41mm. Fixed with `minimumScaleFactor(0.5)` on the
    name and `.fixedSize()` on the status mark, so the NAME shrinks and the status stays whole —
    `layoutPriority(1)` on the name was tried first and merely moved the truncation onto "Frozen".
- **Learnings for future iterations:**
  - **A `.task` screenshot hook on a screen pushed from the LAUNCH COMMAND must be keyed**
    (`.task(id: rows.count)`). It runs before `MainScreenModel.start()` has opened the store, sees
    an empty list, returns — and a plain `.task` never runs again. `MapListView`'s unkeyed hook only
    works because `mapRows` comes from the bundled catalog and is never empty. Cost ~20 minutes of
    "the scroll flag does nothing".
  - **TAKING A DIGIMON OUT REORDERS THE BOX.** The list is `allStates()`, sorted by `birthDate`, and
    US-125's `thaw` shifts the whole timeline forward by the frozen span — so the sort is really by
    EFFECTIVE AGE and a thawed Digimon moves to the end. `PartyRow.id` is a POSITION, so
    `activate(_:)` checks the row still DESCRIBES the record at that position before acting; a list
    held across one switch would otherwise activate the wrong Digimon. US-132's Jogress, which
    consumes two records and inserts one, inherits exactly this.
  - **A simulator's save survives `simctl install`** if the uninstall was silently skipped — the
    46mm container still held a `GameState` born in 2001 from an earlier wave's demo, which made two
    devices disagree about the box's order for reasons that had nothing to do with this story.
    `sqlite3 "$(xcrun simctl get_app_container <udid> com.digivpet.DigiVPet data)/Library/Application
    Support/default.store" "select ZCURRENTDIGIMONID, ZISACTIVESTORAGE, ZBIRTHDATE from ZGAMESTATE"`
    settles it in one command — and is safe here, unlike on `Tests/Fixtures/`.
  - `IdleSpriteView` and not `DigimonSpriteView` is the renderer for any LIST of Digimon: same
    `.interpolation(.none)`, one still frame, memoised — a list of two-frame loops runs one
    `TimelineView` schedule per row for art the player is scrolling past.
  - The DEBUG box seeder inserts straight through `store.container.mainContext`, as
    `GameBoxTests` does. Nothing in the shipped game adds a second Digimon yet; **US-128's drop
    engine is the first thing that will**, and it wants a real `GameStore` method rather than this.
- REMAINS: US-127 onward (13/56 passing).
---

## 2026-07-22 — US-127: One of each Digitama, ever — until it dies
- New `GameState.originDigitamaId` (optional-backed storage, the house pattern) — the egg a Digimon
  hatched from, carried UNCHANGED through evolution because `advance` writes `currentDigimonId`/
  `stage` and never touches it. `init` gained an `originDigitamaId:` param defaulting to
  `currentDigimonId` (every path today is born AS a Digitama and is its own origin); US-132's Jogress
  result is the first caller to pass something else.
- HELD is DERIVED, not stored. `GameStore.heldDigitamaIds()` = `Set` of `originDigitamaId` over the
  LIVING box records; `heldDigitamaIds(in:)` is the pure rule. An unhatched egg IS a `GameState` at
  `.digitama` whose origin is itself, so it needs no special case; a death releases its id for FREE
  because the dead record is filtered out — nothing has to remember to remove it.
- `EvolutionGraph.digitamaRoot(of:)` walks parent edges down a line to its Digitama, for the
  migration: `greymon` -> `agumon` -> `koromon` -> `botamon` -> `agu_digitama`.
- The origin backfill runs at the TOP of `loadOrCreateProfile`, BEFORE the early return, saving only
  when it wrote something. `MainScreenModel.heldDigitamaIds` is the seam US-128 filters map drops on.
- Files: Sources/{GameState,GameStore,EvolutionGraph,MainScreenModel}.swift,
  Tests/HeldDigitamaTests.swift (new, 9 tests). No existing test file edited.
- PASSED: build green. `Executed 1586 tests, with 0 failures` (1577 + 9).
- **DELIBERATE DEVIATION FROM AC1, and it is the one thing to review.** AC1 says "PlayerProfile
  tracks the HELD Digitama ids". Held is a fact about which `GameState`s currently exist and are
  alive, so it lives with the box (`GameStore`), not on `PlayerProfile` — the same reasoning US-124
  used to put "exactly one active" in the store rather than on the record. Storing it separately
  would have five write sites (hatch, evolve, death, reset, jogress) and four chances to drift, and a
  drift here is the exact duplicate-egg bug this story exists to stop. Deriving off `originDigitamaId`
  has zero sync points and is always correct. `PlayerProfile.ownedDigitamaIds` (ever owned) is
  untouched and still exists — held and ever-owned are different questions.
- **AC2 tested against the REAL pre-US-123 fixture**, whose Digimon is a `greymon` with no origin
  column: after `loadOrCreateProfile` the state reads `agu_digitama`, `hasStoredOrigin` is true (a
  real write, not the `?? currentDigimonId` fallback), the value survives a reopen, and the held set
  is `{agu_digitama}`. The backfill is at the top of `loadOrCreateProfile` so it fires for a player
  who ALREADY has a profile too (US-124's lesson) — idempotent.
- AC6 both cases pass, plus the REAL hatch through `MainScreenModel` proves the shipped `advance`
  keeps the origin.
- **Learnings for future iterations:**
  - **"Held" is a query, not a stored field.** Any "the player currently has X" fact that is fully
    determined by the box (`allStates()`) is cheaper and safer to DERIVE than to store and sync —
    `originDigitamaId` on the record makes the box self-describing. Reach for a stored mirror only
    when the fact cannot be recovered from what is already persisted.
  - **A one-time backfill of a NEW `@Model` field must run OUTSIDE the "create the profile" branch.**
    The lifetime/map migrations sit inside `loadOrCreateProfile`'s create path, which a US-123-build
    player never re-enters. An origin (or any per-`GameState`) backfill has to run before that early
    return, gated on the field being unset, so it reaches an already-migrated store. Idempotent +
    save-only-if-changed makes running it every launch free.
  - **`GameState.init`'s argument ORDER is `isActive:` then `originDigitamaId:`.** A call that puts
    `originDigitamaId:` first fails to compile with "argument 'isActive' must precede...". US-132's
    Jogress result will pass both.
  - **`EvolutionGraph.digitamaRoot` only works for the 88 wired nodes.** An orphan id (one of the 780)
    returns nil and the backfill keeps the current id as the origin — fine for a `.digitama` record,
    a best-effort for anything the graph cannot trace. Phase E wiring makes more lines traceable.
- REMAINS: US-128 onward (14/56 passing).
---


## 2026-07-22 — US-128: Digitama drop engine
- `Sources/DigitamaDrop.swift` (new): `DigitamaDropEngine` — a PURE rule. `eligibleSlots(in:context:
  held:)` filters the selected map's slots to those whose conditions ALL hold (`ConditionReveal.allMet`
  — the very predicate the map detail's "Ready to find" mark and `EvolutionEngine` read, so an egg the
  detail PROMISED is exactly the egg that can drop) and whose id is not HELD (US-127). `award(in:
  context:held:using:)` picks ONE with an injected `inout` generator; nil for an empty set or a nil
  map. Also `DigitamaDropAnnouncement` — the roster-built value the banner draws.
- `Sources/DigitamaDropBanner.swift` (new): a light overlay (not a full ceremony), drawn with
  `IdleSpriteView` so it carries `.interpolation(.none)`. Tap to acknowledge.
- `Sources/GameStore.swift`: `grantDigitama(_:now:)` — inserts the egg FROZEN + inactive (so US-124's
  one-active invariant holds and US-125 leaves it untouched until it is taken out), records it in the
  Dex and notes it ever-owned on the profile, all in ONE save. Its `originDigitamaId` defaults to
  itself, so `heldDigitamaIds()` counts it the instant it lands.
- `Sources/MainScreenModel.swift`: `pendingDigitamaDrop` (published), `makeDropGenerator` (injected,
  seeded separately from the battle RNG), `checkForDigitamaDrop()`, `acknowledgeDigitamaDrop()`. The
  check is called at exactly three sites and NEVER on a timer: `finishTraining` (after the session is
  counted), `finishBattle` (after `recordBattle`), and `refresh` (after `creditMapSteps`, after the
  stage settles).
- `Sources/ContentView.swift`: a `pendingDigitamaDrop` overlay below the evolution ceremony.
- `Sources/EvolutionCondition.swift`: `ConditionMetric.isSparseOnHardware` — the seven feature-sourced
  metrics the type comment already NAMES (handwashing, toothbrushing, water, daylight, audio exposure,
  low-cardio-fitness and walking-steadiness events). It only STATED the "never sole" rule before; this
  is what lets a validator ENFORCE it.
- `Sources/MapCatalogValidator.swift`: `MapValidationError.soleSparseCondition`, fired when a slot has
  conditions and every known one is sparse. Collected before the roster lookup, like the blank-hint
  rule, so a bad id does not hide it.
- Files: Sources/DigitamaDrop.swift (new), Sources/DigitamaDropBanner.swift (new),
  Sources/GameStore.swift, Sources/MainScreenModel.swift, Sources/ContentView.swift,
  Sources/EvolutionCondition.swift, Sources/MapCatalogValidator.swift,
  Tests/DigitamaDropTests.swift (new, 19 tests). `xcodegen generate` for the two new files.
- PASSED: build green. `Executed 1605 tests, with 0 failures` (1586 + 19 new). The 19 new were also
  observed passing on their own with `-only-testing`, grepped rather than inferred.
- PASSED, criterion by criterion:
  - AC1/AC2 were already true (US-116/117): `DigitamaSlot` carries `digitamaId` + `[EvolutionCondition]`
    over the `ConditionMetric` vocabulary; `emptyConditionHint` is enforced. Verified the shipped slots
    use `health.handwashing`/`.mindfulMinutes` and the `care.*` counters.
  - AC3: three call sites, each proven END TO END through the real model — `finishTraining` drops a
    `care.trainingSessions` slot the round just met; `finishBattle` drops a `care.battleCount` lifetime
    slot the fight just recorded; `refresh` drops after a step tick once the condition is met (and
    drops NOTHING while it is unmet).
  - AC4/AC5: engine tests (unmet, one, three-deterministic, all-held, held-beside-unheld, no-map,
    empty pool) plus a model test that a second met refresh does NOT re-drop the held egg.
  - AC6: `grantDigitama` records the Dex (a model test asserts `dexIds().contains`) and the banner
    announces (`pendingDigitamaDrop?.id`/`.displayName`).
  - AC7: `soleSparseCondition` fires on a hand-built bad fixture (one sparse, and two sparse together),
    stays silent when a sparse metric is PAIRED with a real one (the shipped pulse pattern) or when a
    slot has no conditions; the shipped catalog reports ZERO of them (pinned twice); and the sparse set
    is pinned to the seven.
- **Learnings for future iterations:**
  - **A US-116 DATA TRAP THIS ENGINE SURFACES, and a real one: 12 of the 57 shipped Digitama slots can
    never drop.** They gate on `care.battleCount` or `care.battleWinRatio` with a **stage** window, and
    `ConditionContext.careValue` returns `.unknown` for both of those over `.stage` — US-084 note 4 is
    explicit that there are NO stage-scoped battle counters and that `care.battleWinRatio` must not be
    repurposed into one. So `ConditionReveal.allMet` is false forever and the egg is unreachable. The
    slots: 01_grassland/pal, 14_farmland/worm, 02_river/beta, 09_lake/bluco, 07_mountains/lioll,
    05_wasteland/gazi, 13_factory_town/pawnchesswhite + /pawnchessblack, 15_dungeon/ghost,
    11_city_night/guil, 12_cyberpunk/v (+ 12_cyberpunk/agu2006 on `care.trainingSessions` LIFETIME,
    also `.unknown`). **Left alone ON PURPOSE**, exactly as US-118 left the `MetricCreditor` wiring: the
    drop engine is correct — an unanswerable condition is never met, the same way an evolution edge
    gated on one never opens — and fixing it is a DATA story (move those windows to `lifetime`/`day`, or
    add stage-scoped win/loss fields per US-084 note 4) that re-tunes difficulty and re-runs the whole
    US-116/117 pass. A validator rule to catch "a metric/window `ConditionContext` can never answer"
    would be the clean guard, but it would fail the shipped-clean sweep with 12 findings and so belongs
    with the data fix, not here.
  - **The `MetricCreditor` gap (US-118) still bites drops too.** `health.*` window totals
    (`stageMetricTotals`, `stageBestDayMetrics`) are permanently zero in the shipped app until that
    wiring story lands, so every `health.*` drop condition is also currently unmeetable by a real
    player. Net: the ONLY slots a real player can currently earn are the ones gated purely on
    `care.trainingSessions`/`care.overfeeds`/`care.sleepDisturbances` (all `stage`) — which ARE tracked
    on `GameState` and answered. The engine needs no change when either wiring lands; the same slots
    simply start coming due.
  - **A dropped egg must be FROZEN at grant, not just inactive.** An unfrozen inactive record's timeline
    keeps running (readings are derived from stored `Date`s — see `Freeze.swift`), so an egg left in the
    box for a week would be a week old when finally activated. `grantDigitama` calls `freeze(at: now)`,
    so US-125's thaw shifts it forward to a FRESH egg whenever it is taken out. The `-partyDemo` seed
    does NOT freeze its inactive records, but that is a screenshot shortcut; the real grant path does.
  - **Reuse `ConditionReveal.allMet`, do not re-implement "all met".** The map detail's ready mark, the
    evolution engine and now the drop engine all read the one predicate, so a slot the detail told the
    player was "Ready to find" is provably the slot that drops. A second reading would be a second
    chance for the promise and the drop to disagree.
  - **A model test that reads the box back should hold the SAME `GameStore` instance the model uses**
    (`makeStore: { [store] in store }`, the BattleTests pattern), not open a second `GameStore` on the
    same file — two containers on one SQLite file is a needless lock/consistency risk. Seed the store,
    pass the instance, read the instance.
  - **A drop check that follows an action must sit AFTER the action's own save** (or grant its own, as
    `grantDigitama` does), so the thing the player did is on disk whether or not an egg then drops.
- REMAINS: US-129 onward (15/56 passing). Phase C's drop engine is done; US-129's failsafe and Phase D's
  Jogress are next. `MainScreenModel.heldDigitamaIds` and `GameStore.grantDigitama` are the seams
  US-129/US-132 will reuse.
---

## 2026-07-22 — US-129: Never stranded — the Agumon failsafe
- `Sources/StrandedFailsafe.swift` (new): `StrandedFailsafe.digitamaId` (`agu_digitama`) and
  `isStranded(in:)` — a PURE predicate over a box of `GameState`. Written as AC1's two clauses
  (no living Digimon, no unhatched Digitama) rather than collapsed to "nothing alive", which they
  are equivalent to today.
- `Sources/GameStore.swift`: `grantFailsafeDigitamaIfStranded(now:)` — returns the granted egg or
  nil. Grants through the existing `grantDigitama`, so the egg lands exactly as a US-128 drop does:
  FROZEN, inactive, in the Dex and on `PlayerProfile.ownedDigitamaIds`, one saved transaction.
- `Sources/MainScreenModel.swift`: `checkForStranding()`, called from `start()` (the launch check)
  and from `refresh()` after `updateDeath`/`checkForDigitamaDrop` (the death check). It is silent —
  it does NOT raise `pendingDigitamaDrop`; see below.
- Files: Sources/StrandedFailsafe.swift (new), Sources/GameStore.swift, Sources/MainScreenModel.swift,
  Tests/StrandedFailsafeTests.swift (new, 18 tests). `xcodegen generate` for the new files.
- PASSED: build green. `Executed 1623 tests, with 0 failures` (1605 + 18 new), `** TEST SUCCEEDED **`.
  The 18 new were also observed passing on their own with `-only-testing`, grepped not inferred.
- PASSED, criterion by criterion:
  - AC1: `testAWipeoutIsHandedTheAgumonEgg` — a store whose only Digimon is dead, with NO map
    selected and no condition met, gets `agu_digitama` in the box, held, in the Dex and ever-owned.
  - AC2: `testTheEggIsGrantedEvenWhenTheDeadDigimonHatchedFromIt` — the corpse's
    `originDigitamaId` IS `agu_digitama` and it is already in `ownedDigitamaIds`, and the egg is
    still handed over. This is why the failsafe deliberately shares nothing with the drop engine:
    no held-set check, no `ConditionContext`, no generator.
  - AC3: idempotent within a wipeout (a second and third call hand over nothing), across a REOPEN
    off disk, and through the model on a second launch — plus `testASecondWipeoutIsHandedAnotherEgg`,
    which is the other half: losing the failsafe egg too is a NEW wipeout and the floor holds again.
  - AC4: launch (`testALaunchOnAWipedOutStoreGrantsTheEgg`) and after a death through the REAL rule
    (`testTheRefreshThatFindsTheDigimonDeadGrantsTheEgg`, 80 hours sick settled by `refresh()`).
    THE JOGRESS CALL SITE DOES NOT EXIST YET — see the honest note below.
  - AC5/AC6: `testLifetimeEnergyAndMapProgressSurviveTheFailsafe`, asserted after a reopen off disk —
    111/222/333/444, `selectedMapId` `02_river`, 900 steps and the `finishedAt` stamp on
    `01_grassland` all unchanged. Structural: nothing here goes near `resetGame`.
  - AC7 is AC3's reopen test, twice — through the store and through `MainScreenModel.start()`.
- **HONEST NOTE ON AC4's THIRD CLAUSE:** "after a Jogress" cannot be observed, because US-132 has not
  been written. The seam is `MainScreenModel.checkForStranding()` and the comment on it names US-132
  as the third caller; US-132's own acceptance criteria in the PRD already include "The US-129
  failsafe check runs afterwards", so the clause is owned by a story that will assert it. Nothing
  else about this story is deferred.
- **Learnings for future iterations:**
  - **Idempotence that is DERIVED needs no flag.** "Once per wipeout, not once per launch" falls out
    of the grant itself ending the wipeout: the egg it inserts is a living unhatched Digitama, so
    `isStranded` is false the instant it returns. No `hasGrantedFailsafe` field to store, migrate or
    get wrong — the same lesson as `heldDigitamaIds` (US-127) and the one-active invariant (US-124).
    Reach for a stored flag only when the condition is about HISTORY rather than about the box.
  - **A FOUND DEFECT, NOT FIXED HERE, and the next iteration to touch the memorial should know:
    `dismissMemorial` → `GameStore.rebirth` → `resetGame` DELETES THE WHOLE BOX.** That was
    deliberate for a total wipe when there was one Digimon (US-124 notes say so), but since US-124
    the box can hold frozen LIVING Digimon and unhatched eggs, and the memorial's single button
    destroys all of them. Concretely for this story: the failsafe egg granted at a death sits behind
    the memorial overlay (which is the topmost overlay in `ContentView`, so the map strip and
    `PartyView` are unreachable while it is up), and the first tap deletes it and starts a random
    fresh egg instead. US-129's ACs are all about the GRANT and are met, so this was left alone
    rather than smuggled in — but a "the memorial takes out your next Digimon instead of wiping the
    box" story is worth writing, and it would change `DeathTests`' two rebirth tests, which is
    exactly why it is not a side effect of this one.
  - **The failsafe fires on EVERY death in the shipped single-Digimon flow**, since one dead Digimon
    with no egg is literally a wipeout. Two visible consequences: `agu_digitama` enters the Dex and
    `ownedDigitamaIds` at every death, and it is HELD while the egg sits in the box — so a map
    cannot drop `agu_digitama` (US-128) while the player is holding a failsafe one. Both are the
    intended readings, but they are new facts about the save.
  - **Do not announce a floor with a reward's banner.** `pendingDigitamaDrop` is deliberately not
    raised: the failsafe fires at exactly the moment the memorial covers the screen, so the banner
    would be hidden under it and then name an egg a rebirth had already deleted.
- REMAINS: US-130 onward (16/56 passing). Phase C is COMPLETE. Phase D (Jogress, US-130..132) is next,
  and US-132 must call `MainScreenModel.checkForStranding()` after it consumes its two parents.
---

## 2026-07-22 — US-130: Jogress data file, model and validator
- `Sources/JogressCatalog.swift` (new): `JogressPair` (the two parents as ONE unordered,
  canonicalised value), `JogressRecipe` (parentA, parentB, result, conditions: [EvolutionCondition]),
  `JogressCatalog` (recipes + a private `byPair` index, `recipe(for:and:)`, `recipes(involving:)`,
  `load(from:)`, fatalError-on-bad-data `bundled`) — the `MapCatalog`/`Roster` shape throughout.
- `Sources/JogressCatalogValidator.swift` (new): `JogressValidationError` (9 cases) +
  `JogressCatalog.validate(roster:)`, returning findings in file order with the duplicate pairs
  last, never throwing — the `MapCatalogValidator` shape.
- `Resources/jogress.json` (new): `{"recipes": []}`. **Deliberately empty — authoring the recipes
  IS US-131.**
- Files: Sources/JogressCatalog.swift (new), Sources/JogressCatalogValidator.swift (new),
  Resources/jogress.json (new), Tests/JogressCatalogTests.swift (new, 12 tests),
  Tests/JogressCatalogValidatorTests.swift (new, 20 tests). `xcodegen generate` for the new files;
  no `project.yml` change was needed and none was made — `Resources` is already a folder reference,
  and the file was confirmed in the built app at `DigiVPet.app/jogress.json`.
- PASSED: build green. `Executed 1655 tests, with 0 failures` (1623 + 32 new), `** TEST SUCCEEDED **`.
  The 32 new were also observed passing on their own with `-only-testing`, grepped not inferred.
- PASSED, criterion by criterion:
  - AC1: the file, the two types and the four fields, decoded through the real path.
  - AC2: asserted four ways — the pair's equality AND hash, lookup in both directions, lookup
    against a catalog that authored the pair the OTHER way round, and `recipes(involving:)` finding
    a parent on either side of the file.
  - AC3/AC4: every rule fired on a hand-built fixture — each the sound one-recipe catalog with
    exactly one thing broken, so a reported error can only be the break. `unknownParent`,
    `unknownResult`, `dexOnlyParent`, `dexOnlyResult`, `sameParents`, `resultBelowParent` and
    `duplicatePair` (twice; in both orders; three times reported ONCE with count 3; and two pairs
    sharing a parent NOT flagged). A control test asserts the sound catalog reports `[]`.
  - AC5: `JogressCatalog.load().validate()` over the shipped file, against the REAL roster —
    zero findings. **Vacuous today, see below.**
- **HONEST NOTE ON AC5.** The shipped file holds no recipes, so "zero findings over the shipped
  file" is literally true and currently proves nothing about authored data — the fixtures are what
  prove the validator works. The test's own doc comment says so rather than leaving a later reader
  to infer it. Two things were done so the emptiness cannot hurt US-131:
  `testTheShippedFileHoldsNoRecipesUntilUS131` is an explicit marker that FAILS the day recipes land
  (delete it then), and `testARecipeDecodesFromTheShippedShape` runs a literal in the authored shape
  through the real `JSONDecoder`, so a renamed key is a failing test rather than the launch trap
  `JogressCatalog.bundled` would otherwise become on US-131's first recipe. A companion test also
  proves the real roster behind AC5 is a check at all (three invented ids → three findings), so the
  zero cannot be a stub saying yes to everything.
- **Learnings for future iterations:**
  - **Canonicalise an unordered key in the TYPE, not at the call sites.** `JogressPair` sorts its two
    ids on construction, which makes "A+B and B+A are the same recipe" a property nothing can forget
    — and it collapses the PRD's two separate rejection rules ("lists both A+B and B+A" and "a
    duplicate parent pair") into ONE finding, because by construction they are the same key. There
    is no `reversedPair` rule to write, and therefore none to get wrong. Both are still tested
    separately, since the ACs name them separately.
  - **The ladder rule for a fusion is `<`, never `<=`.** This roster files WarGreymon, MetalGarurumon
    AND Omegamon at `Stage.ultimate` (the sprite folder is "Ultimate-Super Ultimate", holding both
    Ultimate and Ultra), so a Jogress result is normally LEVEL with its parents. An accidental `<=`
    would reject every real recipe US-131 is about to author while the fixture suite stayed green —
    which is why the equal case is pinned as SOUND from both directions, not just left untested.
  - **An empty shipped data file makes its own "zero findings" test vacuous, and a decode test over
    a LITERAL in the shipped shape is the cheap fix.** `{"recipes": []}` exercises no field name at
    all, so a typo in `parentA` would survive this story and surface as a `fatalError` at launch in
    the next one (`bundled` traps by design). One literal decoded through the real decoder closes it.
    Worth copying whenever a story splits "the machinery" from "the data".
  - **Collect a condition's OWN faults before the roster lookup.** Third validator in a row to want
    this (`EvolutionGraphValidator` for edges, `MapCatalogValidator` for slots, now recipes): a blank
    hint or a typo'd metric is wrong whatever the ids resolve to, and reporting it only after the id
    resolves means the author fixes the id, re-runs, and meets a second error they could have fixed
    in the same pass.
  - **`python3` + `json.load`/`json.dump(indent=2)` round-trips `prd.json` with a 2-line diff.** The
    file's existing formatting survives exactly, so updating a story's `passes`/`notes` needs no
    hand-editing of a 1000-line JSON file and no risk of breaking it. Add the trailing newline back.
- REMAINS: US-131 onward (17/56 passing). US-131 authors the recipes into the empty
  `Resources/jogress.json` and must delete `testTheShippedFileHoldsNoRecipesUntilUS131`; the
  validator is ready for it. US-132 then consumes `recipes(involving:)` + `recipe(for:and:)` and
  must call `MainScreenModel.checkForStranding()` after it consumes its two parents (US-129).
---

## 2026-07-22 - US-131 (Author the Jogress recipes)
- **Resources/jogress.json now holds 14 recipes**, read off the two Bandai charts already committed
  in `Resources/`. Build green, **1659 tests (1655 - 1 marker + 5 new), 0 failures**, and both
  Jogress suites also observed passing on their own with `-only-testing` (16 + 20).
- Files changed: `Resources/jogress.json`, `Tests/JogressCatalogTests.swift` (marker test replaced by
  five that assert the authored data), `Tests/JogressCatalogValidatorTests.swift` (the shipped-file
  sweep's doc comment no longer says it is vacuous, plus a non-empty guard so it cannot become
  vacuous again). No source file changed — US-130's machinery took the data unmodified.
- **THE SOURCE IS THE TWO SHIPPED PDFs, NOT A WEB FETCH.** The PRD's warning about
  `humulos.com/digimon/penc` returning self-referential nonsense meant no web source was used at all.
  `Resources/DIGIMON_COLOR_EVOLUTION.pdf` (Ver.230930, 6 pages) and
  `Resources/DIGIMONPENDULUM COLOR_EVOLUTION.pdf` (Ver.241116, 7 pages) are Bandai's own 資料集
  evolution charts, and every one of the 14 recipes was read off a specific page of one of them.
  Recipe -> source page:
  - Digimon Color 25th, the "與(Ver.N) …對戰" rows at the foot of each 究極體 column:
    BlitzGreymon+CresGarurumon->Omegamon Alter-S (p2 VER.1 and p3 VER.2, stated from both sides);
    Chimairamon+Mugendramon->Millenniumon (p4 VER.3 and p6 VER.5);
    BanchoLeomon+Darkdramon->Chaosmon (p4 VER.3 and p5 VER.4);
    Darkdramon+Mugendramon->Chaosdramon (p5 VER.4 and p6 VER.5).
  - Pendulum Color, the separate box below each 究極體 column:
    WarGreymon+MetalGarurumon->Omegamon (p6 Ver.5 ME and p7 Ver.0 VB);
    Angewomon+LadyDevimon->Mastemon (p2 Ver.1, p4 Ver.3, p7 Ver.0 — three times);
    SaberLeomon+ElDoradimon->Tlalocmon and SaberLeomon+MetalEtemon->Tlalocmon (p2 Ver.1);
    MetalSeadramon+Plesiomon->Aegisdramon (p3 Ver.2);
    MarinAngemon+Hououmon->Mitamamon (p3 Ver.2 and p5 Ver.4);
    Vamdemon+Piemon->VoltoBautamon (p4 Ver.3);
    Griffomon+Pinochimon->Cernumon and Griffomon+Hydramon->Cernumon (p5 Ver.4);
    Mugendramon+HiAndromon->Chaosdramon (p6 Ver.5).
- **LEFT OUT ON PURPOSE, four of them, because AC3 says a guessed recipe fails this story:**
  - `xv-mon` + `stingmon` -> `paildramon`: `stingmon` is dexOnly (asserted in a test). The PRD names
    this one; it is pinned as an ABSENCE so a later iteration re-adding it from a wiki fails.
  - Siriusmon + Arcturusmon -> **Proximamon** (p7 Ver.0 VB, プロキシマモン). Both parents exist and are
    playable, but **Proximamon is in neither `roster.json` nor `16x16 Digimon Sprites/`** —
    `find … -iname '*proxi*'` returns nothing. The result has no sprite, so it cannot ship.
  - ElDoradimon + MetalEtemon -> Tlalocmon, and Pinochimon + Hydramon -> Cernumon. **Ambiguous
    source, not missing data.** Both cells read `<A>` / `+<B> / <C>` / `(3隻其中2隻)` — "2 of these
    3". The `+` prefix anchors A as one parent, which makes A+B and A+C certain; whether B+C alone
    also fuses depends on which way "2 of these 3" is read, and the chart does not settle it. The
    two certain pairs shipped, the ambiguous third did not.
- **THE ROMANISATIONS DO NOT MATCH THE PDFs OR THE PRD, AND FIVE HAD TO BE HUNTED FOR.** The charts
  are Japanese/Chinese only; the PRD's AC5 list uses the English fan spellings. This roster uses a
  third set. Resolved by katakana, then CONFIRMED BY SPRITE: ミレニアモン -> `millenniumon` (not
  "millenniummon"); キメラモン -> `chimairamon`; エルドラディモン -> `eldoradimon` (displayName
  "ElDoradimon"); グリフォモン -> `griffomon` (the PRD calls it "Gryphonmon"); ピノッキモン ->
  `pinochimon` (not "Pinocchimon"/"Puppetmon"). A plain name search would have missed every one.
- **VERIFICATION, all four layers, before the file was written:**
  1. All 34 participant ids resolve in `roster.json`, none `dexOnly`.
  2. Every one has an ANIMATED sheet at `16x16 Digimon Sprites/<stage>/<spriteFile>.png` — checked
     with `os.path.exists`, not assumed from `dexOnly` being absent.
  3. **The sprite art was compared against the PDF thumbnail** for the ten riskiest name resolutions
     (Griffomon, Pinochimon, ElDoradimon, Tlalocmon, Millenniumon, Chimairamon, Cernumon,
     VoltoBautamon, Mitamamon, Mastemon). The charts draw the same 16x16 sprites this game ships, so
     frame 0 beside the chart thumbnail is a direct identity check — all ten matched.
  4. `MapCatalog`-style: the validator sweeps the real file against the real roster and reports [].
- **Learnings for future iterations:**
  - **The two PDFs in `Resources/` are the authoritative tree source and they are READABLE — the
    `.md` beside them is not trustworthy for the Pendulum Color half.** `Digimon_Color_And_Pendulum_
    Color_Evolution_Trees.md` Part 2 lists Agumon/Betamon/Tentomon as Ver.1 Nature Spirits rookies;
    the actual chart (pen p2) has Tentomon/Gotsumon/Otamamon/Angoramon. Part 1 (Digimon Color) did
    check out against the PDF. **Phase E must read the PDFs, not Part 2 of the .md.**
  - **HOW to read them, since neither text-extracts:** the Digimon Color PDF's names are Japanese
    glyphs and the Pendulum one has no Latin text at all, so `zlib`-decompressing the content streams
    gives structure but no names. `pdftotext`/`poppler` is NOT installed and neither is the Read
    tool's `pdftoppm`. **A ~30-line `CGPDFDocument` + `CGImageDestination` tool built with
    `xcrun swiftc` renders any page, or any fractional RECT of a page, to PNG at any scale** —
    `/tmp/us131/render.swift` and `/tmp/us131/crop.swift` (not committed). Cropping the jogress box
    at scale 6 is what made the small print legible. Same throwaway-CoreGraphics-tool pattern the
    screenshot stories use, pointed at a PDF instead of the Simulator.
  - **Page map, so the next story does not re-derive it.** `DIGIMON_COLOR_EVOLUTION.pdf`: p1 legend,
    p2-p6 = VER.1..VER.5. `DIGIMONPENDULUM COLOR_EVOLUTION.pdf`: p1 legend, p2-p7 = Ver.1 NSp,
    Ver.2 DS, Ver.3 NSo, Ver.4 WG, Ver.5 ME, Ver.0 VB. Columns are always
    幼年期I・II/成長期 | 成熟期 (Child) | 完全體 (Perfect) | 究極體 (Ultimate), and the jogress rows are
    the box at the FOOT of the last column. `勝率` = win rate, `C`/`E`/`M`/`O`/`Z` are care counters.
  - **Resolve a Digimon by SPRITE, not by name, when the source is in another language.** Five of the
    fourteen recipes name a Digimon this roster spells differently enough that an exact-name lookup
    misses. Reading the katakana narrows it to a candidate; drawing frame 0 of that candidate's sheet
    next to the chart's thumbnail proves it, because the charts use the same 16x16 art the game
    ships. This is cheap and it is the only check that cannot be fooled by a plausible spelling.
  - **A "no findings over the shipped file" test needs a non-empty guard once the data lands.** The
    sweep passes trivially over `[]`, so `XCTAssertFalse(catalog.recipes.isEmpty)` sits beside it now
    — otherwise a later story that empties the file turns the AC's own test green.
- REMAINS: US-132 onward (18/56 passing). US-132 has 14 real recipes to fuse; entry points are
  `recipes(involving:)` and `recipe(for:and:)`, every shipped recipe has EMPTY conditions (so a pair
  is fusable as soon as both are owned and alive — the ordinary Color-device case), and it must call
  `MainScreenModel.checkForStranding()` after consuming its two parents (US-129). Note for its tests:
  three results are reachable by TWO different pairs (Chaosdramon, Tlalocmon, Cernumon), and the two
  Perfect-stage parents (Angewomon/LadyDevimon, Vamdemon) mean a fusable pair is not always two
  Ultimates.
---

## 2026-07-22 - US-132 (Perform a Jogress)
- **Fusing two owned Digimon now works end to end.** New `Sources/Jogress.swift` (`JogressOffer`,
  `JogressBoard`, `JogressWording`, `JogressOutcome`), new `Sources/JogressView.swift` (the pair list
  and `JogressEntryRow`), `GameStore.performJogress` as one transaction, and
  `MainScreenModel.jogressBoard` / `performJogress(_:)` joining them to `PartyView`. Build green,
  **1690 tests (1659 + 31 new), 0 failures.**
- Files changed: `Sources/Jogress.swift` (new), `Sources/JogressView.swift` (new),
  `Tests/JogressFusionTests.swift` (new, 31 tests), `Sources/GameStore.swift`,
  `Sources/MainScreenModel.swift`, `Sources/PartyView.swift`, `Sources/ContentView.swift`.
- **THE STORY'S REAL TRAP WAS NOT THE FUSION, IT WAS DRAWING THE RESULT.** Every Jogress result is
  one of the 780 orphans no authored line reaches, so the pre-existing `graph.presentation(forId:)`
  answers **nil for every fusion in the game**. Left alone, the player who fused two Ultimates would
  have got `SavedGameUnavailableView` over the Digimon they had just earned, a party row reading
  `omegamon` with no art, and "No Digimon to fight with" from Battle. Fixed with a roster fallback in
  `MainScreenModel.presentation(forId:)`, `PartyRow.rows` and the battle guard. **The graph is still
  asked FIRST** — for a wired id the two agree on name and sprite, but the graph's stage is where the
  ART lives, and that is the one field a disagreement draws a placeholder for.
- **AC6 is the SHAPE of the store method, not a cleanup path.** Every refusal — a record from another
  store, the same record twice, a dead parent, a pair that is not the recipe's, a result the roster
  cannot draw — is decided *before the first mutation*, so the ineligible case needs no rollback at
  all. The only thing that can fail afterwards is the write. `loadOrCreateProfile()` is called up
  front deliberately: it can itself `save()`, and a save in the middle of the fusion would be exactly
  the half-written transaction the AC forbids.
- **AC4's judgement call, worth knowing because it is not forced by the wording:** the result inherits
  the RETURNED egg's origin. Carrying the *other* parent's would leave both ids still held afterwards
  and nothing actually released — the criterion would be true of the delete and false of the box.
- **AC1 strictness:** a recipe condition must hold for BOTH parents. Inert today (all 14 shipped
  recipes have empty conditions), so the choice is about what a later authored gate would MEAN — and
  the strict reading cannot accidentally offer a fusion the data meant to gate.
- **VERIFIED ON BOTH SIMULATORS, clean installs each time** (46mm Series 11, 41mm Series 9):
  entry row reads `Jogress / 1 pair ready` over the seeded pair; the **unavailable** form is present,
  not a button, and reads "None of your Digimon fuse with each other." over an ordinary box (AC2
  observed on screen, not just unit-tested); the pair list fits three sprites, the arrow and
  "WarGreymon + MetalGarurumon" on one line even at 41mm; and `-jogressCeremonyDemo` drove the REAL
  `performJogress`, where a 14-frame burst caught `EvolutionCeremonyView` mid-play (WarGreymon
  full-screen on black, then the white flash) settling to **"Omegamon · Ultimate"** out on the main
  screen with real art.
- **Learnings for future iterations:**
  - **THE BUNDLE ID IS `com.digivpet.DigiVPet`, NOT `com.digivpet.app`.** A wrong id fails with
    `FBSOpenApplicationServiceErrorDomain code=4`, which reads like a crash-on-launch and is not.
    `simctl` then happily screenshots the *previous* screen, so the burst looks plausible and is
    stale. Cost ~10 minutes. Read it from the built app:
    `/usr/libexec/PlistBuddy -c "Print :CFBundleIdentifier" "$APP/Info.plist"`.
  - **`simctl erase` REFUSES on a booted device** (`code=405, Unable to erase contents and settings
    in current state: Booted`) and the script carries on regardless. The sequence that works is
    `terminate` → `shutdown` → `sleep 3` → `erase` → `boot` → `sleep 25-30` → `install` → `launch`.
    Both sims are usually already booted at the start of an iteration, so this bites immediately.
  - **A ceremony needs a burst with NO sleep between frames.** The existing pattern's `sleep 1` is
    right for a walk cycle but far too slow here — 14 back-to-back `simctl io screenshot` calls
    caught the flash; the same count at 1s apart would have landed entirely after it. Then take a
    SECOND, slower burst for the settled screen.
  - **When a value type makes two states exclusive, say so in the initialiser.** `JogressBoard`'s
    init nils `reason` when there are offers and substitutes one when there are none, so "the entry
    point never draws an empty list with nothing to explain it" (AC2) is unrepresentable rather than
    tested. Same move as `JogressPair` sorting its ids in US-130.
  - **An offer that CONSUMES what it indexes must be re-derived from the live box before it acts.**
    `performJogress` re-checks the offer against `jogressBoard.offers` and re-matches the recipe on
    the parents' current ids. Taking a Digimon out reorders the box (US-125's thaw moves a birth
    date), so a party screen held across one switch could otherwise fuse two Digimon the player never
    chose — worse than `activate(_:)`'s stale-row bug, which only put the wrong one out.
  - **Inject the COMMIT, not just the clock, when a story says "a failure part-way rolls back".**
    There is no other way to make SwiftData throw on demand, and the AC is otherwise unobservable.
    `flush: (() throws -> Void)? = nil`, defaulting to `context.save()`; the app never passes it.
  - **`-jogressListDemo` was added on the US-121 `-mapDetailDemo` precedent.** Whenever a story's new
    UI sits one tap below an existing screen, it is unphotographable without a flag — `simctl` has no
    tap command. Budget for the flag as part of the story, not as an afterthought.
- REMAINS: US-133 onward (19/56 passing). Phase D is DONE; Phase E is the roster wiring — 11 device
  trees then 26 orphan sweeps. **Phase E must read the two PDFs in `Resources/`, not Part 2 of the
  `.md` beside them** (US-131 found the `.md`'s Pendulum half is wrong); the page map and the
  `CGPDFDocument` render/crop tooling are in US-131's entry above. Note for it: the roster fallback
  this story added means a newly wired orphan already draws correctly before its edge exists.
---

## 2026-07-22 - US-133 (Wire the Digital Monster Color Version 1 evolution tree)
- **The V1 tree is now complete and playable end to end.** The seed line US-008 called `agumon`
  WAS this tree, authored pruned; this story **renamed it to `dmc-v1`** and filled in the ten nodes
  it never carried. Build green, **1702 tests (1690 + 12 new), 0 failures. Orphans 780 -> 771.**
- Files changed: `Resources/evolutions.json` (11 nodes re-lined, 10 new, 5 new edges on existing
  nodes), `Resources/elements.json`, `Resources/moves.json`, `Sources/DexModel.swift` (new
  `lineTitles`), `Sources/DexView.swift`, `Sources/EvolutionTreeView.swift`,
  `Sources/MinigameAssignment.swift`, `Sources/Notifications.swift`, `docs/evolutions-schema.md`,
  `Tests/DMCVersion1TreeTests.swift` (new, 12 tests) and eleven existing test files.

- **THE STORY IS A RENAME, AND THAT IS THE WHOLE DECISION.** AC2 wants every V1 Digimon on line
  `dmc-v1`, but Botamon/Koromon/Agumon/Greymon/Meramon/Numemon/MetalGreymon were already nodes on
  line `agumon` and `line` is single-valued. Three options, and the third is right:
  1. *Re-line only the new nodes.* Every arrow into them would cross a line boundary, and
     `EvolutionTreeLayout` **drops any connector whose target it did not place** — the Dex would
     draw the V1 tree as six parentless Digimon floating beside the ladder.
  2. *Duplicate the seven into scoped ids.* ~7 more nodes per tree × 11 trees, each needing a
     unique move signature name, and Agumon drawn in four Dex trees.
  3. *Rename the line.* Six keys move (elements/moves `lineDefaults`, `MinigameAssignment.byLine`,
     `-dexLineDemo`, the tree preview host) plus a new `DexModel.lineTitles`, and the tree is one
     closed tree with no crossing edge at all.
  **The other five seed lines map onto the other four DMC versions the same way** — gabumon=V2,
  patamon=V3, piyomon+palmon=V4, gazimon=V5 — so US-134..US-137 should each be the same rename.

- **`DexModel.lineTitles` is new and required, not decoration.** `title(ofLine:)` was
  `graph.node(id: line)?.displayName ?? line`; a device slug names no node, so without a title the
  Dex section header reads `dmc-v1`. **Keep the titles under ~15 characters**: "Digital Monster
  Ver.1" was screenshotted on 41mm and came back as "Digital Monster…", losing the one word that
  says which tree it is. Shipped as **"Color Ver.1"**, which fits untruncated.

- **A REGRESSION NO TEST COULD HAVE CAUGHT, found only because the screenshot was taken.** At 21
  nodes the Adult column is six deep, so the tree grid is 234pt tall. `EvolutionTreeView` was a
  **horizontal-only** `ScrollView`, and a grid taller than the viewport inside one does not
  overflow or clip — **it draws NOTHING**. The V1 tree was a black screen under its own navigation
  title on 41mm. Isolated it by pointing the same build's tree demo at the untouched 10-node
  `gabumon` line, which drew normally, so it was the SIZE and not the new data. Fixed with
  `ScrollView([.horizontal, .vertical])`. **Every remaining Phase E tree is this size**, so this
  had to be fixed once, here.

- **Two prior invariants deliberately relaxed**, each with the reason in the test that pins it:
  - `testEveryNonTerminalChildAndAdultHasTwoToFourOutgoingEdges` (was `...TwoOrThree`). US-061's
    shape is "two earned branches plus the junk fallback", and a device tree gives a Rookie five or
    six Champions. Even split across two Children (US-044's pattern) that needs three earned on
    one of them. **Four is what the whole source document fits in** — check V4's Palmon and V5's
    Gizamon, both six-wide — so this ceiling should hold for all of Phase E.
  - `DexScreenTests.testNoShippedDigimonHasMoreThanFourCandidates` (was `...Three`), to match.
    Measured on 41mm: four candidates draw as 3 + 1 with **both rows on screen at once**.

- **AC11's two absent names, searched with `find -iname` over the whole asset pack, and asserted
  absent by a test rather than only noted:**
  - **Betamon** — only `Idle Frame Only/Betamon.png`, so the roster marks it `dexOnly`.
  - **Tyranomon** — no colour sheet anywhere. The only match is `Black and White Sprites/Adult/
    Tyranomon.png`, and `scripts/import_roster.py` does not index that folder, so there is not even
    a roster entry to point at. **`Black and White Sprites/` is invisible to the roster** — worth
    remembering before declaring a later tree's Digimon present.

- **Choosing a substitute Rookie: pick one NO tree in the document names.** Betamon carries
  Airdramon and Seadramon alone, so without a stand-in they could not be seeded. The obvious
  choices are all spoken for — Otamamon and Gomamon and Syakomon are Pendulum Color rookies (and
  US-131 already warned that the `.md`'s Pendulum half is wrong; the PDF's Ver.1 NSp rookies are
  Tentomon/Gotsumon/Otamamon/Angoramon). **Swimmon appears in neither Part of the document at all**,
  so taking it costs no later story a Rookie. Grep the whole `.md` before choosing one.

- **TRUST THE CODEBASE PATTERNS SECTION ABOVE OVER YOUR OWN GREP.** It already names
  `-noNotificationPrompt`; I grepped `Sources/Notifications.swift`, found nothing, concluded the
  flag did not exist and wrote a second one — it lives in `MainScreenModel.requestNotificationAuthorization`,
  because the ASK is the model's, not the deliverer's. Reverted. **Grep `Sources/` whole**, or just
  read the pattern above. Cost ~20 minutes. Confirmed working sequence, unchanged from what was
  already written down: `-healthAnswered -noNotificationPrompt <demo flag>`, and `simctl privacy`
  has neither a `health` nor a `notifications` service, so an alert already on screen can only be
  cleared by `shutdown` → `erase` → `boot`.

- **Line-scoped ids are the answer to "another line already owns this Digimon".** The V1 tree ends
  Numemon → Monzaemon → ShinMonzaemon, and `shinmonzaemon` is the palmon line's junk Ultimate. Same
  art, same displayName, id `dmcv1_shinmonzaemon` — exactly `piyo_yuramon` from US-045. It needs
  its own `elements.json` and `moves.json` entries with a signature name unique across the file,
  and it is **not** an orphan removed: it is not a roster id, so it never counted as one.

- **What the V1 tree's last row actually is.** "BlitzGreymon -> Ultra: Omegamon Alter-S (Jogress
  with CresGarurumon)" is not an evolution edge — `Resources/jogress.json` already carried exactly
  that pair from US-131. BlitzGreymon is terminal here and a test asserts the recipe instead, so
  the row is covered rather than missing. Expect the same for V2/V4/V5's Ultra rows.

- REMAINS: US-134 onward (20/56 passing). US-134 is Digital Monster Color Version 2 — the
  `gabumon` line renamed to `dmc-v2` and completed, the same shape as this story. Its Ultra row is
  the other half of this one's (CresGarurumon + BlitzGreymon), already in `jogress.json`.
---

## 2026-07-22 - US-134 (Wire the Digital Monster Color Version 2 evolution tree)
- **The V2 tree is now complete and playable end to end.** The seed line US-008 called `gabumon`
  WAS this tree, authored pruned; this story **renamed it to `dmc-v2`** and filled in the thirteen
  nodes it never carried. Build green, **1716 tests (1702 + 14 new), 0 failures. Orphans 771 -> 760.**
- Files changed: `Resources/evolutions.json` (10 nodes re-lined, 13 new, 6 new edges on existing
  nodes), `Resources/elements.json`, `Resources/moves.json`, `Sources/DexModel.swift`,
  `Sources/DexView.swift`, `Sources/MinigameAssignment.swift`, `docs/evolutions-schema.md`,
  `Tests/DMCVersion2TreeTests.swift` (new, 14 tests) and seven existing test files.

- **US-133's prediction held exactly: this story was the same rename.** Six keys move (elements /
  moves `lineDefaults`, `MinigameAssignment.byLine`, `DexModel.lineTitles`, and
  `EvolutionGraphTests`' per-line counts), and the tree is one closed tree with no crossing edge.
  Nothing new was learned about the mechanism — **read US-133's entry below before starting V3.**

- **WHERE V2 IS HARDER THAN V1, and it is the whole of this story: nothing prunes it.** V1 lost
  Betamon (idle-only) and Tyranomon (no colour sheet), which quietly cut Agumon's five Champions to
  four. **Every one of Version 2's seventeen names has a playable 48x64 sheet**, so all five arrows
  out of Gabumon have to exist. Three consequences, and each one is a decision the next tree will
  meet again:
  - **The out-degree ceiling went 4 -> 5**, and the Dex candidate ceiling with it. US-133 wrote
    that "four is what the whole source document fits in" — **that was wrong, and checking the
    document rather than the previous story's note is the lesson**: V4's Palmon and V5's Gizamon are
    SIX wide. Raised one step anyway, deliberately: a ceiling looser than the file it guards catches
    nothing.
  - **The junk fallback moved from Geremon to Vegimon.** A device tree's junk Champion is the one
    BOTH Rookies fall to — Numemon in V1, Vegimon in V2, Scumon in V3 — and Gabumon's five arrows
    end in Vegimon, so there is no room for Geremon beside them. `EvolutionCriteriaTests.junkIds`
    gained `vegimon` and `dmcv2_vademon`, and `SeedRosterTests`' Ver.2 default path is now
    Vegimon -> Vademon -> Ebemon.
  - **A displaced node needs a new parent, not deleting.** Dropping Geremon would have left a
    shipped Digimon reachable from nothing — an orphan re-created by the story whose job is to
    remove them. It hangs off Elecmon as the overfeeding branch (`care.overfeeds atLeast 6`), which
    also keeps WereGarurumon and Gerbemon under it. **Before removing an edge, check whether it is
    the target's only in-edge** (`graph.parents(of:)`), and a test now pins that one.

- **A NAME CAN EXIST AND STILL BE ON THE WRONG RUNG.** Whamon is a Champion in the V2 document and
  a **Perfect** in this asset pack — `Perfect/Whamon.png` is its only animated sheet, and the
  roster's stage comes from the folder. An Adult node would have resolved to no art and failed the
  validator's `missingSprite`. It stands BESIDE MetalMamemon instead of beneath it, off the same
  two Champions the document puts on its row, and shares MetalMamemon's Mega (the one invented
  arrow — no Perfect in the file is terminal, and a terminal Perfect is a Digimon that can never
  evolve again). **Check the sprite's FOLDER, not just that the file exists**; V1's checklist was
  "does the art exist", and that is only half the question. Pendulum Color V2 puts Whamon at
  Perfect too, so US-139 will need a line-scoped id for it.

- **Line-scoped ids again, and they will keep coming.** `dmcv2_vademon` / `dmcv2_ebemon`, because
  the `gazimon` line (US-137's V5) already owns the plain ids. Each needs its own `elements.json`
  and `moves.json` entry with a signature name unique across the file, and neither counts as an
  orphan removed — they are not roster ids. Running tally of aliases in `RosterTests`:
  `dmcv1_shinmonzaemon`, `dmcv2_ebemon`, `dmcv2_vademon`, `extyranomon`, `piyo_tanemon`,
  `piyo_yuramon`.

- **MEASURED on the Simulator, because a raised ceiling is a claim about a screen.** On 41mm
  Gabumon's five candidates draw as **3 + 2**, both rows AND the hint list on one screen: row 1
  tiles at x16-108 / x126-224 / x248-328, row 2 at x35-81 / x135-216, the second band ending at
  y276 of a 430-row screen. Nothing clipped, no name truncated. US-133's black-screen regression
  was re-checked at 23 nodes (one more than the tree that caused it) — the V2 tree draws on both
  watch sizes, and `-dexTreeScrollDemo` reaches the Adult column.
  - New DEBUG flags: **`-dexWidestDetailDemo`** (opens the widest evolution grid the file holds,
    seeding its candidates so the tiles are real art) and **`-dexLineDemo=<line>`**, US-133's flag
    generalised — every Phase E tree needs looking at exactly once, and the bare flag hard-coded
    `dmc-v1`. Both write discoveries to the save; both simulators were erased afterwards.
  - **Getting a mid-sheet screenshot is a two-flag trick worth remembering:**
    `-dexWidestDetailDemo -dexBranchDemo`. `-dexTreeLinkDemo` scrolls to the very bottom and the
    candidate grid is then above the fold; selecting a branch shortens the hint list from eight
    rows to two, which brings the grid back onto the same screen.

- REMAINS: US-135 onward (21/56 passing). US-135 is Digital Monster Color Version 3 — the
  `patamon` line renamed to `dmc-v3` and completed. Note before starting it: `patamon` is ALREADY
  five-wide (Unimon / Centalmon / Ogremon / Bakemon / Scumon), which is the whole of that tree's
  Rookie row, so the ceiling raised here should be enough for it. Its Kunemon side is the new work,
  and V3 has no Ultra/Jogress row at all.
---

## 2026-07-22 - US-135 (Wire the Digital Monster Color Version 3 evolution tree)
- **The V3 tree is now complete and playable end to end.** The seed line US-044 called `patamon`
  WAS this tree, authored pruned; this story **renamed it to `dmc-v3`** and added the three nodes it
  was missing. Build green, **1729 tests (1716 + 13 new), 0 failures. Orphans 760 -> 757.**
- Files changed: `Resources/evolutions.json` (17 nodes re-lined, 3 new, 3 new edges on existing
  nodes), `Resources/elements.json`, `Resources/moves.json`, `Sources/DexModel.swift`,
  `Sources/MinigameAssignment.swift`, `docs/evolutions-schema.md`, `Tests/DMCVersion3TreeTests.swift`
  (new, 13 tests) and four existing test files.

- **THE CHEAPEST TREE OF PHASE E SO FAR, and the reason is worth knowing before US-136.** US-133
  added 10 nodes and US-134 added 13; this one added **three**. US-044 had already authored the
  whole spine — Patamon's five Champions, all three Perfects, all three Megas — and even the two
  Champions it could not reach (Shellmon, Drimogemon) were already named in `SeedRosterTests`'
  `patamonLineVerifiedSet`. **Read the existing line before estimating the work**: only Kunemon (the
  document's second Rookie) and its two exclusive Champions were genuinely absent. Expect US-136
  (`piyomon` -> V4) to be closer to this than to US-134, since US-045 authored it the same way —
  but its Rookie is SIX wide and two of its Champions have no art, so it will need the ceiling
  raised and the rehoming that US-045 already did checked rather than redone.

- **NO CEILINGS MOVED, for the first time in Phase E.** Patamon and Kunemon are five-wide and
  US-134's 2...5 out-degree ceiling and five-candidate Dex ceiling already covered them. **V4's
  Palmon and V5's Gizamon are still SIX wide in the document** — that raise is US-136/US-137's,
  unchanged from what US-134 predicted.
- **Four earned branches is the HARD ceiling for a Child, and it is not a taste question.**
  `SeedRosterTests.testThePatamonChildrensEarnedBranchesUseDistinctEnergies` requires every earned
  edge out of a Child to carry a distinct `requiredEnergy`, and there are only four energy types.
  A five-wide Rookie is therefore always four earned + the junk `isDefault`, and the junk edge must
  share a rival's energy while sitting below it on `minEnergy`. **A SIX-wide Rookie cannot be
  authored this way at all** — US-136 will have to either split the sixth onto another Child (the
  US-045 pattern) or change what distinguishes two edges of the same energy. Decide that BEFORE
  authoring V4, not after the test fails.

- **The same edge can point at the same target from two parents with DIFFERENT energies, and that
  is what made Kunemon fit.** Ogremon is `stamina` from Patamon and Tsukaimon but `strength` from
  Kunemon; Bakemon is `vitality` from all three. `requiredEnergy` is a property of the EDGE, not of
  the target node, so a shared Champion costs a Rookie whichever of its four slots is still free.
  Nothing in the validator or the tests objects. Without this, Kunemon's five could not have been
  authored at all: it shares three of them with Patamon.

- **DO NOT EMPTY A NODE THE SOURCE DOCUMENT DOES NOT CONTAIN.** Tsukaimon is not in the V3 tree —
  US-061 invented it to hold Ogremon and Bakemon when a Child could carry only two earned branches.
  Giving Patamon its five back made Tsukaimon's branches redundant, and deleting them would have
  left a shipped Child that evolves into nothing: a NEW orphan, made by the story whose job is to
  remove them. It keeps them and stands beside Kunemon as a third Rookie the device never had. This
  is US-134's Geremon lesson in the other direction — that one was about a lost IN-edge, this one
  about lost OUT-edges. **Check both directions before pruning.**

- **Version 3 needed NO line-scoped alias**, the first tree in Phase E that did not: none of its
  sixteen names is owned by another line. The running tally in `RosterTests` is unchanged —
  `dmcv1_shinmonzaemon`, `dmcv2_ebemon`, `dmcv2_vademon`, `extyranomon`, `piyo_tanemon`,
  `piyo_yuramon`. A test in `DMCVersion3TreeTests` asserts no `dmcv3_` id exists, so the day one is
  needed the reason has to be written down.
- **Version 3 has no Ultra/Jogress row at all**, unlike V1, V2 and V4. Its three Megas are terminal
  and a test pins that, so a later story adding a recipe for one of them has to say why.

- **AC11's one absent name, searched with `find -iname` over the whole asset pack:**
  - **Poyomon** — only `16x16 Digimon Sprites/Idle Frame Only/Poyomon.png`, no 48x64 sheet under
    `Baby I/`, so it is `dexOnly`. US-044 already substituted Puttimon and wrote the reason onto the
    `puttimon` node; this story only re-asserted it. Every OTHER name in the section is playable.
  - Kunemon, Shellmon and Drimogemon were each checked for FOLDER as well as existence (US-134's
    Whamon lesson): `Child/Kunemon.png`, `Adult/Shellmon.png`, `Adult/Drimogemon.png` — all three
    agree with the rung the document puts them on, so nothing needed rehoming this time.

- **LOOKED AT ON BOTH SCREENS, because every Phase E tree gets photographed once.** 41mm and 46mm,
  `-dexDemo -dexLineDemo=dmc-v3`: the twenty-node tree draws with its ladder and the three-way fork
  out of Tokomon, titled **"Color Ver.3"** from `DexModel.lineTitles` rather than the raw key. No
  black screen — US-133's regression was at 22 nodes and this tree is smaller than the 23-node V2
  one already cleared. Both simulators erased afterwards (`-dexDemo` writes discoveries to the save).

- REMAINS: US-136 onward (22/56 passing). US-136 is Digital Monster Color Version 4 — the `piyomon`
  line renamed to `dmc-v4`. Read the two paragraphs above about the six-wide Rookie and the
  four-energy limit before starting it; that is the one thing this story did not have to solve.
---

## 2026-07-22 - US-136: Wire the Digital Monster Color Version 4 evolution tree
- Renamed the `piyomon` seed line (US-045's pruned V4 tree) to `dmc-v4` and finished it: ONE new
  node, `dmcv4_palmon`, the document's second Rookie. 21 nodes in the line, 115 in the file.
- Files: `Resources/evolutions.json` (line rename, new node, two new edges, four comments),
  `Resources/elements.json`, `Resources/moves.json`, `Sources/MinigameAssignment.swift`,
  `Sources/DexModel.swift` (lineTitles + a stale doc-comment example), `Tests/DMCVersion4TreeTests.swift`
  (new, 16 tests), `Tests/SeedRosterTests.swift`, `Tests/EvolutionGraphTests.swift`,
  `Tests/EvolutionGraphValidatorTests.swift`, `Tests/ElementCatalogTests.swift`,
  `Tests/RosterTests.swift`, `docs/evolutions-schema.md`.
- Build green, 1745 tests (1729 + 16), 0 failures. Screenshotted on 41mm and 46mm.

- **Learnings for future iterations:**

- **THE SIX-WIDE ROOKIE WAS NEVER SIX WIDE. No ceiling moved.** US-134 and US-135 both wrote that
  US-136 would have to raise the 2...5 out-degree ceiling and the five-candidate Dex ceiling,
  because the document gives V4's Palmon six Champions. It does not need to: **BOTH of AC11's
  absent names are in that same row.** Kokatorimon is absent from the pack entirely
  (`find "16x16 Digimon Sprites" -iname '*okatorimon*'` returns nothing — no colour sheet, no
  monochrome one, no roster entry) and Nanimon is idle-only. Six minus two is four, which plus the
  junk fallback is exactly the five edges the ceiling already allowed. **Count the DRAWABLE row
  before pricing a ceiling raise off the document's row** — a previous story's estimate of the work
  is made without having run `ls`, and Phase E prunes a tree by art more often than not.
  **US-137's Gizamon is the remaining six-wide claim and it is a DIFFERENT case**: US-046's notes
  say V5's only omission is Flymon, so Gizamon's drawable row really is five earned + junk = six
  edges, and that one probably WILL need the raise. Check it against `ls` first anyway.

- **A line-scoped alias needs THREE data-file entries, not two, and the third has a uniqueness
  rule.** `dmcv4_palmon` needed `elements.json` and `moves.json` entries like any node — but
  `MoveCatalogTests.testSignatureNamesAreAuthoredRatherThanTemplated` requires every node's
  `signatureName` to be **globally distinct**, so copying the aliased Digimon's move entry verbatim
  fails: `dmcv4_palmon` and `palmon` both claiming "Poison Ivy" was the one red test in this story.
  Give the alias a second real attack from the same Digimon ("Allergy Shower"). US-045 already knew
  this — `piyo_yuramon` is "Dew Spray" where `yuramon` is "Dew Drop" — but nothing wrote it down.

- **A Phase E story can legitimately remove ZERO orphans, and that has to be asserted or it reads
  as a slip.** 757 -> 757, counted with Appendix B over a regenerated `roster.generated.json` and
  cross-checked by running the same count against `git show HEAD:Resources/evolutions.json`. The
  whole V4 section was already wired by US-045/US-061 except its second Rookie, and that Rookie is
  an ALIAS — a second node on a Digimon the `palmon` line has connected since US-008, so it is not
  a roster id and never counted as an orphan. What the story bought is tree CORRECTNESS: Palmon
  sits where the document draws it, and Piyomon gained the Kuwagamon its row always had.
  `DMCVersion4TreeTests.testTheStoryAddedNoNewlyConnectedRosterDigimon` pins the zero.
  **Expect the same shape from any remaining device tree whose seed line was already near-complete.**

- **The Tsukaimon call again, in its purest form.** Hyokomon and Muchomon are in NO source tree —
  US-045 invented them to hold Coelamon, Mojyamon and Kuwagamon while the plain `palmon` id belonged
  to another line. Giving those Champions to `dmcv4_palmon` made both redundant, and emptying them
  would have left two shipped Children that evolve into nothing. **They keep their branches.** That
  is now three stories running (US-134 Geremon, US-135 Tsukaimon, US-136 here); treat it as the
  default, not a judgement call.

- **The same Champion under two parents with different energies keeps paying off** (US-135's
  finding). Leomon is `strength` from both Rookies but Kuwagamon is `stamina` from Piyomon and
  `dmcv4_palmon` while being `strength` from Muchomon. `requiredEnergy` is a property of the EDGE.

- **V4's Ultra row needed no new data.** "Darkdramon -> Ultra: Chaosmon (Jogress with BanchoLeomon)"
  was already a recipe in `Resources/jogress.json` from US-131, so Darkdramon is terminal and a
  comment on the node says where the row went (the V1/V2 pattern, which a test now enforces).
  Worth knowing: that recipe reaches across two device trees — BanchoLeomon is `dmc-v3`'s Mega.

- **A `-dexLineDemo` screenshot needs the right bundle id.** It is `com.digivpet.DigiVPet`
  (`project.yml` line 82), not `com.example.DigiVPet`. `simctl launch` fails with an opaque
  FBSOpenApplicationServiceErrorDomain code=4 and `simctl io screenshot` still writes a plausible
  PNG of the watch face, so a wrong id produces a screenshot that looks like a black-screen bug.

- REMAINS: US-137 onward (23/56 passing). US-137 is Digital Monster Color Version 5 — the `gazimon`
  line renamed to `dmc-v5`. It is the only seed line that already holds BOTH of its Rookies, so
  expect the tree to be near-complete like this one; read the Gizamon paragraph above before
  assuming a ceiling raise, and check `Flymon` with `ls` rather than trusting US-046's note.
---

## 2026-07-22 - US-137: Wire the Digital Monster Color Version 5 evolution tree
- Renamed the `gazimon` seed line (US-046's pruned V5 tree) to `dmc-v5` and finished it: **ZERO new
  nodes and FOUR new edges.** 20 nodes in the line, 115 in the file, unchanged.
- Files: `Resources/evolutions.json` (line rename x20, 4 new edges, 4 comments), `Resources/elements.json`,
  `Resources/moves.json`, `Sources/MinigameAssignment.swift`, `Sources/DexModel.swift` (lineTitles +
  a stale doc-comment example), `Tests/DMCVersion5TreeTests.swift` (new, 21 tests),
  `Tests/SeedRosterTests.swift`, `Tests/EvolutionGraphTests.swift`, `Tests/EvolutionCriteriaTests.swift`,
  `Tests/DMCVersion2TreeTests.swift`, `Tests/RosterTests.swift`, `docs/evolutions-schema.md`.
- Build green, **1766 tests (1745 + 21), 0 failures.** Screenshotted on 41mm and 46mm.

- **Learnings for future iterations:**

- **THE SECOND SIX-WIDE ROOKIE ALSO NEEDED NO CEILING RAISE — and this time the previous story had
  already run `ls` and STILL got it wrong.** US-136 correctly caught that V4's Palmon was not really
  six wide, then wrote that "US-137's Gizamon is a DIFFERENT case: US-046's notes say V5's only
  omission is Flymon, so Gizamon's drawable row really is five earned + junk = six edges, and that
  one probably WILL need the raise." The premise was right and the arithmetic was off by one:
  **six names minus one absent is FIVE drawable, which is four earned plus the junk fallback** —
  the five edges the `2...5` ceiling already allowed. Neither ceiling has moved in Phase E since
  US-134. The general lesson is sharper than "run `ls` first": **a row's edge count is
  `drawable − 1 + 1` only if the junk Champion is IN the row**, and in all five device trees it is
  (Numemon, Vegimon, Scumon, Nanimon, Raremon are each a Champion the document itself lists). So
  the edge count IS the drawable count, and the ceiling question is just "how many of the row have
  sheets?" Four earned is the hard limit anyway — `SeedRosterTests` wants distinct dominant
  energies and there are only four.

- **A Phase E story can add NO node at all.** US-133 added 10, US-134 13, US-135 3, US-136 1, this
  one 0. Every playable name in the Version 5 section was already a node (US-046) and the junk
  branches already existed (US-061); what was missing was four ARROWS the document draws. Orphans
  757 → 757 for the second story running. Counted with Appendix B over a regenerated
  `roster.generated.json` AND cross-checked by recomputing the count against
  `git show HEAD:Resources/evolutions.json` joined to the same roster — identical stage by stage.
  `DMCVersion5TreeTests.testTheStoryAddedNoNodesAndSoRemovedNoOrphans` pins it.
  **Expect the remaining Pendulum trees (US-138..143) to be the opposite shape**: none of them has a
  seed line at all, so every one of them is authoring from an empty start rather than finishing
  someone's pruned tree. US-137 is where the "rename and finish" pattern runs out.

- **The schema doc was already describing the tree this story built.** `docs/evolutions-schema.md`
  said the V5 line "leaves both Rookies with five Champions apiece, four of them shared" — which
  was true of the DOCUMENT and false of the DATA, where both Rookies had three. Prose in a doc is
  not a test. If a claim about the shipped data matters, it belongs in an assertion; the doc line
  had been wrong since US-046 and nothing caught it.

- **AC9 ("every edge has at least one condition with a non-empty hint") cannot be met literally,
  and every Phase E story has quietly re-read it.** `EvolutionCriteriaTests.testEveryJunkFallbackIsReachableByInaction`
  requires the `isDefault` edge to carry NO criteria — US-020's fallback ignores an edge's gates, so
  a gated junk edge is data that lies about how it is taken — and the Digitama/Baby rungs are
  unconditioned for the same reason. The honest reading, asserted here as three separate claims:
  every EARNED edge at Child-and-up is conditioned, every condition in the WHOLE file has a
  non-blank hint, and every junk edge is unconditioned. Say which reading you took in `notes`.

- **A line rename is still exactly six places, four trees running** (`elements.json` lineDefaults,
  `moves.json` lineDefaults, `MinigameAssignment.byLine`, `DexModel.lineTitles`,
  `EvolutionGraphTests`' per-line count dict, `SeedRosterTests`' `$0.line ==` filters) — **plus one
  this tree had that the others did not: OTHER stories' tests asserting on the old slug.**
  `DMCVersion2TreeTests` asserted `shared.line == "gazimon"` and that two `dmcv2_*` node comments
  contained the word "gazimon", because US-134 scoped Vademon and Ebemon AROUND this line. A
  `grep -rn '<old-slug>' Sources Tests docs Resources scripts` and reading every hit is what caught
  it; the node id `gazimon` is also a real id, so most hits must NOT change.

- **`extyranomon` is in `RosterTests`' alias list but is NOT a line-scoped alias.** It is the
  roster's `ex-tyranomon` with the hyphen dropped — one id in two spellings, not one Digimon in two
  trees like `dmcv2_vademon`. A test now pins the difference, because the list reads as though V5
  needed a scoped id when it is in fact the line that OWNS the plain `vademon`/`ebemon` ids US-134
  had to scope for V2. Phase E's alias tally is still four: `dmcv1_shinmonzaemon`, `dmcv2_ebemon`,
  `dmcv2_vademon`, `dmcv4_palmon`.

- **The Tsukaimon call, a fourth time, and it should stop being called a judgement.** Psychemon is
  in no source tree (US-061 invented it while a Child could carry two earned branches); giving
  Gazimon and Gizamon their full rows made its Devidramon redundant, and emptying it would have
  left a shipped Child that evolves into nothing. It keeps its branches. That is US-134's Geremon,
  US-135's Tsukaimon, US-136's Hyokomon/Muchomon and now this — **treat "the node keeps what it
  has" as the default and only deviate with a reason in the data file.**

- **"Mugendramon (Machinedramon)" is one Digimon.** The document writes the dub name in
  parentheses; `Machinedramon` matches nothing in the pack or the roster. A test pins that, because
  a parenthetical in a source tree reads exactly like a second name to wire.

- REMAINS: US-138 onward (24/56 passing). US-138 is Pendulum Color V1 Nature Spirits — the first
  Phase E tree with NO seed line behind it, so read the paragraph above about that before
  estimating it against US-135/136/137's size.
---

## 2026-07-22 - US-138: Wire the Pendulum Color V1 Nature Spirits evolution tree
- Authored `penc-nsp` from NOTHING — **30 new nodes**, the widest line in the file; 115 -> 145.
  **Orphans 757 -> 739 (18 removed.)** Betamon is the section's one absent sprite and its two
  Champions were rehomed. The tree renders in the Dex on both watch sizes.
- Files: `Resources/evolutions.json` (+30 nodes), `Resources/elements.json`, `Resources/moves.json`
  (+30 entries each plus a `penc-nsp` lineDefault), `Sources/MinigameAssignment.swift`,
  `Sources/DexModel.swift`, `Tests/PendulumNatureSpiritsTreeTests.swift` (new, 24 tests),
  `Tests/EvolutionGraphTests.swift`, `Tests/EvolutionGraphValidatorTests.swift`,
  `Tests/ElementCatalogTests.swift`, `Tests/EvolutionCriteriaTests.swift`,
  `Tests/SeedRosterTests.swift`, `Tests/RosterTests.swift`, `Tests/MinigameAssignmentTests.swift`,
  `Tests/EggHatchingTests.swift`, `Tests/DexMoveRowTests.swift`, `Tests/DexTypeBadgeTests.swift`,
  `Tests/DMCVersion5TreeTests.swift`, `docs/evolutions-schema.md`.
- Build green, **1789 tests (1765 + 24), 0 failures.** Validator: zero findings over the whole file.

- **Learnings for future iterations:**

- **AUTHORING FROM NOTHING IS A DIFFERENT JOB FROM FINISHING A SEED, and the cost is in the
  KNOCK-ON, not in the tree.** The 30 nodes were the easy half. What US-133..137 never had to touch,
  because they renamed an existing line rather than adding one, was **six tests that hard-code a
  fact about the WHOLE FILE**: `EvolutionGraphTests`' per-line count dict, `ElementCatalogTests`'
  and `EvolutionGraphValidatorTests`' `graph.nodes.count == 115`, `EggHatchingTests`' set of
  seeded Digitama (a seventh line means a seventh starting egg), `SeedRosterTests`' `seedLines`
  junk-path literals, and `RosterTests`' exact alias list. Budget for those BEFORE writing data.
- **Three tests used a roster-only Digimon BY NAME as their "not in the graph" example, and this
  story ate it.** `megaseadramon` was that example in `ElementCatalogTests`, `DexMoveRowTests` and
  `DexTypeBadgeTests`. Phase E is pulling the whole roster into the graph, so **every one of those
  examples is on a timer** — all three now FIND one at run time (`roster.entries.first { graph.node(id:
  $0.id) == nil }`) instead of naming one. If you write a test that needs "something outside the
  graph", derive it; the next sweep will take whatever you hard-code.
- **THE LINES OUTGREW THE MINIGAMES.** `MinigameAssignmentTests.testEachShippedLineGetsADifferentGame`
  asserted a BIJECTION — six lines, six games — which held only because there happened to be six
  lines. `penc-nsp` is the seventh and US-139..143 add five more, so it is now "every line is in
  the table, all six games are reached", plus a test naming the one sharing pair
  (`palmon`/`penc-nsp`, both nature-flavoured). **Do not re-tighten it.**
- **A NEW LINE'S EGG MUST BE A REAL ROSTER DIGITAMA, NOT A LINE-SCOPED ALIAS — this is playability,
  not taste.** `maps.json` grants a Digitama by ROSTER id (all 57 sprite ids are placed, one per
  map), and a `pencnsp_digitama` alias has NO roster entry, so no map could ever drop it and the
  line would be unstartable however well it was wired. `Resources/roster.json` excludes alias ids
  even though `roster.generated.json` carries them. Pick from the 50 orphan Digitama, and prefer
  one belonging to a Rookie of the tree — this one took `tento_digitama` (Tentomon's, on
  08_jungle). It also buys an orphan.
- **An alias node removes NO orphan.** 30 nodes, 18 orphans. The 12 `pencnsp_` aliases share a
  roster entry that was already wired, so the arithmetic is "new nodes with a plain roster id",
  not "new nodes". Assert the split or the notes read as a miscount.
- **Twelve aliases is the new high-water mark and each one costs THREE entries** (node,
  `elements.json`, `moves.json`) **plus a signature name that must be globally unique** — so it
  cannot be copied from the Digimon it aliases. A Pendulum tree overlaps the Digital Monster trees
  heavily by design; expect US-139..143 to be the same shape. A quick way to price a tree before
  starting: how many of its names already have a node?
- **A DEXONLY ROOKIE COSTS MORE THAN A DEXONLY CHAMPION.** Every earlier Phase E absence was a
  leaf (Flymon, Kokatorimon, Nanimon) and was simply dropped. Betamon is a ROOKIE with two
  Champions and four Digimon above them: dropping it would have stranded six. **Rehome the
  orphaned subtree onto a sibling Rookie and prefer a pairing the FILE already makes** — Seadramon
  went to Agumon because `dmc-v1` already hangs a Seadramon off Agumon, which turns an invention
  into a precedent. Watch the Rookie's out-degree and the distinct-energy rule while you do it.
- **When a document draws no junk branch, you have to invent one, and it should come off ORPHAN
  sheets.** The five `dmc-v*` trees each name their own junk Champion; the Pendulum ones name
  none. PlatinumScumon -> Pumpmon -> NoblePumpmon were all unused, so the neglect path paid for
  itself in orphans instead of costing three more aliases. **Assert the invention** — a test greps
  the tree markdown for all three names and fails if any turns out to be in it after all — and add
  the Child-rung and Adult-rung ones to `EvolutionCriteriaTests.junkIds` or the junk-fallback test
  fails.
- **A "this story added no nodes" test must count ITS OWN LINE, never the file.**
  `DMCVersion5TreeTests` asserted `graph.nodes.count == 115` to say US-137 added nothing, and this
  story broke it by adding a tree somewhere else entirely. A global total cannot say anything
  about what one story did once another lands beside it.
- **`Resources/*.md` IS in the app bundle** (project.yml bundles the whole folder), so a test can
  read the source tree markdown at run time and assert what the document does and does not say.
  That is how "the junk chain is not from the document" became an assertion rather than a comment.
- **`-dexLineDemo=<line>` needs `-dexDemo`** (still true, US-137's note) and the Dex heading is a
  `navigationTitle` that truncates near fifteen characters on 41mm: "Pendulum NSp" fits, a longer
  "Pendulum Nature Spirits" would not have.
- REMAINS: US-139 onward (25/56 passing). US-139..143 are the other five Pendulum trees and are
  the same shape as this one — no seed line, heavy overlap with the Digital Monster trees, no junk
  branch in the document, and a Digitama to choose from the roster.
---

## 2026-07-22 - US-139: Wire the Pendulum Color V2 Deep Savers evolution tree
- Authored `penc-ds` from NOTHING — **31 new nodes**, now the widest line in the file; 145 -> 176.
  **Orphans 739 -> 713 (26 removed.)** The first Phase E tree with NO absent name. The tree renders
  in the Dex on both watch sizes.
- Files: `Resources/evolutions.json` (+31 nodes), `Resources/elements.json`, `Resources/moves.json`
  (+31 entries each plus a `penc-ds` lineDefault), `Sources/MinigameAssignment.swift`,
  `Sources/DexModel.swift`, `Tests/PendulumDeepSaversTreeTests.swift` (new, 24 tests),
  `Tests/EvolutionGraphTests.swift`, `Tests/EvolutionGraphValidatorTests.swift`,
  `Tests/ElementCatalogTests.swift`, `Tests/EvolutionCriteriaTests.swift`,
  `Tests/SeedRosterTests.swift`, `Tests/RosterTests.swift`, `Tests/EggHatchingTests.swift`,
  `Tests/MinigameAssignmentTests.swift`, `Tests/PendulumNatureSpiritsTreeTests.swift`,
  `docs/evolutions-schema.md`.
- Build green, **1813 tests (1789 + 24), 0 failures** — and 1813 is exactly
  `grep -cE '^\s+func test' Tests/*.swift`, so the summary is not a crashed-host undercount.
  Validator: zero findings over the whole file.

- **Learnings for future iterations:**

- **A NAME WITH NO SPRITE IS USUALLY A NAME WITH ANOTHER SPELLING. `find -iname` ON THE DOCUMENT'S
  SPELLING IS HALF A SEARCH.** Six of this section's twenty-seven names would have been reported
  absent or dexOnly by the search every earlier Phase E story ran, and all six are playable:
  - **Nothing under the document's spelling, a real sheet under another** — Pichimon/`Pitchmon`,
    Bukamon/`Pukamon`, Syakomon/`Shakomon`, Dragomon/`Dagomon`. The first three are alternate
    romanizations the pack itself uses; the fourth is a dub name (Wikimon's Dagomon page gives
    Dragomon as the Japanese one).
  - **A dexOnly TWIN under the document's spelling** — the roster holds BOTH `octomon` (idle-only)
    and `octmon` (a real 48x64 sheet), and both `marinedevimon` (idle-only) and `marindevimon`.
    This is the nastier shape: `find -iname '*octomon*'` finds a file and the roster finds an
    entry, so the name looks present, and it is only the `dexOnly` flag that says otherwise.
  **Before calling a name absent, search the roster by SUBSTRING of the display name and check
  every hit's `dexOnly`, not just the exact spelling.** US-138 spent its story rehoming Betamon's
  subtree; this one would have had to do that twice (Dagomon → Pukumon behind Octmon, Leviamon
  behind MarinDevimon) and drop four more, for nothing.
- **Adding a line is now an EIGHT-place knock-on, not six.** US-138 listed six whole-file facts;
  two more surfaced here: `EvolutionCriteriaTests.junkIds` (a new tree invents a junk chain, and
  the Child-rung and Adult-rung targets must be named there or the junk-fallback test fails) and
  `MinigameAssignmentTests`' shared-game assertion. The full list, in the order they fail:
  `EvolutionGraphTests`' per-line count dict; `graph.nodes.count` in `ElementCatalogTests` AND
  `EvolutionGraphValidatorTests`; `EggHatchingTests`' seeded-Digitama set; `SeedRosterTests`'
  `seedLines`; `RosterTests`' exact alias list; `EvolutionCriteriaTests.junkIds`;
  `MinigameAssignmentTests`' sharer map.
- **US-138's own tree test asserted the GLOBAL node count and this story broke it** — the exact
  mistake US-138's notes had already named for US-137's. A story's "I added N nodes" claim must be
  `graph.nodes.filter { $0.line == line }.count`, never `graph.nodes.count`, however obviously
  true the global number is on the day it is written. Rewritten in place here.
- **The lines have outgrown the games AGAIN, and the shared-game test needs a MAP, not a pair.**
  `testTheOnlySharedGameIsTheTwoNatureLines` was written for exactly one sharing pair; `penc-ds` is
  the eighth line and the second sharer (Crown Sprint, beside `dmc-v3`). It is now
  `testTheSharedGamesAreExactlyTheTwoAuthoredPairs` and asserts the whole
  game -> [lines] dictionary, which scales to US-140..143 by adding a line to a literal.
- **An alias's plain id need not belong to a Digital Monster tree any more.**
  `pencds_megaseadramon` and `pencds_metalseadramon` shadow ids US-138 gave to `penc-nsp`. The
  Pendulum trees overlap EACH OTHER as well as the dmc-v* ones, so from US-140 on, price a tree by
  checking the plain id against the WHOLE file rather than against the five device trees.
- **`json.dump` cannot rewrite `evolutions.json`: the file is mixed-encoding.** Older comments hold
  `—` escapes and the US-136-and-later ones hold literal em dashes, so neither
  `ensure_ascii=True` nor `False` round-trips — a dump reformats ~30 unrelated lines either way.
  Append the new nodes TEXTUALLY (`json.dumps` each node, indent it four spaces, splice before the
  final `\n  ]\n}`), and do `elements.json` / `moves.json` by string insertion too, since their
  entries are one-line objects that any dump would explode. Keep non-ASCII out of new comments.
- **The notification alert ate the first screenshot pass again** — it survives uninstall, exactly
  as the pattern above says, and `simctl erase` is still the only fix. Budget the erase into the
  FIRST run rather than discovering it on the shot.
- REMAINS: US-140 onward (26/56 passing). US-140..143 are the last four Pendulum trees, the same
  shape as this one — no seed line, no junk branch in the document, a Digitama to choose from the
  roster, and now overlap with the other Pendulum trees as well as the Digital Monster ones.
---

## 2026-07-22 - US-140 (Wire the Pendulum Color V3 Nightmare Soldiers evolution tree)

Authored `penc-nso` from nothing, the third Phase E device tree with no seed line to rename.
**31 nodes, 176 -> 207**, tying `penc-ds` as the widest line in the file.
**ORPHANS 713 -> 692, 21 removed**, counted with Appendix B over a regenerated
`roster.generated.json` (Digitama 49->48, Baby I 37->36, Baby II 41->40, Child 100->97,
Adult 149->144, Perfect 135->129, Ultimate 186->182; Armor-Hybrid unchanged at 16). The other 10
new nodes are line-scoped `pencnso_` aliases and remove nothing.

The tree, from `Resources/Digimon_Color_And_Pendulum_Color_Evolution_Trees.md` line 124:
Baku Digitama -> Mokumon -> PetiMeramon, forking to Bakumon / Candmon / PicoDevimon, seven
Champion-to-Mega threads above them, and Gokimon -> Darumamon -> Deathmon as the invented junk
chain. Egg is `baku_digitama` (Bakumon's own, already dropped by `03_ocean`, so maps.json was not
touched).

Files: `Resources/evolutions.json`, `Resources/elements.json`, `Resources/moves.json`,
`Sources/DexModel.swift`, `Sources/MinigameAssignment.swift`,
`Tests/PendulumNightmareSoldiersTreeTests.swift` (new, 25 tests),
`Tests/EvolutionGraphTests.swift`, `Tests/EvolutionGraphValidatorTests.swift`,
`Tests/ElementCatalogTests.swift`, `Tests/EvolutionCriteriaTests.swift`,
`Tests/SeedRosterTests.swift`, `Tests/RosterTests.swift`, `Tests/EggHatchingTests.swift`,
`Tests/MinigameAssignmentTests.swift`, `docs/evolutions-schema.md`.

Build green, **1838 tests (1813 + 25), 0 failures** - and 1838 is exactly
`grep -cE '^\s+func test' Tests/*.swift`, so the summary is not a crashed-host undercount.
Validator: zero findings over the whole file.

- **Learnings for future iterations:**

- **GREP THE WHOLE DOCUMENT BEFORE CHOOSING A JUNK NODE.** The junk chain is invented off orphan
  sheets, and the only thing that makes an orphan safe to spend is that no LATER tree needs it.
  US-140's first choice for the junk Perfect was WaruMonzaemon; the story's own
  "none of these three is in the markdown" grep caught it in the **Version 5** Metal Empire
  section (line 162, `Champion: Mekanorimon -> Ultimate: WaruMonzaemon -> Mega: VenomMyotismon`),
  which is US-142's. Taking it would have forced US-142 into an alias for its own earned Ultimate
  AND made a junk node an earned one two stories later. Darumamon replaced it. Write that grep
  FIRST, not as a closing assertion.
- **The same section also spends VenomVamdemon**, which this story wired as an earned Mega. US-142
  will need `pencv5_venomvamdemon`. The Pendulum sections overlap each other more than they overlap
  the dmc-v* ones, so from here on price a tree against the whole file AND against the remaining
  document sections.
- **A dub name can differ from the sprite name by a whole word, not just a letter.** US-138/139's
  lesson was romanization drift (Pichimon/Pitchmon); this section is worse - `Mammothmon` is
  `Mammon`, `Wizardmon` is `Wizarmon`, `Pumpkinmon` is `Pumpmon`, `Piedmon` is `Piemon`,
  `Myotismon` is `Vamdemon`, `Candlemon` is `Candmon`. **A `find -iname '*<documentname>*'` that
  returns nothing is not evidence of absence; try the JAPANESE name.** Nine of this section's
  twenty-eight names would have been called absent by the search every earlier story ran, which
  would have cost the tree a third of its nodes.
- **A tree can be missing a whole RUNG, not just a leaf.** The unlockable slot 6 here has no
  Rookie and no Champion with a sheet, but its Ultimate and Mega both do. The move that saved it
  was checking Wikimon for what actually sits between the surviving rungs (Loogarmon), then
  re-homing that Champion under a Rookie the same page links it to. **Do not drop a thread because
  its bottom is missing** - price the rehome first, and record the reference in the node comment
  (a test greps for "Wikimon").
- **`ConditionContext` in the reachability fixture cannot express a battle win RATIO.** It has
  `battlesLifetime` and no wins, so `care.battleWinRatio` on a CHILD's earned edge makes
  `testEachRookiesEarnedBranchesAreAllReachable` fail with a bare `nil`. Earlier trees only ever
  used that metric at Adult, where no test walks it. Keep `care.battleWinRatio` off Child edges, or
  extend the fixture.
- **The knock-on list is stable at eight places** (US-139's list, unchanged, all eight fired):
  `EvolutionGraphTests`' per-line count dict; `graph.nodes.count` in `ElementCatalogTests` AND
  `EvolutionGraphValidatorTests`; `EggHatchingTests`' seeded-Digitama set; `SeedRosterTests`'
  `seedLines`; `RosterTests`' exact alias list; `EvolutionCriteriaTests.junkIds`;
  `MinigameAssignmentTests`' sharer map. US-139's rewrite of that last one into a
  game -> [lines] dictionary paid off exactly as intended: one literal.
- **`json.dump` still cannot rewrite `evolutions.json`** - mixed encoding, as US-139 recorded.
  Splice new nodes TEXTUALLY (`json.dumps` each node, indent four spaces, insert before the final
  `\n  ]\n}`), and do `elements.json` / `moves.json` by string insertion. Keep new comments ASCII.
- **A generator script is worth writing, and worth re-running rather than patching.** The whole
  31-node tree came out of one `/tmp` python script; when the junk Perfect had to change, reverting
  the three JSON files with `git checkout` and re-running was cleaner than hand-editing 31 nodes.
  But CHECK EVERY `str.replace` LANDS - two of the fix-up replacements silently matched nothing
  because of line wrapping, and the resulting node (PetiMeramon with no edges at all) passed the
  validator and only failed on reachability.
- REMAINS: US-141 onward (27/56 passing). US-141..143 are the last three Pendulum trees, same
  shape - no seed line, no junk branch in the document, a Digitama to choose from the roster, and
  now overlap with the other Pendulum trees as well as the Digital Monster ones.
---

## 2026-07-22 - US-141 (Wire the Pendulum Color V4 Wind Guardians evolution tree)

Authored `penc-wg` from nothing, the fourth Phase E device tree with no seed line to rename.
**31 nodes, 207 -> 238**, joining `penc-ds` and `penc-nso` as the widest lines in the file.
**ORPHANS 692 -> 667, 25 removed**, counted with Appendix B over a regenerated
`roster.generated.json` (Digitama 48->47, Baby I 36->35, Baby II 40->39, Child 97->94,
Adult 144->138, Perfect 129->123, Ultimate 182->175; Armor-Hybrid unchanged at 16). The other 6
new nodes are line-scoped `pencwg_` aliases and remove nothing - the best ratio of any Pendulum
tree so far.

The tree, from `Resources/Digimon_Color_And_Pendulum_Color_Evolution_Trees.md` line 138:
Flora Digitama -> Nyokimon -> Pyocomon, forking FOUR ways to Piyomon / Floramon / Mushmon /
Pteromon, seven Champion-to-Mega threads above them, and Zassoumon -> TonosamaGekomon ->
ElDoradimon as the invented junk chain. Egg is `flora_digitama` (Floramon's), already dropped by
`14_farmland`, so maps.json was not touched.

Files: `Resources/evolutions.json`, `Resources/elements.json`, `Resources/moves.json`,
`Sources/DexModel.swift`, `Sources/MinigameAssignment.swift`,
`Tests/PendulumWindGuardiansTreeTests.swift` (new, 27 tests),
`Tests/EvolutionGraphTests.swift`, `Tests/EvolutionGraphValidatorTests.swift`,
`Tests/ElementCatalogTests.swift`, `Tests/EvolutionCriteriaTests.swift`,
`Tests/SeedRosterTests.swift`, `Tests/RosterTests.swift`, `Tests/EggHatchingTests.swift`,
`Tests/MinigameAssignmentTests.swift`, `docs/evolutions-schema.md`.

Build green, **1865 tests (1838 + 27), 0 failures** - and 1865 is exactly
`grep -cE '^\s+func test' Tests/*.swift`, so the summary is not a crashed-host undercount.
Validator: zero findings over the whole file.

- **Learnings for future iterations:**

- **A tree can be missing a MIDDLE rung and a TOP rung, not just a bottom one, and the same rehome
  move fixes all three shapes.** US-140 rescued a thread whose Rookie and Champion were undrawn;
  this section has no `Deramon` (the Perfect between Kiwimon and Griffomon) and no
  `Crossmon`/`Eaglemon` (the Mega above Garbagemon). Wikimon's own Evolves From / Evolves To lists
  name a stand-in that sits on the SAME thread in each case - Blossomon is listed both as a
  Kiwimon evolution and as a Griffomon prior form, Rafflesimon as a Gerbemon evolution - so the
  document's arrows survive intact. **Fetch the Wikimon page for the rung ABOVE and the rung BELOW
  the gap and intersect the two lists**; that is a five-minute lookup that saved six nodes here.
- **Do not trust a substring search that finds nothing when a letter is INSIDE the name.**
  `Jureimon` is not a substring of `Jyureimon`, so the document's own bracketed spelling misses its
  own sheet. `grep -i` on a prefix (`jure` -> nothing, `jyu` -> the file) is the fix. Two of this
  section's names hid this way and both were nearly reported absent.
- **`garbamon` is NOT `Garbagemon`.** The roster's nearest substring hit is a different Digimon and
  dexOnly, while the real sheet is `Perfect/Gerbemon.png` under the Japanese name - so the name
  looks simultaneously present and unusable, and both readings are wrong. US-139's Octomon/Octmon
  rule with an extra turn of the screw: after checking every substring hit's `dexOnly`, KEEP
  LOOKING under the Japanese name rather than concluding the Digimon is idle-only.
- **An egg does not have to be the default Rookie's own, and pretending otherwise wastes a story.**
  Every Pendulum tree so far took its In-Training's default Rookie's egg; here that egg
  (`piyo_digitama`) went to `dmc-v4` in US-136, because the Digital Monster Ver.4 tree is Piyomon's
  too. The rule that actually matters is only "a real roster Digitama that some map drops" - see
  `flora_digitama`'s comment. **Check the map that drops it and name that map correctly**: the
  first draft of this comment said `02_forest`, which is not even a map in this app; `14_farmland`
  is. A wrong map id in a comment is a lie a later story will act on.
- **The unlockable-slot Rookie is drawable again**, so US-138/139's conditioned In-Training edge
  came back after lapsing in US-140. If a section marks a Rookie "Unlockable Slot N", check for its
  sheet before assuming the shape is gone: Pteromon has one, and its whole thread (Galemon,
  GrandGalemon, Zephagamon) is the only one in this section drawn whole AND unrespelled.
- **A four-way fork at Baby II is fine and is now the widest in the file.** Four Rookies means four
  edges, one per energy type, which is exactly the ceiling `SeedRosterTests`' distinct-energy rule
  allows. No out-degree ceiling moved (still 2...5, unchanged since US-134) because the Child and
  Adult rungs are only two and three wide here.
- **The knock-on list is stable at eight places for the third tree running** (US-139's list,
  unchanged, all eight fired): `EvolutionGraphTests`' per-line count dict; `graph.nodes.count` in
  `ElementCatalogTests` AND `EvolutionGraphValidatorTests`; `EggHatchingTests`' seeded-Digitama
  set; `SeedRosterTests`' `seedLines`; `RosterTests`' exact alias list;
  `EvolutionCriteriaTests.junkIds` (BOTH the Adult and the Perfect junk node);
  `MinigameAssignmentTests`' sharer map. Watch the SORT ORDER in `RosterTests`: `pencwg_` sorts
  after `pencnsp_`, not after `pencnso_`.
- **Terminal nodes in `evolutions.json` OMIT the `evolutions` key entirely** rather than carrying
  `[]`. The decoder accepts both, so this is style, not correctness - but a generator that always
  writes the key produces a 47-node inconsistency in one commit. Emit it only when non-empty.
- **`json.dump` still cannot rewrite `evolutions.json`** - mixed encoding, as US-139 recorded.
  Splice new nodes TEXTUALLY, and do `elements.json` / `moves.json` by string insertion. Keep new
  comments ASCII. The whole 31-node tree came out of one `/tmp` python script, and it was reverted
  with `git checkout` and re-run THREE times (duplicate signature names, the `evolutions: []`
  style, a wrong map id) rather than hand-patched. That is the right move every time.
- **Check new `signatureName`s against the WHOLE file, not just your line.** Three of the first
  draft's collided with the `palmon` line's (Flower Cannon, Thorn Whip, Petal Puff) - unsurprising,
  since this tree draws Togemon, Lilimon and Rosemon too, and a flower tree runs out of flower
  words. `MoveCatalogTests` catches it, but only after a full 23-second test run.
- REMAINS: US-142 onward (28/56 passing). US-142 (Metal Empire) and US-143 (Virus Busters / ZERO)
  are the last two Pendulum trees. US-140's notes already flag two names US-142 must handle:
  WaruMonzaemon is its earned Ultimate over Mekanorimon, and VenomVamdemon is already spent by
  `penc-nso`, so US-142 needs `pencv5_venomvamdemon`.
---

## 2026-07-22 - US-142 (Wire the Pendulum Color V5 Metal Empire evolution tree)

Authored `penc-me` from nothing, the fifth Phase E device tree with no seed line to rename.
**32 nodes, 238 -> 270 - the widest line in the file on its own**, past the three-way tie at 31.
**ORPHANS 667 -> 643, 24 removed**, counted with Appendix B over a regenerated
`roster.generated.json`, with the pre-story 667 REPRODUCED in a `git worktree` at HEAD rather than
trusted from US-141's notes (Digitama 47->46, Baby I 35->34, Baby II 39->38, Child 94->90,
Adult 138->131, Perfect 123->117, Ultimate 175->171; Armor-Hybrid unchanged at 16). The other 8 new
nodes are line-scoped `pencme_` aliases and remove nothing - the WORST alias ratio of any Pendulum
tree, because this section shares Agumon's whole thread with two other trees.

The tree, from `Resources/Digimon_Color_And_Pendulum_Color_Evolution_Trees.md` line 152:
Funbee Digitama -> Choromon -> Caprimon, forking four ways to ToyAgumon / Kokuwamon / Hagurumon /
Junkmon, nine Champion-to-Mega threads above them, and Raremon -> Locomon -> GrandLocomon as the
invented junk chain. Egg is `funbee_digitama`, dropped by `06_industrial`, so maps.json was not
touched.

Files: `Resources/evolutions.json`, `Resources/elements.json`, `Resources/moves.json`,
`Sources/DexModel.swift`, `Sources/MinigameAssignment.swift`,
`Tests/PendulumMetalEmpireTreeTests.swift` (new, 28 tests),
`Tests/EvolutionGraphTests.swift`, `Tests/EvolutionGraphValidatorTests.swift`,
`Tests/ElementCatalogTests.swift`, `Tests/EvolutionCriteriaTests.swift`,
`Tests/SeedRosterTests.swift`, `Tests/RosterTests.swift`, `Tests/EggHatchingTests.swift`,
`Tests/MinigameAssignmentTests.swift`, `Tests/DexScreenTests.swift`,
`Tests/PendulumNightmareSoldiersTreeTests.swift`, `docs/evolutions-schema.md`.

Build green, **1894 tests (1865 + 28 new + 1 backfilled), 0 failures** - and 1894 is exactly
`grep -cE '^\s+func test' Tests/*.swift`, so the summary is not a crashed-host undercount.
Validator: zero findings over the whole file. The app was also installed and launched on the 46mm
Simulator and drew its Dex, which is the cheap proof that no `bundled` catalog traps at launch.

- **Learnings for future iterations:**

- **A tree can be broken at BOTH ends of one thread, and the two breaks fail DIFFERENTLY.** The
  unlockable slot here is Junkmon -> Machmon -> Rebellimon -> HeavyMetaldramon. `Machmon` is
  US-139's twin trap - it IS in the roster and its PNG IS on disk, so `find` and a substring search
  both say "present", and only `dexOnly` says it can never sit on an edge. `HeavyMetaldramon` is
  the other shape: absent from the pack under every spelling tried (`heavymetal*`, `metaldra*`,
  `heavy*` -> only HeavyLeomon). **Run BOTH checks on every name: does a sheet exist, and is the
  roster entry playable.** Either one alone gets one of these two names wrong.
- **The rehome-by-intersection move works on a thread broken twice.** Wikimon lists Minotaurmon in
  Junkmon's *Evolves To* AND in Rebellimon's *Evolves From*, and Gundramon in Rebellimon's
  *Evolves To*: two pages, four lists, and the whole thread survives with the document's arrows
  intact. US-140 did this for a missing bottom, US-141 for a missing middle and a missing top;
  this is both ends of one thread at once. Fetching the page for the rung above the gap and the
  rung below it remains a five-minute lookup that saves a subtree.
- **A junk pick must be grepped against the SECTION, not the whole document - and the whole
  document, not just the section.** Both greps matter and they answer different questions.
  `Raremon` is drawn three times by the Digital Monster Ver.5 tree fourteen lines further up the
  same file, so a whole-file grep would have rejected a pick that is completely fine here (it is
  simply `pencme_raremon`, an alias). `Locomon` and `GrandLocomon` had to clear the whole-file
  grep, because a name a LATER section draws cannot be spent on junk. The test in this story does
  both, and says which is which.
- **US-140's marker test fired exactly as designed and cost nothing.** It asserted
  `XCTAssertNil(graph.node(id: "warumonzaemon"))` with a comment saying US-142 would break it. It
  broke on the first run of this story, pointed straight at the reason, and was rewritten into the
  claim it was always guarding (WaruMonzaemon is a node, it is `penc-me`'s, and it is nobody's junk
  fallback). **Leave the marker; it is worth more than a note in a file nobody greps.**
- **A hard-coded "the roster dwarfs the graph" threshold is on the same timer as a hard-coded
  roster-only example.** `DexScreenTests.testMostOfTheShippedRosterHasNoCandidates` asserted
  `> 800` and this story took it to exactly 800. Fixed by making it a PROPORTION
  (`> Roster.bundled.entries.count / 2`), which survives every remaining sweep. US-138's lesson
  about derived examples applies to derived COUNTS too - Phase E moves both numbers every story.
- **`DexModel.lineTitles` had no test at all, and a missing entry is invisible.** The fallback
  heads the section with the raw slug (`penc-me`), which is cosmetic and would never fail a build.
  Backfilled as a derived test over `EvolutionGraph.bundled`'s lines, so the next tree is TOLD to
  add its title. The eight-place knock-on list has always included this file; it just had nothing
  enforcing it.
- **This tree's threads MERGE, which no earlier one did, and it is the graph shape to expect from
  here on.** Tankmon and Thunderballmon both climb to Knightmon, and the document draws Andromon
  twice - over Revolmon toward Machinedramon and over Guardromon toward HiAndromon. One node
  carries both, so `pencme_andromon` is the file's only Perfect with two parents AND two children,
  and the only one whose second outgoing edge had to be EARNED rather than the usual single
  `isDefault` climb. Nothing in the suite objected: the 2..5 out-degree rule and the junk-fallback
  rule are both scoped to Child and Adult, so a Perfect may branch freely as long as exactly one
  edge is `isDefault`.
- **An egg need not belong to the tree at all.** US-141 found the egg was not the DEFAULT Rookie's;
  here not one of the four Rookies has a Digitama on disk, so all that survives of the rule is "a
  real roster Digitama that a map drops". `funbee_digitama` was chosen off `06_industrial`, the
  machine map, and the comment names that map - a wrong map id in a comment is a lie a later story
  acts on (US-141 shipped `02_forest`, which is not a map in this app).
- **Nine of this section's twenty-eight names are the ONLY one of a pair that exists**, and five
  need the drift rules: Kapurimon/`Caprimon`, Deputymon/`Revolmon`, Mekanorimon/`Mechanorimon`,
  Machinedramon/`Mugendramon`, VenomMyotismon/`VenomVamdemon`. Nothing new in kind - but the count
  has not dropped in five trees running, so budget for it.
- **The knock-on list is stable at eight places for the fourth tree running** (US-139's list, all
  eight fired): `EvolutionGraphTests`' per-line count dict; `graph.nodes.count` in
  `ElementCatalogTests` AND `EvolutionGraphValidatorTests`; `EggHatchingTests`' seeded-Digitama set;
  `SeedRosterTests`' `seedLines`; `RosterTests`' exact alias list; `EvolutionCriteriaTests.junkIds`
  (BOTH the Adult and the Perfect junk node); `MinigameAssignmentTests`' sharer map. Add TWO more
  this story found: `DexScreenTests`' roster-vs-graph threshold, and any earlier tree's marker test.
  Watch the SORT ORDER in `RosterTests`: `pencme_` sorts after `pencds_` and before `pencnso_`.
- **`penc-me` takes the last unshared-but-one minigame.** Button Masher now has `dmc-v1` beside it;
  Power Meter (`dmc-v2`) is the only game a single line still holds. The next tree has no
  unshared game left to take, so US-143 will be picking a SECOND sharer for some game - the
  tie-break to use is the one every story since US-138 has used: fewest Digimon behind the game,
  flavour breaks ties.
- **`json.dump` still cannot rewrite `evolutions.json`** - mixed encoding, as US-139 recorded.
  Splice new nodes TEXTUALLY, and do `elements.json` / `moves.json` by string insertion; the diff
  for those two was 34 added lines each and nothing reformatted. The whole 32-node tree came out of
  one `/tmp` python script with a PRE-FLIGHT CHECKER beside it (sprite on disk, roster stage and
  spriteFile agree, not dexOnly, rung transitions, out-degree, junk-edge shape, distinct earned
  energies, signature names unique against the whole file). The checker caught three signature
  collisions before a 23-second test run did, which is the right place to catch them.
- REMAINS: US-143 onward (29/56 passing). US-143 (Virus Busters / ZERO) is the LAST Pendulum tree
  and the last of the eleven device trees; US-144..169 are the 26 orphan sweeps over the remaining
  643. Two names US-143 must handle are already spent: `greymon`/`metalgreymon`/`wargreymon` are
  now THREE nodes each (dmc-v1, penc-nsp, penc-me), so its Agumon thread needs a fourth copy, and
  Garurumon's thread is already a triple.
---

## 2026-07-22 - US-143 - Wire the Pendulum Color V0 Virus Busters / ZERO evolution tree

Authored `penc-vb` from nothing: 30 nodes, 270 -> 300. It is the LAST of the eleven device trees,
so every `### Version` section of `Resources/Digimon_Color_And_Pendulum_Color_Evolution_Trees.md`
now has a line in the graph, and Phase E's first half is done. Orphans 643 -> 628, 15 removed
(counted with Appendix B, after `python3 scripts/build_roster.py`).

Files: `Resources/evolutions.json` (+30 nodes), `Resources/elements.json` and `Resources/moves.json`
(+30 entries and a `penc-vb` lineDefault each), `Sources/DexModel.swift` (line title),
`Sources/MinigameAssignment.swift` (Power Meter), new `Tests/PendulumVirusBustersTreeTests.swift`
(27 tests), `docs/evolutions-schema.md`, and the eight-place knock-on list plus three marker tests.
Build green, 1921 tests, 0 failures, validator zero findings.

**Learnings for future iterations:**

- **THE FIRST TREE WITH NOTHING ABSENT.** All twenty-six names the section draws have a playable
  48x64 sheet, so no Wikimon stand-in and no rehomed rung were needed - the first since US-139.
  Five are filed under a spelling the document does not use, and the drift rules found every one:
  SnowBotamon/`YukimiBotamon` (a dub name, `find -iname '*snowbotamon*'` returns NOTHING),
  Salamon/`Plotmon` and Gatomon/`Tailmon` (bracketed by the document itself), Wizardmon/`Wizarmon`
  and Stefilmon/`Stiffilmon`. Two letters inside a word again - `Stefilmon` is not a substring of
  `Stiffilmon`, US-140's prefix-search lesson.
- **A DOCUMENT CAN DRAW ONE DIGIMON TWICE UNDER TWO NAMES.** The section draws
  "Angemon -> HolyAngemon -> Seraphimon" and "Wizardmon -> MagnaAngemon -> Cherubimon (Virtue)" -
  and MagnaAngemon IS HolyAngemon, the dub name, with ONE sheet between them. Wired as one node
  with two parents and two children (the `pencme_andromon` shape from US-142: one `isDefault`
  climb, one EARNED second climb). The check that catches this class of thing is the one this
  story added: sweep every `*mon` word out of the section's own text and assert each resolves to a
  playable roster entry under its own spelling or a recorded substitution. Do that instead of
  eyeballing the bullet list - it is what turned MagnaAngemon from a missing name into a duplicate.
- **HALF this tree is aliases, and that is what a LATE device tree costs.** Fifteen of thirty nodes
  are `pencvb_`, the worst ratio in the file, because the V0 device is the one every earlier tree
  borrowed from - Agumon, Gabumon and everything above them were spent by US-133/US-138/US-142
  before this story reached the tree that owns them. Both of the file's triples became QUADRUPLES.
  Expect the orphan yield of a tree to fall as Phase E goes on: 24 removed in US-142, 15 here.
- **The two triple-count marker tests fired exactly as designed, and cost nothing.** US-142's
  `testTheGreymonThreadIsDrawnByExactlyThreeTrees` and US-140's Garurumon equivalent both asserted
  a count with a "has a fourth copy" message; both broke on the first run and both were rewritten
  into the same claim one number higher. Keep writing them. US-138's Omegamon doc-comment ("the
  only tree that holds BOTH of that recipe's parents") was prose, not an assertion, and had to be
  found by hand - if a claim is worth writing, write it as XCTAssert.
- **The junk chain can be chosen for MEANING, not just for being spare.** Turuiemon ->
  Andiramon (Virus) -> Cherubimon (Vice) is the Lopmon line's fall, every arrow of it drawn by
  Wikimon, and its top is the counterpart of the Cherubimon (Virtue) this same document draws as an
  earned Mega - so neglecting a Virus Buster ends it as the thing the tree exists to fight. Two
  sheets of one Digimon, one earned and one junk, in ONE line, which no earlier tree had. Note the
  ONE arrow that is NOT canon: Turuiemon's earned way back up to Asuramon is flavour, and the
  node's `comment` says so rather than implying Wikimon draws it. Do not launder a choice as a
  citation.
- **`penc-vb` takes Power Meter, and the game table is now exactly 2 lines per game.** Twelve
  lines, six games, six pairs - the sharer dictionary in `MinigameAssignmentTests` is at its
  symmetric point. The 26 orphan sweeps (US-144..169) do not add lines, but if anything ever does,
  the next sharer makes a TRIPLE, and the tie-break is unchanged: fewest Digimon behind the game,
  flavour breaks ties.
- **The knock-on list is stable at eight places for the fifth tree running**, plus the two marker
  tests: `EvolutionGraphTests`' per-line count dict; `graph.nodes.count` in `ElementCatalogTests`
  AND `EvolutionGraphValidatorTests`; `EggHatchingTests`' seeded-Digitama set; `SeedRosterTests`'
  `seedLines`; `RosterTests`' exact alias list (sort order: `pencvb_` after `pencnso_`/`pencnsp_`,
  before `pencwg_`); `EvolutionCriteriaTests.junkIds` (both rungs); `MinigameAssignmentTests`'
  sharer map; `DexScreenTests`' line-title assertion. ONE more this story found:
  **`elements.json` and `moves.json` need a `lineDefaults` entry for the new line**, not only the
  per-id ones - `ElementCatalogTests.testEveryGraphLineHasALineDefault` and its moves twin.
- **The pre-flight checker beside the authoring script earned its keep a third time.** It caught
  eight signature-name collisions and one id that should not have been an alias at all (`filmon`
  was in no other line, so `pencvb_filmon` would have spent an alias and removed no orphan) before
  a 23-second test run could. Check "is this plain id already a node?" for EVERY node, not just the
  ones you expect to collide.
- REMAINS: US-144 onward (30/56 passing). The eleven device trees are done; US-144..169 are the 26
  orphan sweeps over the remaining 628, which by stage are Ultimate 166, Adult 129, Perfect 114,
  Child 88, Digitama 45, Baby II 37, Baby I 33, Armor-Hybrid 16.
---

## 2026-07-22 - US-144 - Orphan sweep: Digitama A-K

The first of Phase E's 26 sweeps, and the first story since US-008 to open a line that is not a
device tree. 30 nodes, 300 -> 330. Digitama orphans 45 -> 23; whole-file orphans 628 -> 598 (the
extra 8 are the Baby I this sweep opened, which the Appendix B rule counts as connected the moment
they have an in-edge).

Files: `Resources/evolutions.json` (+30 nodes), `Resources/elements.json` and `Resources/moves.json`
(+30 entries and 5 `lineDefaults` each), `Sources/DexModel.swift` (5 line titles),
`Sources/MinigameAssignment.swift` (5 assignments), `Sources/EvolutionGraph.swift`
(`reachesUltimate(from:)`), `Sources/MainScreenModel.swift` (starting-egg filter), new
`Tests/DigitamaSweepTests.swift` (15 tests), `docs/evolutions-schema.md`, and TEN existing test
files. Build green, 1936 tests, 0 failures, validator zero findings.

**Learnings for future iterations:**

- **THE SWEEP'S RULE, and it is the one US-145..169 should reuse.** A sweep has no document, so
  what stands in for one is: (1) an egg whose species is ALREADY a node hangs off that species'
  line and hatches onto that line's Baby I, adding no rung at all; (2) an egg whose species is NOT
  in the graph opens a thread and brings its own Baby I, taken from Wikimon two hops down. Twelve
  of twenty-two went the first way and ten the second. The first way is what keeps the phase from
  producing dozens of one-node lines, which is an explicit acceptance criterion.
- **AUTHORING ONTO AN EXISTING LINE BREAKS SIX EARLIER STORIES' TESTS, and that is the real cost of
  this story.** 32 failures on the first run, every one of them an assertion that a line has ONE
  egg or has exactly N nodes. Budget for it: the fix is mechanical but it is ten files. The
  generalisation to make is "reachable from ANY egg of the line", and for a count, EXCLUDE the
  sweep's eggs by name (`$0.line == line && !sweepEggs.contains($0.id)`) rather than bumping the
  total — bumping it silently changes what the earlier story's notes claimed.
- **`EvolutionNode` has NO `comment` property.** The key is authored in `evolutions.json` and never
  decoded; a test that wants to grep a comment loads the raw JSON out of `Bundle.main` and reads
  `nodes[]["comment"]`, exactly as every Pendulum tree test already does. Cost one build.
- **The two ACs a Digitama sweep cannot meet literally, with the reasons, so US-145 need not
  re-derive them.** "Every Digimon in the range gets an in-edge" — nothing evolves INTO an egg.
  "No edge is unconditional" — `EggHatcher` ignores `conditions`, and with one hatch edge per egg a
  condition that WAS honoured could leave a player's only egg unable to hatch. Both are recorded in
  `notes` and both are stated as a test rather than left as prose.
- **Nine knock-on places, and the list is different from a device tree's.** `EvolutionGraphTests`'
  per-line dict; `graph.nodes.count` in `ElementCatalogTests` AND `EvolutionGraphValidatorTests`;
  `EggHatchingTests`' candidate set; `MinigameAssignmentTests`' sharer map; `DexModel.lineTitles`;
  `MinigameAssignment.byLine`; `elements.json`/`moves.json` `lineDefaults`; `EvolutionTreeTests`'
  `dmc-v1` layout counts; and every tree test of a line the sweep touched. NOT on the list this
  time: `RosterTests`' alias list and `SeedRosterTests`' `seedLines` — a sweep authors PLAIN roster
  ids only (that is the entire point of it), so no alias is created and no seed line changes.
- **The pre-flight checker earned its keep for the fourth story running.** Roster stage vs authored
  stage, sprite on disk, dexOnly, one-rung transitions, CROSS-LINE edges, symbol+tint uniqueness
  within a line, signature-name uniqueness against the whole file, and `testElementsVaryWithinALine`
  (>1 element per line — easy to trip with a 2-node line). It came back clean first try and the
  only failures the build found were in OTHER stories' tests, which is where a checker cannot look.
- REMAINS: US-145 onward (31/56 passing). US-145 is the other half of the Digitama, 23 eggs for 25
  still-orphaned Baby I — read the allocation pattern at the top of this file BEFORE choosing
  targets, because doubling up an egg there permanently strands a Baby I. Orphans by stage now:
  Ultimate 166, Adult 129, Perfect 114, Child 88, Baby II 37, Baby I 25, Digitama 23, Armor-Hybrid 16.
---

## 2026-07-22 - US-145 - Orphan sweep: Digitama L-Z

The second of Phase E's 26 sweeps, and the one that CLOSES the Digitama stage: 23 eggs wired, all
57 playable eggs now carry their single hatch edge. 35 nodes, 330 -> 365. Digitama orphans 23 -> 0;
Baby I 25 -> 13; whole-file 598 -> 563.

Files: `Resources/evolutions.json` (+35 nodes), `Resources/elements.json` and `Resources/moves.json`
(+35 entries and 2 `lineDefaults` each), `Sources/DexModel.swift` (2 line titles),
`Sources/MinigameAssignment.swift` (2 assignments), new `Tests/DigitamaSweepLToZTests.swift` (15
tests), `docs/evolutions-schema.md`, and NINE existing test files. Build green, 1951 tests, 0
failures, validator zero findings.

**Learnings for future iterations:**

- **THE SWEEP'S RULE HAD TO BE TIGHTENED, and the arithmetic is what tightened it.** US-144's rule
  was "an egg whose species is already a node joins that species' line and adds no rung". That is
  the wrong default once the eggs are nearly gone: `EggHatcher.hatchTarget` reads
  `evolutions.first`, so an egg has ONE hatch edge and a Baby I's only possible in-edge is an egg.
  Every egg spent on an already-reachable Baby I is a Baby I nobody can ever hatch. The rule that
  ships is: **an egg opens the Baby I Wikimon puts two rungs below its species, and doubles up only
  when it has no choice** — and "no choice" is one of five checked facts, not a preference:
  species already wired (4), species idle-only (3), species has NO sheet at all (1, Liollmon), the
  species' own Baby I is idle-only (1, Meicoomon), every candidate Baby I is already in the graph
  (2, Phascomon and Vorvomon). `testEveryDoubledUpEggSaysWhyItHadNoChoice` checks each reason
  against the roster and the graph, so a lazy doubling cannot hide behind a comment.
- **PROVE THE CEILING BEFORE APOLOGISING FOR IT.** Twelve looks short against 25 orphaned Baby I,
  so the story computes the maximum instead of asserting one: build the bipartite graph of "egg's
  species descends from this Baby I" off Wikimon and find the largest matching. Only 13 of the 25
  are on a thread reaching any of these 23 species, and Leafmon and Pururumon are BOTH reachable
  only through `worm_digitama` — so 12 is the most any authoring could have opened. That number is
  now a test (`testTheBabyIStillOrphanedCannotBeReachedByAnyRemainingEgg`, paired with "no unspent
  egg exists"), which is what turns "we ran out" into a handover US-146 is told about.
- **A BABY I WHOSE BABY II IS ALREADY ON THE LINE IS WORTH TWO THAT ARE NOT.** `pipimon` went to
  `palmon` and `tsubumon` to `dmc-v3` because Tanemon and Tokomon are already those lines' Baby II,
  so US-147 closes those dead ends with an EDGE and no new node — and no new line was needed for
  either. Check `Baby I -> Evolves To` against the nodes already in the graph before opening a line;
  it is the cheapest line-count saving in the phase.
- **TWO NEW LINES FOR TWELVE THREADS, and grouping is what bought that.** Four of the twelve are
  Tamers partners (Lopmon, Monodramon, Renamon, Terriermon) and joined US-144's `tamers`, which is
  now 15 nodes. Five more share a device: Wikimon's **Virtual Pets section is machine-readable**
  (`<span class="mw-headline" id="Vital_Bracelet_Digital_Monster">`), so "these five are Vital
  Bracelet Digimon" is a checked fact and not a vibe — that is what `vital` is. Grouping by a
  franchise or a device you can VERIFY beats naming a line after each species.
- **Wikimon parsing, refined from US-144's recipe.** `re.split(r'<span class="mw-headline" id="([^"]+)"', html)`
  gives id/body pairs for the WHOLE page including h3/h4 — US-144's `<h2>` split misses
  `Evolves_From`/`Evolves_To`, which sit under h3, and silently returns empty lists. Read names out
  of `title="..."` and drop anything containing "Card Game" (the Digimon Card Game colour tables
  inject Greymon/Scumon/Koromon into every Evolves From list and they are not evolutions).
  **Fetch the middle rung's page, not the top one**: 50 `curl`s on the Baby II gave the whole
  Baby I -> Baby II -> Child map in one pass, which is what the matching needed.
- **A "one egg per line" assertion can survive US-144 and still bite.** `PendulumWindGuardiansTree
  Tests` seeded its reachability BFS from `flora_digitama` alone; US-144 generalised the other five
  tree tests and missed that one, because no egg had landed on `penc-wg` yet. When a story adds an
  egg to a line, grep that line's test file for BOTH the egg list and the BFS seed.
- **A ledger belongs in the newest story that changed it.** The whole-file dead-end list moved from
  `DigitamaSweepTests` to `DigitamaSweepLToZTests` (8 -> 20 entries) and US-144's version was
  narrowed to "my eight are still dead ends" with a pointer. The next sweep looks at the newest
  sweep; a ledger left in an older file gets edited by nobody.
- **US-144's marker test worked exactly as advertised** — `testTheDigitamaOrphansLeftAreExactly
  TheLToZOnesForUS145` failed on the first run of this story, explained itself, and was rewritten
  into the claim it guarded. Keep leaving them.
- REMAINS: US-146 onward (33/56 passing). US-146 is Baby I, and it inherits a HARD fact: 13
  playable Baby I can never be given an in-edge, because every Digitama is spent. Its AC "every
  Digimon in the range gets at least one in-edge" therefore cannot be met literally either — say
  which reading is taken, the way US-144 and US-145 both did. `ketomon` and `bommon` are the two
  Baby I whose canonical Baby II is idle-only and which need a stand-in from US-147. Orphans by
  stage now: Ultimate 166, Adult 129, Perfect 114, Child 88, Baby II 37, Armor-Hybrid 16, Baby I 13.
---

## 2026-07-22 - US-146 (Orphan sweep: Baby I)

Wired the WHOLE Baby I rung. 38 new nodes and 33 new edges; `Resources/evolutions.json` 365 -> 403,
plus the matching rows in `elements.json`/`moves.json`, two new lines in `Sources/DexModel.swift`
and `Sources/MinigameAssignment.swift`, a new `Tests/DigitamaSweepBabyITests.swift` (16 tests), and
ELEVEN existing test files. Build green, 1967 tests, 0 failures, validator zero findings.

Orphan count for the stage: **Baby I 13 BEFORE, 0 AFTER.** Whole file 563 -> 525; Baby II fell
37 -> 12 as a side effect of authoring the rung above.

**Learnings for future iterations:**

- **THE STORY'S SCOPE WAS THE RUNG, NOT THE 13 NAMES.** The PRD's Appendix A lists 38 orphaned
  Baby I and US-144/US-145 de-orphaned 25 of them by giving them an in-edge from an egg — which
  left the OTHER half of the same AC ("and at least one out-edge") unmet on all 38. Reading the
  scope as "the 13 still orphaned today" would have shipped 20 Baby I that still lead nowhere and
  handed US-147 a rung it is not named for. When a sweep's range has been partly eaten by an
  earlier story, check WHICH criterion that story met before deciding what is left.
- **AN AC THAT CANNOT BE MET IS PINNED AS A LIST, NEVER RELAXED TO A SKIP.** 13 Baby I can never
  have a parent (all 57 eggs spent, one hatch edge each, and US-145 proved 12 was the ceiling), so
  `EvolutionCriteriaTests.testEveryNonEggNodeIsReachableFromSomeDigitama` — a GLOBAL zero since
  US-008 — became a 21-name list: the 13, plus the 8 Baby II only they feed. It still fails on a
  22nd, and a second assertion proves each stranded Baby II has no reachable parent, so the list
  cannot quietly absorb a Baby II nobody bothered to wire. Same shape in the two device-tree
  reachability tests that gained a stranded node (`penc-ds` one, `penc-vb` two).
- **THAT GLOBAL REACHABILITY TEST IS THE ONE TO GREP FOR BEFORE AUTHORING, and it is not where you
  would look.** `EvolutionCriteriaTests`, not `EvolutionGraphTests`. The six `Pendulum*TreeTests`
  and five `DMC*TreeTests` each have a per-line twin (`testEveryNodeInTheLineIsReachableFrom...`),
  so a node with no in-edge breaks the test of WHATEVER LINE it lands on. That constraint, not
  flavour, is what decided most of this story's line assignments: eleven of the thirteen went onto
  sweep lines (`tamers`, `wanyamon`, `vital`, `adventure02`, `algomon`, `commandramon`) or the two
  new ones, and only Puyomon and Pusumon had to touch a device line — because Pukamon and Herissmon
  are the only nodes on disk their threads can possibly reach.
- **A MISSING RUNG AT BABY II IS A REHOME ON THE RUNG ABOVE — fetch the Baby II's own page even
  though it is not on disk.** Wikimon has pages for Digimon the sprite pack lacks, so
  `curl wikimon.net/Gurimon` still yields "Evolves To: Koemon", and the stand-in is whichever Baby
  II ON DISK also evolves into Koemon (Wanyamon). Four of the thirteen went this way (Gurimon,
  Kyaromon-idle-only, Bosamon, Puyoyomon). One sweep of 37 `curl`s over every orphaned Baby II,
  parsed for `Evolves_From`/`Evolves_To`, answered every pairing question in the story AT ONCE —
  including which Baby I each Baby II wants — and US-147 can reuse the same dump.
- **Wikimon's `Evolves To` at Baby I can be a DigiXros list, not an evolution.** Bombmon's is three
  fusions (Mega Decker Greymon, Monochromon, Shoutmon X7) and no ordinary arrow at all, so it has
  no Baby II on any page. Its comment says FLAVOUR in capitals and a test greps for that word —
  the one honest way to author a pairing that has no citation. Do not let a fusion list read as an
  evolution because it is sitting under the right heading.
- **`graph.nodes(at: .babyI)` is 45 and the roster's playable Baby I is 43.** `piyo_yuramon` and
  `pencnsp_botamon` are line-scoped aliases — graph nodes with no roster entry of their own. Assert
  the SHAPE of a rung over graph nodes and its COVERAGE over roster entries; a test that iterates
  roster entries silently skips every alias.
- **The knock-on list for a rung this size, all eleven files, in the order they bit:** node count in
  `ElementCatalogTests` AND `EvolutionGraphValidatorTests`; line sizes in `EvolutionGraphTests`;
  `MinigameAssignmentTests`' game -> [lines] literal; `MoveCatalogTests` wants a UNIQUE
  symbol|tint per line AND a globally unique `signatureName` (three of mine collided with eggs
  authored five stories ago); `ElementCatalogTests.testElementsVaryWithinALine` forbids a
  single-element line, which a 2-node line trips instantly; and `EggHatchingTests`' two starting-egg
  lists move whenever a thread newly reaches an Ultimate.
- **FINISHING a thread promotes its egg, and the promotion is silent.** `lala_digitama` and
  `luce_digitama` became starting-egg candidates the moment Pipimon and Tsubumon got their Baby II,
  because Tanemon and Tokomon already reached an Ultimate. US-145 flagged those two as the cheap
  closes and it was right — they were the only two edges in the story that changed what a new game
  can start on.
- **A ledger belongs in the newest story that changed it, and this one MOVED UP A RUNG.** The
  whole-file dead-end list is now
  `DigitamaSweepBabyITests.testTheOnlyDeadEndsBelowUltimateAreTheBabyIIThisSweepOpened` — 25
  entries, all Baby II. US-144's and US-145's copies were flipped in place to "my Baby I are no
  longer dead ends", so wiring them back into leaves fails too.
- REMAINS: US-147 onward (33/56 passing). US-147 is Baby II and inherits a rung that is 25/37 done:
  every Baby II it needs to wire already has its in-edge, so its whole job is the out-edge to a
  Child plus the 12 Baby II still orphaned (`arkadimon_baby`, `budmon`, `chicchimon`, `cupimon`,
  `hiyarimon`, `kozenimon`, `meicoobaby`, `pokomon`, `sunmon`, `tokomon_x`, `torikaraballmon`,
  `upamon`). Eight of the 25 it wires onward are on the stranded list, so
  their Children will be unreachable too; that is expected and must NOT be authored away by
  inventing an in-edge. Orphans by stage now: Ultimate 166, Adult 129, Perfect 114, Child 88,
  Armor-Hybrid 16, Baby II 12, Baby I 0.
---

## 2026-07-22 - US-147 (Orphan sweep: Baby II)

Wired the WHOLE Baby II rung. 51 new nodes (the 12 Baby II nobody had wired + the 39 Children the
rung now evolves into), 12 new branch edges at Baby I, 38 Baby II out-edges; `Resources/evolutions.json`
403 -> 454, plus the matching rows in `elements.json`/`moves.json`, a new `Tests/BabyIISweepTests.swift`
(19 tests) and TEN existing test files. NO new lines. Build green, 1986 tests, 0 failures, validator
zero findings.

Orphan count for the stage: **Baby II 12 BEFORE, 0 AFTER.** Whole file 525 -> 475; Child fell
88 -> 50 as a side effect of authoring the rung above.

**Learnings for future iterations:**

- **THE ONLY WAY TO GIVE A BABY II AN IN-EDGE IS TO BRANCH A BABY I, and that spends an invariant
  three stories old.** Every Baby I had exactly ONE edge from US-008 to US-146, and US-146 pinned
  that as a claim. Twelve Baby II had no parent and no Digitama is left to open a thread, so nine
  Baby I gained a second edge (Puttimon, Kuramon and Piyo's Yuramon a third). US-146's
  `testBabyIEdgesAreTheDefaultAndUnconditional` was rewritten into
  `testEveryBabyIsFallbackEdgeIsUnconditional` and the "exactly one edge" half MOVED to
  `BabyIISweepTests.testEveryBabyIThatBranchesDoesSoForAnOrphan`, which names the nine and fails if
  a tenth branch is authored for convenience. **The Child sweeps face the same shape one rung up** —
  a Child's only possible parent is a Baby II — so expect to branch a Baby II and to edit that test.
- **AN IN-EDGE FORCES THE LINE, and that is what decides where a sweep node lives — not flavour.**
  `testNoEdgeCrossesALine` means a Baby II sits on its parent's line and a Child on its Baby II's.
  Cupimon, Tokomon X and Upamon went onto `dmc-v3` because Puttimon and Tsubumon are the only
  parents Wikimon gives them, and Hiyarimon onto `penc-vb` for the same reason. **Touching a device
  line is the US-146 precedent and it costs four test edits every time:** the line's node count in
  `EvolutionGraphTests`, the `inLine.count` in that tree's
  `testEveryNodeInTheLineIsReachableFromItsDigitama`, and the `sweepEggs` exclusion set plus the
  `mine.count`/`plain.count` totals in its "N of the M nodes carry a plain roster id" test. That
  exclusion-by-name set is the mechanism those files already provide for exactly this — add the new
  ids to it rather than bumping the totals, or the story's own claim stops being the claim.
- **A SWEEP THAT OPENS A RUNG AS LEAVES BREAKS "EVERY CHILD HAS A JUNK FALLBACK".** Two device tree
  files (`PendulumMetalEmpireTreeTests`, `PendulumVirusBustersTreeTests`) run
  `XCTUnwrap(node.evolutions.first(where: \.isDefault))` over every Child on their line, and a
  brand-new leaf Child has no edges at all. Add `&& !node.evolutions.isEmpty` to the `where`. The
  Child sweeps will hit the mirror image at Adult.
- **AN EGG'S PROMISE IS A TEST, AND IT IS THE ONE THAT CAUGHT A REAL GAP.** US-144/145 hung 57 eggs
  on threads; a thread that never arrives at the egg's own species is a promise the file does not
  keep. `testTheEggsWhoseSpeciesThisRungReachesNowArriveAtIt` BFSes from twenty eggs to the Child
  they name and failed on `lala_digitama`, because Lalamon had been hung off Budmon and the egg
  opens Pipimon -> Tanemon. The fix was a second, cited parent (Tanemon -> Lalamon), not a smaller
  list. Run that BFS BEFORE choosing which parent gets a Child; the egg allocation is the constraint.
- **THE LINE-SCOPED ALIAS IS THE ANSWER WHEN A CITED TARGET BELONGS TO ANOTHER LINE.** Monimon's
  only Wikimon Child is Hagurumon, which is already `penc-me`'s, and `line` is single-valued — so
  `xros_hagurumon` is a second node on the same art, the `pencnsp_koromon`/`dmcv4_palmon` pattern.
  Cost: one entry in `RosterTests`' alias list (it asserts the EXACT set of graph ids with no roster
  entry) and a note that `Roster.bundled.entry(id:)` returns nil for it. The first attempt used
  Solarmon as a rehome instead and had to be undone: Wikimon's Virtual Pets section proved Solarmon
  is on no Xros device at all, which would have made the line's name a lie. **Check the device
  before the arrow.**
- **`Roster.bundled` (Resources/roster.json, sprite-derived) AND `roster.generated.json` ARE
  DIFFERENT SETS.** The generated file mirrors graph ids, so an alias appears in it; roster.json
  does not. Appendix B's orphan count reads the generated one; a test asserting a rung's roster
  count must use the bundled one (Baby II: 49 graph nodes, 47 roster entries).
- **Wikimon's `Evolves From` is EMPTY for more Digimon than you expect, and that is the honest
  FLAVOUR case.** Chicchimon, Kozenimon, Arkadimon (Baby) and Torikara Ballmon have no parent
  anywhere; Torikara Ballmon has neither direction. Each says FLAVOUR in capitals and a test greps
  for the word. What made the pairings defensible was a checkable fact from the page itself rather
  than a resemblance: Chicchimon's own `Evolves To` are already Children of the bird line, and
  Torikara Ballmon's Reference Book profile says it FLIES.
- **The knock-on list for this rung, all ten files:** node count in `ElementCatalogTests` AND
  `EvolutionGraphValidatorTests`; line sizes in `EvolutionGraphTests` AND `DigitamaSweepTests`
  (the older sweep pins `tamers`/`wanyamon` because that is where those lines were opened); the
  stranded list in `EvolutionCriteriaTests`; three of US-146's claims in `DigitamaSweepBabyITests`;
  the three Pendulum tree files above; and `graph.parents(of:) == [x]` assertions in
  `DMCVersion4TreeTests` and `SeedRosterTests`, which become `.contains(x)` the moment a node gains
  a second parent. `MinigameAssignmentTests` and `EggHatchingTests` did NOT move: no new line, and
  no thread newly reached an Ultimate.
- REMAINS: US-148 onward (34/56 passing). US-148..150 are the Child rung, and it is 39/89 done:
  every Child this story opened has its in-edge and needs an Adult, and the 49 still orphaned need
  both. Nine of the 39 are on the stranded list, so their Adults will be unreachable too; that is
  expected and must NOT be authored away by inventing an in-edge. The dead-end ledger is now
  `BabyIISweepTests.testTheOnlyDeadEndsBelowUltimateAreTheChildrenThisSweepOpened`, 39 entries, and
  the Child sweeps are meant to edit it down. Orphans by stage now: Ultimate 166, Adult 129,
  Perfect 114, Child 50, Armor-Hybrid 16, Baby II 0.
---

## 2026-07-22 - US-148 (Orphan sweep: Child A-F)

Wired the A-F third of the Child rung. 43 new nodes — the 14 orphaned Children A-F, the 23 earned
Champions above the whole A-F range, and 6 junk Champions for the lines that had no Champion rung at
all — plus 14 new branch edges across 9 Baby II, and out-edges on the 9 Children US-147 had opened
as A-F leaves. `Resources/evolutions.json` 454 -> 497, the matching rows in `elements.json` /
`moves.json`, a new `Tests/ChildSweepAToFTests.swift` (22 tests) and NINE existing test files. NO
new lines, and NO aliases — new nodes and orphans removed are the same number for the first time
since US-135. Build green, 2008 tests, 0 failures, validator zero findings.

Orphan count for the stage: **Child 50 BEFORE, 36 AFTER — Child A-F 14 BEFORE, 0 AFTER.**
Whole file 475 -> 432; Adult fell 129 -> 100 as a side effect of authoring the rung above.

**Learnings for future iterations:**

- **A CHILD SWEEP IS THREE RUNGS OF WORK, NOT ONE, AND THE THIRD IS THE ONE NOBODY BUDGETS FOR.**
  Giving a Child an in-edge branches a Baby II (expected, US-147 said so). Giving it an out-edge
  costs a Champion — but `EvolutionCriteriaTests.testEveryNonTerminalChildAndAdultHasTwoToFiveOut
  goingEdges` means TWO out-edges, and `...FallsToAJunkEvolution` means one of them lands on a junk
  Champion of the Child's OWN line. Six lines (`algomon`, `commandramon`, `diablomon`, `penc-sw`,
  `tamers`, `wanyamon`) were Digitama-to-Child threads with no Champion rung at all, so each needed
  a junk floor invented before ANY of its Children could branch. That is 6 of the 29 Champions, and
  it is a fixed cost US-149 and US-150 will NOT pay again — every line those two touch now has one,
  except whatever they open. Check `graph.nodes.filter { $0.line == L && $0.stage == .adult }`
  before estimating.
- **JUNK CHAMPIONS CAN COME OUT OF THE ORPHAN POOL INSTEAD OF BEING ALIASES.** The obvious move was
  six line-scoped aliases (`pencme_raremon` precedent), which cost a `RosterTests` alias-list entry
  each and remove NO orphan. The roster still had six junk-flavoured Adults nobody had wired:
  Numemon X, Manekimon, Mimicmon, Troopmon, Damemon, Tsuchidarumon. Using them removed six orphans
  instead of none. `junkIds` is a TEST's list, not a validator rule — US-138/139/140/143 already set
  the precedent of picking a thematically-junk unused sheet — so the only real constraint is that
  the pick reads as junk. Search the orphan pool by flavour before reaching for an alias.
- **THE FOUR ENERGIES ARE A HARD CEILING OF FOUR BRANCHES PER BABY II, and it is what decides where
  a variant lives.** `BabyIISweepTests.testEachBabyIIsBranchesAskForADifferentEnergy` plus
  `EvolutionEngine`'s `dominant == requiredEnergy` filter means a Baby II can carry at most four
  edges, ever. Koromon and Pyocomon were both already full, which is why Agumon 2006 could not go on
  Agumon's own line and Elecmon Violet could not go on its only cited parent's. Run
  `{vitality,strength,spirit,stamina} - used` over every candidate parent BEFORE promising the AC's
  "variants hang off their base form's line" — the honest version is "with its base form OR with its
  variant thread", and `tokomon_x` (US-147's X-Antibody In-Training) took all three X-Antibody
  Children for exactly that reason.
- **A CHILD'S JUNK EDGE SHARES ITS EARNED EDGE'S ENERGY and sits below it on `minEnergy`.** That is
  the Agumon/Numemon shape from US-061 and it is REQUIRED reading: the distinct-energy rule is
  scoped to EARNED edges (`SeedRosterTests`, `BabyIISweepTests`), so a two-edge Child does not need
  two energies, and giving the junk edge its own energy would make it unreachable whenever the
  player's dominant type is the other one.
- **`SeedRosterTests.patamonLineVerifiedSet` IS A WHOLE-LINE CHECK ON `dmc-v3`, and any sweep that
  puts a Champion there trips it.** It reads "every Adult-or-later node on dmc-v3 is in the US-043
  verified set" — a list of DISPLAY NAMES, not ids. Four of US-148's Champions landed there because
  Tokomon X and Upamon are dmc-v3's. Add to the set with a note, the way KingEtemon was added in
  US-061; do not scope the check down.
- **`EvolutionTreeLayoutTests.testTheBundledVersionOneLineIsABranchingTree` pins the dmc-v1 LAYOUT,
  not just its size** — node count, the Adult column's id list IN ORDER, `rowCount` and the
  connector count. Four earlier stories added only Digitama and could leave the branch shape alone;
  the first story to hang a new Child off Koromon moved all four numbers. Anything that widens a
  device line's Adult column has to update it.
- **THE ADULT MIRROR OF US-147's CHILD GUARD LANDED EXACTLY WHERE US-147 PREDICTED.**
  `PendulumNightmareSoldiersTreeTests` runs "every Champion on this line falls to Darumamon" over
  `$0.stage == .adult`, and a brand-new leaf Champion has no edges at all; it needs
  `&& !$0.evolutions.isEmpty`. The Adult sweeps (US-151..153) will hit the Perfect version of this.
- **THE STRANDED LIST GREW BY TWO, AND ONLY TWO, BECAUSE `penc-sw` HAS NO DIGITAMA.** Fujamon was
  already stranded, so the two Champions hung over it are too. That is inherited arithmetic, not an
  omission — but it is worth checking BEFORE choosing a parent: `funbeemon`'s cited parent Puroromon
  is also stranded, and moving it to Tanemon (equally cited) kept a whole thread reachable for free.
  Look up `EvolutionCriteriaTests`' stranded lists while picking parents, not afterwards.
- **The knock-on list for this rung, all nine files:** node count in `ElementCatalogTests` AND
  `EvolutionGraphValidatorTests` (454 -> 497); the per-line dict in `EvolutionGraphTests`; line
  sizes in `DigitamaSweepTests` AND `BabyIISweepTests`; `junkIds` and the stranded lists in
  `EvolutionCriteriaTests`; the dmc-v1 layout in `EvolutionTreeTests`; the dmc-v3 verified set in
  `SeedRosterTests`; and three places in `PendulumNightmareSoldiersTreeTests`. Plus the
  `greymon_x` example in BOTH `MoveCatalogTests` and `ElementCatalogTests`, which US-138's own
  learning had predicted: **a test that needs "something OUTSIDE the graph" must DERIVE it.** Both
  are now `roster.entries.first { catalog[...] == nil && graph.node(id:) == nil }`.
  `MinigameAssignmentTests` and `EggHatchingTests` did NOT move: no new line, and no thread newly
  reached an Ultimate (every new Champion is a leaf).
- **The authoring script pattern is worth repeating.** `/tmp/us148/author.py` held the whole story
  as data — parents, energies, conditions, comments, elements, moves — and REFUSED TO WRITE until
  it had checked, in one pass: no id already a node, no edge crossing a line, no Baby II branching
  twice on one energy, no repeated symbol|tint within a line, no repeated `signatureName` globally,
  and no digit in any hint. It caught three duplicate signature names before a single byte hit the
  repo. Splice `evolutions.json` textually (mixed encoding, see US-136), and note that the nine
  US-147 leaves are matched on their literal `"evolutions": []` line, which makes the replacement
  a one-line regex per node.
- REMAINS: US-149 onward (35/56 passing). US-149 (Child G-L) and US-150 (Child M-Z) face the same
  three-rung shape; the dead-end ledger is now
  `ChildSweepAToFTests.testTheOnlyDeadEndsBelowUltimateAreTheOnesTheSweepsHaveOpened`, 59 entries —
  30 Children for US-149/US-150 and 29 Champions for US-151..US-153 — and both halves are meant to
  be edited down. Orphans by stage now: Ultimate 166, Perfect 114, Adult 100, Child 36,
  Armor-Hybrid 16.
---

## 2026-07-22 - US-149 (Orphan sweep: Child G-L)

Wired the G-L third of the Child rung. 51 new nodes — the 18 orphaned Children G-L, 31 earned
Champions and 2 junk Champions (`xros` and `vital` were the last two lines with no Adult rung at
all) — plus 18 new branch edges across 15 Baby II, and out-edges on the 14 Children US-147 had
opened as G-L leaves. `Resources/evolutions.json` 497 -> 548, the matching rows in `elements.json` /
`moves.json`, a new `Tests/ChildSweepGToLTests.swift` (23 tests) and TWELVE existing test files.
NO new lines, and NO aliases, so new nodes and orphans removed are the same number.
Build green, 2031 tests, 0 failures, validator zero findings; the 23 new tests also observed passing
on their own.

Orphan count for the stage: **Child 36 BEFORE, 18 AFTER — Child G-L 18 BEFORE, 0 AFTER.**
Whole file 432 -> 381; Adult fell 100 -> 67 as a side effect of authoring the rung above.

**Learnings for future iterations:**

- **`roster.generated.json` CONTAINS THE LINE-SCOPED ALIASES AND `Roster.bundled.entries` DOES NOT,
  so a range counted off the generated file is wrong by exactly the aliases in it.** Scoping this
  story off `roster.generated.json` gave 45 G-L Children; the test, which reads
  `Roster.bundled.entries`, gave 43 — `pencvb_gabumon` and `xros_hagurumon` are nodes on borrowed
  art with no roster entry. One of them (`xros_hagurumon`) was IN scope and had to be swept, so
  `sweptChildren` is deliberately not a subset of the range and there is a separate test saying so.
  Use the generated file to count ORPHANS (it mirrors the graph, which is what the Appendix B rule
  needs) and `Roster.bundled` to define a RANGE.
- **A DEVICE TREE PINS ITS OWN In-Training's BRANCH LIST AND EDGE COUNT, and a sweep that hangs one
  more Child off that Baby II breaks a test with nothing to do with counts or lines.** Two fired
  here: `SeedRosterTests.testPagumonBranchesToBothRookiesAndEachReachesUltimate` spells Pagumon's
  targets out as a sorted list, and `PendulumNightmareSoldiersTreeTests.testTheInTrainingRungIs
  UnconditionedBecauseTheUnlockableRookieHasNoSheet` asserts Peti Meramon has exactly three edges
  and that NONE is conditioned — which a sweep's earned branch necessarily is. The fix for the
  second is to scope the claim to the three Rookies the TREE draws rather than to the rung; the
  tree still conditions nothing. `grep -rn '<parent-id>' Tests` before choosing a device-tree
  Baby II as a parent, not after.
- **THE "EVERY CHAMPION OF THIS LINE FALLS TO THE JUNK PERFECT" LOOP NEEDS `&& !$0.evolutions.isEmpty`
  IN EVERY DEVICE-TREE FILE A SWEEP TOUCHES.** US-148 fixed Nightmare Soldiers; US-149 had to fix
  Virus Busters and Metal Empire the same way, because a sweep always leaves the rung above as
  leaves. Wind Guardians and Deep Savers still have the unguarded version and will fail the moment
  a sweep puts an Adult on them — which US-150 will, for `penc-ds`.
- **A HISTORICAL CLAIM STOPS BEING CHECKABLE THE MOMENT A LATER STORY WRITES TO THE SAME FILE — say
  so and keep the half that survives.** `ChildSweepAToFTests.testEachLineThatHadNoChampionRungGained
  AJunkFloor` asserted every Adult on its six lines was one US-148 authored, which WAS how "the line
  had nothing before" got checked from the file. US-149 put sixteen more Champions on `tamers`
  alone, so the assertion is now `authoredAdults.contains(junk)` — the junk floor is still one that
  story authored — and the "there was nothing here before" half moved into the comment and the
  notes. Do not delete such a check silently; replace it with what is still true and record why.
- **CHECK THE STRANDED LISTS BEFORE PICKING A PARENT — one substitution bought two reachable nodes
  for nothing here.** Gammamon's only free citable parent looked like Pusurimon, which is in
  `EvolutionCriteriaTests`' stranded set; Hiyarimon is on the SAME line (penc-vb), equally free, and
  not stranded, so Gammamon and BetelGammamon are reachable rather than being two more names on the
  list. Gaossmon could not be saved the same way — all three `xros` Baby II are stranded, because
  that line has no Digitama and US-144/145 spent all 57 eggs — so the list grew by exactly one Child
  and six Champions, every one of them hanging off something already on it.
- **`adventure02` IS THE LAST LINE WITHOUT A CHAMPION RUNG, AND IT IS US-150's.** Its three Children
  — V-mon, Wormmon, Tinkermon — are all M-Z, so US-150 must invent a junk Adult for it before any of
  them can branch, exactly as US-148 did for six lines and US-149 for `xros` and `vital`.
  `ChildSweepGToLTests.testTheLastTwoLinesWithoutAChampionRungGainedAJunkFloor` states it as a
  one-element list that US-150 either empties or fails.
- **THE JUNK-FALLBACK RULES ARE SCOPED TO Child AND Adult, NOT TO EVERY BRANCHING NODE.** The
  authoring script's first run rejected 61 shipped Perfects for having a gated `isDefault` edge —
  which is correct data: `EvolutionCriteriaTests.branchingNodes` is `child || adult`, and the
  Perfect rung's fallbacks legitimately carry criteria. A pre-write check copied from the test has
  to copy its scope too.
- **THE FOUR-ENERGY CEILING IS WHAT DECIDES WHERE AN X-ANTIBODY CHILD LIVES, and eight of them
  landed in one story.** Six sat on their base form's own In-Training. Gabumon X and Gomamon X
  could not — Tokomon X is full since US-148 and Pukamon is the ONLY In-Training the whole `penc-ds`
  line has — so both joined the X-Antibody thread on dmc-v3, the escape US-148 used for Dracomon X.
  Kokuwamon X is the case worth remembering: Caprimon is full too, but `penc-me` has a SECOND
  In-Training (Kozenimon), so the variant kept its base form's LINE without keeping its parent.
  "Hangs off its base form's line" is satisfiable far more often than "hangs off its base form's
  parent" — check the line for another Baby II before rehoming.
- **The knock-on list for this rung, all twelve files:** node count in `ElementCatalogTests` AND
  `EvolutionGraphValidatorTests` (497 -> 548); the per-line dict in `EvolutionGraphTests`; line
  sizes in `DigitamaSweepTests`, `BabyIISweepTests` AND `ChildSweepAToFTests`; `junkIds` and BOTH
  stranded lists in `EvolutionCriteriaTests`; the dmc-v3 and dmc-v5 verified sets in
  `SeedRosterTests` plus its Pagumon branch list; the earned-branch count and the wired-onward
  ledger in `BabyIISweepTests`; `inLine.count` and the story-scope `sweepEggs` set in
  `PendulumNightmareSoldiersTreeTests`, `PendulumVirusBustersTreeTests` and
  `PendulumMetalEmpireTreeTests`; and the line count in `DMCVersion5TreeTests`, excluded by NAME
  rather than bumped so "US-137 added nothing" stays the claim it was.
  `MinigameAssignmentTests`, `EggHatchingTests`, `DexModel.lineTitles` and the two `lineDefaults`
  did NOT move: no new line, and no thread newly reached an Ultimate.
- **The authoring-script pattern earned its keep again.** `/tmp/us149/author.py` + `data.py` held
  the whole story as data and refused to write until it had checked, in one pass: every id is a
  playable roster entry at the right rung, no id is already a node, no edge crosses a line or skips
  a rung, no edge points at a dexOnly Digimon, no Baby II branches twice on one energy, no Child or
  Adult has fewer than two or more than five out-edges or more than one fallback, no fallback is
  gated, no hint is blank or contains a digit, no repeated symbol|tint within a line, and no
  repeated `signatureName` globally. It caught a real collision before a byte hit the repo —
  Greymon 2010's "Mega Burst" was already GeoGreymon's.
- REMAINS: US-150 onward (36/56 passing). US-150 (Child M-Z) faces sixteen leaves and eighteen
  orphans, plus `adventure02`'s junk floor. The dead-end ledger is still
  `ChildSweepAToFTests.testTheOnlyDeadEndsBelowUltimateAreTheOnesTheSweepsHaveOpened`, now 78
  entries — 16 Children for US-150 and 62 Champions for US-151..US-153 — and both halves are meant
  to be edited down. Orphans by stage now: Ultimate 166, Perfect 114, Adult 67, Child 18,
  Armor-Hybrid 16.
---

## 2026-07-22 - US-150 - Orphan sweep: Child M-Z (the rung is finished)

Wired the M-Z third of the Child rung. 51 new nodes: the 18 orphaned Children M-Z and the 33
Champions above them. 18 new branch edges across 14 Baby II, and out-edges on the 16 Children
US-147 opened as M-Z leaves. `evolutions.json` 548 -> 599.

- Files: `Resources/evolutions.json`, `Resources/elements.json`, `Resources/moves.json`, new
  `Tests/ChildSweepMToZTests.swift` (26 tests), and 17 existing test files for counts and for
  three historical claims that stopped being checkable.
- Build green. 2057 tests, 0 failures. Validator zero findings over the whole file. The 26 new
  tests were also observed passing on their own.
- ORPHAN COUNT (Appendix B, over a regenerated `roster.generated.json`): whole file 381 -> 330.
  Child 18 BEFORE, 0 AFTER. Adult fell 67 -> 34 as a side effect of authoring the rung above.
  Remaining: Ultimate 166, Perfect 114, Adult 34, Armor-Hybrid 16.

**Learnings for future iterations:**

- **`care.battleCount` is answerable ONLY over the `lifetime` window, and every other `care.*`
  counter ONLY over `stage`.** `ConditionContext.careValue` returns `.unknown` for
  `(.careBattleCount, .stage)`, so four edges first authored with `"window": "stage"` on a battle
  criterion were UNREACHABLE rather than merely hard. It was caught by
  `BabyIISweepTests.testEveryBabyIIBranchIsTakenByTheEngineWhenItIsEarned` — the engine test, not
  the validator, which has no opinion about windows. `ChildSweepMToZTests` now pins the rule over
  the whole file.
- **An egg's promise is worth re-running when a sweep WIRES a species an earlier egg doubled up
  for.** US-145 parked `meicoo_digitama`, `phasco_digitama` and `vorvo_digitama` on someone else's
  Baby I because their own species had no node; this story gave all three a node, and choosing
  Phascomon's parent by that BFS (Kozenimon on `penc-me`, its cited Caprimon's line) rather than by
  flavour (Sunmon on `penc-nso`) made all three eggs arrive at their species. The cost was moving
  Solarmon onto its other cited parent, Gigimon, on `tamers`. `DigitamaSweepLToZTests`' two "the
  species is still unwired" guards were replaced by the arrival check they were deferring.
- **A device tree's In-Training claim is the thing a Child sweep breaks, and scoping beats
  relaxing.** `PendulumNatureSpiritsTreeTests.testTheUnlockableSixthSlotIsTheOnlyEarnedInTrainingEdge`
  read "Koromon's ONLY conditioned edge is Angoramon"; hanging YukiAgumon there falsifies it. It is
  now scoped to the three Rookies the SECTION draws, plus an assertion naming the one extra target
  — so a fourth Rookie hung there silently still fails.
- **Three historical claims were replaced rather than bumped, and each says why in its comment.**
  US-149's "every Adult on `vital`/`xros` is one US-149 authored" (ten more landed there; the junk
  FLOOR half survives); the Nature Spirits one above; and `DexScreenTests`' "the roster dwarfs the
  graph" — 599 nodes against 1,025 entries is past half, so the floor is now the 157 idle-only
  Digimon, which can NEVER gain a node because `edgeToDexOnlyNode` forbids it. That last one had
  already been rewritten once from `> 800`; a claim Phase E keeps eroding needs a floor Phase E
  cannot reach.
- **The variant rule again, and the better of the two escapes.** Eight of nine variants sat on
  their base form's line and three on its own In-Training. Shakomon X could not — `penc-ds` has
  exactly ONE In-Training and Pukamon carries all four energies, the trap US-149's Gomamon X hit —
  but instead of a flavour rehome it followed its OTHER Wikimon-cited parent, Chocomon, onto
  `tamers`. Prefer a second citation over a rehome: it is checkable.
- **The knock-on list for this rung, all seventeen files:** node count in `ElementCatalogTests`,
  `EvolutionGraphValidatorTests`, `ChildSweepAToFTests` and `ChildSweepGToLTests` (548 -> 599);
  the per-line dict in `EvolutionGraphTests`; line sizes in `DigitamaSweepTests`,
  `BabyIISweepTests`, `ChildSweepAToFTests` and `ChildSweepGToLTests`; `junkIds` and BOTH stranded
  lists in `EvolutionCriteriaTests`; the dmc-v3 verified set in `SeedRosterTests`; the
  earned-branch count and the wired-onward ledger in `BabyIISweepTests`; the dead-end ledger in
  `ChildSweepAToFTests`; `inLine.count` and the `sweepEggs` set in `PendulumNightmareSoldiersTree
  Tests`, `PendulumVirusBustersTreeTests`, `PendulumMetalEmpireTreeTests` and
  `PendulumNatureSpiritsTreeTests`; the doubled-up-egg reasons in `DigitamaSweepLToZTests`; and the
  roster/graph proportion in `DexScreenTests`. `MinigameAssignmentTests`, `EggHatchingTests`,
  `DexModel.lineTitles` and the two `lineDefaults` did NOT move: no new line, and no thread newly
  reached an Ultimate.
- REMAINS: US-151 onward (37/56 passing). The Adult rung is next and it is the same shape one rung
  up: 34 orphaned Adults over seven stories, and 95 dead ends in
  `ChildSweepAToFTests.testTheOnlyDeadEndsBelowUltimateAreTheOnesTheSweepsHaveOpened` — the Child
  half is now EMPTY and the Adult half is 95 Champions for US-151..US-156 to wire onward. Note the
  arithmetic the Adult sweeps face: the Perfect rung has 114 orphans and only 95 Adults to hang
  them off, so doubling up starts there.
---

## 2026-07-22 - US-151 - Orphan sweep: Adult A-D
Wired the seven playable Champions whose display name begins A-D that had NO edge at all:
Akatorimon, BlackGaogamon, BlackTailmon, Burgermon Mama, Burgermon Papa, DarkLizamon, Deckerdramon.
Each gained one earned in-edge from a Child of its own line and one earned out-edge to a Perfect of
that line, plus the line's unconditioned junk fallback. Orphans 330 -> 319 (Appendix B over a
regenerated `roster.generated.json`): Adult 34 -> 27, Perfect 114 -> 110. 599 -> 610 nodes.

Files: `Resources/evolutions.json` (7 new Champions, 4 new Perfects, 7 new Child branches),
`Resources/elements.json` and `Resources/moves.json` (11 entries each), new
`Tests/AdultSweepAToDTests.swift` (18 tests), and the knock-on edits listed below.

**Learnings for future iterations:**

- **The Adult rung's scope reading is NOT the Child rung's, and the criteria say so.** US-148..150
  read "in-edge AND out-edge on every Digimon in range" as the rung-and-range and swept dead ends
  too. US-151's criteria say "every remaining ORPHAN at stage Adult whose displayName starts A-D",
  and the 95 leaves US-149/US-150 left are not orphans - they have an in-edge. So this story owns
  only the seven with no edge at all, and the 95 stay in
  `ChildSweepAToFTests.testTheOnlyDeadEndsBelowUltimateAreTheOnesTheSweepsHaveOpened` for
  US-152..US-156. Doing it the other way would have meant 95 new Perfects in one iteration.

- **THE PERFECT RUNG IS WHERE THE FILE RUNS OUT, and it is the Adult sweeps' version of the junk
  floor problem US-148/US-149 hit one rung down.** An Adult's out-edge must land on a Perfect of
  its OWN line (`testNoEdgeCrossesALine`, plus a per-line boundary test in all eleven device-tree
  files), and `EvolutionCriteriaTests` demands a junk fallback drawn from `junkIds`. Nine lines
  still have no Perfect at all: `wanyamon` and `tamers` were opened here (BlackMachGaogamon +
  Karakurumon, MegaloGrowmon + CatchMamemon), leaving `xros`, `vital`, `adventure02`, `algomon`,
  `commandramon`, `diablomon` and `penc-sw` for the sweeps that need them. Budget TWO nodes per
  such line: the earned branch and the junk floor under it, and add the floor to `junkIds`.

- **`tamers` is the line to open early and this story did.** It is the largest in the file (71
  nodes) and holds 22 more leaf Champions; every one of them will fall to CatchMamemon.

- **A `parent` for an Adult is almost always a Child an earlier sweep pinned at "exactly two
  edges".** `ChildSweepGToLTests` and `ChildSweepMToZTests` both assert
  `node.evolutions.count == 2` over their swept lists, and Gaomon and Monodramon are on them. The
  fix is the scoping the Codebase Patterns already prescribe: a named exception dictionary
  (`["gaomon": 3]`) rather than dropping the equality, so a Child that grows a third branch nobody
  wrote down still fails.

- **Two of a device tree's Perfects are pinned by an EXACT parent set, not a superset.**
  `PendulumMetalEmpireTreeTests.testAndromonIsTheOneNodeThatJoinsTwoOfTheDocumentsThreads` and
  `PendulumVirusBustersTreeTests.testHolyAngemonCarriesBothOfTheDocumentsDrawingsOfIt` both do
  `XCTAssertEqual(Set(graph.parents(of:)))`. Hanging a swept Champion onto either one falsifies
  them; both were rescoped by NAMING the third parent and saying which arrow it is, since the claim
  each makes is about the document's two drawings.

- **`Roster.bundled` is `Resources/roster.json` (1,022 entries) and is NOT
  `roster.generated.json` (1,025).** The generated mirror carries three line-scoped aliases the
  shipped file does not. Count a story's range off the SHIPPED roster when the number goes into a
  test - Appendix B's orphan count is the generated one, and the two disagree (46 vs 41 playable
  Adults A-D).

- **The blanket "did everything right" `met` context cannot take an `atMost` edge.** Two edges here
  are gated on FEW training sessions and SHORT nights, and the shared fixture every sweep copies
  sets both high. `AdultSweepAToDTests.context(for:)` derives the context from the edge's own
  conditions instead - copy that helper rather than loosening a criterion to fit the fixture.

- **Wikimon wikitext beats the rendered page.** `curl -s "https://wikimon.net/index.php?title=<Page>
  &action=raw"` gives the exact `==Evolves From==` / `==Evolves To==` bullet lists; a summarised
  fetch of the HTML drops and invents entries. Redirects show up as `#REDIRECT [[Real Page]]`
  (DarkLizamon -> Dark Lizamon, BlackGaogamon -> Black Gaogamon).

- **A rung can be unwireable rather than merely missing, and `dexOnly` is why.** All three of
  Burgamon Adult's Wikimon parents are unusable - Ebi Burgamon has no sheet, Tyumon and Burgamon
  are idle-only, and `edgeToDexOnlyNode` forbids an edge to an idle-only Digimon. The rehome went
  to `dmc-v4` because the FILE already holds that family: Torikara Ballmon (whose only canon
  relatives anywhere are the two Burgamon Adult) is a Baby II of that line and Digitamamon, a cited
  Evolves To, is its Perfect. When the parent rung cannot exist, argue from the line the rest of
  the family is already on.

- **A class-level citation is a real citation and is checkable.** Deckerdramon's whole Evolves From
  on Wikimon is "Any Blue Lv.3 Digimon from the Digimon Card Game", and its Evolves To gives "Any
  Digimon that also Evolves from Cyborg Lv.4" - Andromon is reached here from Guardromon, this
  line's Cyborg Champion, so the out-edge satisfies the clause literally and the test asserts that
  parent edge exists. Say in the `comment` which half is cited and which is flavour.

- **The knock-on list for this rung, all fifteen files:** node count in `ElementCatalogTests`,
  `EvolutionGraphValidatorTests`, `ChildSweepAToFTests`, `ChildSweepGToLTests` and
  `ChildSweepMToZTests` (599 -> 610); the per-line dict in `EvolutionGraphTests`; line sizes in
  `DigitamaSweepTests`, `BabyIISweepTests`, `ChildSweepAToFTests` and `ChildSweepMToZTests`;
  `junkIds` in `EvolutionCriteriaTests`; the dead-end ledger in `ChildSweepAToFTests` (95 -> 99);
  the "exactly two edges" claim in `ChildSweepGToLTests` and `ChildSweepMToZTests`; Muchomon's
  exact targets in `DMCVersion4TreeTests`; `inLine.count` in `PendulumWindGuardiansTreeTests`,
  `PendulumVirusBustersTreeTests` and `PendulumMetalEmpireTreeTests` plus the `sweepEggs` set in the
  latter two and the line-node count in the first; the exact parent sets in
  `PendulumMetalEmpireTreeTests` and `PendulumVirusBustersTreeTests`; and BOTH the verified set and
  the Champion split in `SeedRosterTests`. `MinigameAssignmentTests`, `EggHatchingTests`,
  `DexScreenTests`, `DexModel.lineTitles` and the two `lineDefaults` did NOT move: no new line, and
  the DexScreen floor is now the 157 idle-only Digimon, which Phase E cannot reach.

- REMAINS: US-152 onward (38/56 passing). Six Adult sweeps are left and they hold the 27 orphaned
  Champions E-Z; the 95 leaves in the dead-end ledger are theirs too, and the seven lines still
  without a Perfect rung are the cost they have to budget for.
---

## 2026-07-22 - US-152 - Orphan sweep: Adult E-G
Wired the five playable Champions whose display name begins E-G that had NO edge at all: Ebidramon,
FlareLizamon, Gawappamon, Growmon Orange, GulusGammamon. Each gained one earned in-edge from a Child
of its own line and one earned out-edge to a Perfect of that line, plus the line's unconditioned junk
fallback. Orphans 319 -> 314 (Appendix B over a regenerated `roster.generated.json`): Adult 27 -> 22,
and NO other bucket moved. 610 -> 615 nodes.

Files: `Resources/evolutions.json` (5 new Champions, 5 new Child branches, 0 new Perfects),
`Resources/elements.json` and `Resources/moves.json` (5 entries each), new
`Tests/AdultSweepEToGTests.swift` (20 tests), and the knock-on edits listed below.
Full suite 2095 tests green (2075 before), build green.

The five, with the citation each rests on:
  ebidramon      penc-ds  shakomon    -> anomalocarimon        (Pendulum COLOR 2 Deep Savers)
  gawappamon     penc-ds  gomamon     -> pencds_megaseadramon  (Wikimon Da-199 + plain Evolves To)
  flarelizamon   tamers   clearagumon -> megalogrowmon         (closes US-151's half-drawn arrow)
  growmon_orange tamers   guilmon     -> megalogrowmon         (both edges cited; variant, no rehome)
  gulusgammamon  penc-vb  gammamon    -> holyangemon           (Pendulum COLOR ZERO Virus Busters)

**Learnings for future iterations:**

- **AN ADULT SWEEP CAN COST NOTHING AT THE PERFECT RUNG, and checking that FIRST is what makes it a
  five-node story instead of a nine-node one.** US-151 had to open two Perfect rungs; this story
  opened none, because every one of the five had a cited Perfect that was ALREADY on the line the
  citation put it on. The order that produced that: fetch the orphan's Wikimon `Evolves From` AND
  `Evolves To` together, intersect BOTH against the graph, and only then choose the line. Choosing
  the line off the parent alone is what forces a new Perfect later.

- **A HANDOVER CLAIM IS A TEST, AND WHEN THE NEXT STORY HONOURS IT THE TEST FLIPS RATHER THAN DIES.**
  US-151 wrote `testMegaloGrowmonsOtherCitedParentIsLeftForTheEToGSweep`, asserting
  `graph.node(id: "flarelizamon") == nil`. Wiring FlareLizamon necessarily broke it. The fix is not
  to delete it: it became `...WasPickedUpByTheEToGSweep`, asserting MegaloGrowmon's exact parent set
  is now `[darklizamon, flarelizamon, growmon_orange]`. Same fact, other side, still fails if a later
  story quietly hangs a fourth parent there. Leave the next sweep a claim of the same shape.

- **WHEN THE BOLDED WIKIMON PARENT IS idle-only, TAKE THE NEXT CITED PARENT, NOT A REHOME.** Kamemon
  is the bolded name in Gawappamon's `Evolves From` and is `dexOnly` in this pack, which
  `edgeToDexOnlyNode` forbids - the identical trap US-151 hit with Burgamon and had to rehome out
  of. Here Gomamon was three bullets further down the SAME list and is already `penc-ds`'s own
  Rookie, so the citation survived intact and the line argument never had to be made. Read the whole
  Evolves From before deciding a rung is unwireable.

- **`Set(graph.parents(of:))` EQUALITY IS THE FRAGILE ASSERTION AT THIS RUNG, and there are now
  three of them.** `PendulumVirusBustersTreeTests.testHolyAngemonCarriesBothOfTheDocumentsDrawingsOf
  It` went two -> three (US-151) -> four (US-152) parents; `PendulumMetalEmpireTreeTests`' Andromon
  one and US-151's own MegaloGrowmon one are the others. Each time the fix is to NAME the new parent
  and say which arrow it is in the comment, since the claim is about the document's own drawings and
  not about a count. `grep -rn 'Set(graph.parents' Tests` before choosing an out-edge target.

- **THE `sweepEggs` ESCAPE HATCH IS THE STANDARD WAY TO ADD A NODE TO A DEVICE LINE.** Every device
  tree file ends with a "the N orphans this story removed" test whose `mine.count` is the line minus
  a `sweepEggs` set of ids later stories parked there. Adding to that set (rather than bumping the
  count) keeps the tree's own claim about its own thirty-one nodes true. Both files touched here
  wanted it: `penc-ds` took `ebidramon`/`gawappamon`, `penc-vb` took `gulusgammamon`.

- **A Child at THREE earned branches is one short of the ceiling, and that is worth asserting rather
  than remembering.** Shakomon and Gomamon now carry three apiece (four energy types is the hard
  ceiling, per `SeedRosterTests`' distinct-energy rule). `AdultSweepEToGTests.testEveryChildThisStory
  BranchedStillUsesDistinctEnergies` pins the 3 so the next sweep prices a fourth honestly instead of
  discovering the ceiling by failing.

- **The knock-on list for this rung, all thirteen files:** node count in `ElementCatalogTests`,
  `EvolutionGraphValidatorTests`, `AdultSweepAToDTests`, `ChildSweepAToFTests`, `ChildSweepGToLTests`
  and `ChildSweepMToZTests` (610 -> 615); the per-line dict in `EvolutionGraphTests`; line sizes in
  `DigitamaSweepTests`, `BabyIISweepTests`, `ChildSweepAToFTests`, `ChildSweepGToLTests`,
  `ChildSweepMToZTests` and `AdultSweepAToDTests` (`tamers` 71->73, `penc-ds` 34->36, `penc-vb`
  43->44); the "exactly two edges" exception dict in `ChildSweepAToFTests` (new, `clearagumon`) and
  `ChildSweepGToLTests` (`gammamon`, `guilmon`); `inLine.count` plus `sweepEggs` in
  `PendulumDeepSaversTreeTests` and `PendulumVirusBustersTreeTests`; and HolyAngemon's exact parent
  set. `EvolutionCriteriaTests.junkIds`, the dead-end ledger, `MinigameAssignmentTests`,
  `EggHatchingTests`, `DexScreenTests`, `DexModel.lineTitles` and both `lineDefaults` did NOT move:
  no new line, no new node at Perfect or above, and no thread newly reached an Ultimate.

- REMAINS: US-153 onward (39/56 passing). Four Adult sweeps hold the 22 orphaned Champions H-Z, then
  twelve Perfect and Ultimate sweeps and the Armor-Hybrid one. The seven lines still with no Perfect
  rung - `xros`, `vital`, `adventure02`, `algomon`, `commandramon`, `diablomon`, `penc-sw` - are
  untouched, and the 99 leaves in `ChildSweepAToFTests`' dead-end ledger are still owed an out-edge
  by whichever story claims them.
---

## 2026-07-22 - US-153 - Orphan sweep: Adult H-L
Wired the THREE playable Champions whose display name begins H-L that had no edge at all:
KausGammamon, Kinkakumon and Kougamon. Each gained one earned in-edge from a Child of its own line
and one earned out-edge to a Perfect of that line, plus the line's unconditioned junk fallback.
Orphans 314 -> 311 (Appendix B over a regenerated `roster.generated.json`): Adult 22 -> 19, and NO
other bucket moved. 615 -> 618 nodes.

Files: `Resources/evolutions.json` (3 new Champions, 3 new Child branches, 0 new Perfects),
`Resources/elements.json` and `Resources/moves.json` (3 entries each), new
`Tests/AdultSweepHToLTests.swift` (20 tests), and the knock-on edits listed below.
Full suite 2115 tests green (2095 before), build green.

The three, with the citation each rests on:
  kausgammamon  penc-vb  gammamon -> pencvb_weregarurumon  (Pendulum COLOR ZERO Virus Busters, both
                                                            arrows; its Wikimon family is literally
                                                            "Virus Busters")
  kinkakumon    penc-ds  jellymon -> zudomon               (Vital Bracelet BE, both arrows)
  kougamon      penc-wg  mushmon  -> jyureimon             (D-alpha-508 + Digimon Story: Sunburst &
                                                            Moonlight, both arrows)

**Learnings for future iterations:**

- **THE PRD'S PER-LETTER ESTIMATES ARE STALE, AND CHECKING THE REAL RANGE IS THE FIRST THING TO DO.**
  The AC says "roughly 25 orphaned Adult"; there were three. The estimate was taken when the rung
  held 168 orphans, and eleven device trees plus three Child sweeps had already spent 146 of them.
  Run Appendix B and bucket by first letter BEFORE planning the story — US-154..US-156 hold 19
  between them (M-R 9, S-T 5, U-Z 5), not the ~75 the descriptions imply.

- **TWO CITED LINES IS A REAL CHOICE, AND THE TIEBREAK IS THE RUNG ABOVE.** Kinkakumon is an Oni of
  the Journey-to-the-West set and Wikimon cites Fujamon and Takinmon, both `penc-sw` Children, to
  *Pendulum COLOR 6 Saiyu Warriors* — the thematically right home. It also cites Jellymon below and
  Zudomon above to *Vital Bracelet BE*, and both of those were already on `penc-ds`. The first
  reading costs an earned Perfect plus a junk floor under it (`penc-sw` has NO Perfect rung, the bill
  US-151 paid twice); the second costs nothing. Took the second, wrote the first into the node
  `comment`, and pinned it from both ends in `testKinkakumonTookTheCitedLineThatAlreadyHadAPerfect
  Rung`: Fujamon and Takinmon still do not reach it, and `penc-sw` still has no Perfect. That test
  FLIPS the day someone opens that rung, which is the point.

- **A CHILD IS FULL AT THREE EARNED BRANCHES, AND US-153 FILLED ONE.** Gammamon now carries
  BetelGammamon (spirit), GulusGammamon (strength) and KausGammamon (stamina) plus its junk
  fallback. Four energy types is the ceiling `EvolutionEngine` imposes — a fourth earned branch
  cannot be told apart from one of the three — so `ChildSweepGToLTests`' `branchedByALaterSweep`
  entry for `gammamon` is at 4 and can never move again. Said out loud in
  `testEveryChildThisStoryBranchedStillUsesDistinctEnergiesAndGammamonIsFull` so US-154, which has
  WezenGammamon (the last of the four Gammamon Champions) in range, prices the problem before it
  starts rather than discovering it as a failing test: **WezenGammamon cannot hang off Gammamon
  even though Gammamon is its bolded cited parent** — every energy is spoken for. The way out is
  already on the page and was checked while writing this: `Wezen Gammamon`'s `Evolves From` also
  cites **Gabumon** to *Pendulum COLOR ZERO Virus Busters*, and `pencvb_gabumon` is on the same
  line with room left. Prefer that second citation to a rehome, the US-150 rule.

- **THE BOLDED `Evolves To` IS NOT ALWAYS THE ONE TO TAKE.** KausGammamon's is Canoweissmon, which
  has a full Perfect sheet on disk and no node — taking it would have spent a Perfect for nothing,
  because WereGarurumon is cited on the SAME page to the SAME device and was already on the line.
  The arrow NOT taken is named in the comment and pinned in
  `testTheGammamonThreadsLeftForTheLaterSweepsAreStillOpen`, which fails the day Canoweissmon is
  wired and makes whoever wires it say which arrow it is.

- **`PendulumWindGuardiansTreeTests`' UNGUARDED "every Champion falls to the junk Perfect" LOOP
  SURVIVED, and the reason is worth knowing.** The Codebase Patterns section warns it "fails the
  moment a sweep puts an Adult on them". It did not, because a sweep Adult carries a junk fallback
  BY CONSTRUCTION — `EvolutionCriteriaTests` demands one. What actually breaks that loop is an Adult
  with NO edges at all, which is what a Child sweep produces and an Adult sweep does not. Deep
  Savers and Wind Guardians can still be broken by a future story that authors a leaf Champion.

- **The knock-on list for this rung, all fourteen files:** node count in `ElementCatalogTests`,
  `EvolutionGraphValidatorTests`, `AdultSweepAToDTests`, `AdultSweepEToGTests`, `ChildSweepAToFTests`,
  `ChildSweepGToLTests` and `ChildSweepMToZTests` (615 -> 618); the per-line dict in
  `EvolutionGraphTests` (`penc-ds` 36->37, `penc-wg` 33->34, `penc-vb` 44->45) and the same three in
  `AdultSweepAToDTests` and `AdultSweepEToGTests`; `inLine.count` plus `sweepEggs` in
  `PendulumDeepSaversTreeTests`, `PendulumVirusBustersTreeTests` and `PendulumWindGuardiansTreeTests`
  (Wind Guardians had no `sweepEggs` set at all and got one rather than a bumped total);
  `branchedByALaterSweep` in `ChildSweepGToLTests` (`gammamon` 3 -> 4); Zudomon's exact parent set in
  `PendulumDeepSaversTreeTests` (a FOURTH `Set(graph.parents(of:))` equality — the pattern note's
  count of three is now four); and Jellymon's row in that file's per-Rookie reachability table.
  `EvolutionCriteriaTests.junkIds`, the dead-end ledger (still 99), `MinigameAssignmentTests`,
  `EggHatchingTests`, `DexScreenTests`, `DexModel.lineTitles` and both `lineDefaults` did NOT move:
  no new line, no new node at Perfect or above, and no thread newly reached an Ultimate.

- REMAINS: US-154 onward (40/56 passing). Three Adult sweeps hold the 19 orphaned Champions M-Z,
  then twelve Perfect and Ultimate sweeps and the Armor-Hybrid one. The seven lines still with no
  Perfect rung - `xros`, `vital`, `adventure02`, `algomon`, `commandramon`, `diablomon`, `penc-sw` -
  are untouched, and the 99 leaves in `ChildSweepAToFTests`' dead-end ledger are still owed an
  out-edge by whichever story claims them.
---

## 2026-07-22 - US-154 - Orphan sweep: Adult M-R
Wired the NINE playable Champions whose display name begins M-R that had no edge at all: Meramon X,
Mikemon, Monochromon X, MoriShellmon, Musyamon, Nefertimon X, Pegasmon X, Pteranomon X and
RedV-dramon. Each gained one earned in-edge from a Child of its own line and one earned out-edge to a
Perfect of that line, plus the line's unconditioned junk fallback. TWO new Perfects were opened on
`tamers`. Orphans 311 -> 300 (Appendix B over a regenerated `roster.generated.json`): Adult 19 -> 10
AND Perfect 110 -> 108 - the first sweep since US-151 to move a second bucket. 618 -> 629 nodes.

Files: `Resources/evolutions.json` (9 new Champions, 9 new Child branches, 2 new Perfects),
`Resources/elements.json` and `Resources/moves.json` (11 entries each), new
`Tests/AdultSweepMToRTests.swift` (22 tests), and the knock-on edits listed below.
Full suite 2137 tests green (2115 before), build green.

The nine, with the citation each rests on:
  meramon_x      tamers   blucomon       -> grademon         (Digital Monster X Ver.3, both arrows)
  monochromon_x  tamers   guilmon_x      -> mametyramon      (Sx-45/Pendulum X 2.0 in, Bx-43 out)
  pegasmon_x     tamers   lopmon_x       -> grademon         (Digital Monster X Ver.3, both arrows)
  pteranomon_x   tamers   monodramon     -> mametyramon      (Bx-42/Da-484 in, Bx-43 out)
  mikemon        penc-vb  plotmon        -> pencvb_angewomon (Plotmon named; out-edge is the DCG
                                                              "Any Yellow Lv.5" CLASS citation)
  nefertimon_x   penc-vb  plotmon_x      -> pencvb_angewomon (Bx-98/DMX Ver.2 in, St-445 out)
  morishellmon   penc-ds  ganimon        -> anomalocarimon   (Digimon World 2, BOTH arrows)
  musyamon       penc-nso picodevimon    -> vamdemon         (DSSM in, Bo-214 out)
  redv-dramon    penc-wg  pencwg_piyomon -> aerov-dramon     (DWNO in, D1 Tamers/D-Project out)

**Learnings for future iterations:**

- **AN ADULT SWEEP CAN COST TWO PERFECTS AND STILL BE CHEAP, IF THE CITATIONS PAIR UP.** Four of the
  nine are X-Antibody Champions of the `tamers` X thread, and EVERY Perfect any of them cites is a
  sheet on disk with no node - Cyberdramon X, Mamemon X, OmegaShoutmon X, Angewomon X, Hisyaryumon,
  Monzaemon X, Triceramon X, MegaloGrowmon X, Andromon, Blossomon. US-152's "intersect both ends"
  rule finds nothing there, so the rung above HAD to open. The move that made it two nodes instead
  of four was to intersect the four orphans' `Evolves To` lists **with each other** before choosing:
  Grademon is in Meramon X's AND Pegasmon X's, both to Digital Monster X Ver.3; Mametyramon is
  Bx-43, the card that fuses Monochromon X and Pteranomon X, so both pages cite it from their own
  side. **Do the pairwise intersection across the story's own range, not just orphan-by-orphan
  against the graph.** US-157 onward have twelve Perfect and Ultimate sweeps where the same trick
  applies, and the X thread is where it pays.

- **A LINE THAT ALREADY HAS A JUNK FLOOR MAKES A NEW PERFECT COST ONE NODE, NOT TWO.** The two-node
  bill US-151 paid twice (earned Perfect + junk floor + the floor into `junkIds`) is only owed by a
  line with NO Perfect rung. `tamers` got CatchMamemon in US-151, so Grademon and Mametyramon cost a
  node each and `EvolutionCriteriaTests.junkIds` did not move. Check for the floor before pricing.

- **PLOTMON IS FULL, AND PICO DEVIMON WAS ALREADY FULL WHEN THIS STORY USED IT.** Plotmon now carries
  Tailmon (vitality), Wizarmon (spirit), BlackTailmon (strength) and Mikemon (stamina) plus junk:
  five edges, the ceiling `EvolutionCriteriaTests` sets, and all four energies spent. Pico Devimon
  had only strength left and its junk edge to Gokimon is ALSO gated on strength - that is the Scumon
  arrangement US-133 recorded, not a collision, because the earned edge asks 60 and the junk one 0.
  `AdultSweepMToRTests.testMusyamonSharesStrengthWithPicoDevimonsJunkEdgeAndStillWins` proves both
  halves through the engine. **An energy shared with the junk fallback is legal; an energy shared
  with another EARNED branch is not.**

- **A CLASS CITATION IS THE ANSWER WHEN EVERY NAMED ARROW IS UNUSABLE, AND IT IS PINNABLE FOUR WAYS.**
  Mikemon's whole `Evolves To` is unreachable in this pack: Bastemon and Betsumon have no sheet,
  Majiramon is idle-only (`edgeToDexOnlyNode` forbids it), Zudomon is on `penc-ds` where no cited
  parent of Mikemon lives. The same page's "Any Yellow Lv.5 Digimon from the Digimon Card Game" is
  a real citation - US-151's Deckerdramon reading - and Plotmon is a Yellow Lv.3 with Angewomon a
  Yellow Lv.5 above it, so ONE line satisfies both halves of the clause. The test asserts the two
  absent sheets are still absent, Majiramon is still `dexOnly` and Zudomon is still `penc-ds`, so
  the day any of those changes the story is told to revisit.

- **A HISTORICAL "this line has exactly N Perfects" COUNT ROTS; SCOPE IT TO THE NAMES.**
  `AdultSweepAToDTests.testTheTwoLinesWithoutAPerfectRungGainedAJunkFloor` asserted `tamers` had
  exactly two Perfects. US-154 opened two more without touching US-151's pair, so a count failed on
  work that did nothing wrong. Replaced with `wanyamon`'s exact pair plus a superset check on
  `tamers` - the half that stays true - which is the same repair US-149 made to
  `ChildSweepAToFTests`' "this line had no Champion before".

- **`testEveryNodeInTheLineIsReachableFromItsDigitama` CARRIES ITS OWN `inLine.count`, SEPARATE FROM
  THE `sweepEggs` TEST.** Four device-tree files failed on it after the data landed, and it is NOT
  the closing "the N orphans this story removed" assertion the notes list - it is a second count in
  a different test in the same file. `grep -n 'inLine.count' Tests` alongside `grep -n sweepEggs`.

- **WEZENGAMMAMON IS US-156's, NOT US-154's.** US-153's handover note said it was an M-R orphan; its
  display name starts with W, so it is U-Z. The advice it carried is still right and still needed:
  Gammamon is full at four energies, so WezenGammamon cannot hang there and must take the Gabumon
  citation (`pencvb_gabumon`, same line). `AdultSweepMToRTests.testThePerfectSheetsLeftForTheLater
  SweepsAreStillUnwired` re-pins Gammamon's edge count at 4 so that stays true until US-156 acts.
  **Derive a handover's range from the roster, not from memory of the letter bands.**

- **The knock-on list for this rung, all seventeen files:** node count in `ElementCatalogTests`,
  `EvolutionGraphValidatorTests`, `AdultSweepAToDTests`, `AdultSweepEToGTests`, `AdultSweepHToLTests`,
  `ChildSweepAToFTests`, `ChildSweepGToLTests` and `ChildSweepMToZTests` (618 -> 629); the per-line
  dict in `EvolutionGraphTests` (`tamers` 73->79, `penc-vb` 45->47, `penc-ds` 37->38, `penc-nso`
  42->43, `penc-wg` 34->35) and the same lines' `sizes[...]` rows in `AdultSweepAToDTests`,
  `AdultSweepEToGTests`, `AdultSweepHToLTests`, `BabyIISweepTests`, `ChildSweepAToFTests`,
  `ChildSweepGToLTests`, `ChildSweepMToZTests` and `DigitamaSweepTests`; `branchedByALaterSweep` in
  all three Child sweeps (`blucomon` 3, `guilmon_x` 3, `lopmon_x` 3, `monodramon` 3->4,
  `plotmon_x` 3); `sweepEggs` AND `inLine.count` in `PendulumDeepSaversTreeTests`,
  `PendulumNightmareSoldiersTreeTests`, `PendulumVirusBustersTreeTests` and
  `PendulumWindGuardiansTreeTests`; the dead-end ledger in `ChildSweepAToFTests` (99 -> 101, the two
  new Perfects); and `AdultSweepAToDTests`' Perfect-count claim, rescoped. `EvolutionCriteriaTests.
  junkIds`, its stranded lists, `MinigameAssignmentTests`, `EggHatchingTests`, `DexScreenTests`,
  `DexModel.lineTitles` and both `lineDefaults` did NOT move: no new line, and no thread newly
  reached an Ultimate because both new Perfects are leaves.

- REMAINS: US-155 onward (41/56 passing). Two Adult sweeps hold the last 10 orphaned Champions
  (S-T: Saberdramon, ShimaUnimon, Siesamon X, Tyrannomon, Tyrannomon X; U-Z: V-dramon Black,
  WezenGammamon, XV-mon Black, Xiquemon, Youkomon), then twelve Perfect and Ultimate sweeps and the
  Armor-Hybrid one. The seven lines still with no Perfect rung - `xros`, `vital`, `adventure02`,
  `algomon`, `commandramon`, `diablomon`, `penc-sw` - are untouched, and the 101 leaves in
  `ChildSweepAToFTests`' dead-end ledger are still owed an out-edge by whichever story claims them.
---

## 2026-07-22 - US-155 - Orphan sweep: Adult S-T
Wired the FIVE playable Champions whose display name begins S-T that had no edge at all: Saberdramon,
ShimaUnimon, Siesamon X, Tyrannomon and Tyrannomon X. Each gained one earned in-edge from a Child of
its own line and one earned out-edge to a Perfect of that line, plus the line's unconditioned junk
fallback. ONE new Perfect was opened, on `dmc-v3`. Orphans 300 -> 294 (Appendix B over a regenerated
`roster.generated.json`): Adult 10 -> 5 AND Perfect 108 -> 107. 629 -> 635 nodes.

Files: `Resources/evolutions.json` (5 new Champions, 5 new Child branches, 1 new Perfect),
`Resources/elements.json` and `Resources/moves.json` (6 entries each), new
`Tests/AdultSweepSToTTests.swift` (22 tests), and the knock-on edits listed below.
Full suite 2159 tests green (2137 before), build green.

The five, with the citation each rests on:
  saberdramon   dmc-v4    muchomon  -> megadramon     (Da-511 in, Digimon World -next 0rder- out)
  shimaunimon   penc-nso  bakumon   -> mammon         (Digimon World 2, BOTH arrows)
  siesamon_x    tamers    dorumon   -> grademon       (Digital Monster X Ver.3, both arrows)
  tyrannomon    dmc-v1    agumon    -> mamemon        (Digital Monster Ver. 1, both arrows)
  tyrannomon_x  dmc-v3    agumon_x  -> metalgreymon_x (Digital Monster X, both arrows; NEW Perfect)

**Learnings for future iterations:**

- **US-133's "Tyranomon has no colour sheet at all" WAS WRONG, and the reason is worth the note more
  than the fix is.** `DMCVersion1TreeTests.testTheTwoUndrawableNamesAppearNowhere` had carried, since
  the very first device tree, that the Version 1 document's Tyranomon could not be drawn - only
  `Black and White Sprites/Adult/Tyranomon.png` existed. The pack spells the COLOUR sheet with two
  n's: `Adult/Tyrannomon.png`, roster id `tyrannomon`, playable, animated. So the arrow was drawable
  the whole time and Agumon shipped with four of the document's five Champions for twenty-two
  stories. The test is now `testTheOneUndrawableNameAppearsNowhereAndTyranomonIsASpelling` and pins
  BOTH halves - the one-n id must never be a node (it would duplicate art), and the two-n one must be
  playable and on this line. Betamon is genuinely still idle-only. **Wikimon's spelling is not the
  pack's; resolve every name through the roster's display names before believing an absence.**

- **FOUR OF FIVE PLACEMENTS COST NOTHING ONE RUNG UP, WHICH IS THE HIGHEST RATE OF THE FIVE ADULT
  SWEEPS, AND IT IS NOT LUCK.** US-152's rule - intersect `Evolves From` AND `Evolves To` against the
  graph before choosing the line - gets CHEAPER as the file fills, because more Perfects are nodes
  each time. Saberdramon had sixteen cited parents and twenty-odd cited children and exactly one line
  held a node at both ends. ShimaUnimon's Bakumon and Mammon are both cited to Digimon World 2, one
  source for both arrows. Do the intersection before pricing anything; the orphan-by-orphan version
  is enough at this point and US-154's pairwise-across-the-range trick was not needed once.

- **A SWEEP CAN FILL THREE ROOKIES AT ONCE, AND THE KNOCK-ON REACHES THE SEED TESTS.** Agumon,
  Bakumon and Muchomon each spent their LAST free energy here (four earned branches plus junk = five
  edges, the `EvolutionCriteriaTests` ceiling). That is expected late in the rung - a Rookie with one
  energy left is exactly the one whose line already holds both ends - but Agumon is the seed, so its
  branch list is pinned in FIVE places outside the sweep files: `SeedRosterTests` (twice),
  `EnergyBarsTests` (which asserted vitality had NOTHING to reach for), `DexScreenTests`,
  `ConditionRevealTests` and `EvolutionTreeTests` (node count, Adult column, rowCount AND connector
  count). `grep -rn '"agumon"' Tests` before branching a seed Rookie.

- **`SeedRosterTests`' per-line verified-available SETS are a third knock-on family nobody listed.**
  `patamonLineVerifiedSet` (dmc-v3) and `piyomonLineVerifiedSet` (dmc-v4) are closed lists of display
  names that every Adult-and-up node on those lines must be in - US-148 through US-151 appended to
  them, and so did this story. They are the record of which sheets were actually eyeballed, so append
  with a comment naming the sweep; never relax the containment check.

- **AN ADULT'S JUNK ENERGY MUST MATCH ITS DOMINANT FOR `evolutionTarget`, BUT NOT FOR
  `scheduledEvolutionTarget`.** A first draft of `AdultSweepSToTTests` asserted a neglected vitality
  Agumon X falls to Scumon through `EvolutionEngine.evolutionTarget` and got `nil`: that entry point
  matches on the dominant energy, and Scumon's junk edge is on strength. US-154's Musyamon test
  worked only because its junk edge and its dominant were the same energy. The path the app actually
  takes is `scheduledEvolutionTarget`, which falls to `isDefault` whatever the dominant is - use that
  for every "neglect" assertion and keep `evolutionTarget` for the earned ones.

- **The knock-on list for this rung, all sixteen files:** node count in `ElementCatalogTests`,
  `EvolutionGraphValidatorTests`, `AdultSweepAToDTests`, `AdultSweepEToGTests`, `AdultSweepHToLTests`,
  `AdultSweepMToRTests`, `ChildSweepAToFTests`, `ChildSweepGToLTests` and `ChildSweepMToZTests`
  (629 -> 635); the per-line dict in `EvolutionGraphTests` (`dmc-v1` 29->30, `dmc-v3` 46->48, `dmc-v4`
  26->27, `penc-nso` 43->44, `tamers` 79->80) and the same lines' `sizes[...]` rows in
  `AdultSweepAToDTests`, `AdultSweepEToGTests`, `AdultSweepMToRTests`, `BabyIISweepTests`,
  `ChildSweepAToFTests`, `ChildSweepGToLTests`, `ChildSweepMToZTests` and `DigitamaSweepTests`;
  `branchedByALaterSweep` in `ChildSweepAToFTests` (`agumon_x` and `dorumon`, both 2->3); `sweepEggs`
  AND `inLine.count` in `PendulumNightmareSoldiersTreeTests`; Grademon's exact parent set in
  `AdultSweepMToRTests` (a FIFTH `Set(graph.parents(of:))` equality); the dead-end ledger in
  `ChildSweepAToFTests` (101 -> 102, the one new Perfect); `targets(of:)` for Agumon in
  `DMCVersion1TreeTests` and for Muchomon in `DMCVersion4TreeTests`; the two verified-available sets
  and Agumon's branch list in `SeedRosterTests`; and the Agumon rows in `ConditionRevealTests`,
  `DexScreenTests`, `EnergyBarsTests` and `EvolutionTreeTests`. `EvolutionCriteriaTests.junkIds`,
  `MinigameAssignmentTests`, `EggHatchingTests`, `DexModel.lineTitles` and both `lineDefaults` did
  NOT move: no new line, and the one new Perfect is a leaf, so no thread newly reached an Ultimate.

- REMAINS: US-156 onward (42/56 passing). ONE Adult sweep holds the last five orphaned Champions
  (U-Z: V-dramon Black, WezenGammamon, XV-mon Black, Xiquemon, Youkomon - pinned as still-unwired in
  `AdultSweepSToTTests.testTheFiveChampionsLeftForUS156AreStillUnwired`, which also re-pins Gammamon
  at four edges so US-156 is still told to take the `pencvb_gabumon` citation for WezenGammamon).
  Then twelve Perfect and Ultimate sweeps and the Armor-Hybrid one, against 107 orphaned Perfect,
  166 orphaned Ultimate and 16 Armor-Hybrid. The seven lines still with no Perfect rung - `xros`,
  `vital`, `adventure02`, `algomon`, `commandramon`, `diablomon`, `penc-sw` - are untouched, and the
  102 leaves in `ChildSweepAToFTests`' dead-end ledger are still owed an out-edge by whichever story
  claims them. Greymon X is one of them and is the obvious second parent for this story's new
  MetalGreymon X.
---

## 2026-07-22 - US-156 - Orphan sweep: Adult U-Z (the Adult rung is finished)
Wired the LAST FIVE playable Champions with no edge at all: V-dramon Black, WezenGammamon, XV-mon
Black, Xiquemon and Youkomon. Each gained one earned in-edge from a Child of its own line and one
earned out-edge to a Perfect of that line, plus the line's unconditioned junk fallback. THREE new
Perfects were opened, one each on `penc-vb`, `dmc-v4` and `tamers`. Orphans 294 -> 286 (Appendix B
over a regenerated `roster.generated.json`): **Adult 5 -> 0 — the bucket is empty, the first rung
above Child to be finished** — AND Perfect 107 -> 104. 635 -> 643 nodes.

Files: `Resources/evolutions.json` (5 new Champions, 5 new Child branches, 3 new Perfects),
`Resources/elements.json` and `Resources/moves.json` (8 entries each), new
`Tests/AdultSweepUToZTests.swift` (26 tests), and the knock-on edits listed below.
Full suite 2185 tests green (2159 before), build green, validator zero findings.

The five, with the citation each rests on:
  v-dramon_black  penc-wg  pencwg_piyomon -> aerov-dramon   (out-edge cited 8 cards incl. Bo-1113;
                                                             in-edge is a LINE argument, said so)
  xv-mon_black    penc-wg  mushmon        -> aerov-dramon   (St-768 in, Bo-935/Bo-1113 out)
  wezengammamon   penc-vb  gammamon       -> canoweissmon   (Ghost Game 12 + Pendulum COLOR ZERO
                                                             Virus Busters in, GG26/VB/BT10-011 out)
  xiquemon        dmc-v4   piyomon        -> huankunmon     (Digimon New Century, BOTH arrows)
  youkomon        tamers   renamon        -> blackrapidmon  (nine citations in, Digimon
                                                             Championship out)

**Learnings for future iterations:**

- **"GAMMAMON IS FULL" WAS WRONG FOR THREE STORIES, AND THE BUG IS IN WHICH COUNT YOU READ.** US-153
  recorded Gammamon as full at "three earned branches plus the junk fallback"; US-154 and US-155
  both forwarded that as advice, and `AdultSweepHToLTests` pinned it as
  `Set(gammamon.evolutions.compactMap(\.requiredEnergy)).count == 3`. The node had spirit, strength
  and stamina earned and a junk edge on spirit — so vitality was free the whole time. The ceiling is
  FOUR EARNED BRANCHES (one per `EnergyType`) plus the fallback, five edges, which is exactly what
  Plotmon has carried since US-154. **`evolutions.filter { !$0.isDefault }.compactMap(\.requiredEnergy)`
  compared against `Set(EnergyType.allCases)` is the only honest test of fullness**; distinct
  energies over ALL edges under-counts by one whenever the junk edge shares, which it usually does.
  US-156 drew the bolded Ghost Game arrow instead of the stand-in, corrected the claim in US-153's
  own test file rather than deleting it, and left `pencvb_gabumon` the vitality it was told to spend.
  Cost: one test run to notice, ten minutes to fix, and a better citation than the handover offered.

- **THE AC'S "A VARIANT HANGS OFF ITS BASE FORM'S LINE" CAN OUTRANK THE ONLY CITATION THERE IS, AND
  IT IS THE RIGHT CALL WHEN THE OTHER END PAYS FOR IT.** V-dramon Black's whole Wikimon `Evolves
  From` is Guilmon and Guilmon X, both `tamers` — and `tamers` holds NOT ONE of its cited children,
  so US-152's intersection is empty there and taking the citation costs a Perfect on a line with no
  claim on the Digimon. `penc-wg` is the opposite: no source draws Piyomon into it, but AeroV-dramon
  is cited eight cards over, is already a Perfect there, and the base V-dramon and RedV-dramon both
  hang off the same Rookie. **Price both readings before taking the cited one**, and when the
  in-edge ends up resting on a line argument, WRITE "LINE argument" into the comment and grep for it
  in a test — this story does, so the day someone finds a real citation they know what they are
  replacing.

- **TWO ORPHANS OF ONE STORY SHARING A TARGET IS US-154'S PAIRWISE TRICK, AND IT LANDS ON A CARD.**
  Bo-1113 draws AeroV-dramon out of V-dramon (Black) *with or without* XV-mon (Black): one card,
  both of this story's variants, one existing Perfect, zero nodes above. That is two of five
  placements free. Different ENERGIES are what keep them from being the same branch twice (spirit
  off Piyomon, stamina off Mushmon), and a test asserts the pair is distinct rather than assuming it.

- **A REJECTED READING THAT IS THE BETTER THEME IS THE ONE MOST WORTH PINNING.** Xiquemon belongs on
  `penc-sw` — Fujamon and Kamemon are cited to *Pendulum COLOR 6 Saiyu Warriors*, which IS that
  line, and Huankunmon/Sagomon/Sanzomon are the same device's Perfects. It lost only on cost:
  `penc-sw` has no Perfect rung, so that reading is three nodes plus a `junkIds` entry against
  `dmc-v4`'s two. US-153's Kinkakumon set this precedent and this story kept it — the rejected
  reading is in the node `comment` AND pinned four ways
  (`testThePencSwReadingOfXiquemonIsStillTheCheaperOneNotTaken`: `penc-sw` still has no Perfect,
  Fujamon is still on it, Kamemon and Falcomon 2006 are still idle-only). Whoever opens `penc-sw`'s
  Perfect rung is TOLD Xiquemon is a rehome candidate.

- **BOTH OF A DIGIMON'S BOLDED PERFECTS CAN BE IDLE-ONLY, AND THEN THE CITED ALTERNATIVE IS THE
  ANSWER, NOT A CLASS CITATION.** Youkomon's canonical thread is Doumon and Taomon; both exist only
  in `Idle Frame Only/` and `edgeToDexOnlyNode` forbids either edge. BlackRapidmon is cited to
  *Digimon Championship* — the same game that draws three of Youkomon's parents — and `tamers`
  already carries Terriermon, Terriermon X and Galgomon, so the new Perfect lands on a line with a
  claim on it beyond the single arrow. A test asserts both bolded names are STILL idle-only, so the
  day either gains an animated sheet the placement is re-opened.

- **The knock-on list for this rung, all nineteen files:** node count in `ElementCatalogTests`,
  `EvolutionGraphValidatorTests`, all six `AdultSweep*Tests` and all three `ChildSweep*Tests`
  (635 -> 643); the per-line dict in `EvolutionGraphTests` (`penc-wg` 35->37, `penc-vb` 47->49,
  `dmc-v4` 27->29, `tamers` 80->82) and the same lines' `sizes[...]` rows in `AdultSweepAToDTests`,
  `AdultSweepEToGTests`, `AdultSweepHToLTests`, `AdultSweepMToRTests`, `AdultSweepSToTTests`,
  `BabyIISweepTests`, `ChildSweepAToFTests`, `ChildSweepGToLTests`, `ChildSweepMToZTests` and
  `DigitamaSweepTests`; `branchedByALaterSweep` in `ChildSweepGToLTests` (`gammamon` 4->5) and
  `ChildSweepMToZTests` (`renamon` 3); `earnedCounts["pencwg_piyomon"]` 3->4 and `mushmon` 3->4 in
  `AdultSweepMToRTests`/`AdultSweepHToLTests`; `sweepEggs` AND `inLine.count` in
  `PendulumWindGuardiansTreeTests` (35->37) and `PendulumVirusBustersTreeTests` (47->49);
  `targets(of: "piyomon")` in `DMCVersion4TreeTests`; `piyomonLineVerifiedSet` AND the line's
  `champions.sorted()` in `SeedRosterTests`; the dead-end ledger in `ChildSweepAToFTests`
  (102 -> 105, the three new Perfects); and THREE handover tests flipped rather than deleted —
  `AdultSweepHToLTests`' Gammamon claim (corrected), `AdultSweepMToRTests`' and `AdultSweepSToTTests`'
  "still unwired" pins (now "wired, and on this parent"). `EvolutionCriteriaTests.junkIds`, its
  stranded lists, `MinigameAssignmentTests`, `EggHatchingTests`, `DexModel.lineTitles` and both
  `lineDefaults` did NOT move: no new line, and all three new Perfects are leaves.

- REMAINS: US-157 onward (43/56 passing). **The Adult rung is DONE — no story after this one needs
  to touch it.** Twelve Perfect and Ultimate sweeps and the Armor-Hybrid one hold the remaining 286
  orphans: 104 Perfect, 166 Ultimate, 16 Armor-Hybrid. The seven lines still with no Perfect rung —
  `xros`, `vital`, `adventure02`, `algomon`, `commandramon`, `diablomon`, `penc-sw` — are untouched
  and are pinned as a set in `AdultSweepUToZTests.testWhatTheAdultRungHandsToThePerfectSweeps`. The
  105 leaves in `ChildSweepAToFTests`' dead-end ledger are still owed an out-edge by whichever story
  claims them; Galgomon (BlackRapidmon's obvious second parent), Greymon X and now Canoweissmon,
  Huankunmon and BlackRapidmon are among them.
---

## 2026-07-22 - US-157 - Orphan sweep: Perfect A-C (the first sweep at the Perfect rung)
Wired the NINETEEN playable Perfect whose display name begins A-C that had no edge at all. Each
gained one earned in-edge from a Champion of its own line and one `isDefault` climb into an Ultimate
of that line. TEN cost nothing above; the other NINE opened an Ultimate that had no node anywhere.
Orphans 286 -> 257 (Appendix B over a regenerated `roster.generated.json`): **Perfect 104 -> 84 and
Ultimate 166 -> 157**. 643 -> 672 nodes.

Files: `Resources/evolutions.json` (19 new Perfects, 9 new Ultimates, 1 new junk Perfect, 19 new
Champion branches and 9 new junk-floor edges), `Resources/elements.json` and `Resources/moves.json`
(29 entries each), new `Tests/PerfectSweepAToCTests.swift` (25 tests), the throwaway-but-kept
authoring script `scripts/us157_author.py`, and the knock-on edits listed below.
Full suite 2210 tests green (2185 before), build green, validator zero findings.

The nineteen, with the citation each rests on:
  andiramon_data        penc-vb   turuiemon           -> cherubimon_vice      (BOTH bolded)
  angewomon_x           penc-vb   tailmon_x           -> ophanimon_x  NEW     (bolded in, cited out)
  anomalocarimon_x      penc-ds   ebidramon           -> aegisdramon          (NO CITATION, LINE x2)
  archnemon             penc-nso  dokugumon           -> piemon               (both cited)
  astamon               penc-me   porcupamon          -> pencme_venomvamdemon (bolded in, cited out)
  atlurkabuterimon_red  penc-nsp  pencnsp_kabuterimon -> heraklekabuterimon   (BOTH bolded)
  baalmon               tamers    icedevimon          -> beelzebumon   NEW    (both cited)
  blackmegalogrowmon    tamers    blackgrowmon        -> chaosdukemon  NEW    (BOTH bolded, St-731/Bo-550)
  bluemeramon           penc-nso  pencnso_meramon     -> pencnso_boltmon      (both cited, zero cost)
  boutmon               penc-me   thunderballmon      -> kazuchimon    NEW    (cited in, bolded out)
  cannonbeemon          palmon    waspmon             -> tigervespamon NEW    (BOTH bolded)
  cargodramon           penc-me   tankmon             -> pencme_mugendramon   (both cited)
  caturamon             penc-vb   pencvb_leomon       -> pencvb_saberleomon   (both cited)
  cerberumon_x          penc-me   raptordramon        -> pencme_wargreymon    (both cited)
  chimairamon           dmc-v1    airdramon           -> millenniumon  NEW    (BOTH bolded)
  chohakkaimon          penc-sw   hakubamon           -> shakamon      NEW    (cited in, bolded out)
  crescemon             tamers    lekismon            -> dianamon      NEW    (BOTH bolded)
  cryspaledramon        tamers    paledramon          -> hexeblaumon   NEW    (BOTH bolded)
  cyberdramon_x         penc-me   revolmon            -> pencme_mugendramon   (NO CITATION, LINE x2)

**Learnings for future iterations:**

- **A PERFECT SWEEP PAYS THE DEAD-END LEDGER DOWN, NOT UP, AND THAT IS THE WHOLE SHAPE OF THIS RUNG.**
  Every Champion sweep added leaves; this one removed nine. The reason is structural: a Perfect needs
  a Champion beneath it, and after six Adult sweeps most Champions ARE leaves — so the cheapest
  in-edge is almost always one that also clears a debt. `ChildSweepAToFTests`' ledger fell 105 -> 97
  (nine cleared, Pandamon added). Price the in-edge off the ledger FIRST: nine of nineteen came free
  that way, and each one that does saves a later story a whole edit.

- **BUT EVERY LEAF CHAMPION YOU BRANCH COSTS A JUNK FLOOR EDGE IN THE SAME BREATH.** A leaf has no
  `isDefault` edge because it has no edges; the moment it gains an earned branch,
  `EvolutionCriteriaTests.testEveryNonTerminalChildAndAdultFallsToAJunkEvolution` requires one onto a
  junk Perfect of its OWN line. Eight of the nine floors already existed (`andiramon_virus`,
  `locomon` twice, `catchmamemon` four times, `jyagamon`); only `penc-sw` had none. Write the
  fallback in the same edit as the branch or the suite fails on the node you just wired.

- **`Roster.bundled` IS `Resources/roster.json`, WHICH IS ONE ENTRY PER SHEET — APPENDIX B'S 37 AND
  THE TEST'S 34 ARE BOTH RIGHT.** The script in Appendix B reads `roster.generated.json`, which
  mirrors `evolutions.json` and therefore carries the LINE-SCOPED ALIASES (`pencme_andromon`,
  `pencvb_angewomon`, `pencvb_asuramon` in this range alone). A range test written off
  `roster.entries` will disagree with the orphan count by exactly the number of aliases in the band.
  Use `Roster.bundled` for "every Digimon on disk is obtainable" and the generated file only for the
  Appendix B number, and say which is which in the test.

- **OPENING A LINE'S TOP RUNG PROMOTES ITS EGGS, AND `EggHatchingTests` IS WHERE THAT SHOWS UP.**
  `MainScreenModel.startingDigitamaId` filters candidates on `graph.reachesUltimate`, so giving
  `tamers` its first four Ultimates turned FIVE Digitama (Guil, BlackGuil, Imp, Lop, Bluco) from
  unraisable into legal starting eggs in one edit. This is the first promotion since US-146 that came
  from the TOP of a thread rather than the middle. Any Ultimate sweep that opens a rung on `wanyamon`
  — the last line with Perfects and no Mega — should expect the same and grep
  `reachesUltimate` before assuming the egg lists are inert.

- **`penc-sw`'s PERFECT RUNG COST EXACTLY THE THREE NODES US-153 AND US-156 QUOTED, AND PAYING IT
  FLIPS FOUR OLDER TESTS RATHER THAN DELETING THEM.** Cho-Hakkaimon + Shakamon + the junk floor
  Pandamon + one `EvolutionCriteriaTests.junkIds` entry. The knock-on nobody listed:
  `AdultSweepHToLTests.testKinkakumonTookTheCitedLineThatAlreadyHadAPerfectRung` and
  `AdultSweepUToZTests.testThePencSwReadingOfXiquemonIsStillTheCheaperOneNotTaken` both asserted the
  rung was still SHUT, and `testWhatTheAdultRungHandsToThePerfectSweeps` pinned `penc-sw` in the
  no-Perfect set. All three were flipped, not removed — they now name Kinkakumon and Xiquemon as live
  rehome candidates, which is the record those stories were trying to leave. **Neither was rehomed
  here, deliberately**: a rehome drags a Champion, its Perfect, its Rookie's energy budget and four
  device-tree tests across a line, and that is a story of its own.
  ALSO: the whole line stays STRANDED (`penc-sw` has no Digitama), so all three new nodes joined
  `EvolutionCriteriaTests`' stranded list in two new groups, `strandedPerfect` and `strandedUltimate`,
  with the same "parents are all stranded too" loop the rungs below use.

- **A PERFECT'S CLIMB IS ITS `isDefault` EDGE, AND THAT IS THE HOUSE STYLE — DO NOT PUT CONDITIONS ON
  IT.** Every Perfect in this file since US-134 carries one edge: `isDefault`, `requiredEnergy`,
  `minEnergy 150`, `maxCareMistakes 2`, NO conditions. `EvolutionCriteriaTests.branchingNodes` filters
  to Child and Adult, so the 2..5 ceiling and the junk-floor rule do NOT apply at this rung — a
  single-edge Perfect is legal and is what the file already had 81 of. The AC's "no edge is
  unconditional" binds the EARNED edges, which is the reading every rung below recorded; a condition
  on an `isDefault` edge is data that lies, because US-020 takes it whatever the gates say.

- **THE 29-NODE EDIT WAS SCRIPTED, AND THAT IS SAFE ONLY BECAUSE THE JSON ROUND-TRIPS EXACTLY.**
  `json.dumps(d, indent=2, ensure_ascii=False)` reproduces `evolutions.json`, `elements.json` and
  `moves.json` byte-for-byte (no trailing newline). Verify that first, then author from a table —
  hand-editing 29 nodes across three files invites exactly the drift these tests exist to catch.
  `scripts/us157_author.py` is kept as the record of what was authored and why.

- **CHECK moves.json SIGNATURE NAMES AGAINST THE WHOLE FILE, NOT THE LINE.** `projectileSymbol|tint`
  is unique PER LINE but `signatureName` is unique GLOBALLY, and six of the first draft's names
  collided with the base form they were named after (Angewomon X/Ophanimon X/Cyberdramon X shared
  their base's attack, BlackMegaloGrowmon shared MegaloGrowmon's). Variants are exactly where this
  bites. Also keep `signatureSymbol` inside the 21 SF Symbols the file already uses — the render test
  is the only thing that would catch an invented one, and it runs on the simulator.

- **The knock-on list for this rung, all twenty-two files:** node count in `ElementCatalogTests`,
  `EvolutionGraphValidatorTests`, all six `AdultSweep*Tests` and all three `ChildSweep*Tests`
  (643 -> 672); the per-line dict in `EvolutionGraphTests` (`dmc-v1` 30->32, `palmon` 24->26,
  `penc-ds` 38->39, `penc-me` 44->50, `penc-nso` 44->46, `penc-nsp` 33->34, `penc-sw` 9->12,
  `penc-vb` 49->53, `tamers` 82->90) and the same lines' `sizes[...]` rows in every `*Sweep*Tests`;
  `inLine.count` AND `sweepEggs` in `PendulumDeepSaversTreeTests`, `PendulumMetalEmpireTreeTests`,
  `PendulumNatureSpiritsTreeTests`, `PendulumNightmareSoldiersTreeTests` and
  `PendulumVirusBustersTreeTests`; `pencme_mugendramon`'s parent set in `PendulumMetalEmpireTreeTests`
  and Turuiemon's earned list in `PendulumVirusBustersTreeTests`; the dmc-v1 tree in
  `EvolutionTreeTests` (30->32 nodes, 44->46 connectors); BOTH egg lists in `EggHatchingTests`;
  `junkIds` and the stranded list in `EvolutionCriteriaTests`; the four leaf lists in
  `AdultSweepHToLTests`/`MToR`/`SToT`/`UToZ`; the dead-end ledger in `ChildSweepAToFTests`
  (105 -> 97); `AdultSweepEToGTests`' "exactly two edges" claim about Ebidramon, relaxed to "exactly
  one fallback"; `AdultSweepMToRTests`' still-unwired list (Cyberdramon X and Angewomon X left it);
  and the three `penc-sw` pins above. `MinigameAssignmentTests`, `DexModel.lineTitles` and both
  `lineDefaults` did NOT move: no new line.

- REMAINS: US-158 onward (44/56 passing). Eleven Perfect and Ultimate sweeps and the Armor-Hybrid one
  hold the remaining 257 orphans: 84 Perfect, 157 Ultimate, 16 Armor-Hybrid. Six lines still have no
  Perfect rung — `xros`, `vital`, `adventure02`, `algomon`, `commandramon`, `diablomon` — and
  `wanyamon` is now the LAST line with Perfects and no Ultimate above them. Cargodramon (for
  `commandramon`) and Boutmon (for `vital`) are pinned as rehome candidates the day either of those
  rungs opens, as are Kinkakumon and Xiquemon for `penc-sw`. The 97 leaves in `ChildSweepAToFTests`'
  dead-end ledger are still owed an out-edge; nine of this story's own Ultimates are NOT on it, since
  the ledger stops below the top rung.
---

## 2026-07-23 - US-158: Orphan sweep: Perfect D-G

Wired the fifteen playable Perfect whose display name begins D-G that were still orphans, for
FOURTEEN new nodes plus seven new Ultimates plus one rename. Second story at the Perfect rung.

**Orphan count, Appendix B over a regenerated `roster.generated.json`: 257 BEFORE -> 235 AFTER.**
Perfect bucket 84 -> 69 (the D-G band 15 -> 0), Ultimate bucket 157 -> 150. Files changed:
`Resources/evolutions.json`, `Resources/elements.json`, `Resources/moves.json`,
`scripts/us158_author.py` (new, the record of what was authored), `Tests/PerfectSweepDToGTests.swift`
(new), `Sources/DexModel.swift` (one doc comment), and twenty-two existing test files.

The fourteen, each `Champion -> Perfect -> Ultimate`:
  DarkKnightmon      tailmon(penc-nsp)        -> darkknightmon_x  NEW
  DarkSuperstarmon   starmon(tamers)          -> titamon          NEW
  Delumon            kiwimon(penc-wg)         -> griffomon
  DORUguremon        dorugamon(tamers)        -> dorugoramon      NEW
  Duramon            raptordramon(penc-me)    -> pencme_wargreymon
  Entmon             cockatrimon(penc-vb)     -> cherubimon_virtue
  Fantomon           wizarmon(penc-nso)       -> piemon
  Flaremon           firamon(penc-nso)        -> apollomon        NEW
  Garudamon X        pencwg_birdramon(penc-wg)-> hououmon
  Gigadramon         devidramon(dmc-v5)       -> mugendramon
  Gogmamon           gaogamon(wanyamon)       -> ancientvolcamon  NEW
  Gokuwmon           ginkakumon(penc-sw)      -> seitengokuwmon   NEW
  Grappleomon        gryzmon(wanyamon)        -> dinotigermon     NEW
  Gusokumon          ebidramon(penc-ds)       -> plesiomon

**Learnings for future iterations:**

- **AN "ORPHAN" IN APPENDIX B'S COUNT CAN BE A DIGIMON THAT IS ALREADY ON SCREEN — CHECK THE
  SPRITE FILE, NOT ONLY THE ID.** `ex-tyranomon` was the fifteenth orphan in this band and needed
  no node at all: `dmc-v5` had drawn it since US-137 under `extyranomon`, the same `spriteFile`
  with the hyphen dropped. Appendix B reads `roster.generated.json`, which carries the SHEET id
  from disk AND the graph's id, so a graph node whose id does not match its own sheet leaves the
  sheet looking unwired. The fix was a rename — node id, three in-edges, the `elements.json` and
  `moves.json` rows — and it cleared the orphan, retired the drift `DMCVersion5TreeTests` had
  pinned since US-137, and took `extyranomon` off `RosterTests`' alias list where it never
  belonged. **Before authoring a node for an orphan, grep the graph for its `spriteFile`.** One
  more of these may be hiding: `hiandromon`/`hi-andromon` is a KNOWN and deliberate divergence
  (two real sheets, one node) and must NOT be "fixed" the same way — `RosterTests` pins it.

- **A PERFECT SWEEP THAT PICKS ITS PARENTS OFF THE DEAD-END LEDGER PAYS FOR ITSELF TWICE, AND THE
  SECOND SWEEP CAN DO IT WITHOUT AUTHORING A FLOOR.** US-157 cleared nine leaves but had to invent
  Pandamon for `penc-sw`; this story cleared seven and every floor it needed
  (`catchmamemon`, `andiramon_virus`, `darumamon`, `karakurumon`, `pandamon`) already existed, so
  `EvolutionCriteriaTests.junkIds` did not move at all. Ledger 105 -> 97 -> 90. Expect the H-Z
  sweeps to keep this shape: after seven Adult sweeps the cheap parent is nearly always a leaf.

- **`wanyamon` WAS THE LAST LINE WITH PERFECTS AND NO MEGA, AND OPENING IT PROMOTED THE WHOLE
  LINE'S EGGS AT ONCE.** All four `wanyamon` Digitama (Gao, Bear, Koe, Lioll) went from unraisable
  to legal starting eggs in one edit, because `MainScreenModel.startingDigitamaId` filters on
  `graph.reachesUltimate`. That is a bigger jump than US-157's five and it is the LAST of its kind
  — `linesWithAPerfect.subtracting(linesWithAnUltimate)` is now empty, and `PerfectSweepAToCTests`'
  claim about it was flipped rather than deleted. The remaining promotions have to come from the
  MIDDLE of a thread, like Monodra Digitama did here (DORUgamon was a leaf until DORUguremon and
  DORUgoramon went over it). Six lines still have no Perfect rung at all: `xros`, `vital`,
  `adventure02`, `algomon`, `commandramon`, `diablomon` — open one of those and the same egg
  arithmetic applies again.

- **THE MOVES-TABLE COLLISION CHECK MUST RUN BEFORE THE EDIT, NOT AFTER: EIGHT OF THIS STORY'S
  TWENTY-ONE FIRST-DRAFT MOVES COLLIDED.** `signatureName` is unique GLOBALLY and
  `projectileSymbol|tint` is unique PER LINE, and the second is the one that bites at this rung —
  a story adding three nodes to one line is competing with 49 rows already on it, and `flame.fill`
  on `penc-nso` had only two free tints left. Script the check against the real JSON and re-run it
  after every edit to the table; changing the SYMBOL is usually better than reaching for an odd
  tint (Flaremon became `hand.raised.fill`, Apollomon `star.fill`).

- **THE VARIANT RULE READS FROM THE TOP AS WELL AS THE BOTTOM.** DarkKnightmon X hangs off the
  DarkKnightmon this story authored, so "a variant sits on its base form's line" was satisfied by
  authoring the base form first and letting the X follow it — the same shape as US-157's Angewomon
  X but with the base form new. And when a variant's base form has NO node yet (DarkSuperstarmon;
  Superstarmon is an S-Z orphan), say in the node `comment` and in a test where the base form is
  going to have to go, so the later sweep is told rather than free to disagree.

- **A LINE ARGUMENT CAN BE THE CHEAP HALF OF A REHOME, AND SAYING SO IS WHAT MAKES IT DEFENSIBLE.**
  Gokuwmon's three cited parents (Hanumon `penc-nso`, Kinkakumon `penc-ds`, Turuiemon `penc-vb`)
  are all off `penc-sw`, the Saiyu Warriors line it obviously belongs to, so the in-edge rests on
  Ginkakumon — the twin of the Kinkakumon that US-153 and US-157 both pinned as this line's rehome
  candidate. Rehome Kinkakumon and the arrow becomes a citation. Entmon is the other one and is the
  first LINE argument this series has had to make about a CLIMB rather than an in-edge: all three
  of its cited `Evolves To` are undrawable (Ancient Troiamon idle-only, Ornismon and Xuanwumon no
  sheet at all).

- **TWO SHEETS OF ONE DIGIMON ARE TWO NODES — THE OPPOSITE OF US-143's ANSWER, AND FOR THE SAME
  REASON.** `Perfect/Fantomon.png` and `Perfect/Phantomon.png` are both on disk, so the roster has
  two entries and the graph now has two nodes; US-143 merged HolyAngemon and MagnaAngemon because
  ONE sheet served two names. The rule is the roster's: **one entry per sheet.** What must not
  happen is one Champion offering the same Digimon twice under two spellings, so Fantomon hangs off
  Wizarmon while Phantomon keeps Bakemon, and a test pins that the two parent sets are disjoint.

- **The knock-on list for this rung, all twenty-two files:** node count in `ElementCatalogTests`,
  `EvolutionGraphValidatorTests`, all six `AdultSweep*Tests`, all three `ChildSweep*Tests` and
  `PerfectSweepAToCTests` (672 -> 693); the per-line dict in `EvolutionGraphTests` (`dmc-v5` 22->23,
  `penc-nsp` 34->36, `penc-ds` 39->40, `penc-nso` 46->49, `penc-wg` 37->39, `penc-me` 50->51,
  `penc-vb` 53->54, `tamers` 90->94, `wanyamon` 20->24, `penc-sw` 12->14) and the same lines'
  `sizes[...]` rows in every `*Sweep*Tests` and `DigitamaSweepTests`; `inLine.count` AND `sweepEggs`
  in ALL SIX `Pendulum*TreeTests`; the dead-end ledger in `ChildSweepAToFTests` (97 -> 90) and its
  twin in `PerfectSweepAToCTests`; BOTH egg lists in `EggHatchingTests`; `strandedPerfect` and
  `strandedUltimate` in `EvolutionCriteriaTests` (Gokuwmon and Seiten Gokuwmon inherit Ginkakumon's
  strandedness — `penc-sw` still has no Digitama); the `gazimonLineVerifiedSet` in `SeedRosterTests`
  (Gigadramon appended, per the "append, never widen" rule); the alias list in `RosterTests` and the
  matching comment in `Sources/DexModel.swift` (both lost `extyranomon`); `DMCVersion5TreeTests`
  throughout, plus its `laterSweeps` set; `AdultSweepSToTTests`' leaf list (Starmon left) and its
  `extyranomon` pin; the `penc-sw` Perfect-rung pins in `AdultSweepHToLTests` and
  `AdultSweepUToZTests`; `AdultSweepAToDTests`' `wanyamon` Perfect set, relaxed to a superset;
  `PerfectSweepAToCTests`' "raptordramon has exactly two edges", relaxed to "exactly one fallback";
  and the "names its device" tests in `BabyIISweepTests`, `ChildSweepAToFTests` and
  `DigitamaSweepBabyITests`, which is why Seiten Gokuwmon's comment says "Saiyu Warriors".
  `MinigameAssignmentTests`, `DexModel.lineTitles` and both `lineDefaults` did NOT move: no new line.

- REMAINS: US-159 onward (45/56 passing). Ten Perfect and Ultimate sweeps and the Armor-Hybrid one
  hold the remaining 235 orphans: 69 Perfect, 150 Ultimate, 16 Armor-Hybrid. Ebidramon is now one
  earned branch off the ceiling (only spirit free) and is pinned as such. Kinkakumon and Xiquemon
  are still `penc-sw` rehome candidates, Cargodramon still `commandramon`'s and Boutmon still
  `vital`'s. Sagomon, Sanzomon and Shawujinmon are the three Saiyu Warriors Perfects still owed.
  The 90 leaves in `ChildSweepAToFTests`' dead-end ledger are still owed an out-edge; the seven
  Ultimates this story opened are NOT on it, since the ledger stops below the top rung.
---

## 2026-07-23 - US-159: Orphan sweep: Perfect H-L

Wired the ELEVEN playable Perfect whose display name begins H-L that were still orphans, for eleven
new Perfect nodes plus FIVE new Ultimates. Third story at the Perfect rung.

**Orphan count, Appendix B over a regenerated `roster.generated.json`: 235 BEFORE -> 219 AFTER.**
Perfect bucket 69 -> 58 (the H-L band 11 -> 0), Ultimate bucket 150 -> 145. Graph 693 -> 709.
Files changed: `Resources/evolutions.json`, `Resources/elements.json`, `Resources/moves.json`,
`scripts/us159_author.py` (new, the record of what was authored), `Tests/PerfectSweepHToLTests.swift`
(new, 27 tests), and eighteen existing test files. Full suite 2264 green (2237 before), build green,
validator zero findings.

The eleven, each `Champion -> Perfect -> Ultimate`:
  Hangyomon          ebidramon(penc-ds)        -> vikemon
  Hisyaryumon        omekamon(penc-me)         -> ouryumon         NEW
  Insekimon          icemon(penc-nso)          -> pencnso_boltmon
  Jazarichmon        jazardmon(tamers)         -> metallicdramon   NEW
  Karatenmon         igamon(wanyamon)          -> tengumon         NEW
  LadyDevimon        kyubimon(tamers)          -> beelzebumon
  LadyDevimon X      numemon_x(tamers)         -> beelstarmon_x    NEW
  Lavogaritamon      lavorvomon(penc-nso)      -> volcanicdramon   NEW
  Lilamon            sunflowmon(palmon)        -> rosemon
  Lilimon X          togemon_x(palmon)         -> rosemon
  Lucemon Falldown   pencnso_devimon(penc-nso) -> venomvamdemon

**Learnings for future iterations:**

- **NINE OF ELEVEN IN-EDGES CAME OFF A LEAF CHAMPION — THE LARGEST FALL THE DEAD-END LEDGER HAS
  TAKEN (90 -> 81) — AND EVERY JUNK FLOOR ALREADY EXISTED, SO `junkIds` DID NOT MOVE FOR THE SECOND
  STORY RUNNING.** US-157 cleared nine and had to invent Pandamon; US-158 cleared seven and invented
  nothing; this cleared nine and invented nothing. **Price the in-edge off `ChildSweepAToFTests`'
  ledger FIRST**: at this rung the cheap parent is nearly always a leaf, and a leaf costs only an
  `isDefault` edge onto a junk Perfect its line already has. Expect the M-Z sweeps to keep the shape.

- **A JUNK CHAMPION IS A LEGAL PARENT, AND SOMETIMES THE ONLY ONE — AND THAT IS WHAT PROMOTED THE
  TERRIER EGG.** Numemon X is `tamers`' junk Champion (US-148) and is the ONLY cited parent in this
  graph for LadyDevimon X: the bolded one is LadyDevimon itself (a Perfect, so never an in-edge) and
  Velgrmon is Armor-Hybrid. So the earned branch hangs off the junk fall — the Scumon arrangement
  US-133 recorded, now the third of its kind after Raremon and Scumon. The consequence is the
  strangest thread in the file: **Terriermon's ONLY onward edge is the junk fall, so Terrier Digitama
  reaches a Mega only by being neglected first**, which is what took it and Rena Digitama off
  `EggHatchingTests`' unraisable list. Prove a junk parent in BOTH directions through the engine —
  earned branch for a raised Digimon, junk fall for a neglected one — rather than reasoning about it.

- **THAT WAS THE LAST EGG PROMOTION ANY PERFECT SWEEP CAN MAKE.** After US-159 no egg on a line that
  HAS a Perfect rung is unraisable: the eleven still on `EggHatchingTests`' list sit on
  `commandramon`, `algomon`, `diablomon`, `vital` and `adventure02`, and not one of those five lines
  has a Perfect rung at all. Pinned as a disjointness claim rather than a name list in
  `PerfectSweepHToLTests`, so it fails the day someone opens one of those rungs and forgets the eggs.

- **A VARIANT'S ONE POSSIBLE PARENT CAN DECIDE WHERE ITS BASE FORM GOES — READ THE PAIR TOGETHER
  BEFORE CHOOSING EITHER LINE.** LadyDevimon has forty cited parents across nine lines and Piemon on
  `penc-nso` was the obvious reading; LadyDevimon X has exactly one usable parent anywhere,
  Numemon X on `tamers`. Choosing LadyDevimon's line off its OWN citations would have split the pair
  across two lines and broken the variant rule. Same shape as US-158's DarkKnightmon/DarkKnightmon X
  but read from the constrained end: **when a range holds a base form and its X, solve the X first.**

- **A BOLDED PARENT CAN BE UNDRAWABLE FOR A REASON OF SHAPE RATHER THAN OF ART, AND THAT IS A NEW
  KIND OF "cannot".** Lucemon Falldown's sole bolded `Evolves From` is Lucemon — which is a CHILD on
  `dmc-v3`, and `GraphValidationError.invalidStageTransition` refuses a Child -> Perfect edge. Every
  earlier "cannot" in this series was dexOnly art or a missing sheet; check the STAGE of a bolded
  parent before pricing it, and when the canonical arrow is illegal say so in the comment (a test
  greps for it) instead of quietly substituting a Champion.

- **TWO REHOME PINS LEFT, BOTH BECAUSE THE CITED PARENT SITS ON A LINE WITH NO PERFECT RUNG.**
  Hisyaryumon's bolded parents (Ginryumon, Ryudamon) are `commandramon`, so it took the cited
  Omekamon on `penc-me` and is pinned as `commandramon`'s rehome candidate; LadyDevimon X's other
  drawable parent, Mantaraymon X, is `vital`, so it joins Boutmon as that line's. Both pins are node
  comments AND tests. That makes the standing list: Kinkakumon and Xiquemon for `penc-sw`,
  Cargodramon and Boutmon and LadyDevimon X for `vital`/`commandramon`, Hisyaryumon for
  `commandramon`. Opening one of the six Perfect-less rungs should sweep that list first.

- **`EBIDRAMON IS NOW FULL` — the handover US-158 wrote was spent by the very next story.** The
  bolded Hangyomon arrow took its last free energy (spirit); vitality/stamina/strength went to
  Anomalocarimon, Anomalocarimon X and Gusokumon. Four earned branches plus the junk fall is five
  edges, which is `EvolutionCriteriaTests`' ceiling exactly. `PerfectSweepDToGTests`' claim was
  FLIPPED to `Set(EnergyType.allCases)` rather than deleted. A `penc-ds` Perfect after this needs a
  different Champion, not a fifth edge.

- **SCRIPT THE MECHANICAL KNOCK-ONS, BUT NOT WITH "APPEND TO THE LAST STRING LITERAL".** The
  node-count and per-line-size edits span twenty files, so they were done with a small Python pass;
  it appended its note to the last string in the XCTAssertEqual call, which for a message-less
  `XCTAssertEqual(sizes["palmon"], 26)` is the DICTIONARY KEY — three assertions silently became
  `sizes["palmon, plus US-159's two"]` and failed with `nil is not equal to`. Cheap to spot, cheaper
  to avoid: only append when the call already has a message argument.

- **The knock-on list for this rung, all nineteen files:** node count 693 -> 709 in
  `ElementCatalogTests`, `EvolutionGraphValidatorTests`, all six `AdultSweep*Tests`, all three
  `ChildSweep*Tests` and both `PerfectSweep*Tests`; the per-line dict in `EvolutionGraphTests`
  (`palmon` 26->28, `penc-ds` 40->41, `penc-me` 51->53, `penc-nso` 49->53, `tamers` 94->99,
  `wanyamon` 24->26) and the same lines' `sizes[...]` rows in every `*Sweep*Tests` and
  `DigitamaSweepTests`; `inLine.count` AND `sweepEggs` in `PendulumDeepSaversTreeTests`,
  `PendulumMetalEmpireTreeTests` and `PendulumNightmareSoldiersTreeTests`; the dead-end ledger in
  `ChildSweepAToFTests` (90 -> 81, nine cleared) and its twin in both `PerfectSweep*Tests`; the leaf
  lists in `AdultSweepHToLTests` (five left), `AdultSweepMToRTests` (two) and `AdultSweepSToTTests`
  (two); `AdultSweepMToRTests`' still-unwired list (Hisyaryumon left it, re-pinned with the parent it
  took); `AdultSweepUToZTests`' "LadyDevimon is still unwired" claim, FLIPPED to name Kyubimon;
  `PerfectSweepAToCTests`' `beelzebumon` parent SET (Baalmon + LadyDevimon) and its `rena`/`terrier`
  egg pins; `PerfectSweepDToGTests`' Ebidramon energies, its `wanyamon` Ultimate set (relaxed to a
  superset for Tengumon) and its egg pins; BOTH lists in `EggHatchingTests`. `EvolutionCriteriaTests`
  did NOT move at all — no new junk floor and no new stranded node, which is the first sweep at this
  rung where that file was untouched. `MinigameAssignmentTests`, `DexModel.lineTitles`, `RosterTests`
  and both `lineDefaults` did not move either: no new line.

- REMAINS: US-160 onward (46/56 passing). Nine Perfect and Ultimate sweeps and the Armor-Hybrid one
  hold the remaining 219 orphans: 58 Perfect, 145 Ultimate, 16 Armor-Hybrid. Six lines still have no
  Perfect rung — `xros`, `vital`, `adventure02`, `algomon`, `commandramon`, `diablomon`. Sagomon,
  Sanzomon and Shawujinmon are still the three Saiyu Warriors Perfects owed. Ogudomon is pinned in
  Lucemon Falldown's comment as belonging over whichever Demon Lord line a later sweep opens. The 81
  leaves in `ChildSweepAToFTests`' dead-end ledger are still owed an out-edge; the five Ultimates
  this story opened are NOT on it, since the ledger stops below the top rung.
---

## 2026-07-23 - US-160: Orphan sweep: Perfect M

Wired the TWENTY-ONE playable Perfect whose display name begins with M that were still orphans —
the biggest band the Perfect rung has — for 21 Perfect nodes, FIVE new Ultimates and ONE junk
Perfect floor. Fourth story at the Perfect rung.

**Orphan count, Appendix B over a regenerated `roster.generated.json`: 219 BEFORE -> 193 AFTER.**
Perfect bucket 58 -> 37 (the M band 21 -> 0), Ultimate bucket 145 -> 140. Graph 709 -> 736.
Files changed: `Resources/evolutions.json`, `Resources/elements.json`, `Resources/moves.json`,
`scripts/us160_author.py` (new, the record of what was authored), `Tests/PerfectSweepMTests.swift`
(new, 28 tests), and twenty-six existing test files. Full suite 2292 green (2264 before), build
green, validator zero findings.

The twenty-one, each `Champion -> Perfect -> Ultimate`:
  MachGaogamon          gaogamon(wanyamon)          -> ancientvolcamon
  Mamemon X             greymon_blue(dmc-v1)        -> banchomamemon
  Mammon X              shimaunimon(penc-nso)       -> pencnso_skullmammon
  Manticoremon          growmon(tamers)             -> dukemon             NEW
  MarinBullmon          shellmon(dmc-v3)            -> ryugumon            NEW
  MarinChimairamon      octmon(penc-ds)             -> plesiomon
  MegaSeadramon X       hyougamon(penc-nsp)         -> metalseadramon
  MegaloGrowmon Orange  growmon_orange(tamers)      -> dukemon             NEW
  MegaloGrowmon X       blackgalgomon(tamers)       -> chaosdukemon
  Meicrackmon           meicoomon(diablomon)        -> rasielmon           NEW
  Meicrackmon Vicious   meicoomon(diablomon)        -> raguelmon           NEW
  Mephismon             wizarmon(penc-nso)          -> piemon
  Mephismon X           pencnso_devimon(penc-nso)   -> dinorexmon          NEW
  Mermaimon             ikkakumon(penc-ds)          -> vikemon
  MetalGreymon Virus X  devimon(dmc-v1)             -> blitzgreymon
  MetalMamemon X        thunderballmon(penc-me)     -> princemamemon
  MetalPhantomon        bakemon(dmc-v3)             -> gokumon
  MetalTyranomon V2     darktyranomon(dmc-v5)       -> mugendramon
  MetalTyranomon X      cyclomon(dmc-v5)            -> mugendramon
  Monzaemon X           numemon(dmc-v1)             -> dmcv1_shinmonzaemon
  Mummymon              pencnso_bakemon(penc-nso)   -> deathmon
plus the junk floor `diablomon_gerbemon` under Meicoomon.

**Learnings for future iterations:**

- **A JUNK FLOOR CAN HAVE TO BE A LINE-SCOPED ALIAS, BECAUSE THE JUNK-FLAVOURED SHEETS ARE GONE.**
  US-148/151/157 each spent an unused sheet — Numemon X, CatchMamemon, Karakurumon, Pandamon — so
  each floor also removed an orphan. By this story not ONE of the 58 Perfect still orphaned is a gag
  Digimon (they are Okuwamon, Paildramon, Rapidmon, Vamdemon X…), so `diablomon`'s floor draws the
  Gerbemon sheet under a scoped id, the `dmcv2_vademon` pattern. **Consequences to price in
  advance:** it removes NO orphan, `RosterTests`' alias list moves, and `EvolutionCriteriaTests.
  junkIds` gains its first scoped name. Every remaining line without a Perfect rung — `xros`,
  `vital`, `adventure02`, `algomon`, `commandramon` — will pay the same way.

- **FIFTEEN OF TWENTY-ONE PLACEMENTS WERE FREE, AND THE RATIO IS CUMULATIVE RATHER THAN LUCKY.**
  US-157 managed 10/19, US-158 7/14, US-159 6/11, this one 15/21 — because three sweeps' worth of
  Megas are now in place, so US-152's "intersect `Evolves From` against `Evolves To`" closes on a
  line that already has both ends far more often. **Price an M-Z or Ultimate sweep off that**: the
  expensive node is now the one whose whole family is on a line with no Mega, not the average one.

- **THE VARIANT RULE HAS A STRONGER FORM THAN "same line": SAME PARENT — AND AT THE PERFECT RUNG IT
  IS USUALLY AVAILABLE AND ALWAYS CHEAPER.** Six of this story's nine variants hang off the very
  Champion their base form hangs off (Mammon X off ShimaUnimon, MetalGreymon Virus X off Devimon,
  MetalTyranomon V2 off DarkTyranomon and X off Cyclomon, Monzaemon X off Numemon, MegaloGrowmon
  Orange off Growmon (Orange)), which costs no new argument at all and makes the climb the base
  form's own Mega for free. Look up `graph.parents(of: base)` BEFORE reading the variant's own
  `Evolves From` — it is often a better answer than the citation list, and it is what
  `AdultSweepEToGTests.testTheOneVariantSitsWithItsBaseForm` already asserts.

- **AND THE RULE HAS A LIMIT: A BASE FORM'S LINE CAN HOLD THE CLIMB AND NO CITED PARENT.**
  MetalMamemon is `dmc-v2`, whose Metal Garurumon IS cited — but `dmc-v2` has no Champion on
  MetalMamemon X's page, so honouring the line meant inventing an arrow at BOTH ends. It followed
  Thunderballmon on `penc-me` to the cited Prince Mamemon instead, which is
  `ChildSweepMToZTests.testEveryVariantSitsWithItsBaseFormOrFollowsACitedParent`'s escape hatch.
  **Check both ends of the base form's line before promising the variant to it.**

- **A BOLDED PARENT CAN BE WORTH SPENDING ON SOMETHING ELSE — SAY SO IN THE COMMENT.** Growmon is
  MegaloGrowmon Orange's bolded parent AND `tamers`' last leaf Champion. The Orange form is pinned
  to Growmon (Orange) by the variant rule anyway, while Manticoremon's only other readings were
  Mimicmon (`algomon`), Reppamon and Tia Ludomon (`vital`) — each of which would have opened a
  Perfect rung, a junk floor and a Mega for one node. So the leaf went to Manticoremon and bought
  Dukemon, the Mega `tamers` had been owed since US-151. **When a leaf is contested, give it to the
  claimant whose alternatives cost a whole rung.**

- **`diablomon` COST THREE EXTRA NODES AND PROMOTED TWO EGGS, AND FOUR LINES ARE LEFT.** Meicoomon is
  the only parent this pack can draw for either Meicrackmon, so the rung had to open; US-158's rule
  ("owe a Mega in the same story") made it Perfect + Ultimate + junk floor at once, and
  `MainScreenModel.startingDigitamaId` then promoted Kera and Meicoo Digitama — `EggHatchingTests`
  moved in BOTH directions. US-159 said no Perfect sweep could promote another egg; that was right
  about lines that HAVE a Perfect rung and wrong about opening one. The nine eggs still unraisable
  sit on `commandramon`, `algomon`, `vital` and `adventure02`. **`vital` is the cheapest rung left
  to open — 33 nodes, ten Champions, every one of them a leaf — and it already has three pinned
  rehome candidates (Boutmon, Cargodramon, LadyDevimon X).**

- **AN ORPHAN'S "no drawable parent" CAN BE TOTAL, NOT JUST INCONVENIENT.** Mephismon X's whole
  `Evolves From` is Mephismon (a Perfect) and Velgrmon (Armor-Hybrid, and not a node at all) — one
  worse than US-159's LadyDevimon X, which at least had Numemon X. The answer is the variant rule
  plus a flavour argument among the chosen line's Champions, and the comment has to ADMIT it; the
  new test greps for "NO DRAWABLE CITED PARENT". Same for MachGaogamon's climb: both its bolded
  `Evolves To` (Mirage Gaogamon, Z'd Garurumon) are idle-only, so `edgeToDexOnlyNode` forbids the
  canonical top of that thread forever.

- **WIKIMON PAGE TITLES FOR X-ANTIBODY FORMS ARE SPACED, AND THE UNSPACED GUESS RETURNS AN EMPTY
  BODY RATHER THAN A REDIRECT.** `MegaSeadramon (X-Antibody)` is 0 bytes; `Mega Seadramon
  (X-Antibody)` is the page. Same for Megalo Growmon, Metal Greymon (Virus), Metal Mamemon, Metal
  Tyranomon. **Fetch with the spaced spelling first and treat a zero-length body as "try the other
  spacing", not as "no page".** Plain forms redirect (`MachGaogamon` -> `Mach Gaogamon`); the
  parenthesised ones do not.

- **`Set(graph.parents(of:))` EQUALITY ON AN EARLIER STORY'S ULTIMATE IS THE ASSERTION A PERFECT
  SWEEP BREAKS, AND THERE ARE THREE SUCH FILES NOW.** This story hung MegaloGrowmon X under
  US-157's ChaosDukemon and MachGaogamon under US-158's AncientVolcamon. Fix by NAMING the new
  parent in that story's `authoredUltimates` table, never by loosening to a superset — and grep
  `authoredUltimates` across `PerfectSweep*Tests` BEFORE choosing a climb, the same way US-151 said
  to grep `Set(graph.parents`.

- **A `junkFloors` / `swept` table in an earlier sweep asserts `evolutions.count == 2`, and that
  breaks the moment you branch that Champion again.** Three did here: Gaogamon in
  `PerfectSweepDToGTests`, ShimaUnimon in `AdultSweepSToTTests`, and this story's own Meicoomon
  (which carries two Perfects). The fix that keeps the claim is a NAMED exception
  (`parent == "gaogamon" ? 3 : 2`), not a `>=`. Add `grep -n "evolutions.count, 2" Tests` to the
  pre-flight list beside `inLine.count` and `sweepEggs`.

- **The knock-on list for this rung, all twenty-six files:** node count 709 -> 736 in
  `ElementCatalogTests`, `EvolutionGraphValidatorTests`, all six `AdultSweep*Tests`, all three
  `ChildSweep*Tests` and all three `PerfectSweep*Tests`; the per-line dict in `EvolutionGraphTests`
  (`dmc-v1` 32->35, `dmc-v3` 48->51, `dmc-v5` 23->25, `diablomon` 17->22, `penc-ds` 41->43,
  `penc-me` 53->54, `penc-nso` 53->58, `penc-nsp` 36->37, `tamers` 99->103, `wanyamon` 26->27) and
  the same lines' `sizes[...]` rows in every `*Sweep*Tests`, `BabyIISweepTests` and
  `DigitamaSweepTests`; `inLine.count` AND `sweepEggs` in `PendulumDeepSaversTreeTests`,
  `PendulumMetalEmpireTreeTests`, `PendulumNatureSpiritsTreeTests` and
  `PendulumNightmareSoldiersTreeTests`; `laterSweeps` in `DMCVersion5TreeTests`; `nodes.count` and
  `connectors.count` for `dmc-v1` in `EvolutionTreeTests`; the dead-end ledger in
  `ChildSweepAToFTests` (81 -> 77) and its twin in all three `PerfectSweep*Tests`; the leaf lists in
  `AdultSweepHToLTests` (Hyougamon) and `AdultSweepMToRTests` (Meicoomon); `AdultSweepMToRTests`'
  still-unwired list (Mamemon X and Monzaemon X left it, re-pinned with the parents they took) and
  `AdultSweepSToTTests`' MetalTyranomon X claim, FLIPPED to name Cyclomon; the "lines with no
  Perfect rung" set in `AdultSweepUToZTests` and all three `PerfectSweep*Tests` (`diablomon` gone);
  `EvolutionCriteriaTests.junkIds`; `RosterTests`' alias list; `SeedRosterTests`' `patamonLine`
  and `gazimonLine` verified sets; BOTH lists in `EggHatchingTests`. `MinigameAssignmentTests`,
  `DexModel.lineTitles` and both `lineDefaults` did not move: no new line.

- REMAINS: US-161 onward (47/56 passing). Eight Perfect and Ultimate sweeps and the Armor-Hybrid one
  hold the remaining 193 orphans: 37 Perfect, 140 Ultimate, 16 Armor-Hybrid. Five lines still have
  no Perfect rung — `xros`, `vital`, `adventure02`, `algomon`, `commandramon`. Sagomon, Sanzomon and
  Shawujinmon are still the three Saiyu Warriors Perfects owed. Ogudomon is still pinned in Lucemon
  Falldown's comment. The 77 leaves in `ChildSweepAToFTests`' dead-end ledger are still owed an
  out-edge; the five Ultimates this story opened are NOT on it, since the ledger stops below the
  top rung.
---

## 2026-07-23 - US-161: Orphan sweep: Perfect N-R

Authored the fifteen orphaned Perfect whose displayName starts N-R, the seven Ultimates they climb
into, and the two junk Perfect floors the two lines this story OPENS needed first — 24 nodes,
736 -> 760. Perfect orphans 37 -> 22; total playable orphans 193 -> 171 (Ultimate 140 -> 133,
Armor-Hybrid still 16).

Files: `Resources/evolutions.json` (+24 nodes, +6 junk-floor edges on former leaf Champions),
`Resources/elements.json`, `Resources/moves.json`, new `Tests/PerfectSweepNToRTests.swift`,
new `scripts/us161_author.py`, and the 26-file knock-on set the US-160 note predicted.

Build green, 2322 tests pass, `testShippedEvolutionsJsonIsValid` reports zero validator findings.

**Learnings for future iterations:**

- **THE TWO CHEAPEST REMAINING RUNGS ARE NOW OPEN, AND `vital` COST EXACTLY WHAT US-160 PRICED IT
  AT.** US-160 called `vital` "the cheapest rung left to open — 33 nodes, ten Champions, every one
  of them a leaf". It was: Kokeshimon and Tia Ludomon each needed a floor before either could
  branch, and one alias (`vital_darumamon`) served BOTH, because the floor is per-LINE and not
  per-Champion. `xros` paid the same bill with one alias for one Champion. **A line that opens with
  N leaf Champions still buys only ONE junk floor** — price the alias once per line, not once per
  parent, or you will over-estimate the next rung by a factor of N.

- **A PRE-EXISTING JUNK FLOOR THAT IS NOT ITSELF A LEAF IS THE ONLY FREE ONE.** Four of the six
  floors here pre-dated the story, but they are not equivalent: CatchMamemon and Karakurumon are
  terminal, so leaning on them adds a dead end to the ledger, while `locomon` already climbs to
  GrandLocomon and cost `penc-me` nothing at all. **Prefer a junk Perfect that already has an
  out-edge when the line offers a choice** — `grep -n '"'"'junkIds'"'"' Tests/EvolutionCriteriaTests.swift`
  and check each candidate's `evolutions` before picking.

- **THE COUNT-THE-FLOORS ASSERTION IS THE ONE A REUSED FLOOR BREAKS, AND IT FAILS AS "test wrong",
  NOT "data wrong".** The only red in the whole 2322 was
  `testTheSixLeafChampionsGainedTheirLinesJunkFloor`, whose bookkeeping line said two floors
  pre-dated the story when three did — it had been written from the two aliases and never
  re-counted after `locomon` was chosen for Kuwagamon X. **When a sweep's own new test fails on a
  set-difference of its own tables, check the TABLE against `evolutions.json` before touching the
  data**; here the data was right and the assertion was stale.

- **WRITE THE `subtracting(...)`/`intersection(...)` LEDGERS LAST.** Every sweep since US-157 has
  had a pair of these lines partitioning "authored by me" from "already there". They are derived
  from choices made while authoring, so authoring them from the plan rather than from the finished
  `JUNK_FLOORS` dict is how this failure happens. Generate them from the script's own tables at the
  end of the run.

- **THE KNOCK-ON SET IS NOW STABLE AT THE 26 FILES US-160 LISTED** — node count 736 -> 760 in
  `ElementCatalogTests`, `EvolutionGraphValidatorTests`, all six `AdultSweep*Tests`, all three
  `ChildSweep*Tests` and all four `PerfectSweep*Tests`; the per-line dict in `EvolutionGraphTests`
  and the same lines' `sizes[...]` rows everywhere; `inLine.count` AND `sweepEggs` in the four
  Pendulum tree tests; `EvolutionCriteriaTests.junkIds` (two scoped names) and `RosterTests`' alias
  list. **The list in the US-160 note is still accurate — read it before starting a Perfect or
  Ultimate sweep rather than rediscovering it.**

- REMAINS: US-162 onward (48/56 passing). 171 orphans — 22 Perfect, 133 Ultimate, 16 Armor-Hybrid.
  Only `adventure02`, `algomon` and `commandramon` still lack a Perfect rung, and each will pay the
  line-scoped-alias price. Sagomon, Sanzomon and Shawujinmon are still the three Saiyu Warriors
  Perfects owed; Ogudomon is still pinned in Lucemon Falldown's comment. The Ultimate rung is now
  the bulk of the work: 133 orphans against seven remaining sweeps.
---

## 2026-07-23 - US-162: Orphan sweep: Perfect S-Z (the Perfect rung is finished)

Wired the TWENTY-TWO orphaned Perfect whose displayName starts S-Z — which was every Perfect still
orphaned anywhere — the three Ultimates they climb into that had no node, and the two junk Perfect
floors the two lines this story OPENS needed first. 27 nodes, 760 -> 787. **The Perfect rung is
closed: no later sweep can add to it.**

**Orphan count, Appendix B over a regenerated `roster.generated.json`: 171 BEFORE -> 146 AFTER.**
Perfect bucket 22 -> 0, Ultimate 133 -> 130, Armor-Hybrid still 16.
Files: `Resources/evolutions.json`, `Resources/elements.json`, `Resources/moves.json`,
`scripts/us162_author.py` (new, the record of what was authored), `Tests/PerfectSweepSToZTests.swift`
(new, 31 tests) and the 26-file knock-on set US-160 listed. Build green, 2353 tests pass (2322
before), `testShippedEvolutionsJsonIsValid` reports zero validator findings.

The twenty-two, each `Champion -> Perfect -> Ultimate`:
  Sagomon              lianpumon(penc-sw)         -> shakamon
  Sanzomon             hakubamon(penc-sw)         -> shakamon
  SaviorHackmon        firamon(penc-nso)          -> pencnso_boltmon
  Scorpiomon           kuwagamon_x(penc-me)       -> pencme_mugendramon
  Sekkamon             shellmon(dmc-v3)           -> ryugumon
  Shawujinmon          tsuchidarumon(penc-sw)     -> shakamon
  Shishimamon          reppamon(vital)            -> zanbamon
  Shootmon             minotaurmon(penc-me)       -> kazuchimon
  Sirenmon             hookmon(vital)             -> regalecusmon        NEW
  SkullBaluchimon      damemon(commandramon)      -> chaosdramon_x       NEW
  Superstarmon         omekamon(penc-me)          -> princemamemon
  Tekkamon             guardromon(penc-me)        -> pencme_hiandromon
  Triceramon           monochromon(dmc-v4)        -> darkdramon
  Triceramon X         ginryumon(commandramon)    -> chaosdramon_x       NEW
  Vamdemon X           musyamon(penc-nso)         -> venomvamdemon
  Vermillimon          nisedrimogemon(adventure02)-> blackwargreymon     NEW
  WaruSeadramon        pencds_seadramon(penc-ds)  -> leviamon
  WereGarurumon Black  garurumon_black(dmc-v2)    -> cresgarurumon
  WereGarurumon X      pencnso_garurumon(penc-nso)-> pencnso_metalgarurumon
  Xingtianmon          ginkakumon(penc-sw)        -> seitengokuwmon
  Yatagaramon          xv-mon_black(penc-wg)      -> hououmon
  Yatagaramon 2006     pencwg_birdramon(penc-wg)  -> griffomon
plus the junk floors `commandramon_karakurumon` and `adventure02_jyagamon`.

**Learnings for future iterations:**

- **A LINE'S PERFECT RUNG OPENS UNDER ITS JUNK CHAMPION, NOT UNDER ITS BEST ONE — AND THAT IS WHAT
  PROMOTES THE EGGS.** US-161 left `adventure02` closed because XV-mon carries only V Digitama and
  branching it would have stranded Worm Digitama. The answer it missed is Nise Drimogemon, the
  line's junk Adult, which takes the `isDefault` fall of V-mon, Wormmon AND Tinkermon — so ONE
  earned branch off it promoted both eggs at once. **Before pricing a rung, look at which Champion
  every Child of the line falls into, not at which Champion is cited**; the junk one is reachable
  from every thread by construction, and US-133's Scumon rule already makes an earned branch off it
  legal. Same shape one line over: `commandramon`'s Damemon catches both its Children's falls.

- **A LINE CAN BE PERMANENTLY UNOPENABLE, AND `algomon` IS ONE.** Its only egg, Ghost Digitama,
  descends to Algomon (Adult), Mimicmon and Witchmon and stops; the Labramon -> Siesamon thread —
  the only `algomon` Champion any Perfect orphan cites — hangs off `paomon`, one of the thirteen
  Baby I no Digitama can reach since US-145 spent all fifty-seven. So opening the rung under
  Siesamon would have broken US-159's "no unraisable egg on a line that HAS a Perfect rung"
  invariant, and the Perfect rung is now closed, so no story can open it with a real orphan again.
  Shishimamon went to `vital` under Reppamon instead, with the cited Zanbamon already above it.
  **Check `reachesUltimate` from the line's own eggs BEFORE choosing the Champion, not after** — the
  test that catches this is `PerfectSweepNToRTests.testVitalsFiveDigitamaWereAllPromotedAtOnce`, and
  it fires from another story's file, which is easy to miss.

- **A JUNK FLOOR CAN BE UNCITED, AND THEN THE COMMENT HAS TO SAY THE WORD FLAVOUR.** Every floor
  from US-157 through US-161 was on its own Champion's Wikimon `Evolves To`. Neither Damemon nor
  Ginryumon nor Nise Drimogemon cites a junk Perfect at all, so `commandramon_karakurumon` (a
  clockwork automaton over a line of mechanised soldiers) and `adventure02_jyagamon` (a fake mole
  digging up a potato) rest on shape alone. `RosterTests`' alias list and
  `EvolutionCriteriaTests.junkIds` both move, and the new test greps each comment for "FLAVOUR".

- **THE `Set(graph.parents(of:))` LEDGERS ARE NOW THE BULK OF THE KNOCK-ON WORK, AND SEVEN BROKE.**
  Shakamon (+3), Seiten Gokuwmon, Kazuchimon, Ryugumon, Zanbamon, `pencme_mugendramon` (in TWO
  files) and Andromon's thread test. Every one was fixed by NAMING the new parent in the earlier
  story's `authoredUltimates` table. Four `evolutions.count` exceptions moved the same way
  (Musyamon 2->3, Firamon and Ginkakumon 2->3, Omekamon 3->4, Kuwagamon X 3->4, XV-mon Black 3->4).
  **Run `grep -rn 'Set(graph.parents' Tests` and `grep -rn 'evolutions.count,' Tests` for every
  climb target and every parent BEFORE authoring** — it is cheaper than reading 152 failures.

- **A "still owed" PIN FLIPS IN THREE PLACES AT ONCE.** The three Saiyu Warriors Perfects US-157
  pinned were asserted `XCTAssertNil(graph.node(id:))` in `PerfectSweepAToCTests`,
  `PerfectSweepDToGTests` AND `PerfectSweepNToRTests`. Each became "…is on `penc-sw`" rather than
  being deleted. Likewise `penc-sw`'s Perfect-rung census is pinned in two Adult sweep files.
  `grep -rn '<the id>' Tests` for every orphan in the band before starting.

- **EVERY `penc-sw` NODE'S COMMENT MUST CONTAIN "Saiyu Warriors"** — three separate tests enforce it
  (`ChildSweepAToFTests`, `BabyIISweepTests`, `DigitamaSweepBabyITests`). It is the only line in the
  file with that rule, and it is invisible from the node you are writing.

- **SUPERSTARMON SPLIT FROM DARKSUPERSTARMON, WHICH IS THE FIRST BASE-FORM/VARIANT PAIR ON TWO
  LINES.** Starmon is the bolded parent and a `tamers` Adult, but `tamers` holds no cited climb for
  Superstarmon at all, while `penc-me` holds Omekamon AND Prince Mamemon — and Revolmon and
  Thunderballmon, the two other cited `penc-me` parents, are both FULL at three earned branches. So
  the pair is split on purpose and `PerfectSweepDToGTests.testTheVariantsLandedBesideTheirBaseForm`
  records it. **A "full" claim is worth re-counting (US-156's lesson) — here it held.**

- REMAINS: US-163 onward (49/56 passing). 146 orphans — 130 Ultimate, 16 Armor-Hybrid, 0 below.
  Every line now has a Perfect rung except `algomon`, which cannot gain one. Six Ultimate sweeps and
  the Armor-Hybrid one are all that is left. Rehome candidates still pinned: Kinkakumon on `penc-sw`,
  Hisyaryumon and Cargodramon on `commandramon` (whose rung is now OPEN, so both are live),
  Boutmon and LadyDevimon X on `vital`, Paildramon on `adventure02` (also live now). Ogudomon is
  still pinned in Lucemon Falldown's comment. The 67 leaves in `ChildSweepAToFTests`' dead-end
  ledger are still owed an out-edge; the three Ultimates this story opened are not on it.
---

## 2026-07-23 - US-163: Orphan sweep: Ultimate-Super Ultimate A-B (the first sweep at the top rung)

Wired all THIRTY orphaned Ultimate whose displayName starts A-B, for exactly thirty nodes — the
first Phase E story whose orphan count and node count are the same number, because **an Ultimate
sweep is an IN-EDGE sweep and nothing else**. The rung is terminal, so the AC's out-edge clause does
not bind; `EvolutionCriteriaTests.branchingNodes` filters to Child and Adult, so a forked Perfect
owes no junk floor; and every one of the thirty joined a line that already had a Perfect rung.

**Orphan count, Appendix B over a regenerated `roster.generated.json`: 146 BEFORE -> 116 AFTER.**
Ultimate bucket 130 -> 100, Armor-Hybrid still 16, nothing below either. 787 -> 817 nodes.
Files: `Resources/evolutions.json`, `Resources/elements.json`, `Resources/moves.json`,
`scripts/us163_author.py` (new, the record of what was authored), `Tests/UltimateSweepAToBTests.swift`
(new, 29 tests) and a 29-file knock-on set. Build green, 2382 tests pass (2353 before),
`testShippedEvolutionsJsonIsValid` and this story's own validator test both report zero findings.

The thirty, `Perfect -> Ultimate`:
  Agumon YnK           metalgreymon_virus(dmc-v1)     vitality
  Algomon              mummymon(penc-nso)             vitality
  Alphamon             grademon(tamers)               LEAF -> isDefault climb
  Alphamon Ouryuken    doruguremon(tamers)            spirit
  Amaterasumon         shishimamon(vital)             spirit
  AncientBeatmon       atlurkabuterimon_red(penc-nsp) stamina
  AncientMegatheriumon mammon(penc-nso)               vitality
  AncientMermaimon     mermaimon(penc-ds)             vitality
  AncientSphinxmon     mummymon(penc-nso)             spirit
  Anubimon             cerberumon_x(penc-me)          spirit
  Apocalymon           ladydevimon(tamers)            vitality (earned by LOSING)
  Arcturusmon          canoweissmon(penc-vb)          LEAF -> isDefault climb
  Ariemon              marinbullmon(dmc-v3)           strength
  Armagemon            chimairamon(dmc-v1)            stamina
  Armamon              omegashoutmon(xros)            vitality  STRANDED, inherited
  Bagramon             mametyramon(tamers)            LEAF -> isDefault climb
  BanchoLilimon        lilamon(palmon)                spirit
  Barbamon             mephismon(penc-nso)            spirit
  Barbamon X           mephismon(penc-nso)            strength
  Beelzebumon Blast    baalmon(tamers)                strength
  Beelzebumon X        baalmon(tamers)                vitality
  BelialVamdemon       vamdemon(penc-nso)             vitality
  Belphemon Rage       astamon(penc-me)               stamina
  Belphemon X          astamon(penc-me)               vitality
  BlackSaintGalgomon   blackrapidmon(tamers)          LEAF -> isDefault climb
  BlackSeraphimon      holyangemon(penc-vb)           strength (earned by LOSING)
  BlackWarGreymon X    metalgreymon_virus_x(dmc-v1)   stamina
  Blastmon             insekimon(penc-nso)            strength
  Breakdramon          megalogrowmon(tamers)          LEAF -> isDefault climb
  Brigadramon          cargodramon(penc-me)           strength

**Learnings for future iterations:**

- **THE ULTIMATE RUNG IS THE CHEAPEST RUNG IN PHASE E, AND THE NEXT FIVE SWEEPS SHOULD BE SCOPED
  THAT WAY.** No out-edge is owed (terminal), no junk floor is owed (`branchingNodes` is Child and
  Adult only), and no line can be opened because every line except `algomon` already has a Perfect.
  So one orphan costs exactly one node and one edge. Budget US-164..US-168 at ~20 orphans each with
  no structural work at all — the whole cost is the knock-on, which was 204 failures here.

- **THIS IS THE FIRST STORY TO FORK A PERFECT, AND THAT IS WHAT BROKE 204 TESTS.** Every Perfect in
  the file carried exactly one `isDefault` climb until now, and six `PerfectSweep*Tests` assert
  `evolutions.count == 1` over their own `swept` list while four device-tree files assert an exact
  `targets(of:)` list. Fix by NAMING (`branchedByUS163[perfect] ?? 1`) and by reading the climb as
  `first(where: \.isDefault)` rather than as `first` — never by loosening to a `>=`. **Run
  `grep -n "is not a single climb" Tests` and `grep -rn 'XCTAssertEqual(try targets(of:' Tests` for
  every parent BEFORE authoring**; those two greps are the new pre-flight for this rung.

- **A LEAF PERFECT'S FIRST EDGE MUST BE `isDefault`, SO IT CANNOT CARRY CRITERIA.**
  `SeedRosterTests.testEveryNonTerminalNodeHasExactlyOneDefaultEdge` fails a node with edges and no
  fallback, so an in-edge onto a leaf is the climb itself. That is also the AC-4 reading every rung
  below recorded: "no edge is unconditional" binds the EARNED edges, because US-020 takes an
  `isDefault` edge whatever its gates say. Five leaves were cleared this way (67 -> 62 on the ledger).

- **`SeedRosterTests.earnedPath` IS A TRAP AT THIS RUNG AND IT COST TWO PLACEMENTS.** That test
  walks US-002's three named lines by preferring the EARNED edge at every rung, so giving
  `lilimon` or `metalgreymon` an earned branch silently redirects "Palmon -> Togemon -> Lilimon ->
  Rosemon" or "MetalGreymon -> WarGreymon" to the new node. BanchoLilimon went to Lilamon (the
  sister form, cited on the same page) and Agumon YnK to MetalGreymon (Virus) (the same fork one
  arrow over) because of it. **`grep -n earnedPath Tests/SeedRosterTests.swift` before branching any
  Perfect on `palmon`, `dmc-v1` or `dmc-v2`.**

- **A LINE CAN BE CLOSED TO A NODE BY ART RATHER THAN BY SHAPE, AND `diablomon` IS THE FIRST.**
  Armagemon is what the Kuramon swarm becomes and `diablomon` is that line — but its bolded parent
  Kuramon is a Baby I (`invalidStageTransition`) and BOTH Perfect rungs the swarm climbs through,
  Chrysalimon and Infermon, are idle-only (`edgeToDexOnlyNode`). No story can author a Perfect into
  that line either, since US-162 closed the rung. Check the CITED line's Perfect rung for dexOnly
  art before assuming a thematic home is available.

- **AN X-ANTIBODY MEGA USUALLY HAS NO DRAWABLE PARENT OF ITS OWN, BECAUSE ITS WHOLE `Evolves From`
  IS ULTIMATES.** Barbamon X, Beelzebumon X and Belphemon X each cite only Ultimates (their own
  base form is the bolded one), so the strong variant rule — same PARENT, not merely same line — is
  the only reading available and is free: hang the variant on the Perfect its base form hangs on,
  with a distinct energy. Expect the same for every `*mon X` in the C-Z bands.

- **TWO WIKIMON PAGE TITLES IN THIS BAND ARE UNGUESSABLE AND `opensearch` IS THE ANSWER.**
  `Agumon YnK` is `Agumon -Yuki no Kizuna-` and `BlackWarGreymon X` is `Black War Greymon
  (X-Antibody)` — the spaced-title rule US-160 recorded, plus a sheet name that is an abbreviation
  of a subtitle. Use
  `curl -s "https://wikimon.net/api.php?action=opensearch&search=<name>&limit=50&format=json"`
  before concluding a Digimon has no page; a raw fetch of a wrong title returns an empty body, not
  a 404.

- **THE STRANDED LIST GAINS AT MOST ONE NODE PER SWEEP AT THIS RUNG, AND IT IS ALWAYS `xros`.**
  Armamon's only cited Perfect parent anywhere is OmegaShoutmon, and `xros` has no Digitama. Any
  A-Z Ultimate whose citations point only at `xros` or `penc-sw` inherits the same thing; add it to
  `EvolutionCriteriaTests.strandedUltimate` and let the parent loop prove it is inheritance.

- REMAINS: US-164 onward (50/56 passing). 116 orphans — 100 Ultimate (C-Z), 16 Armor-Hybrid, 0
  below. Five Ultimate sweeps and the Armor-Hybrid one are all that is left. 62 Perfect leaves are
  still owed a climb (`ChildSweepAToFTests`' ledger); MetalGreymon X, Huankunmon and
  BlackMachGaogamon are among them. Rehome candidates still pinned: Kinkakumon on `penc-sw`,
  Hisyaryumon and Cargodramon on `commandramon`, Boutmon and LadyDevimon X on `vital`, Paildramon
  on `adventure02`. Ogudomon is still pinned in Lucemon Falldown's comment and belongs to US-167.
---

## 2026-07-23 - US-164 (Orphan sweep: Ultimate-Super Ultimate C-D)
- Wired 20 of 22 orphaned Ultimate C-D as evolution nodes; left Chaosdramon and Chaosmon alone
  because they are Jogress results the DMC Ver.5/Ver.4 + Metal Empire trees reserve as non-nodes.
- Orphans 116 -> 96 (Ultimate 100 -> 80 by edge-count, 78 true once the two Jogress are set aside;
  Armor-Hybrid 16). Nodes 817 -> 837. Dead-end ledger 62 -> 60 (metalgreymon_x, huankunmon cleared).
- Files: Resources/{evolutions,elements,moves}.json (data), scripts/us164_author.py (authoring
  record), Tests/UltimateSweepCToDTests.swift (new), plus ~19 knock-on test files.
- **Learnings for future iterations:**
  - **A JOGRESS RESULT IS OBTAINABLE, SO IT IS NOT ALWAYS AN ORPHAN TO WIRE — BUT ONLY WHEN A DEVICE
    TREE RESERVES IT.** Chaosdramon and Chaosmon are drawn as the Jogress Ultra row in their device
    documents, and DMCVersion5TreeTests / DMCVersion4TreeTests / PendulumMetalEmpireTreeTests each
    assert `graph.node(id:) == nil` for them. Wiring them breaks those tests. BUT aegisdramon,
    millenniumon and cernumon are ALSO jogress results and ARE wired as nodes (no tree reserves
    them). So the rule is: before wiring an Ultimate, run `grep -l "<id>" Tests/*TreeTests.swift`
    and check jogress.json — if a tree pins it to no node, treat it as obtainable and exclude it
    from the sweep (coverage test reads "in-edge OR jogress-result"); otherwise wire it.
  - Appendix B's orphan script counts by EVOLUTION edges only, so a reserved jogress result still
    shows as an "orphan" by that metric. Report both numbers: the raw edge-count and the true
    remainder with jogress results subtracted.
  - **A VARIANT WHOSE BASE FORM IS NOT A NODE (dexOnly, OR a reserved jogress result) follows a
    cited parent.** Dynasmon (dexOnly) -> Dynasmon X on doruguremon; Chaosdramon (jogress) ->
    Chaosdramon V2 on metalgreymon_x. Chaosdramon cites Metal Greymon (X-Antibody), so the V2 hangs
    on the base form's cited parent, same shape as US-163's BlackWarGreymon X escape hatch.
  - The `branchedByUS163` dicts in the six PerfectSweep*Tests are the per-file record of which
    Perfects have been forked; renamed to `branchedBySweeps` and appended when US-164 forked more.
    A Perfect that gains a SECOND earned branch (deathmeramon, meicrackmon got two each: demon+demon_x,
    diablomon+diablomon_x) goes to 3 edges. Grep `evolutions.count, 2` / `branchedBy` before authoring.
  - HolyAngemon is now the FIRST four-edge Perfect (cherubimon_virtue, blackseraphimon, dominimon,
    seraphimon) and is CLOSED — all four energies spent. A later sweep wanting the Virus Busters
    HolyAngemon thread must take a different Perfect.

## 2026-07-23 - US-165 (Orphan sweep: Ultimate-Super Ultimate E-H)
- Wired all 14 orphaned Ultimate E-H as evolution nodes. No jogress result in the E-H range, so no
  reservations to skip. Orphans: Ultimate 80 -> 66 (Appendix B), Armor-Hybrid 16 untouched; total
  96 -> 82. Nodes 837 -> 851. Dead-end ledger 60 -> 59 (Pandamon cleared). Validator: zero findings.
- Files: Resources/{evolutions,elements,moves}.json (data), scripts/us165_author.py (authoring
  record), Tests/UltimateSweepEToHTests.swift (new, 25 tests), plus ~20 knock-on test files.
- Build green, 2433 tests, 0 failures.
- **Learnings for future iterations:**
  - **A "seadra" (or any keyword-rule) roster-only fixture DIES when its last member is wired.**
    US-165 made GigaSeadramon a node, and DexTypeBadgeTests / ElementCatalogTests both pick a
    roster-only Seadramon at RUN TIME to exercise the keyword fallback. There were none left, so
    both broke. FIX: repoint at one of the 157 permanently-`dexOnly` Digimon (ankylomon -> earth) —
    those can NEVER become nodes, so no later sweep can strand the test again. Before choosing a
    keyword fixture, prefer a dexOnly id over any playable one.
  - **A "second design" whose Wikimon page is the OTHER spelling follows a cited parent, the
    Chaosdramon V2 shape.** `hi-andromon` (sprite Hi-Andromon) and `hiandromon` (HiAndromon) are two
    roster entries; Wikimon has only `HiAndromon` (the dmc-v3 node under Andromon). So hi-andromon is
    the pack's 2nd Andromon sprite and hangs on the OTHER Andromon (`pencme_andromon`, penc-me), not
    a duplicate of the first. Run `opensearch` when a page 404s — Hi-Andromon 404s, HiAndromon is the
    real title.
  - **A sweep can make a Perfect a FOUR-EDGE node, and two prior test files pin that Perfect's edge
    count.** doruguremon (was 3) and pencme_andromon (was 3) each gained a 4th edge here. That breaks
    UltimateSweepCToDTests' "these five carry three edges" list AND PerfectSweepDToGTests'
    `branchedBySweeps` dict AND PendulumMetalEmpireTreeTests' `earned` set AND the doruguremon
    edge-order assertion. Grep the parent id across Tests/*.swift BEFORE authoring a 4th edge; each
    hit is a hardcoded count to bump.
  - **The whole-graph live numbers move for EVERY prior sweep, not just the adjacent one.** Bulk
    edits needed: `graph.nodes.count` 837->851 (19 files), `graph.nodes(at:.ultimate).count`
    158->172 (6 Perfect-sweep files), the ultimate orphan bucket 80->66 and true-remainder 78->64
    (both A-B and C-D Ultimate sweeps), dead-end ledger 60->59 (7 files), and 8 line sizes
    (tamers 116->117, penc-nso 73->75, penc-me 70->71, penc-nsp 41->43, penc-wg 43->45, dmc-v4
    31->34, dmc-v5 25->26, penc-sw 18->20) plus the EvolutionGraphTests byLine dict. The tree files
    exclude a sweep's additions via a `sweepEggs`/`notThisStorys` set so their census stays fixed —
    ADD your nodes there, do not bump the census.
  - **A node on the stranded penc-sw (Saiyu Warriors) line must (a) say "Saiyu Warriors" in its
    comment** (ChildSweepAToFTests / BabyIISweepTests / DigitamaSweepBabyITests iterate every penc-sw
    node) **and (b) be added to EvolutionCriteriaTests.strandedUltimate** (the line has no Digitama,
    so nothing on it is egg-reachable). enmamon and erlangmon both landed there — the honest home,
    since enmamon's every citation is an Ultimate and erlangmon's only bolded on-line parent is the
    Pandamon leaf.
  - **Comment keyword assertions are CASE-SENSITIVE `.contains("Wikimon")`.** "ON WIKIMON" fails;
    write "Wikimon" exactly. Three comments here needed a fix before the new test's citation check
    passed.
  - REMAINS: US-166 onward (52/56 passing). 66 Ultimate orphans (I-Z) + 16 Armor-Hybrid = 82 total.
    Four sweeps left: three Ultimate (I-M, N-R, S-Z) and the Armor-Hybrid one (US-169). Ogudomon is
    still pinned for US-167 (display name O).

## 2026-07-23 - US-166 (Orphan sweep: Ultimate-Super Ultimate I-M)
- Wired the 27 orphaned Ultimate whose display name begins I-M. Ultimate orphan bucket 66 -> 39;
  total playable orphans 82 -> 55 (Armor-Hybrid 16 untouched, US-169's). Nodes 851 -> 878.
- Files: Resources/{evolutions,elements,moves}.json (27 nodes + 27 edges + 27 element/move rows),
  scripts/us166_author.py (authoring record, refuses to run twice), Tests/UltimateSweepIToMTests.swift
  (new, 25 tests). ~30 existing test files updated for the moved live-state.
- Build green, 2458 tests (2433 + 25), 0 failures. Validator ZERO findings over evolutions.json.
- PLACEMENTS: 26 earned branches (one health metric + one care counter, distinct requiredEnergy) +
  1 leaf isDefault climb (Karakurumon -> Kaguyamon, dead-end ledger 59 -> 58). FIVE new four-edge
  Perfects, all energies spent: paildramon (3 Imperialdramon Modes), saviorhackmon (3 Jesmon),
  ladydevimon (Lilithmon + Lilithmon X), knightmon (LordKnightmon X), digitamamon (Minervamon X).
- JOGRESS RESULTS wired the Cernumon way: mastemon off Angewomon (both jogress parents are Perfects,
  so a direct Perfect->Ultimate edge), mitamamon one rung below on Garudamon (jogress parents
  Hououmon + MarinAngemon both Ultimates, the Cernumon shape). Neither is tree-reserved.
- VARIANTS: same-parent strong variant (jesmon_x, lilithmon_x, megidramon_x, imperialdramon_fighter_black);
  Dynasmon-X idle-only-base -> cited parent (justimon_x/cyberdramon_x, lordknightmon_x/knightmon,
  magnamon_x/aerov-dramon, minervamon_x/digitamamon); leviamon_x off marindevimon; metalgarurumon_x
  off weregarurumon_x. Every id resolved from Wikimon Evolves From, bolded citation preferred.
- Learnings (also promoted to Codebase Patterns at top):
  - Re-run `xcodegen generate` AFTER writing the new test file; running it only before left the new
    class out of the bundle and the suite went green (2433) without ever running it. Confirmed by the
    count climbing to 2458 and the IToM Test Suite line appearing after a second generate.
  - Forking a Perfect an earlier sweep cleared as a LEAF (megalogrowmon) breaks the A-B leaf tests:
    special-case evolutions.count and read the climb as first(where: \.isDefault), not .first.
  - A jogress-result Ultimate is still wired unless a device tree pins it to no-node (cernumon
    precedent); jougamon/jumbogamemon land stranded on penc-sw (no Digitama) -> strandedUltimate.
- REMAINS: US-167 (N-R), US-168 (S-Z), US-169 (Armor-Hybrid). 39 Ultimate orphans (N-Z) + 16
  Armor-Hybrid = 55 total. Ogudomon still pinned for US-167 (display name O). 53/56 passing.

## 2026-07-23 - US-170 - Metric-to-game conversion config file
- Shipped the single conversion-constant config that US-171..US-192 will read.
- Files (were untracked from a prior interrupted iteration; verified, regenerated, tested, committed):
  - Resources/consumption.json (rides the existing Resources folder reference)
  - Sources/ConsumptionConfig.swift (structs: ConsumptionConfig, MeatRange, HitRateCoefficients,
    ElementDamageMultipliers, StageStats; `bundled` fatalErrors on bad data like MapCatalog.bundled;
    `stats(for: Stage)` keyed on rawValue; `rates`/`caps` shared lists)
  - Sources/ConsumptionConfigValidator.swift (ConsumptionValidationError + validate() returns all
    findings: nonPositiveRate, negativeMax, emptyStatTable)
  - Tests/ConsumptionConfigTests.swift (11 tests)
- Verified: `xcodegen generate` re-run AFTER the test file existed (grep -c ConsumptionConfigTests
  pbxproj == 4); full `xcodebuild test` => TEST SUCCEEDED, all 11 ConsumptionConfig tests passed.
- **Learnings for future iterations:**
  - stageStats is keyed on `Stage.rawValue` strings ("Baby I", "Ultimate-Super Ultimate", "Armor-Hybrid"),
    NOT enum case names. Digitama is deliberately absent (an egg never battles); the test asserts
    `stats(for: .digitama) == nil`.
  - AC#3 ("no constant reworked by US-171..192 stays hard-coded") is a STANDING constraint on the
    later stories, not this one — each of US-171..192 that touches FeedAction/TrainAction/Battle must
    read ConsumptionConfig instead of a literal, or it violates US-170's AC retroactively.
  - `rates` (must be > 0) and `caps` (must be >= 0) are exposed on ConsumptionConfig so the validator
    and the test check the SAME list — add any new numeric field to one of those two lists.
---

## 2026-07-23 - US-171 - Reusable dash-bar component
- Shipped Sources/DashBar.swift: the single visual language every value bar (meat, charges, sleep,
  US-172..192) will render through. `DashBar(filled:total:tint:)` draws `total` equal-width dashes,
  the first `filled` solid (RoundedRectangle.fill) and the rest outline-only (strokeBorder, 1pt),
  NO digits. Pure `DashBarLayout.dashes(filled:total:) -> [Dash]` holds all the clamping so a test
  counts solid vs outline without a view graph.
- Files: Sources/DashBar.swift (new), Tests/DashBarTests.swift (new, 4 tests).
- Verified: full `xcodebuild test` on Series 11 (46mm) => TEST SUCCEEDED, 2472 tests 0 failures.
  DashBar tests cover counts across filled in {0, half, total, total+1}, total==0/negative empties,
  and solid-leads-outline-follows order. Visual AC verified on the 41mm sim (Series 9, 10.4) with a
  throwaway ImageRenderer probe at 162pt content width: the PNG showed exactly 6 solid + 10 outline
  dashes, no number, no horizontal overflow (174pt wide). Probe deleted after; not shipped.
- **Learnings for future iterations:**
  - The narrowest CURRENT-OS sim is 42mm (Series 11); a true 41mm needs Series 9 on watchOS 10.4,
    which was SHUTDOWN and unmatchable by name — boot it and pass `-destination 'id=<UDID>'`
    (FB045EDB-DEB9-40AA-B8FB-819E9E742517). Deployment target is 10.0, so 10.4 devices are valid.
  - To screenshot a bare SwiftUI view (no app run): a gated XCTest using `@MainActor ImageRenderer`
    -> `uiImage.pngData()` written to an ABSOLUTE path. Simulator processes run on the host, so
    "/tmp/foo.png" lands on the HOST /tmp (NOT under CoreSimulator/Devices) — read it there.
    Pass the env var to the test host with the `TEST_RUNNER_` prefix (TEST_RUNNER_DASHBAR_RENDER=1).
  - `strokeBorder` (not `stroke`) for the outline dash: it insets the 1pt line so it stays inside
    the dash frame instead of bleeding onto its neighbour. RoundedRectangle is InsettableShape so it
    has strokeBorder.
  - The xcodegen "generate AFTER the test file exists" rule (US-166) held again: generated once
    after both DashBar.swift + DashBarTests.swift existed; `grep -c DashBarTests pbxproj == 4` and
    the count climbed. Re-generated a THIRD time after deleting the probe to drop it from the target.
- REMAINS: US-172..US-192 (24 stories). US-172 (bottom-align action row) is next by priority.
---

## 2026-07-23 — US-172: Bottom-align the action row and grow the play area
- ContentView's `digimon` VStack now carries, after the map/scrim preference layers:
  `.padding(.bottom, MainScreenLayout.actionRowBottomInset)` then
  `.ignoresSafeArea(.container, edges: .bottom)`. The ignore lets the stack draw into the bottom
  safe-area band watchOS reserves for the corner curvature (26pt on 41mm, 36pt on 46mm); the 4pt
  padding, applied INSIDE the ignore, keeps the row off the very edge. Every reclaimed point lands
  on the sprite's `GeometryReader` because it is the one row at `maxHeight: .infinity`.
- New `MainScreenLayout` enum (actionRowBottomInset = 4) beside `SpriteScale`, pinned by
  `MainScreenLayoutTests.testTheActionRowKeepsExactlyFourPointsBelowIt`.
- Files: Sources/ContentView.swift, Tests/MainScreenLayoutTests.swift.
- Build green (Series 11 46mm). 2473 tests (2472 + 1 new), 0 failures.
- MEASURED before/after, both watch sizes, pre- vs post-story builds. Baseline = HEAD 09c7ea8
  built in a git worktree; identical launch `-healthAnswered -noNotificationPrompt
  -battleAffordableDemo -mapDemo=01_grassland`.
  - AC1 action-row bottom inset: 41mm gap 26pt -> 4.0pt; 46mm gap 36pt -> 4.0pt. Measured as the
    dim colored disc band's lowest row (y421/430 on 41mm, y487/496 on 46mm).
  - AC2 play area grew: map slot 49.0 -> 71.0pt on 41mm (+22, sprite scale 3->4); 63.5 -> 95.5pt on
    46mm (+32, scale 4->5). So the Digimon is a WHOLE step bigger after. MapBackgroundView grows
    with the slot by construction (same SpriteSlotBoundsKey). The before numbers reproduce US-120's
    49.0/64.0pt baseline, which validates the method.
  - AC3 no overlap / chrome unchanged: sprite stays inside its slot (SpriteScale.fitting floors, so
    scale 5 fits); action row is a separate row below the energy bars. Toolbar (lamp/clock/Dex) and
    energy bars (STEP/KCAL/Zz/EXER) byte-identical before and after.
- **Learnings for future iterations:**
  - The two SCREENSHOT patterns added to Codebase Patterns this iteration: a stale SpringBoard
    system alert can survive uninstall/reinstall and only a sim reboot clears it (cost me four dead
    screenshots), and the map slot is measured by a contiguous full-width scan down from its fixed
    top edge — colour/edge-column probes leak into the sprite or the bars.
  - The slot now sits at 71pt (41mm) / 95.5pt (46mm), i.e. scale 4 / 5. US-120's razor-thin 0.5pt
    slack over a 16pt quantum is GONE — the play area has whole points to spare now, so a later
    polish row is no longer one point of font away from shrinking the Digimon. But the 4pt inset is
    load-bearing and pinned; do not pad the action row further without re-measuring the slot.
- REMAINS: US-173..US-192 (20 stories). US-173 (bigger widget sprite) is next by priority.
---

## 2026-07-23 — US-173: Enlarge the Digimon image in the circular complication
- CircularComplicationView drew the sprite at a flat `side: 24` inside a ~52pt circle, so it read
  small at watch-face size. Now the view is a `GeometryReader` wrapping the ZStack, and the sprite
  gets `CircularComplicationLayout.spriteSide(diameter:) = diameter / √2` — the side of the square
  inscribed in the circle. That frame is as big as it can be while its corners only just touch the
  bezel, so the sprite grows to fill the face and clips on no watch size (37pt on a 52pt face, and
  proportionally smaller on a 41mm where a fixed 36pt would have clipped). `.scaledToFit()` and
  `.interpolation(.none)` are untouched, so the pixel art stays crisp.
- New `CircularComplicationLayout` enum beside the view; pinned by
  `ComplicationTests.testTheCircularSpriteFillsTheCircleWithoutClipping` (corner distance == radius,
  and side > the old 24). Updated two stale "24pt sprite in a 52pt circle" comments.
- Files: Sources/ComplicationViews.swift, Tests/ComplicationTests.swift.
- Build green (Series 11 46mm). 2474 tests (2473 + 1 new), 0 failures.
- VISUAL AC verified with `-complicationDemo` before/after screenshots (HEAD build vs this build,
  same 46mm sim). The circular egg goes from ~35% of its AccessoryWidgetBackground square to ~65%,
  well inside the square so a real circular face won't clip it. Confirmed the frame really is the
  inscribed square with a throwaway red `Text("\(Int(diameter))")` overlay: it printed 52, i.e. the
  demo hands the view the full 52pt face and side resolves to ~37pt. Overlay removed before shipping.
- **Learnings for future iterations:**
  - THE SCREENSHOT TRAP THAT COST THE MOST HERE: `xcrun simctl install` was silently installing a
    STALE app from the wrong DerivedData folder, so four rounds of before/after looked byte-identical
    and I nearly concluded the change did nothing. The main project builds to a DerivedData hash you
    must read from `xcodebuild -showBuildSettings | grep BUILT_PRODUCTS_DIR`, NOT the newest
    `DigiVPet-*` dir (a git worktree build creates its own hash and pollutes an `ls -td` guess). Always
    resolve BUILT_PRODUCTS_DIR per build; `simctl uninstall` before `install`; and prove the running
    binary is yours with a temporary on-screen marker before trusting any pixel measurement.
  - A `GeometryReader` as a *sibling* inside a ZStack (`ZStack { background; GeometryReader {...} }`)
    negotiates its size DOWN to the other children and does not report the container size — it read
    ~34pt not 52. Wrap the whole ZStack in the GeometryReader instead to read the real face size.
  - The `-complicationDemo` screen republishes a LIVE snapshot each launch, so the Digimon (an egg in
    the Simulator, HealthKit empty) differs run to run — compare sprite-to-square RATIO, not absolute
    pixels, across a before/after pair.
- REMAINS: US-174..US-192 (19 stories). US-174 is next by priority.

## 2026-07-23 — US-174: Meat is the global feed currency
- Feeding moved off per-Digimon Vitality onto a global meat larder.
  - `PlayerProfile` gained `var meat: Int = 0` (+ init param). The inline default is the migration:
    SwiftData adds the new attribute with it, so every existing save opens with an empty larder.
    Pinned by PlayerProfileTests — the round-trip test now writes/reads meat=12, and the
    pre-migration-fixture test asserts the migrated profile's meat == 0.
  - `FeedAction.feed` now takes `profile:` and spends `meatCostPerFeed` (1). The funds guard sits
    AFTER the hunger>0 refusal guard (a full Digimon still refuses for free, even at 0 meat) and
    blocks with "No meat — go battle." `stageEnergy`/`lifetimeEnergy` are untouched now.
  - `FeedOutcome.fed(cost:)` -> `.fed` (the cost value was consumed by no production code; ~15 test
    call sites updated).
  - `MainScreenModel.feed()` passes `profile`; new `meat`/`meatCap` (=`ConsumptionConfig.bundled.meatCap`)
    computed props. `ContentView` draws `DashBar(filled: meat, total: meatCap, tint: .orange,
    dashHeight: 5)` above `ActionControls`. 5pt is deliberate — it keeps US-172's sprite scale
    (41mm slot 71->66, floor(66/16)=4; 46mm 95.5->90.5, floor=5; both unchanged).
- Files: Sources/PlayerProfile.swift, Sources/FeedAction.swift, Sources/TrainAction.swift (stale
  comment), Sources/MainScreenModel.swift, Sources/ContentView.swift; Tests: FeedingTests,
  CareMistakeTests, WanderingTests, SleepStateTests, LightSwitchTests, FeedMotionTests,
  WakeOnActionTests, PlayerProfileTests.
- Build green (Series 11 46mm). 2474 tests, 0 failures.
- **Learnings for future iterations:**
  - THE RIPPLE: changing a shared *action* (feed) touches every apply-layer suite that drives
    `model.feed()`. They each have their own `startedModel`/`makeModel` helper — the cheap fix is to
    seed `model.profile?.meat = 10` right after `await model.start()` in that ONE helper per file
    (profile is `private(set)` but a class, so mutating its property from a @testable test is fine),
    then only the `.fed(cost:)` -> `.fed` and any energy assertions remain per-test. `perl -0pi -e
    's/\.fed\(cost: FeedAction\.vitalityCostPerFeed\)/.fed/g'` cleaned the mechanical ones.
  - Adding a fixed-height row to the main VStack (spacing 0) costs the sprite slot exactly that
    height, straight off US-172's play area. 41mm has ~7pt of slack over the scale-4 boundary (64pt)
    and 46mm ~15pt over scale-5 (80pt); budget any new bar against that before adding it. The meat
    bar spent 5 of the 41mm 7.
  - Later consumption stories (battle charges, train charges, sleep, HP) each want their own bar.
    There is NOT room for four more full rows without dropping the 41mm sprite a scale step — those
    stories will likely need to co-locate bars or overlay them, not stack them. `meat`/`meatCap` on
    the model is the pattern to copy; per-Digimon charges live on GameState, not the profile.
- REMAINS: US-175..US-192, US-167..US-169 (orphan sweep tail). US-175 (battle wins drop meat) is
  next by priority and is the earning half of this larder.

## 2026-07-23 - US-175 (Battle wins drop meat)
- MeatReward.rolled (Sources/ConsumptionConfig.swift): rolls meatPerBattleWin off an injected
  generator, clamps to max(0, cap - current). The banked value and the displayed value are one thing.
- MainScreenModel.finishBattleRound: on a WIN only, rolls from the SAME generator the fight was
  resolved from (the only half with a generator in hand) and credits profile.meat; the in-memory
  credit persists on finishBattle's existing save beside recordBattle, exactly as the energy cost does.
- BattleBout gains meatGained (default 0); BattleView result screen shows orange fork-and-knife '+N'
  only when >0.
- Tests added to Tests/BattleTests.swift (reused BattleApplyTests.makeModel/fixtureGraph, so no new
  file and no xcodegen-glob trap): MeatRewardTests (pure range/cap arithmetic) +
  testAWonBattleCreditsMeatWithinRangeToTheGlobalLarder (shipped path, first winning seed) +
  testALostBattleDropsNoMeat.
- Build green, 2478 tests, 0 failures.
- **Learnings for future iterations:**
  - "Winning (or completing)" in the AC was read as WIN-only, matching the title "Battle wins drop
    meat" and the reward framing; a loss dropping meat would make losing free. If US-176+ expect
    completion-credit, revisit finishBattleRound's `if report.playerWon`.
  - The battle flow has TWO halves: battle() charges energy + picks opponent + sets pendingBattleRound;
    finishBattleRound(result) resolves the report, builds BattleBout, sets pendingBattle. The generator
    lives on PendingBattleRound and is only alive in finishBattleRound - anything seeded off the fight
    (meat, future drops) must roll there, not in finishBattle.
  - To exercise a WIN through the model without guessing a seed: loop seeds with a strong Digimon
    (strength 40) and `guard bout.report.playerWon else { continue }`. The known loss fixture is
    strength 0, seed 3.
  - profile.meat is surfaced by MainScreenModel.meat / the ContentView DashBar (US-174) and spent by
    FeedAction; US-175 is the earner that closes the walk->battle->feed loop.
---

## 2026-07-23 - US-176 (Battle charges accrue from steps, per Digimon)
- GameState: added `battleCharges` (0..10) and `battleChargeSteps` (remainder toward the next
  charge), both migration-safe optional storage like every other field here. `creditBattleCharges(
  steps:stepsPerCharge:maxCharges:)` adds whole charges from a step delta, banks the sub-300
  remainder so short walks across many refreshes are not lost, and drops the remainder at the cap.
- Wiring: `MainScreenModel.creditMapSteps` now credits charges from the SAME `MetricLedger.claim`
  step delta it credits the map from — one claim, two consumers, as the ledger's own doc requires.
  Credited to `state` (the Digimon that is OUT), so charges are per-Digimon; frozen Digimon don't
  refresh and so accrue nothing.
- battle(): a `guard state.battleCharges > 0` (message "No charge — go walk.") sits after
  wakeIfAsleep and before the energy guard; a started battle spends one charge beside
  `EnergyPurchase.charge`, saved by the same flush. The energy cost was KEPT.
- UI: `MainScreenModel.battleCharges`/`battleChargeCap`; ContentView draws a purple charge DashBar
  co-located with the orange meat bar in one HStack (not a second stacked row) to protect US-172/173.
- Tests: BattleChargeCreditTests (pure: 900->3, 3300->cap10, remainder carry, spend-keeps-remainder,
  two-Digimon independence); two model tests in the battle apply class (spends exactly one charge;
  unavailable at 0 charges and says so); one end-to-end in BackgroundRefreshTests (900 stepped
  refresh banks 3 charges). Seeded `state.battleCharges = ConsumptionConfig.bundled.maxBattleCharges`
  in every battle-driving test helper (PreBattleRound, Battle, BattleCost, DigitamaDrop, Wandering,
  WakeOnAction, LightSwitch, BackgroundRefresh).
- Build green (Series 11 46mm). 2486 tests, 0 failures.
- **Learnings for future iterations:**
  - THE RIPPLE (again): adding a hard gate to `battle()` breaks every apply-suite that drives
    `model.battle()` and does not seed the new resource — 8 files. Same cheap fix as US-174's meat:
    seed the resource right where each helper already stocks energy/meat (before `save()` for the
    build-a-state helpers, after `await start()` for the post-start `startedModel` ones). Placing the
    charge guard AFTER the death/training/wake guards and keeping the energy guard means all existing
    REFUSAL tests (dead, broke, training-in-progress) still fire unchanged once charges are seeded.
  - DECISION: US-176's AC says "spends exactly one charge" but — unlike US-177 for train — never says
    "replacing the energy cost". So the energy cost was KEPT and the charge is an ADDITIONAL gate.
    Removing energy would have gutted US-108's BattleCostTests/PreBattleRoundTests for no AC. If a
    later story wants charges to be the SOLE battle cost, that is a separate, explicit change.
  - The shared `MetricLedger.claim(.healthSteps,...)` SPENDS the delta — a second independent claimer
    gets 0. Any future step-derived consumer (US-179's MetricCreditor) must read the delta
    `creditMapSteps` already claims, not claim its own. Charges follow that rule; copy it.
  - Play-area budget: co-locate new currency bars in the existing meat HStack (US-174), never add a
    stacked row — the 41mm sprite has no more scale slack after meat spent its ~5pt.
- REMAINS: US-177..US-192 (consumption/battle-stats/polish tail), US-167..US-169 (orphan sweep tail).
  US-177 (train charges from calories) is next by priority and per its AC DOES replace the train
  energy cost — mirror this story's GameState field + creditFromDelta pattern, but remove the
  TrainAction energy spend rather than keep it.

## 2026-07-23 - US-177 (Train charges accrue from active calories, per Digimon)
- Repriced training onto a per-Digimon charge burned from active calories, mirroring US-176's battle charge exactly.
- Source changes:
  - GameState.swift: added trainCharges/trainChargeKcal (optional storage, nil reads as 0, migration-safe) + creditTrainCharges(kcal:kcalPerCharge:maxCharges:), a copy of creditBattleCharges' shape with kcal in place of steps.
  - ConsumptionConfig.swift + Resources/consumption.json: added maxTrainCharges (10) and to the caps validator list. kcalPerTrain (50) already existed (US-170).
  - TrainAction.swift: begin() now spends one trainCharge and NO Strength/Stamina energy; blocks at 0 with noChargeReason "No training charge — go exercise." Removed energyCostPerTraining/payableWith/insufficientEnergyReason and the EnergyPurchase call. TrainOutcome=.trained(gain:), TrainingStart=.started (dropped spent/cost).
  - Battle.swift: BattleCost decoupled from TrainAction — now owns energy=5, payableWith=[.strength,.stamina], its own reason string. Battle is the last action that spends energy.
  - MainScreenModel.swift: PendingTraining dropped spent/cost; finishTraining caption is "Grade +N STR"; refresh() calls new creditTrainCharges(_:) reading dayReadings.quantities[.activeEnergy] and claiming it off MetricLedger (shared delta, same rule the map steps use); added trainCharges/trainChargeCap computed props.
  - ContentView.swift: third red DashBar (dashHeight 5) in the meat/battle HStack — same row, no added height, US-172/US-174 sprite scale untouched.
- Tests: 2491 total, 0 failures (Series 11 46mm). New TrainChargeCreditTests (150 kcal->3, cap, remainder, two-Digimon independence + decrement) and ClosedAppRecomputeTests.testExerciseCreditsTrainChargesToTheDigimon (refresh end-to-end). Rewrote the three training suites for the charge currency; stocked trainCharges in ~10 fixture helpers that used to fund training with strength energy; replaced BattleCostTests' "identical to TrainAction" test with one pinning BattleCost's own numbers; added maxTrainCharges to ConsumptionConfigTests' 4 constructors.
- **Learnings for future iterations:**
  - **Battle charge is the template for every "spendable charge" story.** creditBattleCharges (GameState), the MetricLedger.claim shared delta in creditMapSteps, the DashBar in ContentView's currency HStack, and model.battleCharges/battleChargeCap are the exact five seams a new charge copies. US-177 mirrored all five.
  - **Active energy day-total is dayReadings.quantities[.activeEnergy]; steps is [.steps].** Both come off the ONE HealthEnergySource.dayReadings read in refresh, keyed by QuantityMetric. Claim it via metricLedger.claim(.healthActiveEnergy, ...) — that is a ConditionMetric, distinct from the QuantityMetric key. .healthActiveEnergy has no other claim caller today, so it is a clean single consumer.
  - **BackgroundRefreshTests' refresh-level class is ClosedAppRecomputeTests, not "BackgroundRefreshTests".** -only-testing:DigiVPetTests/BackgroundRefreshTests runs 0 tests. The DailyStepFetcher there filters by metric==.steps and returns [] otherwise; a calorie test needs its own fetcher filtering metric==.activeEnergy (added DailyActiveEnergyFetcher).
  - **Changing an action's currency ripples to every test that funded it the old way.** Training was funded with strength energy in ~12 files; all had to seed trainCharges instead or the 0-charge default silently blocks the round (and blocked rounds count no session, so stageTrainingSessions assertions flip). grep 'TrainAction.begin|TrainAction.train(|model.train()' before assuming a fixture is unaffected.
  - **When action A borrows action B's constants and B changes, decouple A first.** BattleCost borrowed TrainAction.energyCostPerTraining etc.; removing them from TrainAction broke Battle.swift's compile. Gave BattleCost its own copies.

## 2026-07-23 - US-178 (Cleaning poop costs a global handwash charge)
- Closed the last arm of the care loop: clearing the mess now spends a charge walked up from HealthKit handwashing, capped at 2 and shared globally across the box.
- Source changes (the battle-charge template, adapted for a GLOBAL currency read from a CATEGORY metric):
  - PlayerProfile.swift: added cleanCharges + handwashProgress (plain stored props with = 0 defaults, migration-safe the way `meat` is, NOT the optional-storage GameState pattern — a fresh @Model attribute with a default is lighter and meat proved it) + init params; creditCleanCharges(events:eventsPerCharge:maxCharges:) (a copy of GameState.creditBattleCharges' while-loop, cap and dropped-remainder), and spendCleanCharge()->Bool. Global like meat, because a habit is the player's, not one pet's.
  - MainScreenModel.swift: added injected `metricReader: HealthMetricReader = HealthMetricReader()`; refresh() calls new `await creditCleanCharges()` after creditTrainCharges; that method reads .healthHandwashing over HealthDay.interval(containing: now) via the reader, claims the day-total off the SHARED MetricLedger under .healthHandwashing (dedup), and credits the profile at handwashPerCleanCharge; added cleanCharges/cleanChargeCap computed props; clean() now guards poopCount>0 FIRST then `profile.spendCleanCharge()`, showing "No charge — go wash." and returning false at zero.
  - ContentView.swift: fourth teal DashBar (dashHeight 5, tint .teal to match the Clean sparkles) in the SAME meat/train/battle HStack — no added height, sprite scale untouched.
  - consumption.json/ConsumptionConfig.swift: NO change needed — handwashPerCleanCharge (1) and maxCleanCharges (2) already shipped in US-177's config and were already in the caps validator list.
- Tests: 2501 total, 0 failures (Series 11 46mm). New CleaningChargeTests.swift (10): charge arithmetic (1->1, cap-drops-remainder, sub-threshold accumulation at eventsPerCharge=3, spend/refuse-at-zero), AC4's two clean() cases + a two-spend-then-empty run, refresh crediting off a HandwashFetcher fixture, dedup (two refreshes count once), and the bar's profile/config inputs. Stocked cleanCharges in 6 existing clean-success tests (CleanTrainMotionTests helper, ComplicationTests x2, MainScreenTests x3, LightSwitchTests helper, NotificationTests x2) exactly the way US-176 stocked battle charges.
- **Learnings for future iterations:**
  - **A CATEGORY health metric (handwashing, toothbrushing, mindful, stand-hours) is NOT in dayReadings.** dayReadings.quantities is keyed by QuantityMetric = steps/activeEnergy/exercise only. Category/event metrics are read via HealthMetricReader.read(ReadableHealthMetric(.healthX)!, in: HealthDay.interval(...)) and deduped by claiming off MetricLedger under the ConditionMetric key. US-178 is the FIRST Sources caller of HealthMetricReader (it was test-only before, per US-118's note).
  - **The default metricReader is LIVE, so refresh-calling tests do a live handwashing query.** It is harmless: the Simulator has no handwashing data (the CLAUDE.md premise), so the query returns noData deterministically and credits nothing — verified non-hanging across the whole suite. Only inject a fixture reader when you are TESTING the credit; everything else can ignore it. (This is unlike the energy path, which every test injects — but energy tests assert on energy; handwashing tests are the only ones that assert on washes.)
  - **clean() ordering: poopCount>0 guard BEFORE the charge guard.** "Nothing to clean" must stay a silent no-op (the two existing no-op tests need no charge stock); the "go wash" message only makes sense when there IS a mess being refused. Same reason battle() checks state before charges.
  - **The face-tapped clean (applyPendingCleanRequest) runs clean() too**, so it also spends a charge — correct, and the ComplicationTests fixture for it needed stocking. It runs at the TOP of refresh, before that refresh's own creditCleanCharges, so it spends charges banked on a PRIOR refresh.
  - **Global currency (meat, cleanCharges) lives on PlayerProfile with a plain `= 0` stored prop; per-Digimon currency (battleCharges, trainCharges) lives on GameState with optional storage.** Pick the home by whether the fact outlives putting one Digimon away — a habit/larder does, a pet's earned charges do not.

## 2026-07-23 - US-179 (Wire the metric ledger into the health-read flow)
- Revived the dead health-condition pipeline: MetricCreditor.credit had 0 callers in Sources (test-only), so stageMetricTotals/lifetimeMetricTotals/stageBestDayMetrics were always empty and every health.* evolution condition was dead (atLeast never passed, atMost passed for free, standing metrics forced to .unknown). Now refresh() calls it beside EnergyCreditor.credit and supplies ConditionContext.readings from HealthMetricReader at every build point.
- Source changes:
  - HealthMetricReader.swift: added `readings(of:in:) -> [ConditionMetric: HealthReading]`, a sequential batch read (matches TodayHealthReader.readToday), used by both models. Callers pass ReadableHealthMetric.all.
  - MainScreenModel.swift: new `healthReadings(dayReadings:)` seeds .healthSteps/.healthActiveEnergy/.healthExerciseMinutes from dayReadings.quantities and .healthSleep from dayReadings.sleep (no double HealthKit read), reads every other ReadableHealthMetric.all over today's window, and KEEPS ONLY .value entries. refresh() calls it, stores conditionReadings, then `MetricCreditor.credit(metrics, ledger: metricLedger, ...)` -> creditedMetrics. The three charge helpers became pure delta consumers: creditMapSteps(_ steps: Double), creditTrainCharges(_ kcal: Double), creditCleanCharges(_ events: Double) — the last is no longer async (its handwashing read folded into the shared metric read). The 4 real ConditionContext builds (jogressBoard, mapDetailContext, evolveIfReady, checkForDigitamaDrop) now pass `readings: conditionReadings`.
  - DexModel.swift + DexView.swift: DexModel gained injected metricReader + calendar and `loadHealthReadings(now:) async`; DexView's new `.task` calls it (sync load() still paints the grid first). load()'s ConditionContext now carries self.readings.
- Tests: 2507 total (2501 + 6 new), 0 failures (Series 11 46mm), ~48s. New MetricWiringTests.swift: AC4 (credited sleep 480 clears atLeast 400, fails atMost 400 through the full chain), AC5 (standing resting-HR via readings answerable, unit), AC1 (a refresh credits flightsClimbed=12 to all three totals — non-evolving graph so enterStage does not wipe them), AC3 (second same-day refresh credits once), AC2/AC5 end-to-end (a resting-HR-52 read drives an atMost-60 gated evolution; 72 blocks it).
- **Learnings for future iterations:**
  - **MetricCreditor.credit is now the SINGLE claimer of the day's metrics off MetricLedger; the map/battle/train/clean charge paths consume the deltas it RETURNS.** This was forced: those paths already claimed steps/activeEnergy/handwashing, so a second claim from MetricCreditor gets 0 and the stage totals never grow. If you add a new consumer of a health metric, take it from `creditedMetrics[.healthX]` in refresh() — do NOT add a fresh `metricLedger.claim(.healthX, ...)`, that is now a double-claim that steals the delta. The one place claim() is called from Sources is MetricCreditor.credit.
  - **conditionReadings is .value-ONLY on purpose.** Dropping .noData/.unavailable keeps ConditionContext's line-96 "unavailable -> unknown" override switched OFF, so accumulating atMost freebies are UNCHANGED (US-180's job to fix). This is why wiring readings into every build point did not break a single one of the 2501 prior tests: in the Simulator all reads are .unavailable -> filtered -> conditionReadings empty -> identical to the old empty-dict behaviour. On a real device with data, standing metrics become answerable (Problem 4) and accumulating totals fill (Problem 1).
  - **Adding ~22 live HealthKit reads per refresh did NOT slow the suite** (2501->2507 tests, ~48s, was already ~47s). Real reads return .unavailable fast in the Simulator (the same reason US-178's single handwashing read was harmless). ReadableHealthMetric.all is ~25; the 3 daily quantities + sleep are seeded from dayReadings so only ~21 are actually queried.
  - **A conditions-gated evolution test must NOT let the Digimon evolve for the wrong reason.** The credit tests (AC1/AC3) need a TERMINAL hero (no edges) because any evolution runs enterStage, which clears stageMetricTotals + stageBestDayMetrics — a passing credit into stage totals then reads back 0 while lifetime survives. The evolution tests (AC2) want the branching graph. Two graphs in one fixture file.
  - **DexModel does live reads now, off a `.task`, not `load()`.** load() stays synchronous (21 test call sites) and paints the grid with empty readings; loadHealthReadings() reads metrics and re-resolves ONLY the hint context. hasReadHealth guards the re-read; a -dexRevealDemo literal context is never overwritten.
  - **NOT DONE, tracked as US-180:** an accumulating atMost health gate still passes for free where the metric was never credited (reads .known(0)). The .value-only filter deliberately does not fire the unavailable-override for that. US-180 makes never-credited read as .unknown via the GameState accessor returning nil.
---

## 2026-07-23 - US-180 (No data is unknown, not zero)
- Closed the freebie US-179 tracked: an accumulating health `atMost` gate passed for free where the metric was never credited, because ConditionContext.healthValue read the totals through `MetricTotals.subscript`, which flattens an absent metric to 0, and 0 satisfies atMost.
- Source changes (only these two files):
  - MetricLedger.swift: added `MetricTotals.known(_ metric:) -> Double?` returning `values[metric.rawValue]` — nil when never credited. The subscript is unchanged (still flattens absent->0 for the `+= delta` arithmetic path); nothing ever writes an explicit 0, so present-in-`values` == really-credited and absent == never-touched. That distinction is invisible through the subscript and is exactly what the condition path needs.
  - ConditionEvaluator.swift: healthValue's accumulating branch now `guard let totals, let value = totals.known(metric) else { return .unknown }` instead of `.known(totals[metric])`. So an uncredited accumulating metric is `.unknown` even when the stage/lifetime/day totals struct is present; a metric credited to exactly 0 is present and stays `.known(0)`.
- Tests: 2510 total (2507 + 3 new), 0 failures (Series 11 46mm), ~48s. The 3 new (in ConditionEvaluationTests.swift) also observed passing on their own:
  - testMetricTotalsKnownDistinguishesUncreditedFromZero: known() nil for never-credited, 0 for credited-0; subscript still 0 either way.
  - testAnUncreditedAccumulatingMetricIsUnknownEvenWithTotalsPresent: healthValue .unknown over stage/lifetime/day when totals present but metric absent; credited-0 stays .known(0) (AC1/AC2).
  - testUncreditedFailsBothGatesWhileACreditedZeroSatisfiesAtMost: no credit -> atLeast(1) AND atMost(500) both false; credited-0 -> atMost(500) true, atLeast(1) false (AC3).
- **Learnings for future iterations:**
  - **MetricTotals now has TWO readers with different jobs.** `subscript` = "the number for arithmetic", absent reads 0 — use it for `+= delta` accrual (MetricCreditor). `known(_:)` = "was this ever credited?", absent reads nil — use it on the condition/eligibility path where never-credited must be `.unknown`, not zero. Do not swap them: reading the subscript on a gate reintroduces the atMost freebie; reading known() in accrual crashes on the first credit.
  - **The `.unavailable` override (line 96) and the never-credited-unknown rule are SEPARATE fixes for the same trap.** US-179's conditionReadings is `.value`-only so the unavailable override stays off in the Simulator; US-180 makes the totals themselves report unknown for a metric that was never written. Together: on real hardware a denied metric (unavailable reading) AND a never-credited metric (absent from totals) both read `.unknown` and both fail atMost, rather than one path leaking a free 0.
  - **Why `Fixture.context(stageSteps: 0)` still satisfies atMost after this change:** it writes `totals[.healthSteps] = 0` through the subscript SETTER, so the key is present with value 0 — a *credited* 0, `.known(0)`. The change only affects metrics ABSENT from `values`. That is why not one of the 2507 prior tests broke.
---

## 2026-07-23 - US-181 (Accumulated sleep hours stored per Digimon)
- Each Digimon now remembers its lifetime HealthKit sleep, so switching which one is out shows the right progress toward its sleep evolution gate (US-183). Distinct from the nightly sleep ENERGY (that tops up stageEnergy via EnergyCreditor and resets) - this only ever grows.
- Source changes (only these two files):
  - GameState.swift: private `accumulatedSleepMinutesStorage: Double?` (optional for the same SwiftData migration reason as every other storage prop; nil reads as 0), inited to 0; computed `accumulatedSleepMinutes` (native unit MetricLedger credits health.sleep in), `accumulatedSleepHours` (minutes/60, the unit a gate is authored in - Agumon needs 16h), and `creditSleep(minutes:)` which guards >0 and just adds - NO cap (it climbs past any gate, like a map's recorded steps past its total).
  - MainScreenModel.swift: `state.creditSleep(minutes: creditedMetrics[.healthSleep])` in refresh(), beside creditMapSteps/creditTrainCharges/creditCleanCharges - the SAME US-179 MetricLedger delta they spend, so a night is de-duplicated once and shared.
- Tests: 2519 total (2510 + 9 new), 0 failures (Series 11 46mm), ~48s. New Tests/SleepAccumulationTests.swift, all 9 also observed passing on their own:
  - SleepAccumulationCreditTests (pure): 405 min -> 6.75 h; many reads sum without a cap (17 h); non-positive deltas are no-ops; two GameStates independent.
  - SleepAccumulationPersistenceTests (GameStore): a banked total survives a reopen; a box of two (active hero 405 + frozen agu_digitama 120) keeps each apart across a reopen.
  - SleepAccumulationRefreshTests (end to end): a fixture night 23:30-06:15 (405 min, wholly inside last night's 18:00-noon window) credits model.state 405 min / 6.75 h on start(); a SECOND refresh of the same night banks it ONCE (405, not 810); a FROZEN box-mate granted before the refresh accrues 0 while the active hero banks 405 (AC2, US-125).
- **Learnings for future iterations:**
  - **The per-Digimon health accrual pattern is now three-strong: creditBattleCharges (steps), creditTrainCharges (kcal), creditSleep (minutes) all live on GameState and are all fed the RETURNED `creditedMetrics[.healthX]` delta in refresh().** Follow it for any new per-Digimon health accumulation: private optional storage + computed accessor + a credit(delta:) that adds; never call `metricLedger.claim` a second time (US-179's single-claimer rule).
  - **"A frozen Digimon does not accrue" is free by construction.** refresh() only ever passes the ACTIVE `state` to creditSleep; a frozen box-mate is never refreshed. So a per-record accumulation IS the freeze behaviour - no clock-offset trick needed here (unlike hunger/age which are Date-derived, US-125).
  - **health.sleep is credited in MINUTES** (MetricLedger.accumulatesOverTime true; SleepAnalysis.asleepMinutes -> .value(minutes)). Store the native minutes; expose hours as minutes/60. A gate authored in hours (US-183) compares against accumulatedSleepHours.
  - **To feed sleep end-to-end in a test:** inject a FixtureSleepFetcher into LastNightSleepReader, set `now` to the morning AFTER (last night's window is 18:00 prev day -> noon today), and the sample must fall wholly inside. 23:30-06:15 = 405 min. The model needs a real GameStore (not nil) for the MetricLedger to exist and MetricCreditor to run.
  - **US-182/US-183 seam:** model.state.accumulatedSleepHours is what the Zz DashBar (US-182) and the detail sleep bar + gate (US-183) read.
---

## 2026-07-23 - US-182 (Zz bar shows the active Digimon's accumulated sleep)
- The main-screen Zz slot now renders the active Digimon's accumulated sleep (US-181) as a DashBar (US-171) instead of the spirit-energy capsule. The other three slots (STEP/KCAL/EXER) stay energy bars, so the 2x2 grid keeps its exact shape and widths - the spirit *energy* still accrues underneath (EnergyProgress is unchanged), it just isn't the thing the Zz slot draws any more.
- Source changes:
  - MainScreenModel.swift: `sleepHours` (Int of `state?.accumulatedSleepHours` - floored to whole-hour dashes, 0 before a game loads, off `state` so switching shows the active pet's own total) and `sleepHoursCap` (a nominal display ceiling = static `sleepHoursDisplayCap` = 16, the PRD's headline requirement; a constant not a GameState value - every bar is the same length, only the fill differs). Added `seedSleepBarDemoIfRequested()` (DEBUG) beside the other seeds: `-sleepBarDemo` seeds 6 accumulated hours, `-sleepBarFullDemo` 14, both on a Child Agumon, so the Zz bar can be screenshotted with a real fill (the Simulator banks no HealthKit sleep).
  - EnergyBarsView.swift: two new params `sleepHours`/`sleepHoursTotal` (defaulted 0, so existing callers compile). In the pair ForEach, `goal.type == .spirit` renders a new private `SleepDashBarRow` (the "Zz" name column from `EnergyType.spirit.shortName` at EnergyBarLayout width, then a `DashBar(tint: .secondary, dashHeight: barHeight, spacing: 1)`, VoiceOver "N of M hours slept") instead of `EnergyBarRow`.
  - ContentView.swift: the one `EnergyBarsView(...)` call now passes `sleepHours: model.sleepHours, sleepHoursTotal: model.sleepHoursCap`.
- Tests: 2522 total (2519 + 3 new), 0 failures (Series 11 46mm), ~49s. New MainScreenSleepBarTests in EnergyBarsTests.swift: AC1 (405 min banked -> sleepHours 6, cap>0), AC2 (a box of two - active 8h, frozen 2h - reads 8 then, after activate(), 2), and empty-before-load reads 0.
- AC3 (screenshot): verified on the Series 11 46mm sim. The Zz slot draws a row of dashes with NO number where the spirit energy capsule/number used to be; `-sleepBarDemo` shows ~6 solid of 16 and `-sleepBarFullDemo` ~14 of 16 - clearly different fill. Screens at /tmp/us182/sleep6.png and sleep14.png.
- **Learnings for future iterations:**
  - **The Simulator queues a notification-authorization system alert AND a HealthKit onboarding gate on a fresh/erased device; both hide the main screen.** To screenshot the main screen, launch with BOTH `-noNotificationPrompt` (already existed, gates `requestNotificationAuthorization`) AND `-healthAnswered` (StubHealthAuthorizer.Outcome, bypasses HealthAuthorizationGate). A pending notif alert survives app uninstall - only `xcrun simctl erase` clears it - so never launch a screenshot build without `-noNotificationPrompt` first, or you have to erase and start over. System Events cannot see the watch-screen alert as a native button, so it can't be dismissed by AppleScript.
  - **The spirit EnergyType is NOT removed - only the Zz VIEW slot changed.** EnergyProgress still carries all four goals (spirit target still gates agumon_black/Devimon at minEnergy 60), and every EnergyBars/DominantEnergy test that reads `progress.goal(.spirit)` still passes untouched. The change is purely in EnergyBarsView's rendering + two new model accessors. If US-183 wants the Zz bar to target the nearest sleep *gate* instead of the fixed cap, swap `sleepHoursTotal` for that - the seam is the one `EnergyBarsView(...)` call in ContentView.
  - **`sleepHoursCap` is a deliberate nominal (16), not the per-edge gate.** The PRD splits it: the main-screen Zz bar is at-a-glance "how rested" against a fixed length; the PRECISE required-vs-earned sleep bar is the DETAIL view (US-183). Don't wire the evolution gate into the main bar - that's US-183's detail bar.

---
## 2026-07-23 - US-183 (Sleep as an evolution gate with a detail dash bar)
- Made accumulated sleep a real evolution gate and drew it as a dash bar in the Dex detail view.
- Files changed:
  - Resources/evolutions.json: added a health.sleep / window:lifetime / atLeast 960 condition to the agumon -> greymon edge ("Let it bank a good long stretch of sleep" - digit-free hint). 960 min = 16 h; this is the "Agumon needs 16 hours" exemplar the PRD names.
  - Sources/SleepGate.swift (NEW): pure helper. requiredHours(in: [EvolutionCondition]) -> Int? = max of the health.sleep/lifetime/atLeast thresholds, /60, rounded (nil = no gate = no bar). earnedHours(in: ConditionContext) -> Int = context.value(.healthSleep, .lifetime) minutes /60 FLOORED, .unknown -> 0. atMost sleep excluded (a ceiling is not a fill-toward bar).
  - Sources/DexView.swift: DexDetailView gained `sleepBar` (moon.zzz icon + DashBar(filled: earnedHours, total: requiredHours, tint: .indigo), no numbers), drawn after the "Evolves into" heading only when SleepGate.requiredHours(over the candidates' conditions) is non-nil.
  - Tests/SleepGateTests.swift (NEW, 10 tests): the gate unmet at 6h/15.9h and met at 16h/20h; .unknown stays shut; the shipped graph authors >=1 accumulated-sleep gate with a non-blank hint; validator clean; the real agumon->greymon edge carries the 16h gate; the ENGINE (evolutionTarget on the real agumon node) falls to numemon at 6h and reaches greymon at 16h; the bar reports filled==earned, total==required; floor behaviour; no bar without an atLeast sleep gate.
  - Tests/ConditionRevealTests.swift: updated testTheDemoScenarioProducesAllThreeLevels expectation [.close,.far] -> [.close,.far,.far] (greymon now has a third, cold, sleep condition in that scenario).
- Tests: 2532 total (2522 + 10 new), 0 failures (Series 11 46mm), ~48s. Build green.
- **Learnings for future iterations:**
  - **The accumulated-sleep gate needed NO ConditionEvaluator change - it is just health.sleep/window:lifetime.** accumulatedSleepMinutes (US-181) and lifetimeMetricTotals['health.sleep'] (US-179) are fed the SAME creditedMetrics[.healthSleep] delta in refresh(), so they are equal by construction; both are lifetime-of-this-Digimon and never reset per stage. The gate reads the lifetime total via the existing pipeline (US-179 fills it, US-180 makes never-credited read .unknown not a free 0). So US-181's on-GameState field is effectively a mirror the Zz bar (US-182) reads; the gate + detail bar read the CONTEXT total instead, which guarantees the bar's solid count matches what the gate compares (no drift possible).
  - **health.sleep is authored in MINUTES everywhere (existing 119 conditions are stage/minutes too); the bar divides by 60 for the hours the player sees.** Do NOT author a sleep gate in hours - the evaluator compares against minute-valued totals, so "16" would mean 16 minutes and pass instantly. requiredHours() and earnedHours() are the ONLY /60 in the codebase.
  - **A lifetime-window condition on a SWEPT edge breaks the sweep tests.** Every sweep "met" context (e.g. ChildSweepAToF) sets stageTotals but NOT lifetimeTotals, so a health.sleep/lifetime gate reads .unknown and the sweep's earned-branch engine test fails. I gated agumon->greymon specifically because Agumon is EXCLUDED from ChildSweepAToF's sweptChildren (a pre-wired device tree) and nothing engine-evolves the real agumon->greymon with a partial context. If a future story adds a lifetime health gate to a SWEPT node, it must also add that metric to the sweep's `met` lifetimeTotals - or the sweep will fail.
  - **Adding a condition to a shipped edge can break a pinned demo-scenario test.** ConditionRevealTests.testTheDemoScenarioProducesAllThreeLevels asserts the EXACT reveal-level list of agumon->greymon; a third condition changed [.close,.far] to [.close,.far,.far]. Grep Tests for the node id you touch before adding a condition - the -dexRevealDemo screenshot pins agumon.
