# Ralph Progress Log
Started: 2026年 7月21日 星期二 16時06分51秒 CST
---

## 2026-07-21 — US-085: English energy-bar labels naming their health source

DONE. All acceptance criteria observed to pass except one that has nothing to
observe (see CAVEAT).

WHAT CHANGED
- `EnergyType.symbol` -> `EnergyType.shortName`, returning the SOURCE of the
  energy rather than the name of the type: strength STEP, vitality KCAL,
  spirit SLEEP, stamina EXER. Display-only, like `displayName`; `rawValue` is
  untouched so no saved game is rewritten (a test pins all four raw values).
- Zero CJK glyphs left in `Sources/` — `grep -rn '力\|活\|心\|耐' Sources` returns
  nothing. They were at GameState.swift:32-35 and ComplicationSnapshot.swift:150.
- New free-standing `enum EnergyBarLayout` in EnergyBarsView.swift holds every
  width the row is built from, in the spirit of `SpriteScale`: a test checks the
  arithmetic without building a view graph. name 25pt @ size 8 (was 11pt @ size
  9), bar floor 18pt, value 28pt (unchanged), column spacing 3, pair spacing 6.
  Two bars therefore cost 2*(25+3+18+3+28)+6 = 154pt of the narrowest 176pt
  screen — 22pt of slack for the system's own insets.
- The bar got an explicit `.frame(minWidth: 18)`, so the wider name column can
  never be paid for out of the thing the row exists to draw.
- `accessibilityLabel` is untouched: VoiceOver still says "Strength", not "STEP".
- `ComplicationPublisher` carries `shortName` into `dominantEnergySymbol`; the
  snapshot's `placeholder` says "STEP".

VERIFICATION
- `xcodebuild build` green on 46mm and 42mm.
- `xcodebuild test` — 1048 tests, 0 failures.
- New tests: EnergyShortNameTests (5) pin the four literals, distinctness,
  non-emptiness, that the short name is not the displayName, that rawValue did
  not move, and that no label is non-ASCII. EnergyBarLayoutTests (3) measure
  each short name against the REAL watchOS `UIFont.systemFont(ofSize: 8)` —
  not a guess at how wide five capitals are — and pin the row-width arithmetic
  and the 18/28/4 constants. ComplicationTests gained one test tying the two
  hard-coded snapshot labels back to `EnergyType`.
- Screenshot, Series 11 42mm, `-healthAnswered -noNotificationPrompt -wanderDemo`:
  all four bars read STEP 0/60, KCAL 0, SLEEP 0, EXER 0/60 — no truncation, bars
  and values intact, sprite and action row unchanged.
- Same on Series 11 46mm.
- Screenshot, 42mm, `-complicationEnergyDemo -complicationDemo`: the rectangular
  family's gauge labelled SLEEP, unclipped, over a half-full bar reading 150.

NEW DEBUG FLAG, and why it was needed
`-complicationEnergyDemo` makes `ComplicationDemoView` draw
`ComplicationSnapshot.longestEnergyLabel` (the placeholder wearing SLEEP, the
widest of the four). Without it the gauge label cannot be photographed at all:
the Simulator has no HealthKit data, so every game it publishes has NO dominant
energy and the face correctly draws "No energy yet". Hand-writing a snapshot
into the app-group JSON does not work either — the app republishes on launch and
overwrites it (verified: the file came back with `dominantEnergySymbol` absent).
The flag chooses only WHO is on the face; the views drawn are the shipping ones.

CAVEAT — the AC's "corner and rectangular families"
There is no corner family to check. `DigiVPetComplication` ships
`.accessoryCircular` + `.accessoryRectangular` only (Sources/Complication/
DigiVPetComplication.swift:14); `.accessoryCorner` exists solely inside the
US-049 refresh spike, which records at RefreshGranularitySpike.swift:116 why it
is deliberately NOT on the shipping complication. The circular family draws the
sprite alone and carries no energy label, so rectangular is the only family this
story could affect, and it was verified.

INCIDENT — prd.json was destroyed and rebuilt mid-iteration
While marking the story passed I ran `git checkout prd.json` to undo a
reformatting, forgetting that the NEW prd.json (elements-battle-depth) was
uncommitted — HEAD still held the previous run's progression-and-play file. The
checkout replaced the new PRD with the old one. Recovered by regenerating
prd.json from `tasks/prd-elements-battle-depth-and-care-polish.md`, which was
untracked but intact, via the ralph skill.
WHAT THE NEXT ITERATION MUST KNOW: US-085's story text is reproduced verbatim
(it had been read into context before the loss). US-086 through US-101 were
re-derived from the source markdown — the ids, titles, priorities and ordering
match the lost file exactly (they had been listed before the loss), and the
criteria are faithful to the PRD, but the wording may differ in detail from the
original conversion. If a criterion reads oddly, trust
`tasks/prd-elements-battle-depth-and-care-polish.md` over prd.json.
LESSON: never `git checkout` a tracked file in this repo without checking
`git status` first — this loop keeps its work uncommitted between stories.

REMAINS
US-086 (element/attribute vocabulary) is next. Nothing in this story blocks it.
